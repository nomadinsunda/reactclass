# 📘 React Hook: `useEffect` 완전 정복

React를 사용하다 보면 `useEffect`는 거의 모든 컴포넌트에서 마주치게 되는 핵심 Hook입니다. 이 챕터는 단순한 사용법 소개를 넘어, **렌더링 사이클과의 관계**, **의존성 배열의 동작 원리**, **클린업 함수의 의미**, 그리고 **고급 활용 패턴**까지 철저히 다룹니다.

---

## 🧠 1. `useEffect`란 무엇인가?

React 함수형 컴포넌트는 상태(state)나 props가 바뀔 때마다 **다시 렌더링**됩니다. 하지만 다음과 같은 작업은 단순 렌더링만으로는 수행할 수 없습니다:

* API 호출
* 브라우저 이벤트 등록
* 외부 라이브러리 연동
* 타이머 설정
* DOM 수동 조작 등

이처럼 컴포넌트의 \*\*렌더링 외부에서 동작하는 부수 효과(Side Effect)\*\*를 처리하기 위해 React는 `useEffect`라는 Hook을 제공합니다.

```jsx
useEffect(() => {
  // 부수 효과 수행 코드 (effect)
  return () => {
    // 정리(clean-up) 코드 (optional)
  };
}, [deps]);
```

---

## 🔄 2. 렌더링 사이클과 `useEffect`

`useEffect`는 \*\*DOM이 그려진 이후 (commit phase)\*\*에 실행됩니다. 이 점에서 `useLayoutEffect`와 구분됩니다.

### 실행 시점 요약:

* **렌더링 → DOM 반영 완료 → `useEffect` 실행**
* **의존성 배열 변경 시마다 재실행**

```jsx
useEffect(() => {
  console.log("마운트 또는 deps 변경 후 실행됨");
}, [deps]);
```

> ⚠️ 의존성 배열이 없으면 **매 렌더링마다 실행**됩니다.

---

## 🎯 3. 의존성 배열의 의미와 원리

의존성 배열은 `useEffect`의 핵심입니다. 배열 내의 값이 변경되었을 때만 effect가 다시 실행됩니다.

### ✅ 사용 예시:

```jsx
useEffect(() => {
  console.log(`user가 바뀔 때마다 실행됩니다`);
}, [user]);
```

### ⚠️ 주의사항:

* 참조형 데이터 (`object`, `array`, `function`)는 항상 새로운 참조일 수 있음 → `useMemo`, `useCallback`으로 안정화 필요

```jsx
const memoizedHandler = useCallback(() => {}, []);
useEffect(() => {
  // memoizedHandler가 바뀌지 않으면 실행 안 됨
}, [memoizedHandler]);
```

---

## ♻️ 4. 클린업(Cleanup) 함수의 역할

컴포넌트가 언마운트되거나, 다음 effect가 실행되기 전에 \*\*기존 effect를 정리(clean up)\*\*하기 위해 `return` 구문을 사용합니다.

### 예: 웹소켓 정리

```jsx
useEffect(() => {
  const socket = new WebSocket('wss://example.com');
  socket.onmessage = (e) => console.log(e.data);

  return () => {
    console.log('컴포넌트 언마운트 → 소켓 종료');
    socket.close();
  }
}, []);
```

> 정리를 하지 않으면 **메모리 누수**나 **이벤트 중복 등록** 등의 문제가 발생합니다.

---

## 🧪 5. 의존성 배열에 따른 실행 차이 예제

### 1️⃣ 의존성 배열 없음

```jsx
useEffect(() => {
  console.log("항상 실행");
});
```

✅ 매 렌더링마다 실행

### 2️⃣ 빈 배열

```jsx
useEffect(() => {
  console.log("마운트 시 1회 실행");
}, []);
```

✅ Mount 시 1회만 실행됨 (componentDidMount와 유사)

### 3️⃣ 특정 값만 감시

```jsx
useEffect(() => {
  console.log("count가 변경될 때만 실행");
}, [count]);
```

✅ `count` 값이 바뀔 때만 실행됨

---

## 🔧 6. 고급 패턴 및 실전 팁

### ✅ 조건부 실행 (if 문으로 내부 제어)

```jsx
useEffect(() => {
  if (!user) return;
  fetchUserData(user);
}, [user]);
```

### ✅ 비동기 함수 호출 (주의 필요)

```jsx
useEffect(() => {
  const fetchData = async () => {
    const res = await fetch('/api/data');
    const data = await res.json();
    setData(data);
  }
  fetchData();
}, []);
```

> `useEffect` 자체는 `async`로 만들 수 없음 → 내부에 async 함수 작성

### ✅ setInterval / setTimeout

```jsx
useEffect(() => {
  const id = setInterval(() => {
    console.log("1초마다 실행");
  }, 1000);

  return () => clearInterval(id);
}, []);
```

---

## 🚨 7. 흔한 실수

| 실수                      | 설명                                    |
| ----------------------- | ------------------------------------- |
| 의존성 배열 누락               | 외부 변수 사용했는데 배열에 포함 안 함 → stale 상태 발생  |
| 비동기 함수를 useEffect에 바로 씀 | `useEffect(async () => {})` ❌ → 오류 발생 |
| 클린업 함수 누락               | 이벤트 리스너, 타이머 정리 안 하면 메모리 누수 위험        |
| 렌더링 막기 용도로 사용           | 로직에 따라 `useMemo`가 더 적절할 수 있음          |

---

## 🧾 8. 비교: `useEffect` vs `useLayoutEffect`

| 항목    | useEffect     | useLayoutEffect    |
| ----- | ------------- | ------------------ |
| 실행 시점 | DOM 업데이트 후    | DOM 업데이트 직전        |
| 용도    | 비동기 작업, API 등 | 레이아웃 측정, DOM 계산    |
| 차이    | 비동기적 실행       | 동기적 실행 (블로킹 발생 가능) |

---

## ✅ 마무리

`useEffect`는 React의 렌더링 사이클과 깊이 연결된 핵심 개념입니다. 단순히 "API 호출용 Hook"이 아니라, **컴포넌트 외부 세계와 연결하는 창구**입니다.

* 의존성 배열은 정확하게 관리하고
* 클린업은 반드시 처리하고
* 불필요한 재실행을 줄이는 구조를 고민해보세요

