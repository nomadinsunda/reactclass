# JavaScript Array-Like 객체란?

```javascript

import React from "react";
import Scrollbars from 'react-scrollbars-custom'

const FancyScrollbar = () => {
    return (        
        <Scrollbars style={{ height: 500}}>
            {Array.from({ length: 50 }, (_, i) => (
                <p key={i}>항목 {i + 1}</p>
            ))}
        </Scrollbars>
    )

}

export default FancyScrollbar;

```

위 코드에서 다음 부분은 JavaScript의 **Array-Like Object(유사 배열 객체)** 개념을 이해하기 좋은 예제입니다.

```jsx
Array.from({ length: 50 }, (_, i) => (
  <p key={i}>항목 {i + 1}</p>
))
```

여기서 가장 눈여겨봐야 할 부분은 이것입니다.

```js
{ length: 50 }
```

이 객체는 **배열이 아닙니다.**

```js
const obj = { length: 50 };

console.log(Array.isArray(obj));
// false
```

그런데 `Array.from()`은 이 객체를 이용해서 **길이가 50인 배열을 만들어낼 수 있습니다.**

왜 가능할까요?

---

## 1. Array-Like란?

Array-Like Object는 우리말로 **유사 배열 객체**라고 합니다.

쉽게 정의하면:

> **배열(Array)은 아니지만, `length`와 숫자 형태의 프로퍼티 키(index)를 이용해 배열과 비슷한 구조를 표현할 수 있는 객체**입니다.

대표적인 모습은 다음과 같습니다.

```js
const arrayLike = {
  0: "Apple",
  1: "Banana",
  2: "Orange",
  length: 3
};
```

배열과 비교해 보겠습니다.

```js
const array = [
  "Apple",
  "Banana",
  "Orange"
];
```

구조적으로 보면 상당히 비슷합니다.

```text
Array

┌─────────────────┐
│ 0 → "Apple"     │
│ 1 → "Banana"    │
│ 2 → "Orange"    │
│ length → 3      │
└─────────────────┘


Array-Like Object

┌─────────────────┐
│ 0 → "Apple"     │
│ 1 → "Banana"    │
│ 2 → "Orange"    │
│ length → 3      │
└─────────────────┘
```

하지만 둘은 같은 것이 아닙니다.

```js
Array.isArray(array);      // true
Array.isArray(arrayLike);  // false
```

---

# 2. 핵심은 `length`

Array-Like 객체에서 중요한 프로퍼티가 바로:

```js
length
```

입니다.

예를 들어:

```js
const obj = {
  0: "A",
  1: "B",
  2: "C",
  length: 3
};
```

이 객체는 다음과 같이 해석할 수 있습니다.

```text
length = 3

index
  ↓
  0 ─── "A"
  1 ─── "B"
  2 ─── "C"
```

즉,

> **0번부터 `length - 1`번까지의 위치가 존재할 수 있는 객체**

라고 생각하면 이해하기 쉽습니다.

---

# 3. 그런데 `{ length: 50 }`에는 실제 엘리먼트가 없다

예제 코드에서 중요한 부분입니다.

```js
{ length: 50 }
```

객체를 실제로 살펴보면:

```text
{
  length: 50
}
```

뿐입니다.

다음과 같은 프로퍼티들은 없습니다.

```text
0: ???
1: ???
2: ???
...
49: ???
```

즉:

```js
const obj = { length: 50 };

console.log(obj[0]);   // undefined
console.log(obj[1]);   // undefined
console.log(obj[49]);  // undefined
```

입니다.

따라서 정확하게 표현하면:

> `{ length: 50 }`은 **50개의 실제 엘리먼트를 가지고 있는 것이 아니라, `length`를 통해 0~49라는 50개의 위치를 표현할 수 있는 Array-Like 객체**입니다.

이 차이가 중요합니다.

---

# 4. 그런데 `Array.from()`은 이것을 처리할 수 있다

`Array.from()`은 **Iterable뿐 아니라 Array-Like 객체도 배열로 변환할 수 있도록 설계되어 있습니다.**

```js
Array.from({ length: 5 })
```

그러면 결과는 개념적으로:

```js
[
  undefined,
  undefined,
  undefined,
  undefined,
  undefined
]
```

가 됩니다.

흐름으로 보면:

```text
{ length: 5 }
      │
      ▼
Array.from()
      │
      │ length 확인
      ▼
   length = 5
      │
      ▼
0 ~ 4 위치 처리
      │
      ▼
┌─────────────────────┐
│ 0 → undefined       │
│ 1 → undefined       │
│ 2 → undefined       │
│ 3 → undefined       │
│ 4 → undefined       │
└─────────────────────┘
      │
      ▼
실제 Array 생성
```

---

# 5. Array.from()의 두 번째 아규먼트

그런데 원래 코드는 다음과 같습니다.

```js
Array.from(
  { length: 50 },
  (_, i) => (
    <p key={i}>항목 {i + 1}</p>
  )
)
```

`Array.from()`은 두 번째 아규먼트로 **mapping function**을 받을 수 있습니다.

```js
Array.from(arrayLike, mapFn)
```

따라서 다음 코드는:

```js
Array.from(
  { length: 5 },
  (_, i) => i
);
```

결과가:

```js
[0, 1, 2, 3, 4]
```

가 됩니다.

여기서 콜백:

```js
(_, i) => i
```

의 파라미터는:

```text
(value, index)
   ↓      ↓
   _      i
```

입니다.

---

# 6. `_`는 무엇인가?

원래 코드:

```js
(_, i) => (
  <p key={i}>항목 {i + 1}</p>
)
```

여기서 `_`는 특별한 JavaScript 문법이 아닙니다.

그냥 **파라미터 이름**입니다.

관례적으로:

> "이 값은 전달되지만 사용하지 않겠습니다."

라는 의미로 `_`를 많이 사용합니다.

실제로는:

```js
(value, index) => {}
```

인데 `value`를 사용하지 않으므로:

```js
(_, index) => {}
```

라고 작성한 것입니다.

그리고 `{ length: 50 }`에는 실제 값이 없으므로 각 `value`는 `undefined`가 됩니다.

```text
첫 번째 호출

value = undefined
index = 0


두 번째 호출

value = undefined
index = 1


세 번째 호출

value = undefined
index = 2

...

50번째 호출

value = undefined
index = 49
```

---

# 7. 그래서 원래 코드는 어떻게 동작하는가?

이제 전체를 연결해보겠습니다.

```jsx
Array.from({ length: 50 }, (_, i) => (
  <p key={i}>항목 {i + 1}</p>
))
```

### STEP 1

Array-Like 객체가 있습니다.

```js
{ length: 50 }
```

실제 값은 없습니다.

```text
length = 50

0  → 없음
1  → 없음
2  → 없음
...
49 → 없음
```

### STEP 2

`Array.from()`이 `length`를 확인합니다.

```text
length = 50
```

따라서 0~49까지 **50개의 위치를 처리**합니다.

### STEP 3

각 위치마다 mapping function을 호출합니다.

```js
(_, i) => (
  <p key={i}>항목 {i + 1}</p>
)
```

개념적으로:

```text
i = 0
 ↓
<p>항목 1</p>

i = 1
 ↓
<p>항목 2</p>

i = 2
 ↓
<p>항목 3</p>

...

i = 49
 ↓
<p>항목 50</p>
```

### STEP 4

최종적으로 React Element들이 들어 있는 **실제 Array**가 만들어집니다.

개념적으로:

```js
[
  <p>항목 1</p>,
  <p>항목 2</p>,
  <p>항목 3</p>,
  ...
  <p>항목 50</p>
]
```

그리고 React가 이 배열을 렌더링합니다.

---

# 8. Array와 Array-Like의 결정적인 차이

여기서 입문자에게 꼭 설명해야 할 부분이 있습니다.

Array-Like라고 해서 **Array가 되는 것은 아닙니다.**

```js
const obj = {
  0: "A",
  1: "B",
  length: 2
};
```

다음은 안 됩니다.

```js
obj.map(x => x);
```

왜냐하면 `obj`는 `Array`가 아니기 때문입니다.

```js
Array.isArray(obj);
// false
```

`Array.prototype.map` 같은 배열 메서드를 자신의 메서드로 가지고 있지 않습니다.

하지만:

```js
Array.from(obj)
```

를 하면 실제 Array로 변환됩니다.

```js
const arr = Array.from(obj);

console.log(arr);
// ["A", "B"]

arr.map(x => x);
// 가능
```

따라서:

```text
Array-Like Object
       │
       │ Array.from()
       ▼
      Array
       │
       ├── map()
       ├── filter()
       ├── reduce()
       ├── find()
       └── ...
```

라고 이해할 수 있습니다.

---

# 9. 실제 Array-Like의 대표적인 예

JavaScript에서 대표적인 Array-Like 객체로는 함수의 `arguments` 객체 등이 있습니다.

```js
function test() {
  console.log(arguments);
}

test("A", "B", "C");
```

개념적으로:

```text
arguments

{
  0: "A",
  1: "B",
  2: "C",
  length: 3
}
```

처럼 사용할 수 있습니다.

DOM API에서도 Array-Like한 컬렉션을 자주 만나게 됩니다. 다만 `NodeList`처럼 **Array-Like이면서 동시에 iterable인 객체도 있으므로**, `Array-Like === iterable`이라고 생각하면 안 됩니다.

---

# 10. `{ length: 50 }`를 어떻게 이해하면 좋은가?

이 코드를:

```js
Array.from({ length: 50 }, ...)
```

보고

> "`{ length: 50 }` 안에 50개의 엘리먼트가 있다."

라고 설명하면 정확하지 않습니다.

오히려 다음과 같이 설명하는 것이 좋습니다.

```text
{ length: 50 }

실제 엘리먼트 50개가 있다는 뜻 ❌

        ↓

0 ~ 49까지
50개의 위치를 표현할 수 있는
Array-Like 객체

        ↓

Array.from()이
length를 이용하여
50개의 Array 엘리먼트를 생성

        ↓

mapping function 실행

        ↓

React Element 50개 생성
```

따라서 기존 코드의 주석도 조금 다듬는 것을 권합니다.

```jsx
// { length: 50 }은 Array가 아니라 Array-Like 객체입니다.
//
// 실제로 0~49 프로퍼티에 50개의 값이 들어 있는 것은 아닙니다.
// 단지 length가 50이므로 Array.from()이 이를 Array-Like으로 처리하여
// 0~49까지 총 50개의 위치를 순회합니다.
//
// 두 번째 아규먼트인 mapping function이 각 위치마다 실행되고,
// i에는 0~49의 index가 전달됩니다.
{Array.from({ length: 50 }, (_, i) => (
  <p key={i}>항목 {i + 1}</p>
))}
```

### 한 문장 정의

> **Array-Like Object란 실제 Array는 아니지만 `length`와 숫자형 index 프로퍼티를 통해 배열과 유사한 구조를 표현할 수 있으며, `Array.from()` 같은 API가 배열처럼 처리할 수 있는 객체입니다.**

그리고 현재 코드에서는 조금 더 구체적으로:

> **`{ length: 50 }`에는 실제 50개의 값이 존재하는 것이 아니라 `length` 정보만 존재하며, `Array.from()`이 이 정보를 이용해 0~49의 50개 위치를 처리하면서 mapping function을 호출하여 실제 배열을 만들어 냅니다.**

이 부분은 **`Array-Like → Array.from() → mapping function → React Element[]`** 흐름으로 그림을 만들면 상당히 직관적으로 설명할 수 있습니다.
