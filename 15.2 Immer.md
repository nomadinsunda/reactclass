
# 1⃣ Immer 한 문장 정의

> **Immer는 “불변 객체를 직접 만들지 않고도, 변경하는 것처럼 코드를 작성하면
> 내부에서 변경 이력을 추적해 새로운 불변 객체를 생성해주는 라이브러리”입니다.**

핵심 키워드:

* *변경하는 것처럼*
* *추적*
* *새로운 불변 객체 생성*

---

# 2⃣ Immer가 등장한 진짜 이유

### Redux/React의 고질적 문제

```js
return {
  ...state,
  todos: state.todos.map(todo =>
    todo.id === id
      ? { ...todo, completed: !todo.completed }
      : todo
  )
}
```

* 읽기 어렵고
* 실수하기 쉽고
* 깊어질수록 지옥

 **불변성은 맞지만, 인간 친화적이지 않다**

Immer의 목표는 단 하나입니다:

> **불변성은 기계가 지키고, 인간은 변경 로직만 쓰게 하자**

---

# 3⃣ Immer의 핵심 아이디어: “Draft”

Immer에서 가장 중요한 개념은 **Draft**입니다.

### Draft란?

* **실제 state가 아님**
* **Proxy로 감싼 가짜 객체**
* 읽고/쓰는 모든 행위를 감시당함

```js
produce(baseState, (draft) => {
  draft.count += 1
})
```

여기서 `draft`는:

* baseState의 복사본 X
* baseState의 Proxy 래퍼

---

# 4⃣ 내부 동작 핵심: Proxy + Trap

Immer는 JavaScript의 **Proxy**를 사용합니다.

### 예시

```js
draft.user.name = 'Kim'
```

내부에서는:

1. `draft.user` 접근 → `get` trap
2. `name`에 값 할당 → `set` trap
3. Immer가 기록:

   * “user.name 경로가 변경됨”
   * “기존 값: Lee → 새 값: Kim”

 **이 시점에는 아직 객체 복사가 일어나지 않습니다**

---

# 5⃣ “언제” 실제 객체가 만들어질까?

`produce()`가 끝나는 순간입니다.

```js
const nextState = produce(prevState, draft => {
  draft.a.b.c = 10
})
```

이 시점에 Immer는:

1. 변경된 경로만 추적
2. 필요한 부분만 **얕은 복사**
3. 나머지는 **참조 재사용**

이를 **구조 공유 (Structural Sharing)** 라고 합니다.

---

# 6⃣ 구조 공유란 무엇인가? (성능의 핵심)

예시 state:

```js
state = {
  a: { x: 1 },
  b: { y: 2 },
  c: { z: 3 }
}
```

`a.x`만 변경했다면:

```js
nextState = {
  a: { x: 10 }, // 새 객체
  b: state.b,  // 기존 참조 재사용
  c: state.c   // 기존 참조 재사용
}
```

O 장점

* deep copy X
* 최소한의 객체 생성
* 참조 비교 기반 렌더링 최적화와 찰떡

---

# 7⃣ Immer의 핵심 API: `produce`

Redux Toolkit은 내부에서 이걸 씁니다.

```js
import { produce } from 'immer'

const next = produce(prev, draft => {
  draft.count++
})
```

RTK의 reducer는 사실상:

```js
produce(prevState, reducerLogic)
```

형태로 감싸져 있다고 보시면 됩니다.

---

# 8⃣ Redux Toolkit + Immer 연결 구조 (중요)

```js
createSlice({
  reducers: {
    increment(state) {
      state.value++
    }
  }
})
```

이 reducer는 실제로는:

```js
(state, action) =>
  produce(state, draft => {
    draft.value++
  })
```

와 동일한 의미입니다.

 **그래서 RTK reducer는 불변성을 직접 관리하지 않아도 됩니다**

---

# 8⃣-1 Immer가 적용되는 “범위”가 어디까지인지 (가장 중요한 오해)

가장 많이 하는 실수가 이것입니다.

> X "RTK를 쓰면 어디서나 state를 그냥 수정해도 된다"

**아닙니다.** Immer가 draft로 감싸주는 곳은 **딱 세 군데**입니다.

| 위치                                        | Immer 적용 |
| ----------------------------------------- | -------- |
| `createSlice`의 `reducers` / `extraReducers` | O |
| `createReducer`의 case reducer | O |
| 직접 호출한 `produce(base, draft => ...)` | O |
| **thunk / 컴포넌트 / 셀렉터 / 이벤트 핸들러** | X |
| **React의 `useReducer`** | X |
| **`configureStore`의 `preloadedState` 조립 코드** | X |

```js
// ❌ thunk 안에서 store의 state를 직접 수정 — 절대 금지
const myThunk = () => (dispatch, getState) => {
  getState().todos.list.push(newTodo) // 여기는 draft가 아니라 "진짜 state"
}

// ❌ 컴포넌트에서 selector로 꺼낸 값 수정
const todos = useSelector(s => s.todos.list)
todos.push(newTodo) // 리렌더링도 안 되고, 개발 모드에선 immutableCheck에 걸립니다

// ✅ 언제나 dispatch로
dispatch(todoAdded(newTodo))
```

그리고 React의 `useReducer`에는 Immer가 **전혀 들어있지 않습니다.**
`useReducer`에서 draft 스타일로 쓰고 싶다면 `use-immer`의 `useImmerReducer`를 별도로 설치해야 합니다.

> 요약: **"직접 수정처럼 쓰기"는 `createSlice`/`createReducer` 안에서만 허용된 특권**이고,
> 그 밖에서는 예전처럼 스프레드/`map`/`filter`로 **불변 업데이트**를 해야 합니다.

---

# 9⃣ Immer가 있어도 “절대 하면 안 되는 것들”

### X 1. draft를 외부로 저장

```js
let leaked

reducer(state) {
  leaked = state // ❌
}
```

draft는 **produce 스코프 안에서만 유효**합니다.

---

### X 2. reducer에서 비동기/부작용

Immer는 불변성만 책임집니다.
Redux의 순수 함수 규칙은 그대로입니다.

```js
reducer(state) {
  fetch(...) // ❌
}
```

---

### X 3. draft 수정과 새 객체 return 혼용

```js
reducer(state) {
  state.x = 1
  return { x: 2 } // ❌ 런타임 에러
}
```

이건 "무시되는" 게 아니라 **Immer가 에러를 던집니다.**

```txt
[Immer] An immer producer returned a new value *and* modified its draft.
Either return a new value *or* modify the draft.
```

즉 reducer를 쓸 때는 **둘 중 하나만** 골라야 합니다.

```js
// ✅ 방법 A: draft를 수정하고 아무것도 반환하지 않는다
increment(state) {
  state.value += 1
}

// ✅ 방법 B: draft를 건드리지 않고 완전히 새 state를 반환한다
reset() {
  return initialState
}
```

### 반환값 규칙 정리

| 케이스                     | 결과                                    |
| ----------------------- | ------------------------------------- |
| draft만 수정, `return` 없음 | 수정 내용이 반영된 새 state O |
| draft 안 건드리고 새 값 `return` | 그 값이 곧 새 state O |
| 둘 다 | **에러** X |
| `return undefined`      | "반환 안 함"으로 취급 → draft 결과 사용 (state를 `undefined`로 만들 수 없음) |
| state 자체를 `undefined`로  | `produce`에서는 `import { nothing } from 'immer'` 후 `return nothing` |

---

# Immer는 “마법”이 아니다 (중요한 인식)

Immer는:

* 상태를 자동으로 깊게 복사해주는 도구 X
* 마음대로 mutate 해도 되는 면허 X

Immer는:

* **변경을 추적해**
* **필요한 만큼만 복사**
* **불변성을 유지**

 규칙은 더 엄격해졌고, 표현만 쉬워진 겁니다.

---

# -1 디버깅: `current()`와 `original()`

reducer 안에서 `console.log(state)`를 찍으면 **Proxy 객체가 그대로 출력**되어 값을 읽기 어렵습니다.
이때 RTK(및 immer)가 제공하는 두 함수를 씁니다.

```js
import { createSlice, current, original } from '@reduxjs/toolkit'

todoAdded(state, action) {
  console.log(state)            // ⚠️ Proxy { ... } — 읽기 힘듦
  console.log(current(state))   // ✅ "지금까지의 변경이 반영된" 일반 객체 스냅샷
  console.log(original(state))  // ✅ 이 reducer가 시작될 때의 원본 state
  state.list.push(action.payload)
  console.log(current(state))   // push가 반영된 상태
}
```

| 함수                 | 의미                                       |
| ------------------ | ---------------------------------------- |
| `current(draft)`   | 현재 draft를 **평범한 불변 객체로 스냅샷** (디버깅/로깅용)   |
| `original(draft)`  | 변경 전 **원본 state** 반환                     |
| `isDraft(value)`   | 값이 draft인지 판별                            |

* 두 함수 모두 **draft에만** 사용할 수 있습니다(일반 값에 쓰면 안 됨).
* `current()`는 매번 스냅샷을 만들므로 **디버깅 용도**로만 쓰고, 반복문 안에서 남용하지 마세요.

---

# 11⃣ Immer와 성능에 대한 진실

### “Proxy 느리지 않나요?”

* 대부분의 UI 상태에서는 문제 없음
* RTK 공식 입장: **불변성 코드 직접 작성보다 실수 비용이 훨씬 큼**

### 언제 주의?

* 초대형 배열
* 초당 수천 번 업데이트
* Canvas/게임 루프

이 경우:

* Immer를 끄거나
* 로컬 state/useRef로 분리

---

# 12⃣ 한 문장으로 최종 요약

> **Immer는 Proxy 기반 Draft를 통해 상태 변경을 기록하고,
> 변경된 부분만 복사하여 새로운 불변 상태를 만들어주는 라이브러리이며,
> Redux Toolkit에서 불변성 관리의 핵심 엔진 역할을 합니다.**

