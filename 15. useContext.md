

# 🧠 `useContext`

---

## ✅ 1. `useContext`란?

> `useContext`는 React에서 **컴포넌트 트리 전체에 전역적으로 데이터를 전달하고 소비**할 수 있도록 해주는 Hook입니다.

즉, **부모 컴포넌트로부터 깊게 내려가는 props들을 생략하고도** 필요한 곳에서 바로 접근 가능하게 만듭니다.

```tsx
const value = useContext(MyContext);
```

---

## 🧩 2. Context가 필요한 이유: props drilling

### ❌ 문제

```tsx
<App>
  <Layout>
    <Sidebar>
      <UserPanel user={user} />
    </Sidebar>
  </Layout>
</App>
```

* `user`를 깊이 있는 자식에게 넘기기 위해 **모든 중간 컴포넌트에 props로 전달**해야 함
* 이런 걸 **props drilling**이라고 함 → 유지보수성 저하

---

## ✅ 3. Context + `useContext`로 해결

### 📌 Context 생성

```tsx
const UserContext = createContext(null);
```

### 📌 Provider로 감싸기

```tsx
<UserContext.Provider value={user}>
  <App />
</UserContext.Provider>
```

### 📌 원하는 곳에서 가져오기

```tsx
const user = useContext(UserContext);
```

✅ 중간 컴포넌트 건너뛰고 바로 user 사용 가능

---

## 📘 4. 실제 예제: 언어 선택기 (LocaleContext)

```tsx
// 1. context.js
import { createContext } from 'react';

export const LocaleContext = createContext('ko'); // 기본값 'ko'
```

```tsx
// 2. App.jsx
import { LocaleContext } from './context';

export default function App() {
  return (
    <LocaleContext.Provider value="en">
      <Page />
    </LocaleContext.Provider>
  );
}
```

```tsx
// 3. Page.jsx
import { useContext } from 'react';
import { LocaleContext } from './context';

function Page() {
  const locale = useContext(LocaleContext);
  return <p>선택된 언어는: {locale}</p>;
}
```

---

## ⚙️ 5. 내부 동작 방식

```tsx
const value = useContext(SomeContext);
```

* `useContext`는 **Provider로부터 가장 가까운 값을 찾음**
* 값이 변경되면 이 Context를 구독 중인 컴포넌트는 **자동 리렌더링**

> ✅ `useContext`는 `Context.Consumer`의 Hook 버전

---

## ⚠️ 6. 주의사항

| 항목                                      | 설명                                             |
| --------------------------------------- | ---------------------------------------------- |
| ✅ Context는 상태를 **보관하지 않음**              | 상태는 여전히 `useState`, `useReducer`, Redux 등에서 관리 |
| ⚠️ Context 값이 바뀌면 **모든 구독 컴포넌트가 리렌더링됨** | 큰 앱에서는 성능 이슈 발생 가능                             |
| ❌ `useContext`는 메모이제이션 불가               | 매번 새로운 참조를 가져옴                                 |

---

## 💡 고급 패턴

### ✅ Context + useReducer 조합 (Mini Redux)

```tsx
const CounterContext = createContext();
const DispatchContext = createContext();

const reducer = (state, action) => {
  switch (action.type) {
    case 'INC': return state + 1;
    case 'DEC': return state - 1;
    default: return state;
  }
};

const App = () => {
  const [state, dispatch] = useReducer(reducer, 0);

  return (
    <CounterContext.Provider value={state}>
      <DispatchContext.Provider value={dispatch}>
        <Counter />
      </DispatchContext.Provider>
    </CounterContext.Provider>
  );
};

const Counter = () => {
  const count = useContext(CounterContext);
  const dispatch = useContext(DispatchContext);
  return (
    <>
      <p>{count}</p>
      <button onClick={() => dispatch({ type: 'INC' })}>+</button>
    </>
  );
};
```

> ✅ Redux 없이도 **Context + useReducer**로 구조적인 상태 관리 가능

---

## ✅ 언제 `useContext`를 써야 하나?

| 상황                           | 쓰면 좋은가?          |
| ---------------------------- | ---------------- |
| 로그인 정보(user), 언어(locale), 테마 | ✅ 전역 데이터에 적합     |
| 자주 바뀌는 데이터                   | ⚠️ 신중히 (리렌더링 주의) |
| 컴포넌트가 깊게 중첩되어 있고 공통 상태 필요    | ✅                |

---

## 🔁 useContext vs Redux vs useReducer

| 비교 항목    | useContext | useReducer        | Redux                                          |
| -------- | ---------- | ----------------- | ---------------------------------------------- |
| 목적       | 전역 데이터 공유  | 복잡한 상태 관리         | 상태 관리 + DevTools + 미들웨어                        |
| 전역 접근성   | ✅          | ❌ (로컬 상태)         | ✅                                              |
| 상태 변경 로직 | 없음         | reducer 함수        | reducer + middleware 가능                        |
| 리렌더링 제어  | ❌ 불가능      | ❌ (state 바뀌면 리렌더) | ✅ `connect`, `memo`, `selector`로 fine-grain 제어 |

---

## 📦 요약

| 항목      | 내용                                |
| ------- | --------------------------------- |
| Hook 이름 | `useContext(MyContext)`           |
| 역할      | props 없이 컴포넌트 트리 깊은 곳에서 전역 데이터 접근 |
| 동작 방식   | 가장 가까운 Provider에서 value 찾고 구독     |
| 주의      | Context 값이 바뀌면 구독한 모든 컴포넌트 리렌더    |
| 확장      | `Context + useReducer`로 구조화 가능    |

