

## O Virtual DOM은 무엇인가?

* **Virtual DOM은 "개념"이며**, 브라우저가 제공하는 기능이나 API가 아닙니다.
* 즉, `document.createElement()` 같은 DOM API처럼 **브라우저에 내장된 표준 기능이 아니라**,
  React(또는 다른 프레임워크)가 **자체적으로 구현한 내부 기술**입니다.
* React, Vue, Inferno, Preact 등에서 각각 **자체 방식으로 구현**합니다.
* 반면 **Svelte, Solid.js는 Virtual DOM을 쓰지 않습니다.** (아래 비교 표 참고)

---

## 표준 DOM vs Virtual DOM

| 구분    | 표준 DOM                                        | Virtual DOM                                |
| ----- | --------------------------------------------- | ------------------------------------------ |
| 정의    | 브라우저가 제공하는 **공식 API와 객체 모델**                  | 라이브러리 내부에서 메모리에 구성된 **자바스크립트 객체 기반 가상 트리** |
| 표준 여부 | O 웹 표준 (WHATWG DOM Living Standard) | X 비표준 (라이브러리마다 다름) |
| 사용 방법 | `document`, `createElement`, `appendChild`, 등 | `React.createElement`, JSX → React 내부 구조   |
| 위치    | 브라우저 메모리 상의 DOM 트리                            | 라이브러리 내부의 JS 객체                            |
| 목적    | 사용자에게 보이는 **실제 UI를 렌더링**                      | UI 변경을 **최소한으로 계산**하고 성능을 최적화              |

---

## React의 Virtual DOM 예시

```jsx
const element = <h1>Hello</h1>;
```

위 코드는 다음과 같이 내부 Virtual DOM 객체로 변환됩니다:

```js
const vdom = {
  $$typeof: Symbol.for('react.transitional.element'), // React 19의 엘리먼트 식별자
  type: 'h1',
  key: null,
  props: {
    children: 'Hello'
  }
};
```

> `$$typeof`는 서버에서 내려온 JSON이 React 엘리먼트인 척 위장하는 **XSS를 막기 위한 심볼**입니다.
> Symbol은 JSON으로 표현할 수 없으므로, `JSON.parse()` 결과는 절대 유효한 엘리먼트가 될 수 없습니다.

그리고 React는 이 Virtual DOM을 기반으로 **이전 VDOM과 비교(diff)** 한 후,
**실제 DOM에 필요한 변경만 적용(patch)** 합니다.

> 정확히는, React가 "이전 상태"로 들고 있는 트리는 위 엘리먼트 객체가 아니라 **Fiber 트리**입니다.
> 엘리먼트는 렌더할 때마다 새로 만들어졌다가 버려지는 **일회용 설계도**이고,
> 상태·DOM 참조·이펙트를 계속 들고 있는 **영속적인 트리는 Fiber**입니다. (→ 3.9 참고)

---

## 그럼 왜 존재하나?

브라우저의 DOM 노드는 프로퍼티가 수백 개 달린 무거운 객체이고,
읽기/쓰기가 레이아웃·스타일 재계산을 유발할 수 있어 **조작 비용이 큽니다.**
→ 이를 **메모리상의 가벼운 객체로 대체해서 변경 전후를 비교하고**,
→ **필요한 변경만 실제 DOM에 반영**해서 불필요한 DOM 조작을 줄입니다.

### 흔한 오해: "Virtual DOM이 실제 DOM보다 항상 빠르다"

**아닙니다.** VDOM 방식은 실제 DOM 조작 비용에 더해 **트리 생성 + diff 비용이 추가로** 듭니다.
무엇을 바꿔야 하는지 이미 알고 있다면, 손으로 쓴 `el.textContent = x` 한 줄이 언제나 VDOM보다 빠릅니다.

React 팀의 공식 입장도 "더 빠르다"가 아닙니다:

> **"Virtual DOM은 충분히 빠르다(fast enough). 진짜 가치는 속도가 아니라,
> `UI = f(state)` 라는 선언적 프로그래밍 모델을 실용적인 성능으로 성립시킨 것이다."**

즉 VDOM이 실제로 이기는 지점은 **"개발자가 직접 최적화하지 않아도 평균적으로 나쁘지 않은 성능이 나온다"** 는 것입니다.
"어떤 DOM을, 어떤 순서로, 어떤 조건에서 고칠지"를 사람이 관리하지 않아도 되고,
그 대가로 React가 diff 비용을 대신 지불하는 **트레이드오프**입니다.

---

## 결론

> Virtual DOM은 웹 표준이 아니라, **React 같은 라이브러리에서 자체적으로 만든 개념적인 최적화 기법**입니다.
> 브라우저가 지원하거나 명세한 것이 아니며, 다른 프레임워크는 **자체 구현 방식이 다릅니다.**


---

# Virtual DOM 구현 원리와 주요 프레임워크 비교

## 1. Virtual DOM이란?

> \*\*Virtual DOM(V-DOM)\*\*은 **실제 브라우저 DOM을 추상화한 메모리 상의 객체 트리 구조**입니다.
> 변경이 발생하면 이 객체 트리를 통해 "차이점(diff)"을 계산하고, 최소한의 DOM 조작만 실제 브라우저에 적용합니다.

---

## 2. Virtual DOM의 구현 원리

### 핵심 3단계

1. **렌더(Render)**
   컴포넌트 함수나 JSX를 기반으로 VDOM 트리 생성
   예:

   ```jsx
   const element = <h1>Hello</h1>;
   ```

   내부적으로는:

   ```js
   {
     type: 'h1',
     props: { children: 'Hello' }
   }
   ```

2. **디프(Diff)**
   변경 전의 Virtual DOM(old)과 변경 후의 Virtual DOM(new)을 비교
   → 어떤 노드가 변경되었는지 계산

   예:

   ```js
   old: <h1>Hello</h1>  
   new: <h1>Hi</h1>
   ```

   → 텍스트 노드 변경만 감지됨

3. **패치(Patch)**
   브라우저 실제 DOM에서 해당 변경만 적용 (innerText 등)

---

## 3. React의 Virtual DOM

* React는 **Fiber 구조**를 이용한 **비동기적 VDOM 비교 엔진**을 사용
* 주요 특징(= Reconciliation 휴리스틱):

  * 트리를 DFS 순회하며 **같은 깊이의 같은 위치끼리만** 비교 (노드가 트리 사이를 이동했는지는 탐색하지 않음)
  * **타입이 같으면** → 기존 노드와 그 state를 **유지**하고, 달라진 props만 갱신
  * **타입이 다르면** → 그 아래 **서브트리 전체를 폐기하고 새로 생성** → 자식들의 state와 DOM 노드도 전부 사라짐
  * **형제 목록은 `key`로 매칭** → 순서가 바뀌어도 key가 같으면 같은 요소로 보고 재사용 (key는 형제 사이에서만 유효)

> 두 트리의 최소 편집 거리를 구하는 일반 알고리즘은 **O(n³)** 입니다.
> React는 위의 두 가지 가정 — **① 타입이 다르면 비교를 포기한다 ② key로 형제를 식별한다** — 을 도입해
> 이를 **O(n)** 으로 낮춥니다.
> 즉 diff의 결과는 "이론적 최소 변경"이 아니라 **"충분히 좋은 근사치"** 이며,
> 이 가정이 어긋나면(예: 조건에 따라 `<div>`↔`<span>`으로 감싸는 태그를 바꾸면)
> 멀쩡한 서브트리가 통째로 재생성되어 **state가 초기화**됩니다.

```jsx
function App() {
  return <div><h1>Hello</h1></div>;
}
```

→ 내부적으로 React는 다음과 같은 VDOM 트리 생성:

```js
{
  type: 'div',
  props: {
    children: {
      type: 'h1',
      props: { children: 'Hello' }
    }
  }
}
```

* React는 이 구조를 기반으로 **reconciliation 알고리즘**으로 변경을 계산하고, 실제 DOM에 **batch update**를 적용

---

## 4. 다른 프레임워크들의 DOM 처리 방식

### O 1. Vue.js (v2, v3)

* Vue 2: React와 유사한 VDOM 구현
* Vue 3: **Proxy 기반 반응형 시스템(Reactivity)** + **VDOM**
* React와 유사하게 template → render function → VDOM → Patch

> `v-if`, `v-for` 등을 통해 노드 변경 시 diffing 처리

---

### O 2. Preact

* React의 **경량화 버전 (3kb)**
* 동일한 API (JSX 사용 가능), 내부 구현은 더 단순
* VDOM 사용 방식은 React와 거의 동일하지만, 일부 최적화 제거

> `h()`, `render()`, `diff()`, `commitRoot()` 함수로 구성된 간단한 아키텍처

---

### O 3. Inferno.js

* React 스타일이지만, **VDOM diffing이 매우 빠름**
* **컴파일 타임 힌트 + 최적화된 patch 알고리즘**
  → JSX를 컴파일할 때 각 VNode에 "자식이 정적인가 / key가 있는가" 같은 `flags`, `childFlags`를 미리 심어두고,
  런타임 diff에서는 해당 검사 자체를 건너뜀

> Inferno를 "JIT 컴파일"로 설명하는 자료가 많지만, 정확히는 **빌드 타임 최적화(babel-plugin-inferno)** 입니다.
> "10만 노드를 1ms에 diff" 같은 수치도 특정 벤치마크 조건의 값이므로 일반화해서 받아들이면 안 됩니다.

---

### O 4. Svelte

* X Virtual DOM 사용 안 함
* **Compile-time에 DOM 조작 코드를 생성**
* 예: `<h1>{message}</h1>` → `document.querySelector('h1').textContent = message;`

> 즉, **컴파일러가 DOM 변경 코드를 만들어내므로 diffing 자체가 없음**

장점: 성능 최상 / 단점: 동적 구조는 제약 있음

---

### O 5. Solid.js

* X Virtual DOM 사용 안 함
* **Fine-grained reactivity**: signal이 바뀐 곳만 직접 업데이트

예:

```js
const [count, setCount] = createSignal(0);
```

> `count()`가 쓰인 DOM만 업데이트됨 (정확한 대상만 정밀 추적)

→ Reactivity Graph로 추적

---

## 5. 비교 요약

| 프레임워크      | VDOM 사용 | 처리 방식                 | 특징                    |
| ---------- | ------- | --------------------- | --------------------- |
| React | O 예 | VDOM diff → patch | Fiber, JSX |
| Vue (v2/3) | O 예 | VDOM + reactivity | Proxy (v3) |
| Preact | O 예 | VDOM | React 호환, 경량 |
| Inferno | O 예 | VDOM 고속 diff | 빌드 타임 flags 최적화 |
| Svelte | X 아니요 | 컴파일 시 DOM 생성 | Virtual DOM 없음 |
| Solid.js | X 아니요 | signal 추적 → 직접 DOM 변경 | Fine-grained reactive |

---

## O 결론

* Virtual DOM은 표준이 아닌 **프레임워크 내부 구현 기법**이며, 목적은 "속도"보다 **선언적 모델의 실용화**
* 모든 프레임워크가 VDOM을 사용하는 것은 아님
* 최신 경향은 **런타임 diff를 컴파일 타임으로 옮기는 방향**(Svelte, Solid, Vue 3의 컴파일러 최적화)
* React 19 역시 같은 흐름 위에 있습니다. **React Compiler**(React 19에 내장된 기능이 아니라 별도 도입하는 opt-in 빌드 도구)가 `useMemo`/`useCallback`/`memo`를 자동으로 삽입해
  **"불필요한 재렌더 자체를 줄이는" 방식**으로 접근합니다.
  단, React는 VDOM을 버리는 것이 아니라 **VDOM을 만들기 전 단계를 컴파일러로 줄이는** 쪽을 택했습니다.

