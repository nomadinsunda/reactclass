
# 🔗 React Hook 개요: 클래스 없이 상태와 생명주기 관리하기

---

## 📌 Hook이란?

**Hook**은 React 16.8 버전에서 새롭게 도입된 기능입니다. 클래스(`class`)를 작성하지 않고도 **state**, **lifecycle**, **context** 같은 React의 핵심 기능들을 **함수형 컴포넌트(Function Component)** 안에서 사용할 수 있게 해줍니다.

Hook은 기존 클래스 컴포넌트의 기능을 함수형 방식으로 **“연결(hook into)”** 한다는 의미에서 이름 붙여졌으며, React의 **기능적 선언형 패러다임**을 한층 더 강화해주는 도구입니다.

---

## 🧱 Hook의 목적과 등장 배경

Hook은 다음과 같은 이유로 등장했습니다:

1. **클래스의 복잡성**
   `this`, 바인딩, 긴 생명주기 메서드 등 클래스 컴포넌트는 초보자에게 진입장벽이 높습니다.

2. **로직 재사용의 어려움**
   HOC, Render Props는 로직 재사용은 가능하지만 컴포넌트 구조가 복잡해지고 가독성이 떨어집니다.

3. **관심사의 분리 부족**
   하나의 생명주기 메서드에 여러 역할이 섞여 있어 코드 유지보수가 어렵습니다.

Hook은 이 문제들을 해결하면서도 하위 호환성을 유지합니다. 기존의 클래스 컴포넌트를 무조건 Hook으로 바꿀 필요는 없으며, **새로 만드는 컴포넌트부터 Hook을 적용하면 됩니다.**

---

## 🧪 간단한 예시: useState Hook

```jsx
import React, { useState } from 'react';

function Example() {
  const [count, setCount] = useState(0);

  return (
    <div>
      <p>You clicked {count} times</p>
      <button onClick={() => setCount(count + 1)}>Click me</button>
    </div>
  );
}
```

* `useState(0)`은 `count`라는 상태를 `0`으로 초기화하고,
* `setCount`는 상태를 업데이트하는 함수입니다.
* 이 두 값은 배열 구조 분해로 받아옵니다.

Hook은 **컴포넌트가 다시 렌더링되어도 상태를 유지**합니다. `this.setState`와 비슷한 역할을 하지만, **객체 병합 없이** 상태를 완전히 교체하는 방식입니다.

---

## 🌱 여러 개의 상태 선언도 가능

```jsx
function ExampleWithManyStates() {
  const [age, setAge] = useState(42);
  const [fruit, setFruit] = useState('banana');
  const [todos, setTodos] = useState([{ text: 'Learn Hooks' }]);
}
```

Hook은 **호출 순서에 의존**하므로 반복문, 조건문, 중첩 함수 안에서는 호출하면 안 됩니다. 이는 아래에서 설명할 **Hook의 사용 규칙**과도 관련됩니다.

---

## ⚡ useEffect: 부수 효과(side effects) 처리

Hook은 **라이프사이클 메서드(componentDidMount, componentDidUpdate, componentWillUnmount)** 도 대체할 수 있습니다. 이 역할을 하는 것이 바로 `useEffect`입니다.<br/>
**React에서 컴포넌트가 마운트된다는 것은** :
> **"해당 컴포넌트가 가상 DOM(Virtual DOM)에 생성되어 실제 DOM에 렌더링되는 과정"** 을 의미합니다.

```jsx
import React, { useState, useEffect } from 'react';

function Example() {
  const [count, setCount] = useState(0);

  useEffect(() => {
    document.title = `You clicked ${count} times`;
  });

  return (
    <div>
      <p>You clicked {count} times</p>
      <button onClick={() => setCount(count + 1)}>Click me</button>
    </div>
  );
}
```

* 이 코드는 매 렌더링 이후에 실행되며,
* DOM 업데이트가 끝난 후 `document.title`을 변경합니다.

`useEffect`는 **정리(cleanup)** 함수도 지원합니다. WebSocket 연결 및 해제:

```jsx
import { useEffect, useRef, useState } from 'react';

function ChatRoom({ roomId }) {
  const socketRef = useRef(null);
  const [messages, setMessages] = useState([]);

  useEffect(() => {
    // WebSocket 연결 설정
    const socket = new WebSocket(`wss://example.com/chat/${roomId}`);
    socketRef.current = socket;

    socket.addEventListener('message', (event) => {
      const message = JSON.parse(event.data);
      setMessages(prev => [...prev, message]);
    });

    socket.addEventListener('open', () => {
      console.log('웹소켓 연결됨');
    });

    socket.addEventListener('close', () => {
      console.log('웹소켓 종료됨');
    });

    // ✅ 정리(clean-up): 소켓 종료
    return () => {
      console.log('컴포넌트 언마운트 또는 roomId 변경 → 웹소켓 정리');
      socket.close();
    };
  }, [roomId]); // roomId가 바뀌면 기존 소켓 종료 → 새 연결

  return (
    <div>
      <h3>💬 채팅방 ({roomId})</h3>
      <ul>
        {messages.map((msg, i) => <li key={i}>{msg.text}</li>)}
      </ul>
    </div>
  );
}
```

---

## 📐 Hook 사용 규칙

Hook은 일반 함수처럼 보이지만, 두 가지 중요한 규칙을 반드시 따라야 합니다:

1. **컴포넌트 최상단에서만 Hook을 호출할 것**
   반복문, 조건문, 중첩 함수 안에서는 호출하면 안 됩니다.

2. **React 함수형 컴포넌트 또는 Custom Hook에서만 Hook을 호출할 것**
   일반 JavaScript 함수에서는 사용할 수 없습니다.

👉 이 규칙을 강제하고 실수를 방지하기 위해 React는 **eslint 플러그인**을 제공합니다.

---

## 🛠 Custom Hook: 나만의 Hook 만들기

Hook의 진정한 힘은 **상태 로직을 추출하여 재사용할 수 있다는 점**입니다. 다음은 친구의 온라인 상태를 추적하는 Custom Hook 예시입니다.

```jsx
function useFriendStatus(friendID) {
  const [isOnline, setIsOnline] = useState(null);

  useEffect(() => {
    function handleStatusChange(status) {
      setIsOnline(status.isOnline);
    }

    ChatAPI.subscribeToFriendStatus(friendID, handleStatusChange);
    return () => {
      ChatAPI.unsubscribeFromFriendStatus(friendID, handleStatusChange);
    };
  }, [friendID]);

  return isOnline;
}
```

이제 다음과 같이 여러 컴포넌트에서 재사용할 수 있습니다:

```jsx
function FriendStatus({ friend }) {
  const isOnline = useFriendStatus(friend.id);
  return isOnline ? 'Online' : 'Offline';
}
```

Custom Hook은 **기능이라기보단 관례(convention)** 입니다. `use`로 시작하고 내부에 다른 Hook을 사용하면 Custom Hook으로 간주됩니다.

---

## 🎛 기타 내장 Hook들

| Hook 이름                   | 설명                                 |
| ------------------------- | ---------------------------------- |
| `useContext`              | Context API를 함수형 컴포넌트에서 사용할 수 있게 함 |
| `useReducer`              | 복잡한 상태 관리를 reducer 함수로 처리할 수 있음    |
| `useRef`                  | DOM 요소 참조, 또는 상태와 무관한 값 저장         |
| `useMemo` / `useCallback` | 렌더링 최적화를 위한 메모이제이션 도구              |

---

## ✨ 마무리

React Hook은 단순한 API 추가가 아닙니다.
**React의 함수형 선언적 UI 철학을 구현한 핵심 진화 도구**입니다.

* 클래스 없이도 상태와 효과를 처리할 수 있게 되며,
* 복잡한 로직을 Custom Hook으로 깔끔하게 분리 가능하며,
* 코드 재사용성과 가독성이 획기적으로 향상됩니다.

Hook은 더 이상 선택이 아닌 **현대 React의 표준입니다.**

---

📚 더 자세한 문서는 다음에서 확인하세요:

* [Using the State Hook](https://react.dev/learn/state-a-components-memory)
* [Using the Effect Hook](https://react.dev/learn/synchronizing-with-effects)
* [Rules of Hooks](https://react.dev/learn/rules-of-hooks)
* [Building Your Own Hooks](https://react.dev/learn/reusing-logic-with-custom-hooks)

