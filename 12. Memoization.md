
# 🧠 Memoization (메모이제이션) 이란?

> **한 번 계산한 결과를 저장해두고**, **같은 입력값에 대해서는 다시 계산하지 않고 저장된 값을 반환**하는 기법입니다.

즉, **함수의 결과값을 캐싱(Caching)해서, 중복 계산을 피하는 것**이 핵심입니다.

---

## 🔁 비유로 이해하기

> 📦 계산 결과를 기억하는 “메모장”을 함수가 하나 들고 있다고 생각하세요.

```js
function add(a, b) {
  return a + b;
}
```

이 함수는 `add(1, 2)`를 100번 호출하면 100번 계산합니다.
하지만 메모이제이션을 적용하면?

```js
function memoizedAdd(a, b) {
  const key = `${a},${b}`;
  if (cache[key]) return cache[key];     // 💡 이미 계산된 값
  const result = a + b;
  cache[key] = result;                   // 💾 결과 저장
  return result;
}
```

✅ 이렇게 하면 **동일한 입력값이 들어올 때 재계산 없이 캐시에서 바로 반환**됩니다.

---

## 🧩 Memoization의 동작 원리

1. 입력값을 키(Key)로 만들어
2. 결과를 메모리에 저장 (예: `Map`, 객체, WeakMap 등)
3. 동일한 입력이 다시 들어오면 캐시된 결과를 반환

> Memoization = "기억(Memory) + 최적화(Optimization)"

---

## ✅ 어디서 사용될까?

| 분야       | 사용 예                                   |
| -------- | -------------------------------------- |
| 📚 알고리즘  | 피보나치 수열, 동적 계획법(DP)                    |
| ⚛️ React | `useMemo`, `useCallback`, `React.memo` |
| 🔢 계산    | 무거운 함수 (sort, filter, reduce 등) 캐싱     |
| 🌐 HTTP  | 동일 요청을 캐싱하는 프론트엔드/백엔드 로직               |

---

## 📈 예시: 계산이 비싼 피보나치 함수

```js
function fib(n) {
  if (n <= 1) return n;
  return fib(n - 1) + fib(n - 2);
}
```

위 함수는 매우 비효율적 → O(2ⁿ)

### 메모이제이션 적용

```js
const memo = {};
function fib(n) {
  if (n in memo) return memo[n];
  if (n <= 1) return n;
  return memo[n] = fib(n - 1) + fib(n - 2);
}
```

→ 성능: O(n)으로 극적인 개선
→ 이미 계산된 `fib(5)`는 `fib(7)`에서도 재사용됨

---

## 🧠 함수형 프로그래밍 관점

* 메모이제이션은 **pure function** (입력 → 항상 동일한 출력)일 때만 안전
* 입력 → 출력이 일관되지 않으면 (e.g. 랜덤, 시간, 네트워크), 메모이제이션은 사용하면 안 됨

---

## ⚠️ 주의할 점

| 주의 사항     | 설명                                  |
| --------- | ----------------------------------- |
| 메모리 누수    | 너무 많은 결과를 캐싱하면 메모리가 고갈될 수 있음        |
| 참조 타입 키   | 객체/배열은 키로 사용하면 `===`이 다르기 때문에 캐시 실패 |
| 부작용 있는 함수 | I/O, API 호출 등은 메모이제이션 대상 ❌          |

---

## 🔍 정리

| 항목    | 설명                                                                |
| ----- | ----------------------------------------------------------------- |
| 정의    | 동일한 입력에 대해 이전 계산 결과를 저장하고 재사용                                     |
| 장점    | 성능 개선, 중복 연산 방지                                                   |
| 단점    | 메모리 사용 증가, 잘못된 캐시 관리 시 버그 가능성                                     |
| 관련 기술 | `useMemo`, `memoizeOne`, `lodash.memoize`, `React.memo`, 동적 계획법 등 |

---

## ✨ 마무리

* 메모이제이션은 "시간"과 "공간"을 **트레이드오프**하는 기술입니다.
* React에서도 중요하지만, 알고리즘 최적화나 네트워크 캐싱 등 **컴퓨팅 전반**에 걸쳐 널리 사용됩니다.

