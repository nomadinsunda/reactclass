# **Hook 메모리 슬롯이란?**

 **React가 함수형 컴포넌트의 각 Hook(useState, useEffect 등)을 저장하기 위해
Fiber 노드 안에 만들어 놓은 “연결 리스트 형태의 저장 공간(slot)”입니다.**

즉, 컴포넌트 안에서 Hooks를 **순서대로 호출한 그대로** React가 기록해두는 저장소입니다.

---

## 왜 이런 저장소가 필요한가?

함수형 컴포넌트는 **매 렌더링마다 함수가 다시 실행**됩니다.

```jsx
function Counter() {
  const [count, setCount] = useState(0);  // ← 이 값은 함수가 재실행되어도 유지돼야 함
}
```

문제:

* 일반 함수라면 변수가 유지되지 않음
* Hook은 함수 내부에 있으니 함수 실행이 끝나면 사라져야 함
* 그런데 React는 이 상태를 **계속 유지해야** 함

 그래서 React는 “컴포넌트 외부”에 상태를 저장하는 영역을 만듭니다.
그게 바로 **Hook 메모리 슬롯**입니다.

O Hook 메모리 슬롯은 “컴포넌트 인스턴스(Fiber)” 당 하나씩 만들어집니다.

즉,

 컴포넌트 하나가 렌더링될 때, 그 컴포넌트 전용의 Hook 저장 슬롯(Fiber.memoizedState)이 생성됩니다.
 이 슬롯은 그 컴포넌트가 살아 있는 동안 유지되며, 리렌더될 때마다 재사용됩니다.

---

## Hook 메모리 슬롯의 실제 구조

React는 각 컴포넌트를 Fiber node로 관리합니다.

```
FiberNode {
  memoizedState: Hook | null
}
```

이 memoizedState 아래에 Hook들이 **연결 리스트(Linked List)** 로 저장됩니다.
> memoizedState = 해당 컴포넌트의 Hook 메모리 슬롯(List)의 시작점(head)
> React는 함수형 컴포넌트를 렌더링할 때, Hook 데이터를
> Fiber.memoizedState에 연결 리스트 형태로 저장합니다.

예를 들어:

```jsx
function App() {
  const [a, setA] = useState(0);        // Hook Slot 1
  const [b, setB] = useState(0);        // Hook Slot 2
  useEffect(() => {...}, []);           // Hook Slot 3
}
```

React 내부 구조는 다음과 같은 형태:

```
Fiber.memoizedState → Hook1 → Hook2 → Hook3 → null
```

각 Hook 노드는 이런 형태입니다:

```
Hook {
  memoizedState: any,               // useState 값, useEffect의 effect 객체, useRef의 ref 객체 등
  baseState: any,                   // 아직 처리되지 않은 업데이트의 기준 상태
  baseQueue: Update | null,         // 우선순위 때문에 건너뛴 업데이트들
  queue: UpdateQueue | null,        // setState로 쌓인 업데이트 큐
  next: Hook | null                 // 다음 Hook
}
```

즉, 컴포넌트 하나마다…

 “Hook들이 순서대로 저장된 Linked List”를 React가 가지고 있습니다.
 이 저장소 하나하나를 **Hook 메모리 슬롯**이라고 합니다.

---

## mount와 update는 **서로 다른 dispatcher**가 처리합니다

Hook 함수(`useState` 등)는 사실 **껍데기**입니다.
`useState`는 “지금 활성화된 dispatcher의 `useState`를 대신 불러 주는” 함수일 뿐입니다.

```
React 내부: ReactCurrentDispatcher.current.useState(initialState)
```

React는 컴포넌트를 호출하기 **직전에** 이 `current`를 상황에 맞게 바꿔 끼웁니다.

| 상황                       | 활성 dispatcher              | `useState`가 하는 일                                      |
| ------------------------ | -------------------------- | ----------------------------------------------------- |
| **첫 렌더 (mount)**         | `HooksDispatcherOnMount`   | 새 Hook 노드를 만들어 리스트 **끝에 append**, `initialState`를 저장   |
| **재렌더 (update)**         | `HooksDispatcherOnUpdate`  | 이전(current) 리스트의 **다음 노드를 복사**해 오고, queue를 처리해 새 값 계산 |
| 렌더 중 setState (re-render) | `HooksDispatcherOnRerender` | 같은 렌더 안에서 큐를 즉시 재계산                                    |
| **렌더링 밖**                | `ContextOnlyDispatcher`    | 무조건 `Invalid hook call` 오류를 던짐                         |

여기서 두 가지 중요한 사실이 나옵니다.

1. **`useState`는 “처음인지 아닌지”를 스스로 판단하지 않습니다.**
   React가 dispatcher를 갈아 끼워서 결정하며, 그래서 mount 경로에는 `if (첫 렌더)` 같은 분기가 없습니다.
2. **렌더링 밖에서 Hook을 부르면 `ContextOnlyDispatcher`에 걸립니다.**
   “Hook은 React 함수 안에서만 호출” 규칙이 강제되는 지점이 바로 여기입니다.

### Fiber는 두 벌(더블 버퍼링)입니다

```
current Fiber (화면에 반영된 것)      workInProgress Fiber (지금 계산 중인 것)
  memoizedState → H1 → H2 → H3         memoizedState → H1' → H2' → H3'
                    ▲                                    │
                    └──────── 복사(clone) ────────────────┘
```

* update 렌더에서 React는 `currentHook`(이전 리스트)을 따라가며 노드를 하나씩 복제해
  `workInProgressHook` 리스트를 새로 만듭니다.
* 커밋이 끝나면 두 트리의 역할이 뒤바뀝니다(`current` ↔ `workInProgress`).
* 이 “한 칸씩 따라가며 복제”가 바로 **순서 의존성의 실체**입니다.
  이번 렌더의 n번째 호출은 무조건 이전 렌더의 n번째 노드와 짝지어집니다.

---

# 렌더링 시 React는 이렇게 동작합니다:

1. 컴포넌트를 렌더링하며 Hooks를 순서대로 호출함
2. 각 호출 시 React는 다음 Hook 슬롯으로 이동
3. 이전 렌더에서 저장해둔 값(`memoizedState`)을 가져옴
4. 업데이트 큐(queue)에 있는 pending update를 적용해 새 값 계산
5. 다음 Hook 슬롯으로 이동

이때 “순서”가 매우 중요합니다.

---

# 왜 Hooks는 조건문 안에서 호출하면 안 되나?

이유는 하나입니다.

### Hook 메모리 슬롯은 “순서” 기반으로 매칭되기 때문입니다.

예시(잘못된 코드):

```jsx
if (login) {
  const [a, setA] = useState(0); // ← Hook Slot 1 이었다가
}
const [b, setB] = useState(0);   // ← Hook Slot 2
```

다음 렌더링에서 `login=false`라면:

```
1차 렌더(login=true) :  a → Slot1,  b → Slot2
2차 렌더(login=false):  b → Slot1  ← b가 a의 값/큐를 읽어버림
                        Slot2는 짝이 없음
```

이때 나타나는 결과는 **두 가지**이며, 더 무서운 쪽은 두 번째입니다.

1. **호출 개수가 줄어든 경우** → React가 감지해 즉시 오류를 던집니다.

   ```
   Error: Rendered fewer hooks than expected.
          This may be caused by an accidental early return statement.
   ```

2. **개수는 같은데 종류만 뒤바뀐 경우** → **오류 없이 조용히 잘못된 슬롯을 읽습니다.**

   ```jsx
   if (login) {
     const [a] = useState(0);   // Slot1
   } else {
     const ref = useRef(null);  // 역시 Slot1 → 이전 렌더의 state 값이 ref로 둔갑
   }
   ```

   개발 모드에서는 “Rendered more hooks than…”·“previous render … next render” 형태의
   경고가 나올 수 있지만, 프로덕션에서는 **경고 없이 값만 엉키는** 최악의 버그가 됩니다.

React는 Hook을 **이름이 아니라 “실행된 순서”로만** 식별합니다.
변수명이 `a`인지 `b`인지, `useState`인지 `useRef`인지는 매칭에 아무 영향을 주지 않습니다.
조건문·반복문·early `return` 뒤에 Hook이 들어가면 이 순서가 깨져 매칭이 불가능해집니다.

### O 올바른 해법: Hook은 최상위, 조건은 Hook “안”으로

```jsx
// ❌ Hook 자체를 조건부로
if (login) { const [a, setA] = useState(0); }

// ✅ Hook은 항상 호출하고, 조건은 값/effect 내부로 옮긴다
const [a, setA] = useState(0);
useEffect(() => {
  if (!login) return;   // 조건은 effect 내부에서
  fetchProfile();
}, [login]);
```

* 조건에 따라 컴포넌트가 완전히 달라져야 한다면,
  Hook을 조건부로 만들지 말고 **컴포넌트 자체를 둘로 분리**해 조건부로 렌더하세요.

---

# Hook 메모리 슬롯을 아주 간단히 비유하면

### “React가 Hook들을 **호출 순서대로 한 줄로 꿴 사슬**에 저장해두고,

다음 렌더링 때도 그 사슬을 **처음부터 한 칸씩** 따라가며 다시 사용하는 것”

> **“배열”이 아니라 “연결 리스트”입니다.**
> 많은 설명이 이해를 돕기 위해 `hooks[0]`, `hooks[1]` 같은 배열 비유를 쓰지만,
> 실제 React 구현은 `next` 포인터로 이어진 **단방향 연결 리스트**입니다. 차이는 실질적입니다.
>
> * **인덱스로 건너뛸 수 없습니다.** n번째 Hook에 닿으려면 head부터 n번 `next`를 따라가야 합니다.
>   그래서 React는 “현재 위치 포인터(`workInProgressHook`)”를 들고 **호출될 때마다 한 칸씩 전진**합니다.
> * 이 포인터는 **호출이 일어날 때만** 전진합니다.
>   조건문 때문에 호출이 하나 빠지면 포인터가 덜 전진해, 그 뒤의 모든 Hook이 **한 칸씩 밀려** 잘못된 노드와 짝지어집니다.
> * 길이를 미리 알 수 없고 “몇 개짜리 배열”을 예약하지도 않습니다.
>   mount 때 호출되는 만큼 노드가 **append**되고, 그 길이가 그대로 이후 렌더의 계약이 됩니다.

그래서 다음이 성립합니다:

* useState를 3번 쓰면 Hook 슬롯도 3개
* useEffect를 쓰면 그 다음 슬롯에 effect 정보 저장
* useRef도 슬롯 하나 사용

즉:

 “컴포넌트를 렌더링할 때 React가 Hook 호출 순서를 따라가며
정해진 슬롯에 상태/효과 정보를 저장하고 재사용하는 메커니즘”

---

# 최종 요약

### O Hook 메모리 슬롯이란?

**React Fiber가 함수형 컴포넌트의 상태·effect 등을 저장하는
hook 전용 저장 공간이며, Hook 호출 순서대로 연결 리스트 형태로 유지되는 구조.**

### O 왜 중요한가?

* Hook을 어떤 순서로 호출했는지 기억
* 다음 렌더링에도 같은 Hook과 매칭
* Hook의 규칙(조건문 금지)이 이 구조 때문에 강제됨

### O 어떤 Hook이든 모두 이 슬롯에 저장된다

| Hook          | 슬롯(`hook.memoizedState`)에 저장되는 것            |
| ------------- | ------------------------------------------- |
| `useState`    | 현재 state 값 + `queue`(updater 큐)             |
| `useReducer`  | 현재 state 값 + `queue`(dispatch된 action)      |
| `useEffect`   | effect 객체 `{ create, destroy(cleanup), deps, tag }` |
| `useMemo`     | `[계산된 값, deps]` 튜플                          |
| `useCallback` | `[함수 참조, deps]` 튜플                          |
| `useRef`      | `{ current }` ref 객체 (한 번 만들고 계속 재사용)       |
| `useContext`  | **슬롯을 쓰지 않음** — Fiber의 의존성 목록에 등록됨          |

* `useMemo`/`useCallback`이 “이전 deps”를 기억할 수 있는 이유가 바로 이 슬롯입니다.
* `useRef`가 렌더 간에 같은 객체를 유지하는 것도, 슬롯에 만들어 둔 객체를 그대로 돌려주기 때문입니다.

### O 이 규칙은 도구가 강제한다

* **`eslint-plugin-react-hooks`의 `rules-of-hooks`** 가 조건부/반복문/일반 함수 안의 Hook 호출을 정적으로 잡아냅니다.
  런타임 오류가 “값이 엉킨 뒤”에야 드러나는 것과 달리, 린트는 **작성 시점에** 막아 줍니다.
* **React Compiler**도 이 규칙 준수를 전제로 자동 메모이제이션을 수행하며,
  규칙을 위반한 컴포넌트는 최적화 대상에서 제외합니다.

> **예외 하나**: React 19의 `use(...)`는 Hook이 아니라 API여서
> 조건문·반복문 안에서도 호출할 수 있습니다. 슬롯 순서에 의존하지 않는 방식으로 구현되었기 때문입니다.
> 하지만 `use` 역시 **컴포넌트/Hook 본문 안에서만** 호출해야 한다는 제약은 그대로입니다.


