
## O 1. 인터페이스란?

### 일반 의미:

> 어떤 시스템이 외부와 **소통하는 방식** 또는 **약속된 사용법**을 말함.

---

## O 2. Web API는 브라우저가 제공하지만, **JS 코드로 호출 가능해야 한다**

* 브라우저는 내부적으로 복잡한 C++ 기반 엔진(Blink 등)으로 DOM을 구현합니다.
* 하지만 개발자는 JavaScript로 이렇게 쉽게 호출하죠:

```js
const el = document.createElement("div");
```

### 이게 가능하려면?

→ 브라우저는 `document` 객체를 **JavaScript에서 접근 가능하게 만들고**,
그 객체에 `.createElement()`라는 함수를 붙여줘야 함.

→ 이를 JavaScript 입장에서 보면:

> document는 브라우저가 제공하는 Document 인터페이스의 인스턴스이며,
> 이 인터페이스는 DOM 표준에 따라 다양한 메서드(createElement, querySelector 등)를 제공합니다.
> JavaScript에서는 해당 객체를 통해 DOM을 제어할 수 있도록 접근 권한이 열려 있습니다.



---

## O 3. JavaScript 인터페이스란?

> 브라우저가 내부적으로 만든 객체(document, window 등)에 대해
> **JavaScript에서 접근하고 조작할 수 있도록 제공되는 메서드와 속성의 집합**입니다.

즉,

* `document.createElement()`
* `window.alert()`
* `navigator.geolocation.getCurrentPosition()`

이 모든 것들은 **브라우저 내부 객체**이지만,
**JavaScript 코드에서 접근 가능**하므로
→ **JavaScript 인터페이스**라고 부릅니다.

---

## O 표준에서 쓰는 정확한 용어는 "Web IDL 인터페이스"입니다

 먼저 짚고 갈 점: **"JavaScript 인터페이스"는 W3C/WHATWG의 공식 용어가 아닙니다.**
설명을 위해 흔히 쓰는 관용 표현일 뿐이고, 표준 문서가 실제로 사용하는 용어는 **Web IDL(Interface Definition Language) 인터페이스**입니다.

WHATWG/W3C 명세는 DOM API를 **언어 중립적인 IDL**로 먼저 정의합니다:

```webidl
[Exposed=Window]
interface Document : Node {
  [NewObject] Element createElement(DOMString localName);
  Element? querySelector(DOMString selectors);
  readonly attribute Element? documentElement;
};
```

그리고 **Web IDL 명세의 "ECMAScript Binding"** 장이, 이 IDL을 자바스크립트 세계로 번역하는 규칙을 정합니다.

* `interface Document` → 전역에 `Document`라는 **생성자 함수**와 `Document.prototype` 생성
* `Element createElement(...)` → `Document.prototype.createElement` **메서드**로 노출
* `readonly attribute` → `Document.prototype`의 **getter 접근자 프로퍼티**로 노출
* `DOMString`, `long` 등 IDL 타입 → JS 값으로 변환하는 규칙까지 명세가 규정
* `[Exposed=Window]` → 이 인터페이스가 **어느 전역에 노출될지** 지정 (`Worker`에는 `document`가 없는 이유)

> 정리하면: **호스트 환경이 IDL로 정의된 인터페이스를 구현하고,
> Web IDL의 ECMAScript 바인딩 규칙에 따라 JS 객체로 노출한 것**이 우리가 쓰는 `document`, `window`입니다.

---

## O 그래서 DOM 객체도 그냥 "프로토타입 체인"입니다

인터페이스 상속은 마법이 아니라 **프로토타입 체인**으로 구현되어 있습니다.

```js
document instanceof Document;   // true
document instanceof Node;       // true
document instanceof EventTarget;// true

const div = document.createElement('div');
// HTMLDivElement → HTMLElement → Element → Node → EventTarget → Object
Object.getPrototypeOf(div) === HTMLDivElement.prototype; // true

// createElement는 document 자신의 프로퍼티가 아니다
document.hasOwnProperty('createElement');            // false
Document.prototype.hasOwnProperty('createElement');  // true
```

* `addEventListener`가 거의 모든 DOM 객체에서 동작하는 이유 = 체인 최상단의 **`EventTarget`** 에 정의되어 있기 때문
* 실제 동작은 C++이지만, **JS에서 관찰되는 형태는 평범한 프로토타입 메서드**입니다 (이를 native function이라 부름)

---

## 구분 예시

| 항목    | JavaScript 내장 객체          | JavaScript 인터페이스 (Web API)                    |
| ----- | ------------------------- | --------------------------------------------- |
| 정의 주체 | ECMAScript (JS 자체)        | 브라우저 (Web Platform)                           |
| 예시    | `Math`, `Date`, `Promise` | `document`, `window`, `fetch`, `localStorage` |
| 실행 환경 | JS 엔진이면 어디서든              | 브라우저 or 호환 환경에서만                              |
| 타입    | JS 표준 타입                  | 브라우저 확장 객체                                    |
| 사용 목적 | 계산, 날짜 처리 등               | DOM 조작, 네트워크 요청 등                             |

---

## O 한 줄 요약

> (관용적으로 부르는) JavaScript 인터페이스란, 웹 브라우저가 미리 만들어 제공하는 객체들로, 자바스크립트 코드가 브라우저의 기능(DOM, 네트워크, 화면 등)에 접근하고 제어할 수 있도록 해주는 창구입니다
> → `document`, `window`, `navigator`, `fetch` 등이 대표적 예시입니다.
> → 표준 용어로는 **Web IDL 인터페이스 + ECMAScript 바인딩**이라고 부릅니다.

