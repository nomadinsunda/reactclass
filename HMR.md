# HMR(Hot Module Replacement)이란?

> **코드를 수정했을 때, 페이지 전체를 새로고침하지 않고
> 변경된 모듈(파일)만 교체하여 즉시 반영하는 개발 기능입니다.**

즉, **브라우저를 새로고침(refresh)하지 않고 실시간으로 UI를 업데이트**하는 기술입니다.

---

# 왜 HMR이 필요한가?

### O 기존 방식: 전체 새로고침(Full Reload)

* HTML 전체 다시 로딩
* JS 전체 다시 실행
* 앱 상태(state) 초기화
  (React 앱이면 컴포넌트 상태가 모두 초기화됩니다)

### O HMR 방식: 필요한 것만 교체

* 수정된 모듈만 업데이트
* 브라우저 새로고침 없음
* React 컴포넌트 상태 유지
* 매우 빠른 피드백 루프

---

# 어떻게 작동하는가? (핵심 흐름)

HMR은 다음 4가지 요소가 협력하여 동작합니다.

---

## ① 개발 서버(dev server)

Webpack Dev Server, Vite, Parcel 등이 변경된 파일을 감지합니다.

---

## ② HMR WebSocket 연결

브라우저는 dev server와 WebSocket으로 연결되어 있습니다.

* 파일 변경 감지 → dev server가 WebSocket 메시지 전송
* 브라우저가 "갱신된 모듈" 정보를 받음

---

## ③ 수정된 모듈만 다시 컴파일

* **Webpack 계열**: 기존 번들 전체가 아니라 변경분만 **부분 번들(partial bundle)** 로 다시 만들어 전달합니다.
* **Vite**: 번들을 만들지 않고, 변경된 **모듈 파일 하나만 변환**해서 그대로 응답합니다. 브라우저가 그 URL만 다시 `import` 합니다.

---

## ④ 런타임에서 해당 모듈을 교체

브라우저에 로딩된 JS 모듈 중 단 하나만 바꿉니다.

React라면 다음처럼 됩니다:

* 변경된 컴포넌트만 교체
* 컴포넌트 상태(State) 유지
* 앱 전체 렌더링 불필요

이것이 **Fast Refresh**입니다. (React 팀이 만든 HMR 개선 기술)

---

# HMR 동작 구조(아주 중요한 개념)

### O Full Reload

→ HTML 전체 재호출
→ JS 전체 로딩
→ React state 초기화

### O HMR (Hot Module Replacement)

→ 변경된 모듈만 교체
→ React state 유지
→ 새로고침 없음

### O Fast Refresh (React 전용)

→ 변경된 컴포넌트만 빠르게 재로드
→ React Hook 상태 유지 (예: useState, useEffect)

> **Fast Refresh는 "HMR보다 상위 기술"이 아니라 "React 전용 HMR 핸들러"입니다.**
> HMR은 "모듈을 갈아끼울 수 있다"는 **일반적인 메커니즘**만 제공하고,
> 갈아끼운 뒤 **무엇을 어떻게 복구할지는 프레임워크가 직접 정의**해야 합니다.
> React가 그 자리에 등록한 구현이 `react-refresh`(Fast Refresh)이고, Vue는 vue-loader/plugin-vue가 같은 일을 합니다.
> 즉 관계는 **HMR(그릇) ⊃ Fast Refresh(React용 내용물)** 입니다.

---

## Fast Refresh가 상태를 유지하지 **못하는** 경우

강의 중 "왜 갑자기 state가 초기화되죠?"라는 질문이 나오는 지점입니다. 규칙은 단순합니다.
**react-refresh는 "이 파일은 React 컴포넌트만 들어 있다"고 확신할 때만** 상태를 지킵니다.

| 상황                                        | 결과                                                        |
| ----------------------------------------- | --------------------------------------------------------- |
| 파일이 컴포넌트 외의 것(상수, 유틸 함수 등)도 함께 export     | **전체 새로고침(full reload)** → 상태 날아감                          |
| 컴포넌트 이름이 소문자로 시작 (`function myApp()`)      | 컴포넌트로 인식하지 못함 → 상태 날아감                                     |
| `export default () => {...}` (익명 함수)      | 이름이 없어 추적 불가 → 상태 날아감. **기명 함수로 선언 후 export** 할 것          |
| 파일 안의 **Hook 호출 순서나 개수**를 바꿈              | 기존 상태를 새 구조에 맞출 수 없어 **의도적으로 상태를 리셋**                      |
| 커스텀 Hook 파일만 수정                           | 그 Hook을 쓰는 컴포넌트까지 갱신되지만 상태는 초기화될 수 있음                      |

```jsx
// ❌ 저장할 때마다 전체 새로고침 (컴포넌트가 아닌 값을 같이 export)
export const MAX_COUNT = 10;
export default function Counter() { ... }

// ✅ 상수는 별도 파일로 분리 → 상태 유지되는 Fast Refresh 동작
export default function Counter() { ... }
```

> 그래서 실무 규칙이 **"한 파일에는 컴포넌트 하나만"** 입니다.
> 코드 스타일 취향이 아니라, **Fast Refresh가 동작하기 위한 조건**입니다.

---

# 왜 HMR 비용이 번들 단위에서 느려질까?

앞선 "전통적인 번들러의 문제점" 표에 나온 다음 항목을 풀어서 설명합니다.

> **HMR 비용 = 번들 단위로 핫 리로드 → 의존성 체인이 길수록 느림**

이 이유는 다음과 같습니다.

---

## Webpack(전통 번들러)의 경우

Webpack은 "하나의 거대한 번들"을 기반으로 실행됩니다.

1. 어떤 파일이 변경되면
2. 해당 파일이 속한 번들을 다시 빌드해야 함
3. 그 번들이 다른 모듈에 연결되어 있으면
4. 의존성 체인을 따라 재빌드됨
5. 브라우저는 번들 단위로 모듈을 교체함

→ 모듈 간 의존성이 깊고 많을수록 "HMR 비용"이 커집니다.
→ HMR이 느려지는 이유입니다.

**Webpack의 구조적 한계입니다.**

---

## Vite가 빠른 이유는?

Vite는 **개발 단계에서 소스 코드를 번들링하지 않고**,
**브라우저의 ES Module import** 를 그대로 활용합니다.

→ 모듈(파일) 단위로 HMR이 가능
→ 다시 만들어야 할 범위가 "변경된 모듈 + 그 HMR 경계까지"로 한정됨
→ 변한 파일만 즉시 다시 전송
→ **프로젝트가 커져도 HMR 시간이 거의 늘지 않음**

그래서 Vite의 HMR은 Webpack보다 압도적으로 빠릅니다.

> **주의: "Vite는 번들링을 아예 안 한다"는 절반만 맞습니다.**
> 번들링하지 않는 것은 **내가 작성한 소스 코드**뿐이고, `node_modules`의 의존성은 개발 서버 기동 시 한 번 **사전 번들링(pre-bundling)** 합니다.
> 그래서 `react`, `react-dom`을 수정할 일이 없는 한 이 부분은 HMR 비용에 포함되지 않습니다.

### Vite HMR이 빠른 진짜 이유 (한 문장)

Webpack의 HMR 비용은 **프로젝트 전체 크기에 비례**하지만,
Vite의 HMR 비용은 **변경된 모듈의 영향 범위에만 비례**합니다.
그래서 파일이 10개든 10,000개든 저장 후 반영 시간이 비슷합니다.

### 직접 HMR을 다루고 싶다면: `import.meta.hot`

Vite는 표준 HMR API를 노출합니다. React를 쓸 때는 `@vitejs/plugin-react`가 이 API를 알아서 호출해 주므로 직접 쓸 일이 거의 없지만, 원리를 확인할 때 유용합니다.

```js
if (import.meta.hot) {
  // 이 모듈이 교체될 때 실행 — 타이머/구독 해제 등 뒷정리
  import.meta.hot.dispose(() => {
    clearInterval(timer)
  })
  // 교체를 이 모듈에서 받아 처리하겠다고 선언
  import.meta.hot.accept((newModule) => {
    console.log('갱신됨', newModule)
  })
}
```

* `accept`를 선언한 모듈이 **HMR 경계**가 됩니다. 경계를 만나지 못하고 진입점까지 거슬러 올라가면 그때 **전체 새로고침**이 발생합니다.
* 이 코드는 **개발 모드에서만 살아 있고**, `vite build` 시 `import.meta.hot`이 `undefined`로 치환되어 통째로 제거됩니다.

---

# 핵심 정리

| 용어                 | 의미                           |
| ------------------ | ---------------------------- |
| **HMR**            | 변경된 모듈만 교체하여 새로고침 없이 개발하는 기능 |
| **Webpack HMR 비용** | 번들 단위로 재생성 → 의존성이 많으면 느려짐    |
| **Vite HMR 비용**    | 파일 단위 업데이트 → 매우 빠름           |
| **Fast Refresh**   | React에서 상태 유지한 채 HMR 지원하는 기술 |

---

# 한 문장 요약

> **HMR은 페이지 전체를 새로고침하지 않고 변경된 모듈만 교체하는 기술이며, 번들 구조(Webpack)에서는 느려지고, 파일 단위 모듈 구조(Vite)에서는 매우 빠릅니다.**


