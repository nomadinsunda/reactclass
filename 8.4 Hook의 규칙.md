# 🧩 React Hook : **Hook의 규칙 (Rules of Hooks)**

---

## 🎯 Hook은 왜 특별할까?

React의 **Hook**은 단순한 함수처럼 보이지만, 실제로는 **렌더링 사이클과 내부적으로 긴밀히 연결된 API**입니다.
이러한 특성 때문에 **"Hook은 반드시 지켜야 할 규칙"이 존재**하며, 이를 어기면 앱이 예측 불가능하게 동작할 수 있습니다.

React 팀은 이를 **"Hook의 규칙(Rules of Hooks)"** 이라고 부르며, 실제로 ESLint 플러그인을 통해 이 규칙들을 코드 수준에서 검증할 수 있게 해주고 있습니다.

---

## 🔐 Hook의 두 가지 핵심 규칙

### ✅ 1. 최상위에서만 Hook을 호출하세요

**반복문, 조건문, 중첩 함수 내부에서는 절대 호출하지 마세요.**

```jsx
// ❌ 잘못된 예시
if (someCondition) {
  useEffect(() => { console.log('실행 안 될 수도 있음'); });
}

// ✅ 올바른 예시
useEffect(() => {
  if (someCondition) {
    console.log('이 안에서 조건 분기');
  }
});
```

> 이유: Hook은 호출 **순서**에 따라 **state** 를 연결합니다.
> 조건부로 Hook을 호출하면 다음 Hook들이 **엇갈려 연결**되어 버그가 발생합니다.

---

### ✅ 2. 오직 React 함수 안에서만 Hook을 호출하세요

Hook은 다음의 **두 가지 컨텍스트** 안에서만 사용해야 합니다:

| 위치          | 설명                                           |
| ----------- | -------------------------------------------- |
| 함수 컴포넌트     | `function Component() { useEffect(...) }`    |
| Custom Hook | `function useSomething() { useState(...); }` |

**일반 JavaScript 함수에서는 절대 호출하지 마세요!**

```js
// ❌ 일반 함수 내부에서 Hook 사용 → 오류 발생
function notAComponent() {
  useState(); // ❌
}
```

---

## 🛡️ ESLint로 규칙을 강제하자

React 팀은 공식적으로 **`eslint-plugin-react-hooks`** 를 제공합니다.

### 📦 설치

```bash
npm install eslint-plugin-react-hooks --save-dev
```

### ⚙️ `.eslintrc` 설정 예시

```json
{
  "plugins": ["react-hooks"],
  "rules": {
    "react-hooks/rules-of-hooks": "error",        // 규칙 위반 시 오류
    "react-hooks/exhaustive-deps": "warn"         // 의존성 배열 누락 경고
  }
}
```

> 이 플러그인은 **Create React App(CRA)** 에 기본 탑재되어 있습니다.

---

## 🔍 Hook은 순서에 의존한다

React는 렌더링할 때 Hook의 **"호출 순서"** 로 state를 추적합니다.

### 예시:

```jsx
function Form() {
  const [name, setName] = useState('Mary');              // ①
  useEffect(() => { localStorage.setItem('name', name); }); // ②
  const [surname, setSurname] = useState('Poppins');     // ③
  useEffect(() => { document.title = name + ' ' + surname; }); // ④
}
```

React는 다음과 같이 처리합니다:

#### ✔️ 첫 번째 렌더링

| 순서 | Hook                     |
| -- | ------------------------ |
| ①  | `useState('Mary')`       |
| ②  | `useEffect(persistForm)` |
| ③  | `useState('Poppins')`    |
| ④  | `useEffect(updateTitle)` |

#### ✔️ 두 번째 렌더링

* **순서만 같다면** 아규먼트트는 무시하고 기존 state에 연결됩니다.

---

## 🚨 규칙 위반 시 일어나는 일

아래와 같은 코드를 보면 어떤 문제가 생길까요?

```jsx
if (name !== '') {
  useEffect(() => {
    localStorage.setItem('formData', name);
  });
}
```

* 첫 렌더링: `name !== ''` → Hook 실행됨
* 다음 렌더링: `name === ''` → Hook이 **건너뛰어짐**
* 그 다음 Hook들의 **순서가 틀어짐 → state 연결이 붕괴됨**

결과적으로 다음과 같은 버그가 발생:

| 의도한 state  | 실제 연결된 state      |
| ------- | -------------- |
| name    | surname에 연결됨   |
| surname | effect에 연결됨    |
| effect  | 이상하게 동작하거나 무시됨 |

---

## 💡 해결 방법

Hook 호출은 무조건 실행하되, **조건은 내부에서 처리**하세요:

```jsx
useEffect(() => {
  if (name !== '') {
    localStorage.setItem('formData', name);
  }
}, [name]);
```

---

## ✨ 요약

| 규칙                  | 설명                          |
| ------------------- | --------------------------- |
| 🔺 최상위에서만 Hook 호출   | 조건문, 반복문, 중첩 함수 ❌           |
| 🔺 React 함수 안에서만 호출 | 컴포넌트 or Custom Hook 내에서만 사용 |
| 🔺 ESLint 플러그인 추천   | 실수 방지 및 유지보수 향상             |

---

## ✅ 마무리

React Hook은 **렌더링과 state의 일관성**을 보장하기 위해 **특별한 호출 규칙**을 따릅니다.
처음엔 제약처럼 느껴질 수 있지만, 이는 React가 성능과 일관성을 유지하도록 도와주는 **설계적 선택**입니다.

