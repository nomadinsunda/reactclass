

## 🧠 왜 `forwardRef`가 필요한가?

### ✅ 기본 `ref` 사용 (DOM 요소에 직접 연결)

```jsx
const inputRef = useRef();
<input ref={inputRef} />;
inputRef.current.focus();
```

→ 잘 작동하죠. 하지만 **커스텀 컴포넌트**로 감싸는 순간?

```jsx
// MyInput.jsx
function MyInput({input}) {
  return <input />;
}
```

```jsx
// App.jsx
import { useRef } from 'react';
import MyInput from './MyInput';

export default function App() {
  const inputRef = useRef();

  return (
    <>
      <MyInput ref={inputRef} /> {/* ❌ 작동하지 않음 */}
      <button onClick={() => inputRef.current.focus()}>포커스</button>
    </>
  );
}


```

### ❌ 왜 안 될까?

함수형 컴포넌트는 React 내부적으로 `ref`를 일반 `props`로 넘기지 않기 때문입니다. 즉, `ref`는 특별한 reserved prop이며, **React는 이를 `DOM` 또는 `class 컴포넌트`에만 연결해줍니다.**

---

## ✅ `forwardRef`의 역할

> 함수형 컴포넌트에도 **ref를 전달받을 수 있도록 허용**하고,
> 그 ref를 **내부의 실제 DOM 요소**에 연결할 수 있게 합니다.

```tsx
import { forwardRef } from 'react';

const MyInput = forwardRef((props, ref) => {
  return <input ref={ref} {...props} />;
});
```

이제 `<MyInput ref={ref} />`처럼 사용 가능하며, 외부에서 `.focus()`, `.value`, `.scrollIntoView()` 등을 자유롭게 조작할 수 있게 됩니다.

---

## ⚙️ 내부 작동 방식

`forwardRef`는 HOC(Higher-Order Component)입니다.

```tsx
const MyInput = forwardRef((props, ref) => {
  return <input ref={ref} {...props} />;
});
```

* `props`: 부모 컴포넌트가 넘긴 일반 props
* `ref`: 부모가 넘긴 ref (기존에는 함수형 컴포넌트가 받을 수 없던 것)

```tsx
<MyInput ref={inputRef} /> // ✅ inputRef는 내부 input DOM을 가리킴
```

---

## 🧪 실전 예제 ①: 외부에서 focus 가능한 입력창

```tsx
const TextInput = forwardRef((props, ref) => {
  return <input type="text" ref={ref} {...props} />;
});

function App() {
  const inputRef = useRef();

  const focusInput = () => inputRef.current.focus();

  return (
    <>
      <TextInput ref={inputRef} />
      <button onClick={focusInput}>포커스</button>
    </>
  );
}
```

---

## 🧪 실전 예제 ②: 외부에서 메서드 제어 — `useImperativeHandle` 조합

```tsx
const FancyInput = forwardRef((props, ref) => {
  const innerRef = useRef();

  useImperativeHandle(ref, () => ({
    focus: () => innerRef.current.focus(),
    clear: () => (innerRef.current.value = ''),
    scrollTo: () => innerRef.current.scrollIntoView({ behavior: 'smooth' }),
  }));

  return <input ref={innerRef} {...props} />;
});

function App() {
  const inputRef = useRef();

  return (
    <>
      <FancyInput ref={inputRef} />
      <button onClick={() => inputRef.current.focus()}>Focus</button>
      <button onClick={() => inputRef.current.clear()}>Clear</button>
      <button onClick={() => inputRef.current.scrollTo()}>Scroll</button>
    </>
  );
}
```

✅ 이렇게 하면 외부에서 내부 로직을 **제어 가능한 API처럼 노출**시킬 수 있습니다.

---

## 🧩 패턴 활용 전략

| 패턴                                   | 설명                   | 추천 상황                                  |
| ------------------------------------ | -------------------- | -------------------------------------- |
| `forwardRef` 단독 사용                   | DOM 조작만 필요할 때        | `<input>`, `<textarea>` 포커스 등          |
| `forwardRef` + `useImperativeHandle` | 외부에서 제어할 수 있는 API 제공 | 커스텀 컴포넌트에서 `.scrollTo()`, `.reset()` 등 |
| `forwardRef` + `memo`                | 성능 최적화               | `ref` 지원 + props 비교 최적화                |

---

## 🔒 캡슐화의 관점에서의 이점

`forwardRef`는 외부에서 내부 DOM에 접근할 수 있게 만들지만,
`useImperativeHandle`을 활용하면 **접근 가능한 메서드를 제한**할 수 있어 **캡슐화를 유지**할 수 있습니다.

```tsx
// 외부에서는 focus, clear만 가능. 내부 로직은 은닉.
useImperativeHandle(ref, () => ({
  focus: () => inputRef.current.focus(),
  clear: () => inputRef.current.value = ''
}));
```

---

## 🚫 언제 `forwardRef`를 쓰지 말아야 하나?

| 상황                 | 설명                       |
| ------------------ | ------------------------ |
| 단순한 UI 렌더링만 필요한 경우 | 내부 로직에 접근할 필요가 없으면 불필요   |
| 비제어 컴포넌트라면         | 외부에서 값 관리하면 `ref` 없이도 충분 |

---

## 📦 요약

| 항목       | 내용                                  |
| -------- | ----------------------------------- |
| 목적       | ref를 함수형 컴포넌트에 전달하고 내부 DOM에 연결      |
| 사용 이유    | 외부에서 내부 DOM 제어 (focus 등)            |
| advanced | `useImperativeHandle`로 외부 공개 API 구성 |
| 사용 예     | 폼 라이브러리, 모달 제어, 애니메이션 트리거 등         |
| 대안       | 외부 state로 제어하거나, class 컴포넌트 사용      |

