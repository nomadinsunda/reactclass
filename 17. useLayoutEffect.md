

# `useLayoutEffect`: `DOM-측정-시점` 제어를 위한 React 고급 훅

---

## O 1. `useLayoutEffect`란?

> DOM이 렌더링된 **직후**, 브라우저가 **화면에 그리기 전**(paint 이전)에 실행되는 **동기적(effect blocking)** 훅

```tsx
useLayoutEffect(() => {
  // DOM은 이미 구성됐지만 아직 사용자에게 보이지 않음
  // → DOM을 측정하거나 수정할 수 있음
}, [deps]);
```

---

## 2. 실행 순서: 리액트 렌더링 사이클 속 위치

```txt
1. 컴포넌트 함수 실행 (렌더링 준비)
2. Virtual DOM 계산 (reconciliation)
3. 실제 DOM 업데이트 (commit / mutation)
4. 🔹 ref 연결 (ref.current = DOM 노드, 콜백 ref 호출)
5. 🔹 useLayoutEffect 실행 (동기적)
6. 브라우저 Paint (화면 반영)
7. 🔸 useEffect 실행 (비동기적, paint 이후)
```

| 단계 | 작업                | 실행 시점                     |
| -- | ----------------- | ------------------------- |
| 4  | ref 연결            | **useLayoutEffect보다 먼저**  |
| 5  | `useLayoutEffect` | **DOM은 변경됐지만 화면엔 아직 미반영** |
| 7  | `useEffect`       | **화면이 사용자에게 보인 이후 실행**    |

> **ref는 `useLayoutEffect`보다 먼저 연결**됩니다.
> 그래서 `useLayoutEffect` 안에서는 `ref.current`가 **항상 최신 DOM 노드**임이 보장됩니다.

> **React 19 참고**: `useLayoutEffect`의 동작 자체는 React 18과 **동일**합니다.
> 다만 ref 관련해서 두 가지가 달라졌습니다.
> * 콜백 ref가 **cleanup 함수를 반환**할 수 있습니다. (cleanup 역시 paint 이전, layout 타이밍에 실행)
> * 콜백 ref가 **함수가 아닌 값을 반환하면 에러**입니다. → `ref={el => (r.current = el)}` X / `ref={el => { r.current = el; }}` O

---

## 왜 필요할까?

> DOM의 **레이아웃 측정**, **스타일 조정**, **스크롤 동기화**, **깜빡임(flickering) 방지** 같은 **정밀한 UI 조작**이 필요할 때 사용

---

## 문제 예시: `useEffect`로 인한 깜빡임

```tsx
function Box() {
  const ref = useRef(null); // ⚠️ React 19 타입(@types/react v19)부터 useRef는 인자가 필수입니다

  useEffect(() => {
    ref.current.style.transform = 'translateX(100px)';
  }, []);

  return (
    <div
      ref={ref}
      style={{ width: 100, height: 100, background: 'blue' }}
    />
  );
}
```

### 결과

* 초기 렌더링: `blue` 상자가 왼쪽에 나타남
* 다음 프레임에 `transform` 적용 → **화면이 한 프레임 깜빡임**

---

## O 해결 예시: `useLayoutEffect`

```tsx
useLayoutEffect(() => {
  ref.current.style.transform = 'translateX(100px)';
}, []);
```

O DOM이 렌더링되자마자 바로 위치가 바뀌므로 **사용자는 처음부터 옮겨진 모습만 보게 됨**
→ UI 품질 유지 (안정적이고 자연스러운 렌더링)

---

## 실전 예시: `getBoundingClientRect`로 높이 측정

```tsx
function ResizableBox() {
  const ref = useRef(null);

  useLayoutEffect(() => {
    const { height } = ref.current.getBoundingClientRect();
    if (height > 300) {
      ref.current.style.border = '3px solid red';
    }
  }, []);

  return <div ref={ref}>내용이 많을 수도 있어요</div>;
}
```

* `height`를 측정하고 그에 따라 border를 조정
* 반드시 **layout 계산 이후, paint 이전**에 실행되어야 함 → `useLayoutEffect`가 적합

---

## 주요 사용처 요약

| 상황           | 사용 이유                               |
| ------------ | ----------------------------------- |
| DOM 크기/위치 측정 | `getBoundingClientRect()`           |
| 외부 라이브러리     | Chart.js, D3.js, canvas 등과의 연동      |
| 레이아웃 동기화     | 여러 DOM 간 동기적인 정렬 계산 필요              |
| 스크롤 제어       | 특정 위치로 스크롤해야 할 때 `scrollIntoView()` |
| 깜빡임 방지       | 배경색, 위치 등 동적으로 조절해야 할 경우            |

---

## 주의사항

### 1. **렌더링 차단(blocking) → 남용하면 성능 저하**

* `useLayoutEffect`는 **paint 이전에 동기 실행**되므로, 이 안의 코드가 끝날 때까지 **브라우저가 화면을 그리지 못합니다.**
* 즉 여기에 무거운 코드를 넣으면 그대로 **화면 지연(느린 첫 페인트, 버벅임)** 으로 이어집니다.
* 특히 조심할 것:

  * 무거운 반복 계산, 큰 리스트 순회
  * **네트워크 요청 / 비동기 작업** → 이런 건 반드시 `useEffect`로
  * **읽기(`getBoundingClientRect`)와 쓰기(`style.xxx`)를 번갈아 반복** → `layout thrashing` (강제 동기 레이아웃)

    * 측정은 한 번에 몰아서 읽고, 쓰기도 한 번에 몰아서 수행하세요.

#### `useLayoutEffect` 안에서의 `setState`

```tsx
useLayoutEffect(() => {
  const { height } = ref.current.getBoundingClientRect();
  setHeight(height); // 👉 paint 전에 "동기적으로" 리렌더가 한 번 더 발생
}, []);
```

* 이 패턴은 **깜빡임을 없애기 위해 일부러 쓰는 정석 패턴**입니다. (측정 → 즉시 반영 → 한 번에 그리기)
* 다만 **렌더가 2배로 일어나는 것**이므로, 정말 “측정 후 조정”이 필요한 경우에만 쓰세요.
* 조건 없이 매번 `setState`하면 **무한 루프**가 되니, 값이 실제로 달라졌을 때만 갱신하세요.

> **기본값은 언제나 `useEffect`입니다.**
> “`useEffect`로 했더니 눈에 보이는 깜빡임이 있다”가 확인됐을 때만 `useLayoutEffect`로 승격하세요.
> 그리고 CSS(예: `transform`, `transition`, `visibility`)로 풀 수 있으면 그게 가장 저렴합니다.

### 2. **SSR (Next.js 등)에서의 경고**

#### 무슨 일이 일어나나?

* **서버에는 DOM도, 브라우저 paint도 없습니다.** 그래서 `renderToString`/`renderToPipeableStream` 같은 서버 렌더링에서는
  **`useLayoutEffect`가 아예 실행되지 않습니다.** (`useEffect`도 서버에서는 실행되지 않습니다)
* React는 이때 개발 모드에서 다음과 같은 경고를 냅니다.

```txt
Warning: useLayoutEffect does nothing on the server, because its effect
cannot be encoded into the renderer's output format. This will lead to a
mismatch between the initial, non-hydrated UI and the intended UI.
To avoid this, useLayoutEffect should only be used in components that
render exclusively on the client.
```

* 핵심은 **“서버가 만든 HTML”과 “레이아웃 이펙트가 적용된 UI”가 달라진다**는 점입니다.
  즉 이 경고는 성능 경고가 아니라 **하이드레이션 불일치(hydration mismatch) 경고**입니다.

#### 대응 패턴 ① 서버/클라이언트 분기 (`useIsomorphicLayoutEffect`)

가장 널리 쓰이는 라이브러리 관용 패턴입니다.

```tsx
import { useEffect, useLayoutEffect } from 'react';

// 모듈 최상단에서 딱 한 번 결정 (컴포넌트 안에서 조건 분기하면 Hook 규칙 위반)
export const useIsomorphicLayoutEffect =
  typeof window !== 'undefined' ? useLayoutEffect : useEffect;
```

```tsx
function Box() {
  useIsomorphicLayoutEffect(() => {
    // 클라이언트에서는 paint 이전, 서버에서는 아예 실행 안 됨
  }, []);
}
```

* O 경고는 사라집니다.
* 하지만 **경고를 “숨기는” 것에 가깝습니다.** 서버 HTML과 클라이언트 최종 UI가 실제로 다르다면
  깜빡임은 그대로 남습니다. 근본 해결은 아래 ②·③입니다.

#### 대응 패턴 ② 클라이언트 전용으로 렌더하기

측정이 꼭 필요한 컴포넌트라면, **처음부터 클라이언트에서만 렌더**하는 게 정직한 해법입니다.

```tsx
// Next.js App Router
'use client';

// 또는 dynamic import로 SSR 자체를 끔
const Chart = dynamic(() => import('./Chart'), { ssr: false });
```

#### 대응 패턴 ③ 애초에 측정이 필요 없게 만들기

* CSS로 표현 가능한 것(중앙 정렬, sticky, `aspect-ratio`, `position: absolute` 등)은 CSS로
* 크기 변화 추적은 `ResizeObserver`(콜백 ref + React 19 cleanup 조합)로
* 외부 스토어 구독은 `useSyncExternalStore`로

> **정리**: SSR 환경에서 `useLayoutEffect` 경고가 뜨면
> “분기해서 끄기” 전에 **“이게 정말 paint 이전이어야 하는가?”** 를 먼저 되물어보세요.

---

## 고급 예제: 애니메이션 시작 전 layout 측정

```tsx
function AnimatedBox() {
  const ref = useRef(null);

  useLayoutEffect(() => {
    const startX = ref.current.getBoundingClientRect().left;
    // 예: X 위치에 따라 트랜지션 결정
    ref.current.style.transition = 'transform 0.3s ease';
    ref.current.style.transform = `translateX(${window.innerWidth - startX}px)`;
  }, []);

  return <div ref={ref} className="box" />;
}
```

---

## `useLayoutEffect` vs `useEffect`: 요약 비교

| 항목          | `useEffect`         | `useLayoutEffect`          |
| ----------- | ------------------- | -------------------------- |
| 실행 시점       | paint **이후**        | paint **이전** (DOM 커밋·ref 연결 직후) |
| 실행 방식       | 비동기 (지연 실행)         | 동기 (paint 차단)              |
| 주 사용 용도     | 상태 동기화, 구독, 네트워크 요청 | DOM 측정/조작, 깜빡임 방지          |
| 서버에서 실행되나? | X 실행 안 됨 | X 실행 안 됨 |
| SSR 경고 | 없음 | **경고 발생** (하이드레이션 불일치 경고) |
| 성능 영향       | 낮음                  | 높음 — 남용 시 화면 지연            |
| 기본 선택 | O **이걸 먼저** | 깜빡임이 실제로 확인될 때만 |

---

## O 마무리 요약

| 항목    | 설명                                                |
| ----- | ------------------------------------------------- |
| 실행 시점 | 렌더링 → DOM 구성 완료 → **화면 그리기 직전**                   |
| 동작 방식 | 동기적 (렌더링 블로킹)                                     |
| 사용 목적 | **레이아웃 측정, DOM 조작, flicker 제거**                   |
| 주의 사항 | 과도한 사용은 화면 지연 유발(paint 차단), SSR에서는 실행되지 않고 경고 발생   |
| 대안    | `useEffect`, CSS, `ResizeObserver`, `MutationObserver`, `useSyncExternalStore` |

