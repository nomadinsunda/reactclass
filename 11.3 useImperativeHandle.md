

# 🚀 React `useImperativeHandle`

`useImperativeHandle`은 React에서 **자식 컴포넌트가 부모에게 노출하고 싶은 메서드만 선택적으로 공개**할 수 있도록 도와주는 Hook입니다.

즉:

> ✔ **부모 → 자식 방향의 단방향 데이터 흐름을 잠시 벗어나서**,
> ✔ **부모가 자식 컴포넌트의 내부 함수를 직접 호출할 수 있게 해주는 특수한 기술**

입니다.

이 기능은 무분별하게 사용하면 React의 선언적 패턴을 해칠 수 있기 때문에
**필요한 경우에만 신중하게 사용해야 하는 고급 기능**입니다.

---

# 🧩 1. 왜 `useImperativeHandle`이 필요한가?

기본적으로 React는 다음과 같은 규칙을 가지고 있습니다.

* 부모 → 자식으로 **props**가 내려간다.
* 부모는 자식의 **내부 상태나 메서드**에 직접 접근할 수 없다.

하지만 어떤 상황에서는
**자식 컴포넌트 내부에만 있는 기능을 부모가 실행해야 할 필요가 있습니다.**

예시:

* 자식 컴포넌트 내부 input 요소에 focus() 해야 할 때
* 미디어 플레이어 컴포넌트가 play(), pause() 같은 기능을 부모에게 노출해야 할 때
* 부모가 특정 Form의 validate() 함수를 호출하고 싶을 때
* ScrollView 컴포넌트가 scrollToBottom() 같은 메서드를 제공해야 할 때

이럴 때 `ref` + `useImperativeHandle` 조합이 필요합니다.

---

# 🧪 2. 기본 사용 구조 요약

```
부모 컴포넌트
   ⬇ (ref 전달)
자식 컴포넌트
   ⬅ useImperativeHandle로 노출할 기능 정함
```

---

# 🧩 3. 기본 코드 예제

## 📌 부모 → 자식에게 ref 전달

```jsx
// Parent.jsx
import React, { useRef } from "react";
import Child from "./Child";

function Parent() {
  const childRef = useRef();

  const handleClick = () => {
    childRef.current.sayHello();   // 부모가 자식의 내부 함수 호출!
  };

  return (
    <div>
      <Child ref={childRef} />
      <button onClick={handleClick}>자식 기능 실행</button>
    </div>
  );
}

export default Parent;
```

---

## 📌 자식에서 useImperativeHandle 정의

```jsx
// Child.jsx
import React, { forwardRef, useImperativeHandle } from "react";

function Child(props, ref) {
  useImperativeHandle(ref, () => ({
    sayHello() {
      alert("안녕하세요! 자식 컴포넌트입니다!");
    }
  }));

  return <div>자식 컴포넌트</div>;
}

export default forwardRef(Child);
```

여기서 2가지가 중요합니다:

### ① `forwardRef` 로 컴포넌트를 감싸야 ref를 받을 수 있다

React 함수 컴포넌트는 기본적으로 ref를 받지 못합니다.
그래서 `forwardRef()`가 필요합니다.

### ② useImperativeHandle(ref, → { 공개할 메서드들 })

부모에게 노출하고 싶은 메서드만 골라서 지정합니다.

---

# 🧠 4. 내부 동작 메커니즘 (React Fiber 관점으로 깊이 이해)

사용자님은 React Fiber 내부 구조, memoizedState 등도 관심이 있으시므로
조금 더 깊게 설명드리겠습니다.

### 🎯 핵심 아이디어

* React에서는 각 컴포넌트가 Fiber Node를 가진다.
* ref는 Fiber Node의 **ref 필드**에 저장된다.
* forwardRef를 사용하면 부모로부터 ref가 렌더 함수의 두 번째 인자로 전달된다.
* useImperativeHandle은 **해당 ref 객체의 current에 사용할 수 있는 값을 할당**한다.

### 내부 흐름 (개념도)

```
부모 컴포넌트 렌더 → childRef 생성
          ↓
forwardRef를 통해 자식으로 ref 전달
          ↓
자식에서 useImperativeHandle(ref, createFn)
          ↓
React가 ref.current = createFn() 실행
          ↓
부모에서 childRef.current.{exposedMethod} 사용 가능
```

즉,
**브라우저 DOM 요소 대신 우리가 원하는 임의 객체를 ref.current에 넣는 것**
이라고 볼 수 있습니다.

---

# 📚 5. 더 현실적인 예제: input focus()

React 공식 문서도 강조하는 매우 일반적인 패턴입니다.

## Parent.jsx

```jsx
import React, { useRef } from "react";
import CustomInput from "./CustomInput";

export default function Parent() {
  const inputRef = useRef();

  const focusInput = () => {
    inputRef.current.focus(); // 자식 input의 focus() 실행
  };

  return (
    <div>
      <CustomInput ref={inputRef} />
      <button onClick={focusInput}>입력창 포커스</button>
    </div>
  );
}
```

## CustomInput.jsx

```jsx
import React, { useRef, forwardRef, useImperativeHandle } from "react";

function CustomInput(props, ref) {
  const realInput = useRef();

  useImperativeHandle(ref, () => ({
    focus() {
      realInput.current.focus();
    },
    clear() {
      realInput.current.value = "";
    }
  }));

  return <input ref={realInput} />;
}

export default forwardRef(CustomInput);
```

---

# 💡 6. 꼭 알아야 할 주의사항

## ❌ useImperativeHandle은 남용하면 안 됩니다

React의 선언적 UI 원칙을 깨기 때문입니다.

React가 추구하는 방향은:

```
상태 → UI를 만드는 선언형 렌더링
```

useImperativeHandle은:

```
부모가 직접 자식을 명령적으로 조작하는 방식
```

따라서 다음 상황에서만 사용하세요:

### ✔ 꼭 필요한 케이스

* 스크롤 제어
* 포커스 제어
* 애니메이션 시작/정지
* 동기적 imperative API 필요
* Form validate(), reset() 등 외부 노출이 필요한 기능

### ❌ 피해야 할 케이스

* 상태 변경을 imperative 방식으로 하려고 할 때
* props/state로 충분히 해결되는 로직
* 자식의 UI를 강제로 변경하는 방식

---

# 🧩 7. 실제 고급 사용 예시 (폼 검증 API 노출)

```jsx
// ChildForm.jsx
import React, { useImperativeHandle, useState, forwardRef } from "react";

function ChildForm(props, ref) {
  const [value, setValue] = useState("");

  useImperativeHandle(ref, () => ({
    validate() {
      return value.length >= 3;
    },
    reset() {
      setValue("");
    }
  }));

  return (
    <input
      value={value}
      onChange={(e) => setValue(e.target.value)}
      placeholder="3글자 이상 입력"
    />
  );
}

export default forwardRef(ChildForm);
```

---

# 🎉 마무리 정리

| 기능                       | 설명                                      |
| ------------------------ | --------------------------------------- |
| 🔧 `useImperativeHandle` | 자식이 부모에게 노출하고 싶은 메서드를 선택적으로 지정          |
| 🧲 `forwardRef`          | 부모 ref를 자식 컴포넌트가 받을 수 있도록 허용            |
| 🎯 ref.current           | imperative handle로 노출된 API가 저장되는 위치     |
| ⚠ 주의사항                   | React의 선언적 패턴을 해칠 수 있으므로 꼭 필요한 상황에서만 사용 |
