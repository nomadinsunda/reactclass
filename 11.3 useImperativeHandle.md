React의 `useImperativeHandle`은 **“부득이할 때 쓰는 비밀 통로”** 같은 훅입니다.
부모가 자식 컴포넌트 내부의 메서드나 값을 *명령형으로* 호출해야 할 때, 그때만 살짝 문을 열어주는 역할을 합니다. 😊

아래에서 개념 → 문법 → 예제 → 내부 동작 → 주의사항까지 정리해 보겠습니다.

---

## 0. 한 줄 정의 🧠

> **`useImperativeHandle`은 `forwardRef`와 함께 사용하여, 부모 컴포넌트가 `ref.current`를 통해 접근할 수 있는 “커스텀 인스턴스 객체”를 정의하는 훅** 입니다.
> 즉, *“DOM 노드나 자식 전체” 대신, 내가 허용한 메서드/값만 ref로 노출*하게 해 줍니다.

---

## 1. 왜 `useImperativeHandle`이 필요한가? 🤔

원래 `ref`는 두 가지에 많이 씁니다.

1. **DOM에 직접 접근** (예: `<input>`에 포커스 주기)
2. **자식 컴포넌트 인스턴스에 접근** (클래스 컴포넌트에서 `this.someMethod()` 호출하던 것)

하지만 함수형 컴포넌트 시대에는:

* 함수형 컴포넌트는 `new`로 인스턴스를 만들지 않습니다.
* 그래서 기본적으로는 **함수형 컴포넌트에 ref를 달면, DOM이 아니라 “그 컴포넌트의 DOM 루트 노드”를 ref로 받는 식**으로 쓰는 게 일반적입니다. (forwardRef로 넘겨서)

문제는, 때때로 이런 요구가 있습니다:

* 부모가 자식의 메서드를 직접 호출하고 싶다.

  * 예: `childRef.current.focus()`
  * 예: `modalRef.current.open()`, `modalRef.current.close()`
  * 예: `formRef.current.validate()`, `formRef.current.reset()`
* 그런데 **자식 컴포넌트 내부 구현(DOM 구조)을 숨기고 싶다.**

이때 `useImperativeHandle`을 사용하면,

* 부모 입장: `ref.current`에 **“노출하고 싶은 메서드만 담긴 객체”**를 받게 되고,
* 자식 입장: 내부 DOM 구조/상태는 감추고, 일부 메서드만 외부에 노출할 수 있습니다.

이게 바로 **“명령형 핸들(Imperative Handle)”**입니다. 🔧

---

## 2. 기본 문법 & 시그니처 📜

### 공식 시그니처

```ts
useImperativeHandle<T, R = T>(
  ref: React.RefObject<T> | React.MutableRefObject<T> | ((instance: T | null) => void),
  createHandle: () => R,
  deps?: readonly any[]
): void
```

일반 사용 패턴(JavaScript):

```tsx
import React, { forwardRef, useImperativeHandle, useRef } from 'react';

const Child = (props, ref) => {
  // 내부에서 진짜 DOM이나 상태를 관리
  const inputRef = useRef(null);

  useImperativeHandle(ref, () => ({
    focus() {
      if (inputRef.current) {
        inputRef.current.focus();
      }
    },
    clear() {
      if (inputRef.current) {
        inputRef.current.value = '';
      }
    },
  }), []); // deps는 나중에 설명

  return <input ref={inputRef} placeholder="자식 컴포넌트 인풋" />;
};

export default Child;
```

부모에서:

```tsx
function Parent() {
  const childRef = useRef(null);

  const handleFocus = () => {
    childRef.current?.focus();
  };

  const handleClear = () => {
    childRef.current?.clear();
  };

  return (
    <div>
      <Child ref={childRef} />
      <button onClick={handleFocus}>포커스</button>
      <button onClick={handleClear}>지우기</button>
    </div>
  );
}
```

여기서 중요한 포인트:

* `forwardRef`로 자식이 `ref`를 받을 수 있게 감싸야 합니다.
* `useImperativeHandle(ref, () => ({ ... }))` 안에서

  * **부모가 사용할 `ref.current` 객체의 “모양”과 “메서드”를 정의**합니다.
  * 부모 입장에서는 `Child` 컴포넌트의 “공개 API”만 보게 되는 셈입니다.

---

## 3. `forwardRef` + `useImperativeHandle` 구조 한눈에 보기 🔍

자식 컴포넌트 구조를 도식처럼 보면:

```tsx
const Child = forwardRef((props, ref) => {
  // 1) 내부에서 실제 DOM ref, state 등 관리
  const inputRef = useRef(null);

  // 2) useImperativeHandle로 부모에게 노출할 메서드를 정의
  useImperativeHandle(ref, () => {
    return {
      focus() {
        inputRef.current?.focus();
      },
      clear() {
        if (inputRef.current) inputRef.current.value = '';
      },
    };
  }, []);

  // 3) 내부 렌더링
  return <input ref={inputRef} />;
});
```

부모에서:

```tsx
function Parent() {
  const childRef = useRef(null); // ref.current 타입은 { focus: () => void; clear: () => void } | null

  return (
    <>
      <Child ref={childRef} />
      <button onClick={() => childRef.current?.focus()}>포커스</button>
    </>
  );
}
```

즉,

* `forwardRef` : 부모 → 자식으로 `ref`를 전달하는 통로
* `useImperativeHandle` : 그 통로로 **“어떤 객체를 보낼지”** 결정하는 필터

---

## 4. 간단 예제 1 – 포커스/클리어가 가능한 Input 컴포넌트 ✏️

### ChildInput.tsx

```tsx
import React, { forwardRef, useImperativeHandle, useRef } from 'react';

export type InputHandle = {
  focus: () => void;
  clear: () => void;
  getValue: () => string;
};

type Props = {
  label?: string;
};

const ChildInput = forwardRef<InputHandle, Props>(({ label }, ref) => {
  const inputRef = useRef<HTMLInputElement | null>(null);

  useImperativeHandle(ref, () => ({
    focus() {
      inputRef.current?.focus();
    },
    clear() {
      if (inputRef.current) {
        inputRef.current.value = '';
      }
    },
    getValue() {
      return inputRef.current?.value ?? '';
    },
  }), []);

  return (
    <div>
      {label && <label style={{ marginRight: 8 }}>{label}</label>}
      <input ref={inputRef} />
    </div>
  );
});

export default ChildInput;
```

### Parent.tsx

```tsx
import React, { useRef } from 'react';
import ChildInput, { InputHandle } from './ChildInput';

function Parent() {
  const inputHandleRef = useRef<InputHandle | null>(null);

  return (
    <div>
      <ChildInput ref={inputHandleRef} label="이름" />
      <button onClick={() => inputHandleRef.current?.focus()}>
        포커스
      </button>
      <button onClick={() => inputHandleRef.current?.clear()}>
        지우기
      </button>
      <button
        onClick={() => {
          alert(inputHandleRef.current?.getValue());
        }}
      >
        값 보기
      </button>
    </div>
  );
}

export default Parent;
```

여기서 부모는 **DOM 세부 구조를 몰라도** `focus`, `clear`, `getValue`만 알고 쓰면 됩니다.
바로 이것이 **캡슐화 + 명령형 핸들**의 장점입니다. 💡

---

## 5. 내부 동작 마인드셋 🧬

`useImperativeHandle(ref, createHandle, deps)`는 크게 이렇게 동작한다고 생각하시면 됩니다:

1. 컴포넌트가 **마운트**될 때

   * `createHandle()`을 호출해서 객체를 만듭니다.
   * `ref.current = createHandle()`로 세팅합니다.
2. `deps`가 바뀌어서 **업데이트**될 때

   * 다시 `createHandle()`을 호출해 새로운 객체를 반환하고,
   * `ref.current`를 그 객체로 교체합니다.
3. 컴포넌트가 **언마운트**될 때

   * `ref.current = null`로 정리합니다.

그래서:

* `deps`를 `[]`로 두면, **한 번만 핸들을 만들어서 재사용**합니다.
* 내부에서 참조하는 것이 **바뀔 수 있는 상태**라면, 그 상태를 `deps`에 넣어서 최신 상태를 반영해야 합니다. (아래 주의사항에서 설명)

---

## 6. 실전 예제들 ⚔️

### 6-1. Modal의 `open`, `close` 메서드 노출하기

#### Modal.tsx

```tsx
import React, { forwardRef, useImperativeHandle, useState } from 'react';

export type ModalHandle = {
  open: () => void;
  close: () => void;
};

type ModalProps = {
  title: string;
  children?: React.ReactNode;
};

const Modal = forwardRef<ModalHandle, ModalProps>(({ title, children }, ref) => {
  const [isOpen, setIsOpen] = useState(false);

  useImperativeHandle(ref, () => ({
    open() {
      setIsOpen(true);
    },
    close() {
      setIsOpen(false);
    },
  }), []);

  if (!isOpen) return null;

  return (
    <div
      style={{
        position: 'fixed',
        inset: 0,
        background: 'rgba(0,0,0,0.4)',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
      }}
    >
      <div style={{ background: 'white', padding: 20, borderRadius: 8, minWidth: 300 }}>
        <h2>{title}</h2>
        <div style={{ marginTop: 10 }}>{children}</div>
        <button onClick={() => setIsOpen(false)}>닫기</button>
      </div>
    </div>
  );
});

export default Modal;
```

#### Parent.tsx

```tsx
import React, { useRef } from 'react';
import Modal, { ModalHandle } from './Modal';

function Parent() {
  const modalRef = useRef<ModalHandle | null>(null);

  const handleOpen = () => {
    modalRef.current?.open();
  };

  const handleClose = () => {
    modalRef.current?.close();
  };

  return (
    <div>
      <button onClick={handleOpen}>모달 열기</button>
      <button onClick={handleClose}>모달 닫기</button>

      <Modal ref={modalRef} title="알림">
        <p>useImperativeHandle로 제어하는 모달입니다.</p>
      </Modal>
    </div>
  );
}

export default Parent;
```

이 패턴은 **라이브러리에서 모달, 드로어, 토스트 등**을 만들 때 꽤 자주 등장합니다.

---

### 6-2. 스크롤 가능한 리스트에서 `scrollToBottom` 제공하기 📜

```tsx
import React, { forwardRef, useImperativeHandle, useRef } from 'react';

export type ScrollableListHandle = {
  scrollToBottom: () => void;
};

type ScrollableListProps = {
  items: string[];
};

const ScrollableList = forwardRef<ScrollableListHandle, ScrollableListProps>(
  ({ items }, ref) => {
    const containerRef = useRef<HTMLDivElement | null>(null);

    useImperativeHandle(ref, () => ({
      scrollToBottom() {
        const el = containerRef.current;
        if (!el) return;
        el.scrollTop = el.scrollHeight;
      },
    }), []);

    return (
      <div
        ref={containerRef}
        style={{
          maxHeight: 200,
          overflowY: 'auto',
          border: '1px solid #ccc',
          padding: 8,
        }}
      >
        {items.map((item, idx) => (
          <div key={idx}>{item}</div>
        ))}
      </div>
    );
  }
);

export default ScrollableList;
```

부모:

```tsx
function Chat() {
  const listRef = useRef<ScrollableListHandle | null>(null);
  const [messages, setMessages] = React.useState<string[]>([]);

  const addMessage = () => {
    setMessages((prev) => [...prev, `메시지 ${prev.length + 1}`]);
    // setState는 비동기라 다음 렌더 이후에 스크롤 하려면, useEffect를 함께 사용하는 패턴도 있음
    // 여기서는 단순 예시
    setTimeout(() => listRef.current?.scrollToBottom(), 0);
  };

  return (
    <div>
      <button onClick={addMessage}>메시지 추가</button>
      <ScrollableList ref={listRef} items={messages} />
    </div>
  );
}
```

---

## 7. `useImperativeHandle` vs 그냥 `ref` vs `useRef` 📚

### 7-1. 그냥 DOM ref만 쓸 때

```tsx
const Input = forwardRef((props, ref) => {
  return <input ref={ref} />;
});
```

* 부모는 `ref.current`로 **그냥 DOM 노드**를 받습니다.
* DOM 메서드(`.focus()`, `.scrollIntoView()`, ...)만 쓸 수 있습니다.

### 7-2. `useImperativeHandle`을 쓸 때

```tsx
const Input = forwardRef((props, ref) => {
  const innerRef = useRef(null);

  useImperativeHandle(ref, () => ({
    focus() {
      innerRef.current?.focus();
    },
    clear() {
      if (innerRef.current) innerRef.current.value = '';
    },
  }), []);

  return <input ref={innerRef} />;
});
```

* 부모는 `ref.current`로 **커스텀 객체**를 받습니다.
* DOM 메서드가 아니라 `focus`, `clear` 같은 **내가 설계한 API**만 보게 됩니다.
* 자식 내부 DOM 구조를 바꿔도, API만 유지하면 부모 코드는 그대롭니다. (캡슐화)

---

## 8. 의존성 배열(deps)과 stale closure 문제 ⚠️

`useImperativeHandle(ref, createHandle, deps)`의 `deps`는 **“핸들 객체가 어떤 값에 의존하는지”**를 React에게 알려주는 역할입니다.

예를 들어, 핸들 안에서 `count` 상태를 읽는다면:

```tsx
const [count, setCount] = useState(0);

useImperativeHandle(ref, () => ({
  logCount() {
    console.log('현재 count:', count);
  },
}), [count]);
```

* `count`가 변할 때마다 새 핸들이 만들어지고, `ref.current.logCount`는 최신 `count`를 캡처합니다.
* 만약 `deps`를 `[]`로 두면,

  * 처음 렌더 때의 `count`(예: 0)만 기억하고 계속 그 값만 출력하는 **stale closure** 문제가 생깁니다.

**실무 팁**:

* 핸들이 **DOM ref나 stable한 함수만 사용**한다면 `[]`로 두어도 괜찮습니다.
* 핸들 내부에서 **state, props 값**을 읽는다면

  * 해당 값들을 `deps`에 포함시키거나,
  * 혹은 `useRef`로 상태를 mirror하여 관리하는 등의 패턴을 사용해야 합니다.

---

## 9. 선언형(Declarative) vs 명령형(Imperative) ⚖️

React는 기본적으로 **선언형** 패턴을 지향합니다.

* “상태를 이렇게 만들면 UI가 이렇게 보인다”는 *데이터 → UI* 관계를 사용하는 것이 정석
* 부모가 자식을 직접 명령(`doSomething()`)하는 것은 **가급적 피하라**는 것이 React 철학

그렇다면 `useImperativeHandle`은 **언제 써야 할까?**

> 👉 “정말 선언형으로 풀기 어려운 문제”를 다룰 때, **최후의 수단**처럼 사용하는 것이 좋습니다.

예:

* 포커스 제어
* 스크롤 제어
* 애니메이션 라이브러리 연동
* 매우 복잡한 third-party DOM 라이브러리와의 브릿지

**상태 변화로 해결될 수 있는 로직이라면**,
`props`/`state`/`context`로 표현하는 것이 더 React스럽습니다.

---

## 10. TypeScript에서의 타입 지정 ✍️

자주 쓰는 패턴:

```tsx
export type SomeHandle = {
  methodA: () => void;
  methodB: (value: string) => number;
};

type Props = { ... };

const SomeComponent = forwardRef<SomeHandle, Props>((props, ref) => {
  useImperativeHandle(ref, () => ({
    methodA() { ... },
    methodB(value) { ... },
  }), []);

  return (...);
});
```

부모에서:

```tsx
const someRef = useRef<SomeHandle | null>(null);

// someRef.current?.methodA();
// someRef.current?.methodB('hello');
```

이렇게 하면:

* `ref.current`에 대한 사용 시점에 **자동 완성 + 타입 체크**가 잘 됩니다.
* 실수로 `ref.current.nonExistMethod()` 같은 걸 호출하면 컴파일 에러로 잡아줍니다. ✅

---

## 11. 자주 하는 실수 & 주의사항 🔍

1. **`forwardRef`를 안 쓰고 useImperativeHandle만 쓰는 경우**

   * `useImperativeHandle`은 반드시 `forwardRef` 컴포넌트 안에서 사용해야 합니다.
   * 아니면 React가 `ref`를 전달할 통로가 없습니다.

2. **의존성 배열을 무시**

   * 핸들 내부에서 state, props를 사용하면서도 `[]`로 두면 stale 데이터 문제가 발생합니다.
   * 꼭 필요한 값은 `deps`에 넣거나 다른 방식으로 처리해야 합니다.

3. **과도한 사용**

   * 자꾸 `ref.current.xxx()`로 호출하는 패턴이 늘어나면, 컴포넌트 구조가 “명령형 스파게티 코드”가 되기 쉽습니다.
   * *"이걸 상태로 풀 수는 없을까?"*를 먼저 고민한 후, 안 될 때 쓰는 것을 추천합니다.

4. **DOM을 그대로 노출 vs 커스텀 API만 노출**

   * `useImperativeHandle` 안에서 그냥 `return inputRef.current` 같은 식으로 DOM만 전달할 수도 있습니다.
   * 하지만 그것보다는 **내부 구현을 숨기고, 최소한의 API만 노출하는 것이 더 좋은 설계**입니다.

---

## 12. 언제 쓰면 좋은지 정리 🎯

**적절한 사용 예시**

* 포커스/스크롤/애니메이션/플레이/정지 등,

  * **“UI의 물리적인 동작”을 부모가 직접 제어**해야 할 때
* Form 라이브러리에서 필드 컴포넌트가 `validate()`, `reset()` 같은 메서드를 제공할 때
* 외부 DOM 기반 라이브러리(차트, 캔버스, 비디오 플레이어 등)를 래핑할 때

**피해야 할 사용 예시**

* 단순히 데이터 전달/상태 변경을 위해

  * → props/state/context로 해결 가능
* 부모/자식 간 호출이 뒤엉켜서 구조를 복잡하게 만드는 경우

---

## 13. 요약 🧾

* `useImperativeHandle`은 **부모가 자식의 내부 메서드를 호출할 수 있게 해주는 “명령형 API 훅”**입니다.
* 반드시 `forwardRef`와 함께 사용합니다.
* `useImperativeHandle(ref, () => ({ ... }), deps)` 안에서
  **ref로 노출할 메서드/값을 정의**합니다.
* 의존성 배열을 잘못 사용하면 **stale closure** 문제가 생깁니다.
* React의 선언형 철학과 상충되므로, **정말 필요할 때만** 사용하는 것이 좋습니다.

