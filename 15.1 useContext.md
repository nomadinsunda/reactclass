

# 🧠 React `useContext`

---

## ✅ 1. `useContext`란?

> `useContext`는 **React 컴포넌트 트리 전체에 전역적으로 공유되는 데이터를 읽는** Hook입니다.
> 상위 트리의 `Context.Provider`가 제공한 값을 **props 없이 바로 하위에서 소비**할 수 있게 해줍니다.

```tsx
const value = useContext(MyContext);
```

* `MyContext`는 `React.createContext()`로 생성된 Context 객체입니다.
* Context는 React가 기본 제공하는 **전역 데이터 공유 시스템**입니다.

---

## 🌐 2. Context의 구조

| 구성 요소             | 설명                            |
| ----------------- | ----------------------------- |
| `createContext()` | Context 객체 생성                 |
| `Provider`        | 값을 제공 (컴포넌트 트리 상위에 위치)        |
| `useContext()`    | Context 값을 구독하여 읽어옴 (하위 컴포넌트) |

```tsx
const ThemeContext = React.createContext('light');
```

---

## 🔁 3. 왜 Context가 필요한가? (props drilling 문제 해결)

---

### ❌ 문제: props drilling

**state**는 최상위(App)에서 정의되었고, 하위(UserPanel)에서 필요합니다.
그러나 중간 컴포넌트(`Layout`, `Sidebar`)가 **필요도 없는데 props를 전달만** 하고 있습니다.

```tsx
// App.jsx
function App() {
  const user = { name: '홍길동' };

  return <Layout user={user} />;
}

// Layout.jsx
function Layout({ user }) {
  return <Sidebar user={user} />;
}

// Sidebar.jsx
function Sidebar({ user }) {
  return <UserPanel user={user} />;
}

// UserPanel.jsx
function UserPanel({ user }) {
  return <p>👤 사용자: {user.name}</p>;
}
```

🔻 문제점:

* **중간 컴포넌트들은 user를 사용하지 않는데도** 계속 전달해야 함
* 앱이 커질수록 **관리 복잡도 증가**

---

### ✅ 해결: Context + `useContext` 적용

1. `UserContext` 생성
2. 최상위에서 한 번만 `Provider`로 감싸기
3. 필요한 곳에서만 `useContext()`로 읽기

```tsx
// UserContext.js
import { createContext } from 'react';
export const UserContext = createContext(null);
```

```tsx
// App.jsx
import { UserContext } from './UserContext';
import Layout from './Layout';

function App() {
  const user = { name: '홍길동' };

  return (
    <UserContext.Provider value={user}>
      <Layout />
    </UserContext.Provider>
  );
}
```

```tsx
// Layout.jsx
import Sidebar from './Sidebar';

function Layout() {
  return <Sidebar />;
}
```

```tsx
// Sidebar.jsx
import UserPanel from './UserPanel';

function Sidebar() {
  return <UserPanel />;
}
```

```tsx
// UserPanel.jsx
import { useContext } from 'react';
import { UserContext } from './UserContext';

function UserPanel() {
  const user = useContext(UserContext);
  return <p>👤 사용자: {user.name}</p>;
}
```

---

### ✅ 결과 비교 요약

| 항목              | props drilling    | Context + useContext |
| --------------- | ----------------- | -------------------- |
| 중간 컴포넌트 `props` | 필요 없는 props 계속 전달 | 필요 없음                |
| 유지보수성           | 변경에 취약            | 유연하고 명확한 흐름 유지 가능    |
| 트리 깊이           | 깊을수록 복잡도 증가       | 깊이에 무관하게 접근 가능       |

---

### 🧠 핵심 개념 강조

* Context는 **공통 상태를 전역으로 공유**할 수 있게 해주며,
* `useContext`는 **그 상태를 구독하고 소비**할 수 있게 해줍니다.































---

## ⚙️ 4. 내부 동작 원리

* `useContext`는 현재 컴포넌트가 트리 상단에 있는 **가장 가까운 Provider의 value를 구독**합니다.
* 이 value가 변경되면 → 이 Context를 사용하는 모든 하위 컴포넌트가 **자동으로 리렌더링**됩니다.
* 구독 기반이므로 `useContext()`는 단순 조회가 아니라 \*\*"값을 감시하는 동작"\*\*입니다.

---

## ⚠️ 5. 주의사항 및 성능 고려

| 주의 사항                         | 설명                                      |
| ----------------------------- | --------------------------------------- |
| ❗ Provider의 `value`가 바뀌면      | 모든 `useContext()` 구독 컴포넌트가 리렌더링         |
| ❗ 객체/함수는 매 렌더링마다 참조값이 바뀜      | `useMemo`, `useCallback`으로 고정 필요        |
| ❗ Context는 상태(state)를 저장하지 않음 | 상태는 별도로 `useState`, `useReducer` 등으로 관리 |
| ❗ Provider 바깥에서 사용 시          | 기본값 또는 `undefined` 반환 (에러 방지 필요)        |

### ✅ 예시: 객체 리렌더 방지

```tsx
const contextValue = useMemo(() => ({ user, setUser }), [user, setUser]);

<UserContext.Provider value={contextValue}>
  ...
</UserContext.Provider>
```

---

## 📘 6. 실제 예제

### 🎯 다국어 선택기 (LocaleContext)

```tsx
// context.js
export const LocaleContext = createContext('ko');
```

```tsx
// App.jsx
<LocaleContext.Provider value="en">
  <Page />
</LocaleContext.Provider>
```

```tsx
// Page.jsx
const locale = useContext(LocaleContext);
return <p>언어: {locale}</p>;
```

---

## 🧠 7. 고급 패턴: `useReducer` + `useContext`

> 상태 업데이트 로직을 분리하고, 전역으로 공유하는 패턴.
> Redux 없이 "전역 상태 관리" 구조를 구성할 수 있습니다.

```tsx
const CounterContext = createContext();
const DispatchContext = createContext();

function CounterProvider({ children }) {
  const [count, dispatch] = useReducer((s, a) => s + a, 0);
  return (
    <CounterContext.Provider value={count}>
      <DispatchContext.Provider value={dispatch}>
        {children}
      </DispatchContext.Provider>
    </CounterContext.Provider>
  );
}
```

```tsx
const count = useContext(CounterContext);
const dispatch = useContext(DispatchContext);
```

✅ `count`와 `dispatch`를 원하는 곳에서 바로 접근
✅ 전역 상태 관리 로직 분리 (Mini Redux처럼 동작)

---

## 📊 8. 비교: `useContext` vs 다른 상태 관리 도구

| 비교 대상           | 역할               | 전역 상태 | 상태 변경 구조화 | 성능 최적화          |
| --------------- | ---------------- | ----- | --------- | --------------- |
| `useState`      | 로컬 상태            | ❌     | ❌         | ✅               |
| `useReducer`    | 구조적 상태 전이        | ❌     | ✅         | ✅               |
| `useContext`    | 전역 데이터 공유        | ✅     | ❌         | ❌ 전체 리렌더        |
| **Redux (RTK)** | 전역 상태 + DevTools | ✅     | ✅         | ✅ (memoization) |

---

## 🧪 9. 언제 `useContext`를 쓰면 좋은가?

| 상황                       | 권장 여부      |
| ------------------------ | ---------- |
| 로그인 정보 (`user`)          | ✅ 매우 적합    |
| 테마, 언어 설정                | ✅ 적합       |
| 매우 자주 바뀌는 값              | ⚠️ 비추      |
| 비동기 로딩 상태, API 결과 등      | ⚠️ 조심      |
| 수십 개의 상태를 전역으로 공유해야 할 경우 | ❌ Redux 권장 |

---

## ✅ 10. 정리

| 항목        | 설명                                |
| --------- | --------------------------------- |
| 목적        | 상위 Provider로부터 값을 구독하여 하위에서 바로 읽음 |
| 장점        | props 없이 전역 데이터 전달 가능             |
| 단점        | 값이 바뀌면 모든 구독자 리렌더링                |
| 성능 최적화 방법 | `useMemo`로 value 고정, Context 분리   |
| 고급 활용     | `Context + useReducer`, 커스텀 훅 추상화 |

