`React.memo`는 **함수 컴포넌트의 불필요한 리렌더링을 줄이기 위한 메모이제이션 기능**입니다.

핵심부터 말하면:

> **부모 컴포넌트가 리렌더링되어도, 자식 컴포넌트가 받은 `props`가 이전과 같다면 자식 컴포넌트 함수의 재실행을 건너뛰게 해주는 기능**입니다.

즉, `React.memo`는 **컴포넌트 자체를 메모이제이션**한다고 이해하면 됩니다.

---

## 1. 먼저 React의 기본 리렌더링 규칙

다음 코드를 보겠습니다.

```jsx
function App() {
  const [count, setCount] = useState(0);

  return (
    <>
      <button onClick={() => setCount(count + 1)}>
        count: {count}
      </button>

      <Header />
    </>
  );
}

function Header() {
  console.log("Header 실행");

  return <h1>React 공부</h1>;
}
```

컴포넌트 트리는 다음과 같습니다.

```text
App
├── button
└── Header
```

`count`가 변경되면 `App`이 다시 실행됩니다.

```text
count 변경
    ↓
App 리렌더링
    ↓
App 함수 다시 실행
    ↓
새 React Element 생성
    ↓
Header도 다시 렌더링
```

따라서 버튼을 클릭할 때마다:

```text
Header 실행
Header 실행
Header 실행
...
```

이 출력됩니다.

중요한 것은 `Header`의 내용이 바뀌었기 때문이 아닙니다.

단지 **부모인 `App`이 리렌더링되었기 때문에 자식 `Header`도 기본적으로 다시 렌더링되는 것**입니다.

---

# 2. React.memo 사용

이제 `Header`를 `React.memo`로 감싸보겠습니다.

```jsx
const Header = React.memo(function Header() {
  console.log("Header 실행");

  return <h1>React 공부</h1>;
});
```

그러면 구조적으로 다음과 같은 의미가 됩니다.

```text
App 리렌더링
     ↓
Header에 전달된 props 확인
     ↓
이전 props === 새로운 props ?
     ↓
   YES
     ↓
Header 렌더링 생략
```

따라서 `count`가 바뀌어도 `Header`는 다시 실행되지 않습니다.

```text
App          ✅ 다시 실행
│
├── button   ✅ 갱신
│
└── Header   ❌ 다시 실행하지 않음
```

이것이 `React.memo`의 핵심입니다.

---

# 3. props가 있는 경우

`React.memo`는 본격적으로 **props 비교**를 수행할 때 의미가 있습니다.

```jsx
const User = React.memo(function User({ name }) {
  console.log("User render");

  return <h2>{name}</h2>;
});
```

부모:

```jsx
function App() {
  const [count, setCount] = useState(0);

  return (
    <>
      <button onClick={() => setCount(count + 1)}>
        {count}
      </button>

      <User name="Tom" />
    </>
  );
}
```

`count`가 변경되면:

```text
App 리렌더링
      ↓
<User name="Tom" />
      ↓
이전 props
{name: "Tom"}

새 props
{name: "Tom"}
      ↓
같음
      ↓
User 리렌더링 생략
```

따라서:

```text
App       ✅
User      ❌
```

가 됩니다.

---

# 4. React.memo는 무엇을 비교하는가?

기본적으로 `React.memo`는 props를 **얕은 비교(shallow comparison)** 합니다.

React 내부적으로는 개념적으로 다음과 비슷합니다.

```js
Object.is(prevProp, nextProp)
```

각 prop을 `Object.is()` 방식으로 비교합니다.

예를 들어:

```jsx
<User
  name="Tom"
  age={20}
/>
```

이전 props:

```js
{
  name: "Tom",
  age: 20
}
```

새 props:

```js
{
  name: "Tom",
  age: 20
}
```

문자열과 숫자는 값이 동일하기 때문에:

```text
name
"Tom" → "Tom"   same

age
20 → 20         same
```

결과:

```text
리렌더링 생략
```

입니다.

---

# 5. 객체 props에서 문제가 생긴다

여기서 `React.memo`를 처음 공부할 때 가장 중요한 문제가 등장합니다.

```jsx
function App() {
  const [count, setCount] = useState(0);

  const user = {
    name: "Tom"
  };

  return (
    <>
      <button onClick={() => setCount(count + 1)}>
        {count}
      </button>

      <User user={user} />
    </>
  );
}
```

`User`:

```jsx
const User = React.memo(function User({ user }) {
  console.log("User render");

  return <h2>{user.name}</h2>;
});
```

겉으로 보면 `user`의 내용은 계속 같습니다.

```js
{
  name: "Tom"
}
```

하지만 `App`이 실행될 때마다:

```js
const user = {
  name: "Tom"
};
```

새로운 객체가 생성됩니다.

메모리 관점에서는:

```text
첫 번째 렌더링

user
 ↓
0x100
{name:"Tom"}


두 번째 렌더링

user
 ↓
0x200
{name:"Tom"}
```

내용은 같지만 참조값이 다릅니다.

따라서:

```js
Object.is(prevUser, nextUser)
```

결과는:

```js
false
```

입니다.

그래서 `React.memo`가 있어도:

```text
App 리렌더링
      ↓
새 user 객체 생성
      ↓
prev.user !== next.user
      ↓
User 리렌더링
```

됩니다.

---

# 6. 이때 useMemo가 등장한다

객체 자체의 참조를 유지하고 싶다면 `useMemo`를 사용할 수 있습니다.

```jsx
function App() {
  const [count, setCount] = useState(0);

  const user = useMemo(() => {
    return {
      name: "Tom"
    };
  }, []);

  return (
    <>
      <button onClick={() => setCount(count + 1)}>
        {count}
      </button>

      <User user={user} />
    </>
  );
}
```

이제:

```text
첫 렌더링

user
 ↓
0x100


두 번째 렌더링

user
 ↓
0x100
```

참조가 유지됩니다.

그래서:

```text
React.memo
     ↓
props 비교
     ↓
prev.user === next.user
     ↓
true
     ↓
User 렌더링 생략
```

됩니다.

그래서 흔히:

```text
React.memo
   +
useMemo
```

가 함께 등장합니다.

---

# 7. 함수 props에서도 같은 문제가 발생한다

더 흔한 경우가 함수입니다.

```jsx
const Button = React.memo(function Button({ onClick }) {
  console.log("Button render");

  return <button onClick={onClick}>Click</button>;
});
```

부모:

```jsx
function App() {
  const [count, setCount] = useState(0);

  const handleClick = () => {
    console.log("click");
  };

  return (
    <>
      <p>{count}</p>

      <button onClick={() => setCount(count + 1)}>
        increase
      </button>

      <Button onClick={handleClick} />
    </>
  );
}
```

`App`이 리렌더링될 때마다:

```js
const handleClick = () => {
  console.log("click");
};
```

새로운 함수 객체가 만들어집니다.

```text
Render #1

handleClick
     ↓
Function 0x100


Render #2

handleClick
     ↓
Function 0x200
```

따라서:

```js
prev.onClick !== next.onClick
```

이 되어 `Button`도 다시 렌더링됩니다.

---

# 8. 이때 useCallback

함수 참조를 유지하려면 `useCallback`을 사용할 수 있습니다.

```jsx
const handleClick = useCallback(() => {
  console.log("click");
}, []);
```

그러면:

```text
React.memo
     ↓
props 비교
     ↓
onClick 참조 동일
     ↓
Button 리렌더링 생략
```

이 됩니다.

따라서 세 가지를 연결해서 이해하는 것이 좋습니다.

```text
React.memo
     │
     │ props 변경 여부 확인
     ↓
컴포넌트 렌더링 생략


useMemo
     │
     └─ 객체 / 계산 결과 참조 유지


useCallback
     │
     └─ 함수 참조 유지
```

---

# 9. React.memo의 정확한 역할

여기서 중요한 오해가 있습니다.

`React.memo`는:

> **컴포넌트를 절대 다시 렌더링하지 않게 만드는 기능이 아닙니다.**

정확히는:

> **부모의 리렌더링으로 인해 자식에게 렌더링 기회가 왔을 때, props가 이전과 같다면 해당 자식 컴포넌트의 렌더링 작업을 건너뛸 수 있게 하는 최적화 기능**입니다.

구조를 그리면:

```text
부모 state 변경
      ↓
Parent 렌더링
      ↓
Child 렌더링 후보
      ↓
React.memo ?
      ↓
    YES
      ↓
props 비교
   ↙      ↘
same      changed
 ↓          ↓
skip       render
```

---

# 10. 자신의 state가 바뀌면?

`React.memo`로 감싸도 **자기 자신의 state가 변경되면 다시 렌더링됩니다.**

```jsx
const Counter = React.memo(function Counter() {
  const [count, setCount] = useState(0);

  console.log("Counter render");

  return (
    <button onClick={() => setCount(count + 1)}>
      {count}
    </button>
  );
});
```

여기서 `count`가 변경되면:

```text
Counter state 변경
        ↓
Counter 리렌더링
```

됩니다.

`React.memo`는 이를 막지 않습니다.

즉:

```text
부모 때문에 렌더링
      ↓
React.memo가 막을 수 있음


자신의 state 변경
      ↓
React.memo가 막지 않음
```

입니다.

---

# 11. Context가 변경되어도 다시 렌더링된다

이 부분도 중요합니다.

```jsx
const ThemeContext = createContext();

const Header = React.memo(function Header() {
  const theme = useContext(ThemeContext);

  return <h1>{theme}</h1>;
});
```

Context 값이 변경되면:

```text
ThemeContext 변경
       ↓
Header가 Context를 구독
       ↓
Header 리렌더링
```

됩니다.

`React.memo`가 있어도 Context 업데이트는 막지 못합니다.

따라서:

```text
React.memo가 비교하는 핵심 대상
         ↓
        props
```

라고 이해하는 것이 좋습니다.

---

# 12. React.memo 내부 동작을 단순화하면

React 내부를 매우 단순화하면 다음과 같은 느낌입니다.

```js
function updateMemoComponent(
  prevProps,
  nextProps
) {

  if (shallowEqual(prevProps, nextProps)) {
    return previousResult;
  }

  return renderComponent(nextProps);
}
```

실제 React 구현은 Fiber, lanes, context, refs 등 훨씬 많은 조건을 고려하지만 개념적으로는:

```text
이전 props
     │
     │ compare
     ▼
새 props
     │
     ├── same ─────→ 기존 작업 재사용
     │
     └── different → 컴포넌트 렌더링
```

이라고 볼 수 있습니다.

---

# 13. Fiber 관점에서 보면

조금 더 깊게 들어가면 `React.memo`는 **reconciliation 과정에서 불필요한 작업을 줄이는 최적화**입니다.

일반적인 흐름:

```text
Parent Fiber 업데이트
        ↓
Child Fiber 확인
        ↓
Child Component 실행
        ↓
React Element 생성
        ↓
자식 reconciliation
```

`memo` 조건이 만족되면:

```text
Parent Fiber 업데이트
        ↓
Memo Child Fiber 확인
        ↓
props 비교
        ↓
변경 없음
        ↓
Bailout
        ↓
Child Component 실행 생략
```

여기서 중요한 용어가:

> **Bailout**

입니다.

즉 React가:

```text
"이 컴포넌트는 다시 계산할 필요가 없겠네."
```

라고 판단하고 해당 렌더링 작업을 건너뛰는 것입니다.

---

# 14. React.memo가 줄여주는 비용

컴포넌트 렌더링에는 생각보다 여러 단계가 있습니다.

```text
Component 함수 실행
        ↓
JSX 실행
        ↓
React Element 생성
        ↓
Fiber reconciliation
        ↓
자식 비교
```

`React.memo`는 조건이 맞으면 이 작업 일부를 건너뛰게 합니다.

```text
Parent render

       ↓

React.memo

       ↓

props unchanged

       ↓

       ✕ Component 실행
       ✕ JSX 계산
       ✕ 하위 reconciliation

       ↓

Bailout
```

따라서 컴포넌트가 복잡할수록 효과가 커질 수 있습니다.

---

# 15. 그렇다고 모든 컴포넌트에 React.memo를 붙이면 좋은가?

그렇지는 않습니다.

예를 들어:

```jsx
const Title = React.memo(function Title({ text }) {
  return <h1>{text}</h1>;
});
```

이런 매우 작은 컴포넌트까지 무조건 메모이제이션하면:

```text
렌더링 비용
vs
props 비교 비용
```

이 추가됩니다.

즉 `React.memo` 자체도 공짜가 아닙니다.

매번:

```text
이전 props
     ↓
비교
     ↓
새 props
```

작업을 해야 합니다.

따라서 단순한 컴포넌트에서는:

```text
그냥 다시 렌더링

```

하는 것이 오히려 충분히 빠를 수 있습니다.

---

# 16. React.memo가 효과적인 경우

대표적으로 다음과 같은 경우입니다.

* 컴포넌트 렌더링 비용이 큰 경우
* 부모가 자주 리렌더링되는 경우
* 자식의 props는 자주 바뀌지 않는 경우
* 동일한 UI를 반복적으로 계산하는 경우

예:

```jsx
const ProductList = React.memo(function ProductList({
  products
}) {

  return products.map(product => (
    <ProductCard
      key={product.id}
      product={product}
    />
  ));

});
```

`ProductList`가 수백 개의 아이템을 렌더링한다면 부모가 unrelated state 때문에 자주 렌더링될 때 memo가 의미 있을 수 있습니다.

---

# 17. custom comparison 함수

`React.memo`는 두 번째 파라미터로 직접 비교 함수를 전달할 수도 있습니다.

```jsx
const User = React.memo(
  function User({ user }) {
    return <h1>{user.name}</h1>;
  },

  function areEqual(prevProps, nextProps) {
    return (
      prevProps.user.name ===
      nextProps.user.name
    );
  }
);
```

형식은:

```js
React.memo(Component, arePropsEqual)
```

입니다.

여기서 중요한 점:

```js
return true;
```

이면:

> props가 동일하다고 판단 → 렌더링하지 않음

```js
return false;
```

이면:

> props가 변경되었다고 판단 → 렌더링

입니다.

보통 직관과 반대처럼 느껴질 수 있으므로 주의해야 합니다.

---

# 18. React.memo, useMemo, useCallback 차이

이 세 가지를 확실히 구분해야 합니다.

| 기능            | 메모이제이션 대상 |
| ------------- | --------- |
| `React.memo`  | 컴포넌트 렌더링  |
| `useMemo`     | 값         |
| `useCallback` | 함수        |

예를 들면:

```jsx
const Child = React.memo(ChildComponent);
```

```jsx
const value = useMemo(
  () => expensiveCalculation(),
  [a, b]
);
```

```jsx
const callback = useCallback(
  () => doSomething(),
  []
);
```

한 문장으로 정리하면:

```text
React.memo
"이 컴포넌트를 다시 실행해야 하나?"

useMemo
"이 값을 다시 계산해야 하나?"

useCallback
"이 함수 객체를 새로 만들어야 하나?"
```

입니다.

---

# 19. 전체 관계를 한 번에 보면

아래 그림이 가장 중요합니다.

```text
             Parent
               │
        state 변경 → render
               │
               ▼
        새로운 props 생성
               │
               ▼
          React.memo
               │
        ┌──────┴──────┐
        │             │
     props same    props changed
        │             │
        ▼             ▼
     Bailout        Child render
        │
        │
   렌더링 생략
```

그런데 객체나 함수가 props라면:

```text
Parent render
     │
     ├─ {}        → 새로운 객체
     │
     └─ () => {}  → 새로운 함수
           │
           ▼
       참조 변경
           │
           ▼
React.memo가 변경으로 판단
```

그래서:

```text
객체
 ↓
useMemo

함수
 ↓
useCallback
```

이 함께 사용되는 경우가 많습니다.

---

## 마지막으로 가장 중요한 정의

`React.memo`를 강의에서 한 문장으로 설명한다면 저는 이렇게 정리하는 게 가장 정확하다고 봅니다.

> **`React.memo`는 부모 컴포넌트의 리렌더링으로 자식 컴포넌트도 다시 렌더링될 상황에서, 이전 props와 새로운 props를 비교하여 값이 같다면 해당 컴포넌트의 렌더링 작업을 건너뛰도록 하는 React의 성능 최적화 기능입니다.**

그리고 Fiber 관점까지 포함하면:

> **`React.memo`는 reconciliation 과정에서 props가 변경되지 않은 컴포넌트에 대해 bailout을 수행하여 컴포넌트 함수 실행과 하위 렌더링 작업을 줄이는 최적화 기법입니다.**

특히 `React.memo`를 제대로 이해하려면 **“부모 리렌더링 → 자식 렌더링 후보 → props 비교 → bailout”** 이 네 단계를 머릿속에 잡아두는 것이 핵심입니다.
