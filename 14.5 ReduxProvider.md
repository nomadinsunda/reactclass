## Redux Provider란?

react-redux에서 제공하는 **`<Provider />`** 는
👉 **Redux Store를 React 컴포넌트 트리 전체에 “주입(inject)”하는 역할**을 합니다.

```jsx
import { Provider } from 'react-redux'
import { store } from './store'

root.render(
  <Provider store={store}>
    <App />
  </Provider>
)
```

---

![Image](https://cdn-blog.scalablepath.com/uploads/2022/08/redux-data-management-diagram-1.webp)

![Image](https://miro.medium.com/0%2Afr58WxnWgRG_dTZj.gif)

![Image](https://redux.js.org/assets/images/ReduxAsyncDataFlowDiagram-d97ff38a0f4da0f327163170ccc13e80.gif)

---

## 1️⃣ Redux Provider가 없으면 무슨 일이 벌어질까?

Redux Store는 **전역 객체**이지만,
React 컴포넌트는 **Store를 직접 참조하지 않습니다.**

❌ Provider 없이 이렇게 쓰면:

```js
const dispatch = useDispatch()
```

👉 즉시 에러 발생:

```
could not find react-redux context value
```

이유는 간단합니다.

> **React는 “전역 변수”를 자동으로 공유하지 않기 때문입니다.**

---

## 2️⃣ Provider의 본질: React Context

`<Provider />`의 정체는 이것입니다 👇

> **Redux Store를 React Context에 올려두는 컴포넌트**

내부적으로는 대략 이런 구조입니다(개념):

```jsx
<ReduxContext.Provider value={store}>
  {children}
</ReduxContext.Provider>
```

그리고:

* `useDispatch()`
* `useSelector()`

는 이 Context에서 **store를 꺼내 쓰는 Hook**입니다.

---

## 3️⃣ useDispatch / useSelector가 동작하는 정확한 이유

```js
const dispatch = useDispatch()
```

실제로 일어나는 일:

1️⃣ React Context에서 store 조회
2️⃣ `store.dispatch` 반환

```js
dispatch === store.dispatch // true
```

```js
const state = useSelector(selector)
```

1️⃣ store.subscribe 등록
2️⃣ state 변경 시 selector 실행
3️⃣ 결과 비교 후 리렌더링 여부 결정

👉 **모든 출발점은 Provider가 주입한 store**

---

## 4️⃣ Provider와 configureStore의 관계 (핵심)

이 둘은 **완전히 역할이 다릅니다.**

| 구분               | 역할                         |
| ---------------- | -------------------------- |
| `configureStore` | Redux Store **생성**         |
| `Provider`       | Redux Store를 React에 **공급** |

```txt
configureStore  →  store 객체 생성
Provider        →  store를 React 트리에 연결
```

👉 둘 중 하나라도 없으면 Redux는 작동하지 않습니다.

---

## 5️⃣ 왜 Provider는 보통 “최상단”에 둘까?

```jsx
<Provider store={store}>
  <App />
</Provider>
```

이 구조의 의미는:

> **App 이하의 모든 컴포넌트는
> Redux Store에 접근 가능하다**

### 만약 이렇게 하면?

```jsx
<App />
<Provider store={store}>
  <SomeComponent />
</Provider>
```

👉 `App` 내부에서는 `useDispatch` / `useSelector` 사용 ❌

---

## 6️⃣ Provider는 여러 개 둘 수 있을까?

👉 **기술적으로는 가능하지만, 거의 쓰지 않습니다.**

```jsx
<Provider store={storeA}>
  <A />
</Provider>

<Provider store={storeB}>
  <B />
</Provider>
```

이 경우:

* A는 storeA만
* B는 storeB만 접근 가능

📌 **실무에서는 “앱 = store 하나”가 원칙**

(멀티 store는 마이크로 프론트엔드 같은 특수 케이스)

---

## 7️⃣ Redux Provider를 “아키텍처적으로” 해석하면

이게 강의에서 정말 중요한 포인트입니다.

> **Provider는
> “상태 관리 인프라의 경계(Boundary)”를 선언하는 컴포넌트입니다.**

즉,

* UI 컴포넌트 ❌
* 비즈니스 로직 ❌
* 상태 변경 ❌

👉 **의존성 주입 컨테이너(DI Container)** 역할

Spring으로 치면:

```txt
ApplicationContext
```

와 매우 유사합니다.

---

## 8️⃣ 지금 사용 중인 코드와 연결해서 보면

```js
export const useCards = (listid) => {
  const dispatch = useDispatch()
  const cards = useSelector(...)
}
```

이 훅이 **Redux에 직접 의존하지 않는 것처럼 보이는 이유**는:

* store를 import ❌
* configureStore를 import ❌

👉 **Provider가 이미 의존성을 주입해 줬기 때문**

이게 바로 **React + Redux의 핵심 설계 미학**입니다.

