# React `useContext`

## — Props Drilling 없이 Context 값을 읽고 구독하는 Hook

React에서 컴포넌트 사이에 데이터를 전달하는 가장 기본적인 방법은 **props**입니다.

```text
Parent
  │
  │ props
  ▼
Child
```

하지만 로그인 사용자, 테마, 언어 설정처럼 **트리의 여러 위치에서 공통으로 사용하는 값**을 props만으로 전달하면 문제가 생길 수 있습니다.

```text
App
 │ user
 ▼
Layout
 │ user
 ▼
Header
 │ user
 ▼
UserMenu
 │ user
 ▼
UserAvatar
```

실제로 `user`를 사용하는 곳이 `UserAvatar`뿐이라도 `Layout`, `Header`, `UserMenu`가 계속 `user`를 전달해야 합니다.

이것이 **Prop Drilling**입니다.

React의 **Context API**는 이런 상황에서 중간 컴포넌트를 거치지 않고 값을 전달할 수 있는 별도의 통로를 제공합니다.

그리고 함수 컴포넌트가 그 통로의 값을 읽기 위해 사용하는 대표적인 Hook이 바로:

```jsx
useContext(Context)
```

입니다.

---

# 1. useContext 한 문장 정의

> **`useContext`는 현재 컴포넌트가 특정 Context의 값을 읽고, 그 Context의 변경을 구독하도록 만드는 React Hook입니다.**

여기서 중요한 단어가 두 개 있습니다.

```text
useContext(Context)
       │
       ├── ① 값 읽기
       │
       └── ② Context 구독
```

단순히 값을 한 번 가져오는 함수가 아닙니다.

Context의 값이 변경되면 `useContext`로 해당 Context를 구독한 컴포넌트가 새로운 값을 사용하도록 React가 업데이트를 스케줄링합니다.

---

# 2. 왜 useContext가 필요한가?

다음과 같은 컴포넌트 구조가 있다고 해봅시다.

```jsx
<App>
  <Layout>
    <Header>
      <UserMenu>
        <UserAvatar />
      </UserMenu>
    </Header>
  </Layout>
</App>
```

`App`이 로그인 사용자 정보를 가지고 있습니다.

```jsx
function App() {
  const user = {
    name: '홍길동',
    role: 'admin'
  };

  return <Layout user={user} />;
}
```

props만 사용한다면:

```jsx
function Layout({ user }) {
  return <Header user={user} />;
}

function Header({ user }) {
  return <UserMenu user={user} />;
}

function UserMenu({ user }) {
  return <UserAvatar user={user} />;
}

function UserAvatar({ user }) {
  return <div>{user.name}</div>;
}
```

데이터는 다음 경로를 따라 이동합니다.

```text
App
 │
 │ user
 ▼
Layout       ← 사용하지 않음
 │
 │ user
 ▼
Header       ← 사용하지 않음
 │
 │ user
 ▼
UserMenu     ← 사용하지 않음
 │
 │ user
 ▼
UserAvatar   ← 실제 사용
```

중간 컴포넌트들은 값을 사용하지 않으면서 전달만 담당합니다.

컴포넌트 트리가 깊어질수록 이런 props 연결을 유지하기 어려워집니다.

---

# 3. Context를 사용하면 전달 경로가 달라진다

Context를 사용하면 구조가 달라집니다.

```text
                 UserContext
                     │
App ── Provider ─────┤
                     │
     Layout          │
       │             │
     Header          │
       │             │
    UserMenu         │
       │             │
    UserAvatar ◀─────┘
```

`Layout`, `Header`, `UserMenu`가 `user`를 전달할 필요가 없습니다.

`UserAvatar`가 필요한 순간 직접:

```jsx
const user = useContext(UserContext);
```

라고 읽습니다.

즉,

```text
Props 방식

App
 ↓
Layout
 ↓
Header
 ↓
UserMenu
 ↓
UserAvatar


Context 방식

Provider
   │
   └──────────────▶ UserAvatar
                     useContext()
```

입니다.

---

# 4. Context API의 세 가지 역할

`useContext`를 이해하려면 먼저 Context API의 세 역할을 구분해야 합니다.

```text
① createContext()
        │
        ▼
   Context 생성
        │
        ▼
② Provider
        │
        │ value 공급
        ▼
  Component Tree
        │
        ▼
③ useContext()
        │
        ▼
   value 읽기 + 구독
```

역할을 정리하면:

| 역할         | API                     |
| ---------- | ----------------------- |
| Context 생성 | `createContext()`       |
| 값 공급       | `<Context value={...}>` |
| 값 읽기/구독    | `useContext(Context)`   |

React 19에서는 Context 객체 자체를 Provider로 렌더링할 수 있습니다.

```jsx
<ThemeContext value={theme}>
  <Page />
</ThemeContext>
```

기존 React 코드에서는 다음 문법도 많이 볼 수 있습니다.

```jsx
<ThemeContext.Provider value={theme}>
  <Page />
</ThemeContext.Provider>
```

---

# 5. 1단계 — Context 만들기

먼저 Context 객체를 만듭니다.

```jsx
import { createContext } from 'react';

export const ThemeContext =
  createContext('light');
```

개념적으로:

```text
createContext('light')
        │
        ▼
┌─────────────────────┐
│    ThemeContext     │
│                     │
│ default: 'light'    │
└─────────────────────┘
```

여기서 중요한 점이 있습니다.

`createContext()`는 **state를 만드는 함수가 아닙니다.**

즉:

```text
createContext()
      ≠
상태 저장소 생성
```

보다 정확하게는:

```text
createContext()
      ↓
Context를 식별하는 객체 생성
      ↓
값을 전달할 통로 정의
```

라고 이해하는 것이 좋습니다.

---

# 6. 2단계 — Provider가 값을 공급한다

이제 실제 값을 Context에 공급합니다.

```jsx
import { useState } from 'react';
import { ThemeContext } from './ThemeContext';

function App() {
  const [theme, setTheme] = useState('light');

  return (
    <ThemeContext value={theme}>
      <Page />
    </ThemeContext>
  );
}
```

여기서 상태를 실제로 보관하는 것은:

```jsx
useState('light')
```

입니다.

Context는 그 값을 아래 트리로 전달합니다.

```text
useState
   │
   │ 상태 보관
   ▼
 theme
   │
   │ value
   ▼
ThemeContext
   │
   │ 값 공급
   ▼
 Page subtree
```

따라서:

> **State는 `useState`가 관리하고, Context는 그 값을 전달합니다.**

이 구분이 매우 중요합니다.

---

# 7. 3단계 — useContext로 값 읽기

하위 컴포넌트에서는 다음과 같이 읽습니다.

```jsx
import { useContext } from 'react';
import { ThemeContext } from './ThemeContext';

function ThemeButton() {
  const theme = useContext(ThemeContext);

  return (
    <button>
      현재 테마: {theme}
    </button>
  );
}
```

핵심 코드는:

```jsx
const theme = useContext(ThemeContext);
```

입니다.

개념적으로:

```text
ThemeContext
value = "light"
      │
      │
      ▼
ThemeButton
      │
      │ useContext(ThemeContext)
      ▼
 theme = "light"
```

처럼 보입니다.

하지만 내부적으로는 한 가지 중요한 일이 더 발생합니다.

---

# 8. useContext는 값을 읽기만 하는 것이 아니다

`useContext`를 다음처럼 단순히 생각하기 쉽습니다.

```text
useContext
    ↓
값 가져오기
```

그러나 실제 개념은:

```text
useContext(Context)
       │
       ├── 현재 Context 값 읽기
       │
       └── 현재 Fiber를
           해당 Context의 소비자로 등록
```

입니다.

즉:

```jsx
const theme = useContext(ThemeContext);
```

가 실행되면 현재 컴포넌트는 `ThemeContext`를 **구독하는 컴포넌트**가 됩니다.

그래서 나중에 Provider의 `value`가 변경되면 해당 컴포넌트가 새로운 Context 값을 사용할 수 있도록 React가 업데이트를 스케줄링합니다.

---

# 9. 가장 가까운 Provider의 값을 사용한다

Provider는 중첩할 수 있습니다.

```jsx
<ThemeContext value="light">

  <Header />

  <ThemeContext value="dark">
    <Sidebar />
  </ThemeContext>

</ThemeContext>
```

구조는:

```text
ThemeContext
value = "light"
│
├── Header
│     │
│     └── useContext()
│            ↓
│          light
│
└── ThemeContext
    value = "dark"
      │
      └── Sidebar
            │
            └── useContext()
                   ↓
                 dark
```

`useContext(ThemeContext)`는 현재 컴포넌트 기준으로 적용되는 **가장 가까운 Provider의 value**를 읽습니다.

따라서 Context는 JavaScript의 진짜 전역 변수와 다릅니다.

```text
Global Variable
      │
      └── 프로그램 전체


Context
      │
      └── Provider가 감싼 Subtree
```

---

# 10. Provider가 없다면?

다음 Context가 있다고 해봅시다.

```jsx
const ThemeContext = createContext('light');
```

그리고 Provider 없이:

```jsx
function Button() {
  const theme = useContext(ThemeContext);

  return <button>{theme}</button>;
}
```

사용하면:

```text
theme = "light"
```

가 됩니다.

`createContext('light')`의 `'light'`가 **default value**이기 때문입니다.

하지만 중요한 규칙이 있습니다.

> default value는 **Provider가 존재하지 않을 때만** 사용됩니다.

예를 들어:

```jsx
<ThemeContext value={undefined}>
  <Button />
</ThemeContext>
```

이면:

```text
light       X

undefined   O
```

입니다.

Provider가 존재하기 때문에 `undefined` 자체가 Context 값입니다.

---

# 11. 실전에서는 undefined + 커스텀 훅 패턴을 많이 사용한다

Provider 누락을 빨리 발견하고 싶다면 다음처럼 만들 수 있습니다.

```tsx
const AuthContext =
  createContext<AuthContextValue | undefined>(
    undefined
  );
```

그리고:

```tsx
export function useAuth() {
  const context = useContext(AuthContext);

  if (context === undefined) {
    throw new Error(
      'useAuth must be used within AuthProvider'
    );
  }

  return context;
}
```

컴포넌트에서는:

```jsx
function Header() {
  const { user, logout } = useAuth();

  // ...
}
```

이제 구조가:

```text
Component
    │
    ▼
 useAuth()
    │
    ▼
useContext(AuthContext)
    │
    ▼
AuthContext value
```

가 됩니다.

이 패턴의 장점은:

* Context 접근 코드를 한 곳에 모을 수 있습니다.
* Provider 누락을 즉시 발견할 수 있습니다.
* 컴포넌트가 Context 구현 세부사항에 덜 의존합니다.

---

# 12. 현실적인 AuthContext 구조

실무에서는 다음과 같은 구조를 많이 사용합니다.

```text
AuthProvider
│
├── useState
│      │
│      └── user
│
├── login()
│
├── logout()
│
└── AuthContext
       │
       │ value
       ▼
    App Subtree
       │
       ├── Header
       │     └── useAuth()
       │
       ├── Dashboard
       │     └── useAuth()
       │
       └── LoginForm
             └── useAuth()
```

예를 들어:

```tsx
export function AuthProvider({ children }) {
  const [user, setUser] = useState<User>(null);

  const login = useCallback((name: string) => {
    setUser({
      id: Date.now(),
      name
    });
  }, []);

  const logout = useCallback(() => {
    setUser(null);
  }, []);

  const value = useMemo(
    () => ({
      user,
      login,
      logout
    }),
    [user, login, logout]
  );

  return (
    <AuthContext value={value}>
      {children}
    </AuthContext>
  );
}
```

여기서도 역할을 구분할 수 있습니다.

```text
useState
   ↓
상태 저장


login / logout
   ↓
상태 변경


AuthContext
   ↓
값 전달


useAuth()
   ↓
값 소비
```

---

# 13. Provider의 value가 변경되면?

다음 구조를 생각해봅시다.

```text
AuthContext
value = { user }
│
├── Header
│     └── useAuth()
│
├── Sidebar
│     └── useAuth()
│
└── Main
      └── Context 사용 X
```

`user`가 변경되면 Provider의 `value`도 변경됩니다.

```text
이전

value
  │
  ▼
{ user: Alice }


setUser(Bob)
     │
     ▼

새로운 value
  │
  ▼
{ user: Bob }
```

그러면 해당 Context를 구독하고 있는 컴포넌트가 새로운 값을 사용하도록 업데이트됩니다.

```text
AuthContext value 변경
          │
          ├──────────────┐
          ▼              ▼
       Header         Sidebar
     re-render       re-render
```

여기서 핵심은:

> **Provider 아래의 모든 컴포넌트가 Context 때문에 무조건 리렌더되는 것이 아니라, 해당 Context를 소비하는 컴포넌트가 Context 변경의 직접적인 업데이트 대상이 됩니다.**

단, 일반적인 부모 → 자식 리렌더 규칙에 의해 다른 컴포넌트가 함께 실행되는 상황은 별개의 문제입니다.

---

# 14. React.memo로 Context 업데이트를 막을 수 있을까?

다음 코드를 보겠습니다.

```jsx
const Sidebar = React.memo(function Sidebar() {
  const { user } = useAuth();

  return <aside>{user?.name}</aside>;
});
```

`React.memo`가 있으므로 Context 변경도 막을 것 같지만 그렇지 않습니다.

```text
React.memo
    │
    └── props 비교


useContext
    │
    └── Context 구독
```

Context 값이 변경되면:

```text
AuthContext 변경
       │
       ▼
   Sidebar Fiber
       │
       ▼
    re-render
```

됩니다.

왜냐하면 Context 업데이트는 props 변경과 다른 경로이기 때문입니다.

따라서:

> **Context를 직접 소비하는 컴포넌트는 `React.memo`로 감싸도 Context value가 변경되면 다시 렌더링됩니다.**

---

# 15. value 객체의 참조 문제

다음 코드는 흔히 볼 수 있습니다.

```jsx
<AuthContext
  value={{
    user,
    login,
    logout
  }}
>
  {children}
</AuthContext>
```

JavaScript 객체 리터럴은 실행할 때마다 새로운 객체를 만듭니다.

```text
Render #1

value ─────▶ Object A


Render #2

value ─────▶ Object B
```

따라서 내용이 같아 보여도:

```js
ObjectA !== ObjectB
```

입니다.

Provider가 다른 이유로 리렌더될 때 새로운 `value` 객체가 생성되면 Context 소비자에게 불필요한 업데이트가 발생할 수 있습니다.

---

# 16. useMemo로 value 참조 안정화

필요한 경우 `useMemo`를 사용합니다.

```jsx
const value = useMemo(
  () => ({
    user,
    login,
    logout
  }),
  [user, login, logout]
);
```

그리고:

```jsx
<AuthContext value={value}>
  {children}
</AuthContext>
```

이렇게 하면 의존성이 변하지 않는 동안 같은 객체 참조를 재사용할 수 있습니다.

```text
user 변경 X

Render #1 ──▶ Object A
Render #2 ──▶ Object A


user 변경 O

Render #3 ──▶ Object B
```

단, `useMemo`는 모든 Context에 무조건 적용해야 하는 규칙이 아닙니다.

Provider의 리렌더 빈도와 소비자 규모를 보고 **실제 불필요한 Context 업데이트가 문제가 될 때 사용하는 최적화 수단**으로 이해하는 것이 좋습니다.

---

# 17. Context를 분리해야 하는 이유

다음처럼 모든 값을 하나의 Context에 넣는다고 생각해봅시다.

```text
AppContext
│
├── user
├── theme
├── locale
├── sidebarOpen
└── notifications
```

`sidebarOpen` 하나만 바뀌어도 `AppContext`의 value가 변경됩니다.

그러면 `user`만 사용하는 컴포넌트도 같은 Context를 구독하고 있기 때문에 영향을 받습니다.

그래서 성격이 다른 값은 Context를 분리할 수 있습니다.

```text
AuthContext
   │
   └── user


ThemeContext
   │
   └── theme


UIContext
   │
   └── sidebarOpen
```

이렇게 하면 Context 변경 범위를 더 명확하게 제어할 수 있습니다.

---

# 18. useContext의 Fiber 관점 내부 동작

이제 내부 구조를 조금 더 깊게 보겠습니다.

컴포넌트가:

```jsx
const theme = useContext(ThemeContext);
```

를 실행하면 개념적으로 두 가지 일이 발생합니다.

```text
ThemeContext
     │
     ▼
현재 Context value 읽기
     │
     ▼
theme 반환


현재 Fiber
     │
     ▼
ThemeContext dependency 등록
```

Fiber를 단순화하면:

```text
┌───────────────────────────┐
│        Page Fiber         │
│                           │
│ memoizedState             │
│ ├── useState              │
│ └── useEffect             │
│                           │
│ dependencies              │
│ └── ThemeContext          │
└───────────────────────────┘
```

여기서 중요한 점은:

> **Context 구독 정보는 `useState`처럼 Hook의 `memoizedState` 슬롯에 저장되는 것이 아니라 Fiber의 Context dependency 정보로 관리됩니다.**

따라서 Provider value가 변경되면 React는 이 Context에 의존하는 Fiber들을 업데이트 대상으로 표시할 수 있습니다.

---

# 19. Context 변경 → 리렌더 전체 흐름

지금까지의 내용을 연결하면:

```text
① createContext()
       │
       ▼
Context 객체 생성
       │
       ▼
② Provider
       │
       │ value 공급
       ▼
③ Component
       │
       │ useContext(Context)
       ▼
④ 현재 value 읽기
       +
   Context 구독 등록
       │
       ▼
⑤ Provider value 변경
       │
       ▼
⑥ 해당 Context에 의존하는
   Fiber 업데이트 스케줄링
       │
       ▼
⑦ 컴포넌트 리렌더
       │
       ▼
⑧ 새로운 Context value 읽기
```

이 흐름을 이해하면 `useContext`가 단순한 값 조회 함수가 아니라는 것이 명확해집니다.

---

# 20. React 19의 use(Context)

React 19에서는 Context를 다음과 같이 읽을 수도 있습니다.

```jsx
import { use } from 'react';

function Panel({ compact }) {
  if (compact) {
    const theme = use(ThemeContext);

    return <span>{theme}</span>;
  }

  return <FullPanel />;
}
```

`useContext`와 중요한 차이는 호출 위치입니다.

| 구분             | `useContext(C)` | `use(C)` |
| -------------- | --------------- | -------- |
| Context 읽기     | O               | O        |
| Context 변경 구독  | O               | O        |
| 조건문 안          | X               | O        |
| 반복문 안          | X               | O        |
| Promise 읽기     | X               | O        |
| 컴포넌트/Hook 본문 밖 | X               | X        |

따라서 일반적인 경우에는:

```jsx
useContext(Context)
```

를 사용하고,

조건부로 Context를 읽어야 하는 React 19 코드에서는:

```jsx
use(Context)
```

를 사용할 수 있습니다.

---

# 21. useContext와 상태 관리를 구분하자

가장 중요한 개념입니다.

```text
useState / useReducer
        │
        ▼
   상태 저장 및 변경
        │
        ▼
      Context
        │
        ▼
     값 전달
        │
        ▼
useContext / use
        │
        ▼
    값 읽기 + 구독
```

따라서 다음 표현은 정확하지 않습니다.

```text
Context = 전역 상태 저장소
```

보다 정확한 표현은:

> **Context는 컴포넌트 트리의 특정 범위에 값을 전달하는 메커니즘이고, `useContext`는 그 값을 읽고 변경을 구독하는 Hook입니다.**

---

# 22. 언제 useContext를 사용하면 좋은가?

대표적인 예는:

```text
ThemeContext
LocaleContext
AuthContext
Router Context
UI Library Context
Form Context
Dependency Injection
```

등입니다.

공통적인 특징은:

> **여러 컴포넌트가 필요로 하지만 중간 컴포넌트가 직접 사용할 필요는 없는 값**

입니다.

반대로 매우 자주 변경되고 많은 컴포넌트가 서로 다른 일부 값만 필요로 하는 상태라면 Context 하나에 모두 넣는 것은 비효율적일 수 있습니다.

이런 경우에는 Redux, Zustand 등의 selector 기반 상태 관리 방식을 고려할 수 있습니다.

---

# 23. 최종 정리

`useContext`를 이해할 때 다음 흐름을 기억하면 됩니다.

```text
createContext()
      │
      ▼
Context 객체
      │
      ▼
Provider
      │
      │ value
      ▼
Component
      │
      │ useContext(Context)
      ▼
┌─────────────────────┐
│ ① 현재 value 읽기   │
│ ② Context 구독      │
└─────────────────────┘
      │
      ▼
Provider value 변경
      │
      ▼
구독 Fiber 업데이트
      │
      ▼
Component re-render
      │
      ▼
새 value 사용
```

한 문장으로 압축하면:

> **`useContext`는 현재 컴포넌트에 적용되는 가장 가까운 Provider의 Context 값을 읽고, 해당 Context의 변경을 현재 Fiber가 구독하도록 만드는 React Hook입니다.**

그리고 세 API의 역할을 반드시 구분해서 기억해야 합니다.

```text
createContext
     ↓
통로 정의

Provider
     ↓
값 공급

useContext
     ↓
값 읽기 + 구독
```

이 구조가 이해되면 `useContext`뿐 아니라 React Router, 인증 Provider, Theme Provider 그리고 여러 React 라이브러리가 Context를 내부적으로 사용하는 이유까지 자연스럽게 연결됩니다.