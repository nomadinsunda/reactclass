

# `React.createContext`로 다중 Context 관리

---

## O 1. 왜 "다중 Context"가 필요한가?

### X 단일 Context 문제

```tsx
const AppContext = createContext({
  user: null,
  theme: 'light',
  language: 'ko',
});
```

> 하나의 Context에 여러 상태를 묶으면…

* 한 값만 바뀌어도 **모든 구독 컴포넌트가 리렌더링**
* 값의 변화 추적이 어려워지고, 성능 저하 발생

---

## O 2. 해결: Context를 역할별로 **분리**

### 각각 독립적인 Context 생성

```tsx
// contexts/UserContext.js
export const UserContext = createContext(null);

// contexts/ThemeContext.js
export const ThemeContext = createContext('light');

// contexts/LanguageContext.js
export const LanguageContext = createContext('ko');
```

---

## O 3. Provider 중첩으로 공급하기 (React 19 문법)

```tsx
import { UserContext } from './contexts/UserContext';
import { ThemeContext } from './contexts/ThemeContext';
import { LanguageContext } from './contexts/LanguageContext';

export default function App() {
  return (
    // React 19: Context 객체 자체가 Provider 역할을 합니다.
    <UserContext value={{ name: '홍길동' }}>
      <ThemeContext value="dark">
        <LanguageContext value="en">
          <MainApp />
        </LanguageContext>
      </ThemeContext>
    </UserContext>
  );
}
```

> React 18까지는 `<UserContext.Provider value={...}>` 처럼 `.Provider`를 붙였습니다.
> **아직 동작하지만 향후 릴리스에서 deprecated 예정**이라, 새 코드에서는 위 문법을 쓰세요.
> (render prop 방식인 `<UserContext.Consumer>`도 레거시입니다.)

O 각 Context는 **독립적으로 관리되고 리렌더링 범위도 좁음**

---

## O 4. `useContext`로 필요한 것만 꺼내기

```tsx
const user = useContext(UserContext);
const theme = useContext(ThemeContext);

// React 19에서는 조건부로 읽어야 할 때 use()도 사용 가능
// if (isAdmin) { const perm = use(PermissionContext); ... }
```

> 필요한 Context만 구독하면, **구독하지 않은 Context가 바뀔 때는 리렌더되지 않습니다.**

여기서 오해하기 쉬운 두 가지를 짚고 갑니다.

* “**그 컴포넌트만** 리렌더된다”는 정확히는 틀립니다.
  구독 컴포넌트가 리렌더되면 그 **자식들도 기본적으로 함께 리렌더**됩니다. (`memo`나 `children` 통과 구조로만 끊을 수 있음)
* Context를 아무리 잘게 쪼개도 **“한 Context 값의 일부 필드만 구독”은 불가능**합니다.
  `UserContext`의 `email`만 쓰는 컴포넌트도, `user` 객체가 바뀌면 리렌더됩니다.
  React 19에도 공식 `useContextSelector`는 없습니다. 부분 구독이 꼭 필요하면 Zustand 같은 selector 기반 store를 쓰세요.

---

## 고급 패턴: `ContextProvider` 컴포넌트로 감싸기

> 여러 개의 Provider를 하나의 컴포넌트로 추상화하면 코드가 깔끔해집니다.

```tsx
// AppProviders.jsx
import { useMemo } from 'react';
import { UserContext } from './contexts/UserContext';
import { ThemeContext } from './contexts/ThemeContext';
import { LanguageContext } from './contexts/LanguageContext';

export default function AppProviders({ children }) {
  // 객체 value는 반드시 참조를 고정 (문자열/숫자 같은 원시값은 불필요)
  const user = useMemo(() => ({ name: '홍길동' }), []);

  return (
    <UserContext value={user}>
      <ThemeContext value="dark">
        <LanguageContext value="ko">
          {children}
        </LanguageContext>
      </ThemeContext>
    </UserContext>
  );
}
```

> `children`을 그대로 통과시키는 구조라서, `AppProviders`가 리렌더돼도 **`children` 엘리먼트는 재생성되지 않습니다.**
> 이게 Provider 컴포넌트를 따로 빼는 숨은 이점입니다.

```tsx
// main.jsx
<AppProviders>
  <App />
</AppProviders>
```

---

## 주의: 리렌더링과 성능 최적화

* 각 Context는 **독립적인 값**을 가져야 함
* `value`에 객체/배열/함수를 인라인으로 넣으면 **부모가 리렌더될 때마다 참조가 새로 생겨** 모든 소비자가 리렌더됩니다.
  → `useMemo` / `useCallback`으로 **참조 고정**

```tsx
// ❌ App이 리렌더될 때마다 새 객체 → UserContext 소비자 전원 리렌더
<UserContext value={{ name: '홍길동' }}>{children}</UserContext>

// ✅ 참조 고정
const user = useMemo(() => ({ name: '홍길동' }), []);
<UserContext value={user}>{children}</UserContext>
```

> 그리고 이건 `React.memo`로 막을 수 없습니다.
> `memo`는 props만 비교하는데, 컨텍스트 구독은 props가 아니기 때문입니다.
> **컨텍스트를 읽는 컴포넌트는 memo로 감싸도 value가 바뀌면 반드시 리렌더**됩니다.
> 유일한 해법은 (1) value 참조를 안정화하거나 (2) 구독 범위를 좁히는 것입니다.

---

## 실전 적용 예시

| Context 이름          | 역할          | 값 예시                   |
| ------------------- | ----------- | ---------------------- |
| `UserContext`       | 로그인 유저 정보   | `{ id, name, email }`  |
| `ThemeContext`      | 테마 (다크/라이트) | `'light'` 또는 `'dark'`  |
| `LocaleContext`     | 언어 설정       | `'en'`, `'ko'`, `'ja'` |
| `PermissionContext` | 권한 설정       | `{ isAdmin: true }`    |

---

## `createContext(defaultValue)`의 기본값, 정확히 이해하기

Context를 여러 개 만들다 보면 “기본값을 뭘로 줘야 하지?”에서 막힙니다. 규칙은 딱 하나입니다.

> `defaultValue`는 **트리에 해당 Provider가 하나도 없을 때만** 사용됩니다.

```tsx
const ThemeContext = createContext('light');

// (1) Provider 없음        → useContext(ThemeContext) === 'light'  ✅ 기본값 사용
// (2) <ThemeContext value="dark">  → 'dark'
// (3) <ThemeContext value={undefined}> → undefined  ❌ 'light'로 안 돌아감!
```

* (3)이 핵심입니다. **Provider가 존재하는 순간 기본값은 완전히 무시**됩니다.
  “값을 안 주면 기본값으로 채워주겠지”는 흔한 오해입니다.
* 그래서 실무에서는 기본값을 **일부러 `null` / `undefined`로 두는** 경우가 많습니다.
  → “Provider를 감싸는 걸 깜빡했다”를 조용한 버그가 아니라 **명시적 에러**로 만들 수 있기 때문입니다.

### 안전한 조합: 기본값 `null` + 커스텀 훅 래퍼

```tsx
// contexts/ThemeContext.tsx
import { createContext, useContext, useMemo, useState, ReactNode } from 'react';

type ThemeValue = { theme: 'light' | 'dark'; toggle: () => void };

// 👇 기본값을 null로 두어 "Provider 없음"을 감지 가능하게
const ThemeContext = createContext<ThemeValue | null>(null);

export function ThemeProvider({ children }: { children: ReactNode }) {
  const [theme, setTheme] = useState<'light' | 'dark'>('light');
  const value = useMemo<ThemeValue>(
    () => ({ theme, toggle: () => setTheme(t => (t === 'light' ? 'dark' : 'light')) }),
    [theme]
  );
  return <ThemeContext value={value}>{children}</ThemeContext>;
}

// 👇 소비자는 이 훅만 쓴다. Context 객체는 export하지 않아도 된다.
export function useTheme(): ThemeValue {
  const ctx = useContext(ThemeContext);
  if (ctx === null) {
    throw new Error('useTheme must be used within <ThemeProvider>');
  }
  return ctx;   // 여기서부터 타입은 ThemeValue (null 아님) 로 좁혀짐 ✅
}
```

이 패턴의 이점:

* **타입 안전**: 훅 밖으로 나가는 순간 `null`이 제거되어, 소비자 쪽에서 `ctx?.theme` 같은 옵셔널 체이닝이 사라집니다.
* **실수 조기 발견**: Provider 누락 시 “값이 이상함”이 아니라 **명확한 에러 메시지**로 즉시 드러납니다.
* **캡슐화**: Context 객체 자체를 export하지 않으면, 아무도 엉뚱한 곳에서 Provider를 덧씌울 수 없습니다.

---

## O 마무리 요약

| 항목            | 설명                                              |
| ------------- | ----------------------------------------------- |
| 다중 Context 목적 | 역할별 상태를 나누고, 불필요한 리렌더를 줄이기 위함                   |
| 구성 방법         | `createContext`로 각 역할별 Context 생성               |
| Provider 문법   | React 19는 `<MyContext value={...}>` (`.Provider`는 향후 deprecated) |
| Provider 관리   | 중첩된 Provider는 `AppProviders`로 추상화               |
| 기본값 규칙        | `defaultValue`는 **Provider가 아예 없을 때만** 사용됨      |
| 소비 방법         | `useContext(C)` 또는 React 19의 `use(C)` (조건문 안 호출 가능) |
| 안전 패턴         | 기본값 `null` + `useXxx` 커스텀 훅에서 throw             |
| 최적화 팁         | `value`에 객체/함수 넣을 땐 `useMemo`, `useCallback` 사용 |


