리액트의 Context API는
“컴포넌트 트리 전체에 전역(or 준전역) 데이터를 깔끔하게 공유하기 위한 공식 메커니즘” 입니다. 🌐
`props drilling` 지옥에서 탈출하고, 테마/로그인 정보/언어 설정 같은 걸 트리 깊숙한 컴포넌트까지 자연스럽게 전달하는 데 쓰죠.


---

## 1. Context API는 왜 생겼나? 🤔

리액트에서 데이터 전달의 기본은 `props`입니다.

```jsx
<App>
  <Layout>
    <Header>
      <UserMenu />
    </Header>
  </Layout>
</App>
```

예를 들어 `UserMenu`에서 현재 로그인 유저 정보를 써야 한다고 해보겠습니다.

```txt
App (user)
 └─ Layout (user)
     └─ Header (user)
         └─ UserMenu (user를 실제로 사용하는 곳)
```

모양새가 이렇게 됩니다:

```jsx
function App() {
  const user = { name: '홍길동', role: 'admin' };
  return <Layout user={user} />;
}

function Layout({ user }) {
  return <Header user={user} />;
}

function Header({ user }) {
  return <UserMenu user={user} />;
}

function UserMenu({ user }) {
  return <div>{user.name}님 안녕하세요</div>;
}
```

문제점 😵

* `Layout`, `Header`는 **user를 전혀 사용하지 않는데도** props로 계속 전달해야 함 (props drilling).
* 전역적으로 쓰는 값이 많아질수록 (`theme`, `auth`, `locale`, `config` …)
  → 중간 컴포넌트가 “택배 아저씨”가 되어버림.
* 구조가 바뀌거나 컴포넌트가 추가되면, **중간 계층들의 props를 계속 수정**해야 해서 유지보수가 힘들어짐.

➡️ 이런 상황을 해결하기 위해 나온 것이 **Context API**입니다. 🙌

---

## 2. Context API의 핵심 개념 3가지 🧠

Context API는 크게 세 가지로 이해하시면 됩니다:

1. `createContext` – “전역 상태의 종류”를 정의하는 **Context 객체 생성**
2. `Context.Provider` – 이 Context의 값을 실제로 **공급하는 공급자**
3. `useContext(Context)` – 현재 트리에서 **가장 가까운 Provider의 값**을 꺼내 쓰는 훅

조합하면 한 문장으로:

> “Context 객체를 만들고, Provider로 값을 감싼 뒤, useContext로 그 값을 꺼내 쓰는 패턴”

---

## 3. 가장 기본적인 사용 예제: 테마 Context 🎨

### 3-1. Context 만들기 – `createContext`

```jsx
// theme-context.js
import { createContext } from 'react';

// Context를 하나 만든다. (디폴트 값은 'light')
export const ThemeContext = createContext('light');
```

여기서 `createContext`는 **“테마라는 종류의 전역 데이터 통로”** 를 하나 만든다고 생각하시면 됩니다.

* `ThemeContext` 안에는 리액트가 내부적으로 관리하는 정보(Provider/Consumer 등이 포함)들이 들어있어요.
* 아직 실제 값(디폴트 값만 있음)은 없습니다. 그냥 “타입, 채널, 통로” 정의에 가깝습니다.

---

### 3-2. Provider로 값 공급하기 – `ThemeContext.Provider`

```jsx
// App.jsx
import { useState } from 'react';
import { ThemeContext } from './theme-context';
import Page from './Page';

function App() {
  const [theme, setTheme] = useState('dark');

  const toggleTheme = () => {
    setTheme((prev) => (prev === 'light' ? 'dark' : 'light'));
  };

  return (    
    // { theme, toggleTheme } --> ES2015(ES6)부터 도입된 객체 프로퍼티 단축 표기
    // { theme:theme, toggleTheme:toggleTheme } 와 등가!!!!!!!!!!!!!!!!!!!!
    // Provider로 감싸고 value에 실제 값을 넣어준다.
    <ThemeContext.Provider value={{ theme, toggleTheme }}>
      <Page />
    </ThemeContext.Provider>
  );
}

export default App;
```

여기에서 중요한 포인트:

* `ThemeContext.Provider`는 **특정 서브트리 전체에 값을 흘려보내는 역할**을 합니다.
* `value` prop에 넣은 값은 트리 어디에서든 `useContext(ThemeContext)`로 읽을 수 있습니다.
* `value`가 변경되면, **해당 Context를 구독 중인 모든 하위 컴포넌트가 다시 렌더링**됩니다.

---

### 3-3. useContext로 값 소비하기 – `useContext(ThemeContext)`

```jsx
// Page.jsx
import { useContext } from 'react';
import { ThemeContext } from './theme-context';

function Page() {
  const { theme, toggleTheme } = useContext(ThemeContext);

  const styles = {
    backgroundColor: theme === 'light' ? '#ffffff' : '#111827',
    color: theme === 'light' ? '#111827' : '#f9fafb',
    minHeight: '100vh',
    padding: 24,
  };

  return (
    <div style={styles}>
      <h1>Context API Theme Demo</h1>
      <p>현재 테마: {theme}</p>
      <button onClick={toggleTheme}>테마 토글</button>
    </div>
  );
}

export default Page;
```

여기서 `useContext(ThemeContext)`는 내부적으로 이렇게 동작한다고 생각하시면 됩니다:

1. 현재 이 컴포넌트가 속한 Fiber 트리에서,
2. **가장 가까운 `ThemeContext.Provider`를 찾고**,
3. 그 Provider의 `value`를 반환한다.
4. Provider의 `value`가 바뀌면, 이 컴포넌트도 다시 렌더링된다.

---

## 4. Context의 디폴트 값과 Provider가 없을 때 🪫

`createContext(defaultValue)`에서 준 값은 **Provider가 없을 때만** 사용됩니다.

```jsx
const LocaleContext = createContext('ko'); // 디폴트: 'ko'

function ShowLocale() {
  const locale = useContext(LocaleContext);
  return <div>현재 언어: {locale}</div>;
}
```

* 만약 `ShowLocale`이 **어떤 Provider로도 감싸져 있지 않다면** → `locale`은 `'ko'`.
* Provider로 감싸서 `value="en"`을 넣으면 → `locale`은 `'en'`.

```jsx
// 1) Provider 없이 사용할 때
<ShowLocale />          // 'ko'

// 2) Provider로 감싼 경우
<LocaleContext.Provider value="en">
  <ShowLocale />        // 'en'
</LocaleContext.Provider>
```

---

## 5. Context 업데이트는 어떻게 동작하나? 🔄

Context의 실제 값은 보통 **state + Provider** 조합으로 관리합니다.

```jsx
const [user, setUser] = useState(null);

<UserContext.Provider value={{ user, setUser }}>
  {children}
</UserContext.Provider>
```

여기서 중요한 점:

* `value` prop 자체가 **새로운 참조**로 바뀌면,
  그 Context를 사용하는 모든 하위 컴포넌트가 렌더링됩니다.

예:

```jsx
// 안 좋은 패턴 ⚠
<UserContext.Provider value={{ user, setUser, extra: Math.random() }}>
  {children}
</UserContext.Provider>
```

* 렌더링할 때마다 `Math.random()`이 바뀌면서 `value` 객체가 항상 새로워짐
* user가 안 변해도 `value` 참조가 매 렌더마다 다름 → 구독 중인 컴포넌트가 전부 리렌더

그래서 보통 `useMemo`로 감싸거나, Context를 잘 쪼개는 패턴을 쓰게 됩니다. (뒤에서 설명)

```jsx
const value = useMemo(
  () => ({ user, setUser }),
  [user] // user가 바뀔 때만 새 객체
);

<UserContext.Provider value={value}>{children}</UserContext.Provider>
```

---

## 6. Context + useReducer 패턴 (전역 상태 관리 미니 버전) ⚙️

Context는 **“전역 state를 어디서든 꺼내 쓸 수 있게 하는 통로”** 이고,
`useReducer`는 **“state 변경 로직을 깔끔하게 관리하는 도구”** 입니다.

두 개를 합치면 미니 Redux 같은 구조를 만들 수 있습니다. 🧩

### 6-1. 카운터 예제

```jsx
// counter-context.js
import { createContext, useContext, useReducer } from 'react';

const CounterStateContext = createContext(null);
const CounterDispatchContext = createContext(null);

function counterReducer(state, action) {
  switch (action.type) {
    case 'INCREMENT':
      return state + 1;
    case 'DECREMENT':
      return state - 1;
    default:
      throw new Error(`Unknown action: ${action.type}`);
  }
}

export function CounterProvider({ children }) {
  const [count, dispatch] = useReducer(counterReducer, 0);

  return (
    <CounterStateContext.Provider value={count}>
      <CounterDispatchContext.Provider value={dispatch}>
        {children}
      </CounterDispatchContext.Provider>
    </CounterStateContext.Provider>
  );
}

// 커스텀 훅으로 감싸기
export function useCounterState() {
  const state = useContext(CounterStateContext);
  if (state === null) {
    throw new Error('useCounterState must be used within CounterProvider');
  }
  return state;
}

export function useCounterDispatch() {
  const dispatch = useContext(CounterDispatchContext);
  if (dispatch === null) {
    throw new Error('useCounterDispatch must be used within CounterProvider');
  }
  return dispatch;
}
```

사용 측:

```jsx
// App.jsx
import { CounterProvider } from './counter-context';
import CounterDisplay from './CounterDisplay';
import CounterButtons from './CounterButtons';

function App() {
  return (
    <CounterProvider>
      <CounterDisplay />
      <CounterButtons />
    </CounterProvider>
  );
}
```

```jsx
// CounterDisplay.jsx
import { useCounterState } from './counter-context';

function CounterDisplay() {
  const count = useCounterState();
  return <h1>Count: {count}</h1>;
}

export default CounterDisplay;
```

```jsx
// CounterButtons.jsx
import { useCounterDispatch } from './counter-context';

function CounterButtons() {
  const dispatch = useCounterDispatch();

  return (
    <div>
      <button onClick={() => dispatch({ type: 'DECREMENT' })}>-1</button>
      <button onClick={() => dispatch({ type: 'INCREMENT' })}>+1</button>
    </div>
  );
}

export default CounterButtons;
```

여기서 패턴 포인트:

* state (`count`)와 action 발행 (`dispatch`)을 **다른 Context로 분리**
  → 디스패치만 쓰는 컴포넌트는 state 변경 시 리렌더 영향을 덜 받게 설계 가능.
* 커스텀 훅(`useCounterState`, `useCounterDispatch`)으로 감싸서
  **Provider 안에서만 사용되도록 강제** + 타입/사용 위치를 명확히.

---

## 7. Context API 내부 동작 개념 (Fiber 기준으로) 🧬


### 7-1. 트리에 “Context 값 레이어”를 깔아 둔다

* 각 Provider는 Fiber 트리에 **“이 Context의 현재 값”을 들고 있는 노드**입니다.
* 자식 컴포넌트가 `useContext(SomeContext)`를 호출하면,

  * 리액트는 현재 Fiber에서 위쪽으로 올라가면서,
  * `SomeContext`에 해당하는 **가장 가까운 Provider**를 찾고,
  * 그 Provider가 가진 `value`를 가져와서 hook의 `memoizedState`에 저장합니다.

### 7-2. Context 값 변경 → 구독 중인 Fiber 다시 렌더

* Provider의 `value`가 바뀌면,
* 리액트는 이 Context를 사용하는 하위 Fiber들을 찾아서 **업데이트 큐에 등록**합니다.
* 다음 렌더 사이클에서 해당 컴포넌트들의 함수가 다시 실행되면서
  `useContext` 호출 시 **새로운 value**를 읽어오게 됩니다.

이 관점에서 보면:

* Context + useContext는 사실상 `useState`와 비슷하지만,
  “**여러 컴포넌트가 동일한 상태를 공유**”한다는 점이 다릅니다.
* 그리고 구독 단위가 **Context당, Provider 하위 서브트리 전체**라는 점이 포인트입니다.

---

## 8. 언제 Context를 써야 할까? ✅ / ❌

### ✅ Context를 쓰기 좋은 경우

* **전역 설정값** (테마, 언어, 시간대 등)
* **현재 로그인 유저 정보** (user, 권한, 토큰 등)
* **라우터 정보** (현재 라우트, params 등 – 실제로 React Router도 Context를 사용)
* **폼 라이브러리, UI 라이브러리의 내부 공유 상태**

특징:

* “앱의 많은 곳에서 공유해야 하는 값”
* “중간 컴포넌트가 그 값을 직접 사용하진 않지만, 아래로 계속 전달해야 하는 값”

---

### ❌ Context를 과하게 쓰기 좋은(?) 안좋은 경우

* 그냥 **두세 단계 정도만 내려가는 데이터**인데도 무조건 Context부터 도입하는 경우
* 고빈도 업데이트 되는 값(실시간 입력값, 마우스 위치 등)을 큰 Context 하나에 모두 넣는 경우
  → 구독 중인 컴포넌트가 전부 리렌더 → 성능 이슈

리액트 팀의 공식 가이드도:

> Context는 “전역적인” 느낌이 강해서 **남용하면 코드 추적이 어려워지고**
> 테스트/디버깅 난이도가 올라간다고 경고합니다. 😅

---

## 9. 성능 이슈 & 최적화 패턴 🚀

Context의 가장 큰 단점 중 하나가 **리렌더 전파 범위**입니다.

> “Provider의 value가 바뀌면, 그 Context를 사용하는 모든 하위 컴포넌트가 리렌더된다.”

### 9-1. value 객체를 useMemo로 감싸기

```jsx
function ThemeProvider({ children }) {
  const [theme, setTheme] = useState('light');

  // 매 렌더마다 새로운 객체를 생성하지 않기 위해
  const value = useMemo(() => ({ theme, setTheme }), [theme]);

  return (
    <ThemeContext.Provider value={value}>{children}</ThemeContext.Provider>
  );
}
```

* `setTheme`는 원래부터 참조가 고정된 함수지만,
* `{ theme, setTheme }` 객체는 매 렌더마다 새로 만들어질 수 있으므로 `useMemo`로 최적화.

---

### 9-2. Context 쪼개기

하나의 Context에 상태를 다 몰아넣으면:

```jsx
// 나쁜 예: 모든 전역 상태를 한 Context에 몰아넣음
const AppContext = createContext({
  user: null,
  theme: 'light',
  locale: 'ko',
  setUser: () => {},
  setTheme: () => {},
  setLocale: () => {},
});
```

* user가 변경될 때, theme만 사용하는 컴포넌트도 괜히 리렌더.

➡️ 상황에 따라 **Context를 역할별로 분리**하는 것이 좋습니다.

```jsx
const UserContext = createContext(null);
const ThemeContext = createContext('light');
const LocaleContext = createContext('ko');
```

---

### 9-3. “Selector” 패턴 (고급)

리액트 18 기준으로 공식 `useContextSelector`는 없지만,
외부 라이브러리(예: `use-context-selector`)나 Zustand 같은 상태관리 라이브러리들이
“Context + selector” 패턴을 이용해 **부분적인 구독**을 구현합니다.

개념적으로는:

* 전체 state를 전역 store로 관리
* 각 컴포넌트는 **자기에게 필요한 조각만 구독**하도록 최적화

Context 순정만으로는 이 부분이 제한적이라,
복잡한 전역 상태를 다뤄야 하면 Redux/Zustand/Recoil 같은 전용 라이브러리를 고려하는 편이 좋습니다.

---

## 10. 실전 패턴: “Provider 컴포넌트” & “커스텀 훅” 🧱

Context를 프로젝트에서 잘 쓰기 위한 골든 패턴:

1. Context 객체는 **별도 파일**에 정의
2. Provider를 감싼 `XXXProvider` 컴포넌트를 만든다.
3. `useXXX` 커스텀 훅으로 `useContext`를 감싼다.

예시: Auth Context

```jsx
// auth-context.js
import { createContext, useContext, useState, useMemo } from 'react';

const AuthContext = createContext(null);

export function AuthProvider({ children }) {
  const [user, setUser] = useState(null);

  const login = (userInfo) => {
    setUser(userInfo);
  };

  const logout = () => {
    setUser(null);
  };

  const value = useMemo(
    () => ({ user, isLoggedIn: !!user, login, logout }),
    [user]
  );

  return <AuthContext.Provider value={value}>{children}</AuthContext.Provider>;
}

// 커스텀 훅
export function useAuth() {
  const ctx = useContext(AuthContext);
  if (ctx === null) {
    throw new Error('useAuth must be used within an AuthProvider');
  }
  return ctx;
}
```

실제 사용:

```jsx
// App.jsx
import { AuthProvider } from './auth-context';
import AppRoutes from './AppRoutes';

function App() {
  return (
    <AuthProvider>
      <AppRoutes />
    </AuthProvider>
  );
}
```

```jsx
// UserInfo.jsx
import { useAuth } from './auth-context';

function UserInfo() {
  const { user, isLoggedIn, logout } = useAuth();

  if (!isLoggedIn) {
    return <p>로그인해주세요.</p>;
  }

  return (
    <div>
      <p>{user.name}님 환영합니다!</p>
      <button onClick={logout}>로그아웃</button>
    </div>
  );
}

export default UserInfo;
```

이 패턴의 장점:

* 컴포넌트 입장에서는 그냥 `useAuth()` 쓰면 되니 **API가 깔끔**
* Provider 밖에서 잘못 사용하는 경우를 **에러로 바로 잡을 수 있음**
* Context 관련 로직을 한 파일에 모아 둬서 **응집도↑, 추상화 수준↑**

---

## 11. Context vs 다른 상태 관리 도구 비교 ⚖️

| 도구              | 주 용도                     | 장점                                   | 단점                              |
| --------------- | ------------------------ | ------------------------------------ | ------------------------------- |
| `useState`      | 단일 컴포넌트/근접한 컴포넌트 간 상태    | 간단, 직관적                              | 깊은 트리 전파가 어렵고 props drilling 발생 |
| Context API     | 전역/준전역 설정, 여러 곳에서 참조되는 값 | 리액트 내장, 비교적 가벼움                      | 남용 시 구조 파악 어려움, 성능 튜닝 필요        |
| Redux / Zustand | 복잡한 전역 상태, 비즈니스 로직, 캐싱 등 | devtools, 미들웨어, selector, 구조화된 패턴 제공 | 러닝커브, 보일러플레이트 혹은 추가 의존성         |

실무적인 요약:

* **간단한 전역 설정 정도** → Context API + useReducer 조합으로 충분
* **복잡한 도메인 상태/캐싱/서버 상태 동기화** → Redux, Zustand, TanStack Query 등 고려

---

## 12. 자주 나오는 오해 & FAQ 😵‍💫

### Q1. Context 쓰면 “진짜 전역 변수” 같은 건가요?

* 아니요. Context는 **컴포넌트 트리 구조에 종속된 전역**입니다.
* Provider로 감싼 서브트리 안에서만 유효하고,
  트리 구조에 따라 다른 값도 동시에 존재할 수 있습니다.

```jsx
<ThemeContext.Provider value="light">
  <Header />
  <ThemeContext.Provider value="dark">
    <Sidebar />  // 여기만 dark
  </ThemeContext.Provider>
</ThemeContext.Provider>
```

### Q2. Context를 쓰면 Redux는 필요 없나요?

* 간단한 수준에서는 그렇지만,
  **미들웨어, DevTools, 액션 로깅, strict한 패턴** 등이 필요하다면 Redux 계열이 여전히 유용합니다.
* Context는 어디까지나 **전달 메커니즘**이고,
  비즈니스 로직 구조화는 개발자가 따로 설계해야 합니다.

### Q3. Context 안에서 `useState`, `useReducer` 막 써도 되나요?

* 네, 오히려 일반적인 패턴입니다.
* 다만 자주 바뀌는 값이 많은 경우 Context를 쪼개거나, selector 패턴, 외부 상태 라이브러리를 고려해야 합니다.

---

## 마무리 ✨

정리하면, React Context API는:

* props drilling 없이 깊은 곳까지 값을 전달해주는 **공식 통로**이고,
* `createContext` + `Provider` + `useContext` + (옵션으로 `useReducer`, `useMemo`) 조합으로
* **전역 설정 / 로그인 정보 / 라우팅 정보 / UI 내 공통 상태**를 깔끔하게 공유하게 해주는 도구입니다.

