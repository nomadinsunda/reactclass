# 🧠 Context API
React는 기본적으로 단방향 데이터 흐름(top-down, parent → child)을 따릅니다.
하지만 어떤 상태나 값을 여러 하위 컴포넌트들이 공유해야 할 경우, 매번 props로 전달하는 것은 번거롭고 비효율적입니다.
→ 이 문제를 해결하기 위해 Context API가 도입되었고, 이를 함수형 컴포넌트에서 간결하게 쓰기 위한 것이 useContext입니다.

---

## 🪜 Context API가 필요한 이유: Props Drilling의 문제

```tsx
<App>
  <Layout>
    <Sidebar>
      <Profile username="Alice" />
    </Sidebar>
  </Layout>
</App>
```

여기서 `username`이 `<App>`에서 내려왔다면, `Layout`과 `Sidebar`는 그 값을 단순히 "전달만" 하기 위해 props를 받을 필요가 있습니다.
→ 이를 **props drilling**이라고 하며, **유지보수성과 추적성이 크게 떨어집니다.**

🔧 해결책: 전역적 데이터 공유를 위한 **Context API + `useContext` Hook**

---

# 🧱 1. Context 구조 요약

```tsx
const MyContext = React.createContext(defaultValue);
```

위 코드로 생성되는 `MyContext`는 다음과 같은 속성을 가집니다:

* `MyContext.Provider`
* `MyContext.Consumer`

이 두 컴포넌트가 Context의 핵심 구성 요소입니다.

---

## 🟦 2. Provider (제공자)

### 정의:

`Context.Provider`는 하위 컴포넌트에게 데이터를 "공급"하는 컴포넌트입니다.

```tsx
<MyContext.Provider value={someValue}>
  <Child />
</MyContext.Provider>
```

### 특징:

| 항목    | 설명                                                            |
| ----- | ------------------------------------------------------------- |
| value | 하위 트리에 전달할 값                                                  |
| 역할    | 하위 컴포넌트들이 `useContext()` 또는 `Context.Consumer`를 통해 접근 가능하도록 함 |
| 범위    | "하위 트리 전체"에 영향을 미침                                            |
| 리렌더링  | value가 변경되면, \*\*모든 구독자(Component)\*\*가 리렌더링됨                 |

### 예시:

```tsx
const ThemeContext = React.createContext('light');

function App() {
  return (
    <ThemeContext.Provider value="dark">
      <Toolbar />
    </ThemeContext.Provider>
  );
}
```

→ `<Toolbar />` 및 하위 컴포넌트들은 `"dark"` 값을 받게 됩니다.

---

## 🟨 3. Consumer (소비자)

### 정의:

`Context.Consumer`는 클래스 컴포넌트에서 context를 사용할 수 있도록 하는 컴포넌트입니다.

```tsx
<MyContext.Consumer>
  {value => (
    // value는 Provider가 공급한 값
    <div>{value}</div>
  )}
</MyContext.Consumer>
```

### 특징:

| 항목       | 설명                                |
| -------- | --------------------------------- |
| children | **함수**여야 함 (render prop 패턴)       |
| 사용 대상    | 클래스 컴포넌트 or useContext를 쓸 수 없는 상황 |
| 단점       | 중첩이 많아지면 코드가 복잡해짐                 |

---

## 🆚 4. `useContext` vs `Context.Consumer`

| 항목   | `useContext`        | `Context.Consumer`   |
| ---- | ------------------- | -------------------- |
| 대상   | 함수 컴포넌트 전용          | 함수 & 클래스 컴포넌트 모두 가능  |
| 구문   | 직관적 (간단한 훅)         | render prop (가독성 낮음) |
| 필요조건 | `React 16.8+`       | 오래된 방식에서도 사용 가능      |
| 리렌더링 | context 값 변경 시 리렌더링 | 동일                   |

---

## 🎯 5. 내부 흐름 예시 (Provider → Consumer → useContext)

```tsx
const MyContext = createContext('기본값');

function Parent() {
  return (
    <MyContext.Provider value="값 전달됨">
      <Child />
    </MyContext.Provider>
  );
}

function Child() {
  const value = useContext(MyContext);
  return <p>{value}</p>;  // "값 전달됨"
}
```

위 예시에서:

1. `MyContext.Provider`는 "값 전달됨"을 설정.
2. `useContext(MyContext)`는 이 값을 구독.
3. 만약 Provider가 없었다면 `"기본값"`이 반환됨.

---

## 🔄 리렌더링 흐름: Provider의 value가 바뀌면?

```tsx
<MyContext.Provider value={value}>
  <A />
</MyContext.Provider>
```

* `A` 안에서 `useContext(MyContext)`를 호출하고 있다면,
* `value`가 바뀔 때마다 `A`는 자동으로 리렌더링 됩니다.
* 하위 컴포넌트에서도 동일하게 발생.

🔧 **최적화 팁:**

```tsx
const memoizedValue = useMemo(() => ({ user }), [user]);
<MyContext.Provider value={memoizedValue}>
```

---

## 💡 Provider 중첩도 가능

```tsx
<AuthContext.Provider value={auth}>
  <ThemeContext.Provider value={theme}>
    <App />
  </ThemeContext.Provider>
</AuthContext.Provider>
```

* 각각의 `useContext(AuthContext)`, `useContext(ThemeContext)`는 독립적으로 동작.
* 이처럼 **다중 Context 조합**은 흔히 사용됨.

---

## 🚨 주의사항

1. **`Provider` 없이 `useContext()` 호출하면 defaultValue만 반환됩니다.**
2. **리렌더링이 전파되므로 value는 반드시 메모이제이션 해야 함.**
3. `useContext`는 동적 Context를 지원하지 않습니다 → 하나의 `createContext`로 여러 상태를 구분하는 건 비추천

---

## 📌 결론 요약

| 항목       | Provider                   | Consumer                              |
| -------- | -------------------------- | ------------------------------------- |
| 역할       | 값을 "제공"                    | 값을 "소비"                               |
| 구조       | `<Provider value={...}>`   | `<Consumer>{value => ...}</Consumer>` |
| 함수형 컴포넌트 | useContext로 대체             | 사용 가능                                 |
| 리렌더링     | value가 변경되면 모든 하위 구독자 리렌더링 | 동일                                    |

---

### ✅ 최종 한줄 요약

> **Provider는 값을 "내려주는 역할", Consumer는 값을 "받는 역할", useContext는 Consumer의 Hook 버전**입니다.

---

원하시면 **Provider → Consumer → useContext 흐름이 표시된 도식**이나,
**클래스 컴포넌트에서 Consumer를 사용하는 패턴**,
또는 **Provider를 통한 상태 전파 시 리렌더링 계층 구조**도 시각적으로 정리해드릴 수 있습니다.

추가 요청 주세요.
