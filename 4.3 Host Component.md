# Host Component

Host Component(호스트 컴포넌트)는 React가 실행되는 타겟 환경(호스트 환경, 예: 웹 브라우저, 모바일 OS 등)에 내장된 **기본 원시(Primitive) UI 요소** 를 의미합니다.

React의 컴포넌트는 크게 개발자가 만드는 **Custom Component**와 런타임 플랫폼이 제공하는 **Host Component** 두 가지로 나뉩니다.

---

### 1. Custom Component vs Host Component 비교

| 구분 | Custom Component (사용자 정의) | Host Component (호스트) |
| --- | --- | --- |
| **태그 표기** | 대문자로 시작 (`<Header/>`, `<Card/>`) | 소문자로 시작 (`<div />`, `<span />`, `<button />`) |
| **`element.type`** | 함수나 클래스 참조값 (`function Header()`) | 문자열 태그 이름 (`"div"`, `"h1"`) |
| **실체** | React의 로직 단위 (화면 직결 X) | 플랫폼 네이티브 UI 노드와 1:1 매핑 |
| **웹 환경 예시** | `<UserProfile/>`, `<Navigation/>` | `<div>`, `<span>`, `<input>`, `<svg>` |
| **모바일(RN) 예시** | `<ProfileScreen/>` | `<View>`, `<Text>`, `<Image>` (네이티브 뷰 매핑) |

---

### 2. React 내부 동작 관점에서의 역할

React는 렌더 단계(Render Phase)에서 사용자가 작성한 **Custom Component**들을 재귀적으로 실행하여, 최종 산출물이 순수한 Host Component(문자열 태그)만으로 이루어진 트리가 될 때까지 껍질을 벗겨냅니다.

```text
[1] <UserProfile />                  // Custom Component (type: function UserProfile)
        ↓ (함수 호출)
[2] <Card><Avatar /></Card>          // Custom Component (type: function Card, Avatar)
        ↓ (함수 호출)
[3] <div><img /><p>이름</p></div>     // Host Component (type: "div", "img", "p")
        ↓ (Commit Phase)
[4] document.createElement('div')   // 실제 브라우저 DOM 노드 생성 및 화면 렌더링

```

* **EndPoint 역할:** React 재귀 렌더링의 끝은 항상 Host Component입니다.
* **실제 DOM 생성:** React는 `element.type`이 `"div"` 같은 문자열(Host Component)을 만났을 때 비로소 `document.createElement("div")`를 호출하여 실제 브라우저 DOM 노드를 생성하고 속성(class, eventListener 등)을 연결합니다.