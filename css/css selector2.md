

# 🌐 CSS 셀렉터 “완전 정복” — 모든 셀렉터 총정리 (2026 기준)

아래 카테고리별로 **모든 셀렉터를 빠짐없이** 정리합니다.

---

# 1. 🔤 기본 셀렉터 (Basic Selectors)

| 셀렉터       | 설명        |
| --------- | --------- |
| `*`       | 전체 요소 선택  |
| `E`       | 타입(태그) 선택 |
| `.class`  | 클래스 선택    |
| `#id`     | ID 선택     |
| `E, F`    | 그룹 선택     |
| `E.class` | 타입 + 클래스  |
| `E#id`    | 타입 + ID   |

---

# 2. 🧩 속성 셀렉터 (Attribute Selectors)

| 셀렉터               | 설명               |                                          |
| ----------------- | ---------------- | ---------------------------------------- |
| `[attr]`          | 속성이 존재하는 요소      |                                          |
| `[attr="value"]`  | 값이 정확히 동일        |                                          |
| `[attr~="value"]` | 공백으로 구분된 목록 중 하나 |                                          |
| `[attr            | ="value"]`       | `"value-"` 또는 `"value"`로 시작 (언어코드에서 사용됨) |
| `[attr^="value"]` | value로 시작        |                                          |
| `[attr$="value"]` | value로 끝남        |                                          |
| `[attr*="value"]` | value 문자열 포함     |                                          |

---

# 3. 🧬 관계 셀렉터 (Combinators)

| 셀렉터     | 설명                  |    |                                        |
| ------- | ------------------- | -- | -------------------------------------- |
| `A B`   | A의 **후손 요소** B      |    |                                        |
| `A > B` | A의 **직계 자식** B      |    |                                        |
| `A + B` | A **바로 뒤**에 오는 형제 B |    |                                        |
| `A ~ B` | A 뒤에 있는 모든 형제 B     |    |                                        |
| `A      |                     | B` | **컬럼(Column)** 결합자 → CSS Table 레이아웃 전용 |

👉 `||` 컬럼 결합자(잘 알려지지 않은 셀렉터)

```css
col || td {
  background: #fafafa;
}
```

---

# 4. 😎 가상 클래스 (Pseudo-classes)

가상 클래스는 **40개 이상** 존재합니다.
대부분의 개발자는 10~15개 정도만 알고 있죠.

## 4-1. UI / 상호작용 관련

* `:hover`
* `:active`
* `:focus`
* `:focus-visible`
* `:focus-within`
* `:visited`
* `:link` (아직 방문하지 않은 링크)
* `:target` (#hash anchor로 지정된 요소)

---

## 4-2. 입력 요소 상태

* `:required`
* `:optional`
* `:disabled`
* `:enabled`
* `:checked`
* `:indeterminate`
* `:valid`
* `:invalid`
* `:in-range`
* `:out-of-range`
* `:read-only`
* `:read-write`
* `:autofill` (자동완성된 input)
* `:placeholder-shown`

---

## 4-3. DOM 구조 기반

### 기본 구조 셀렉터

* `:root`
* `:empty` (텍스트/공백/자식 모두 없는 요소)
* `:first-child`
* `:last-child`
* `:only-child`
* `:nth-child(n)`
* `:nth-last-child(n)`

### 타입 기반

* `:first-of-type`
* `:last-of-type`
* `:only-of-type`
* `:nth-of-type(n)`
* `:nth-last-of-type(n)`

---

## 4-4. 논리/조건 기반

* `:not(selector)`
* `:is(selector-list)`
* `:where(selector-list)`
* `:has(selector)` ← CSS4의 혁신적인 "부모 선택자"

---

## 4-5. 시간·애니메이션 관련

* `:current`, `:past`, `:future`
  (미디어 타임라인 관련, 거의 안 알려짐)

---

# 5. ✂️ 가상 요소 (Pseudo-elements)

## 5-1. 일반적인 가상 요소

* `::before`
* `::after`
* `::first-letter`
* `::first-line`
* `::selection`
* `::placeholder`

---

## 5-2. 폼/브라우저 UI 관련 (대부분 모르고 지나감)

* `::file-selector-button`
* `::backdrop`
* `::marker` (li의 글머리표)
* `::cue` (WebVTT cue styling)
* `::cue-region`
* `::part()` (Web Components Shadow DOM의 exposed part)
* `::slotted()` (Slot 내부 내용 선택)

---

## 5-3. 스크롤 관련

* `::-webkit-scrollbar`
* `::-webkit-scrollbar-thumb`
* `::-webkit-scrollbar-track`
* (공식 표준은 아니지만 크롬 기반에서 많이 사용)

---

## 5-4. HTML5 미디어 관련

* `::track`
* `::progress-value`
* `::progress-bar`

---

# 6. 🧱 Shadow DOM & Web Components 관련 고급 셀렉터

Web Components를 깊게 다루시면 아래 셀렉터를 만나게 됩니다.

| 셀렉터                   | 설명                      |
| --------------------- | ----------------------- |
| `:host`               | Web Component 루트 선택     |
| `:host(selector)`     | 조건부 host 선택             |
| `::slotted(selector)` | `<slot>` 내부 노드 선택       |
| `::part(name)`        | 커스텀 요소 내부 exposed 부분 선택 |

예:

```css
:host(.dark-mode) {
  background: #222;
}

::slotted(img) {
  border-radius: 8px;
}
```

---

# 7. 🧠 CSS4 & 최신 셀렉터

2026 기준 대부분의 최신 브라우저가 지원하는 현대 셀렉터:

### ✔ `:is()`

특이성 유지하면서 선택자 리스트 결합

### ✔ `:where()`

특이성 0

### ✔ `:has()`

→ 사실상 CSS의 “부모 선택자”

### ✔ `:focus-visible`

키보드 네비게이션 UX에서 사용

### ✔ `:popover-open`

HTML `<dialog popover>` API 관련

### ✔ `:modal`

HTML `<dialog>` 전용

### ✔ `:nth-col()` (CSS Table Layout)

세로 열 기반 선택

### ✔ `:nth-last-col()`

---

# 8. 🧊 전문가들이 아는 희귀 셀렉터

### 8-1. 문자열 기반 `:lang()`

```css
:lang(ko) {
  font-family: "Pretendard";
}
```

언어 속성 비교용입니다.

---

### 8-2. `:scope`

CSS와 JS DOM API에서 “현재 스코프 요소” 선택

```css
:scope > li {
  color: red;
}
```

React는 Shadow DOM을 쓰지 않지만,
동적 DOM styling 로직에서 종종 활용됩니다.

---

### 8-3. `:optional`, `:required`

유효성 검증 UI 만들 때 좋음.

---

### 8-4. `:current`, `:future`, `:past`

타임라인 UI (비디오 트랙 등) 제어용입니다.
대부분 모릅니다.

---

### 8-5. `E:not(:root):not(:empty)`

이런 식으로 조합해서 고급 구조 필터링을 할 수 있음.

---

# 9. 🧨 현재 실험 중 / 미래 셀렉터 (Draft Level)

CSS Working Group이 제안한 기능이며 일부 브라우저에만 존재:

### 🚧 9-1. `:nth-match()`

선택된 요소 중 n번째 매칭

### 🚧 9-2. `:nth-last-match()`

### 🚧 9-3. `:contains()`

옛날 jQuery에서 가능했던 “텍스트 포함 선택자”의 CSS 버전
→ 보안/속도 문제로 아직 불안정

### 🚧 9-4. `:matches()`

→ `:is()`로 대체됨

### 🚧 9-5. `@scope`

CSS 범위 스코핑 제어 규칙 (이미 일부 브라우저 지원됨)

---

# 10. 📦 CSS 셀렉터 전체 분류표 (Full Taxonomy)

최대한 구조적으로 정리하면 다음과 같습니다:

## A. 기본

* Type, Class, ID, Universal

## B. 속성

* `[attr]`, `[attr="v"]`, `[attr*="v"]`, …

## C. 관계

* 후손, 자식, 형제(+), 일반 형제(~), 컬럼(||)

## D. 가상 클래스

1. 상호작용
2. 폼 상태
3. 구조 기반
4. 논리 기반
5. 시간 기반
6. 브라우저 상태 기반

## E. 가상 요소

1. before/after
2. 텍스트 조각(first-line/letter, selection)
3. 폼 요소 버튼
4. WebVTT
5. Web Components(part, slotted, backdrop)

## F. Shadow DOM

* :host, :host(), ::slotted(), ::part()

## G. CSS4 최신

* :is(), :where(), :has(), :focus-visible, :popover-open …

## H. 실험적

* :nth-match(), :contains(), @scope …

---

# 🔥 결론 — “CSS 셀렉터는 생각보다 훨씬 방대하다”

대부분의 프론트엔드 개발자들이 아는 셀렉터는 **전체의 20~25% 정도**입니다.
하지만 강의용 자료나 실무용 시스템을 설계할 때는
이 전체 지도를 알고 계시면 스타일 아키텍처가 훨씬 깔끔해집니다.


