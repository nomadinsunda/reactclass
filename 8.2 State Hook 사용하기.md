

# 🧠 React Hook: **State Hook 사용하기 (`useState`)**

---

## 🪄 useState란?

`useState`는 React 16.8에서 처음 도입된 **State Hook**입니다. 이 Hook을 사용하면 클래스 없이도 **함수형 컴포넌트 안에서 상태(state)를 관리**할 수 있습니다.

```jsx
import React, { useState } from 'react';

function Example() {
  const [count, setCount] = useState(0); // 상태 선언

  return (
    <div>
      <p>You clicked {count} times</p>
      <button onClick={() => setCount(count + 1)}>Click me</button>
    </div>
  );
}
```

이 코드는 매우 간단하지만, `useState`의 핵심 개념을 모두 담고 있습니다.

---

## 🧰 클래스 방식 vs Hook 방식

이전에는 상태를 쓰려면 클래스를 써야 했습니다:

```jsx
class Example extends React.Component {
  constructor(props) {
    super(props);
    this.state = { count: 0 };
  }

  render() {
    return (
      <div>
        <p>You clicked {this.state.count} times</p>
        <button onClick={() => this.setState({ count: this.state.count + 1 })}>
          Click me
        </button>
      </div>
    );
  }
}
```

Hook을 사용하면 `this`, `constructor`, `setState` 같은 문법 없이도 동일한 기능을 훨씬 **간결하게** 구현할 수 있습니다.

---

## ✨ useState의 기본 문법

```js
const [변수명, 변수갱신함수] = useState(초기값);
```

* `변수명`: 현재 상태 값
* `변수갱신함수`: 상태를 변경할 때 호출
* `초기값`: 상태의 초기값 (숫자, 문자열, 객체, 배열 등 자유롭게 가능)

예시:

```jsx
const [fruit, setFruit] = useState('banana');
```

이 코드는 `fruit`이라는 상태를 선언하고 `'banana'`로 초기화하며, `setFruit`을 통해 값을 갱신할 수 있습니다.

---

## 📦 useState는 무엇을 반환하나요?

```js
const [state, setState] = useState(initialValue);
```

`useState()`는 배열 `[현재값, 갱신함수]`를 반환합니다.
이는 **배열 구조 분해(destructuring)** 문법을 활용한 것으로, 다음과 같은 코드와 동일합니다:

```js
const statePair = useState(0);
const count = statePair[0];
const setCount = statePair[1];
```

그러나 구조 분해를 사용하면 **더 읽기 쉬운 코드**를 작성할 수 있습니다.

---

## 🔄 상태 갱신은 어떻게 하나요?

```jsx
<button onClick={() => setCount(count + 1)}>
  Click me
</button>
```

* `setCount(newValue)`를 호출하면 상태가 변경되고 컴포넌트가 리렌더링됩니다.
* 클래스에서는 `this.setState()`를 사용했지만, Hook은 `this` 없이 간단하게 사용할 수 있습니다.

---

## ⚠️ useState 관련 팁 & 주의사항

### ❓ 왜 `createState`가 아닌 `useState`일까요?

* Hook은 렌더링마다 **동일한 순서로 호출되어야 하며**, 현재 상태를 유지해야 합니다.
* 따라서 Hook은 컴포넌트가 렌더링될 때마다 "생성(create)"되는 것이 아니라, \*\*기존 상태를 "사용(use)"\*\*하는 것이기 때문에 `useState`라는 이름이 붙었습니다.

### ❗ 클래스처럼 객체 병합이 일어나지 않음

```js
// 클래스에서는 일부 필드만 바꾸는 경우 병합
this.setState({ name: 'Kim' }); // 다른 필드는 유지됨

// useState는 병합이 아닌 완전 교체
setUser({ name: 'Kim' }); // 이전 state의 다른 필드는 사라짐
```

원하는 경우 객체 spread로 수동 병합해야 합니다:

```js
setUser(prev => ({ ...prev, name: 'Kim' }));
```

---

## 🧮 여러 개의 state 사용하기

함수형 컴포넌트에서는 **여러 개의 `useState` 호출**로 다양한 상태를 분리해서 관리할 수 있습니다:

```jsx
function ExampleWithManyStates() {
  const [age, setAge] = useState(42);
  const [fruit, setFruit] = useState('banana');
  const [todos, setTodos] = useState([{ text: 'Learn Hooks' }]);
}
```

각 상태는 독립적으로 동작하므로 갱신도 독립적으로 할 수 있습니다:

```js
setFruit('orange');
```

---

## 📝 요약 코드 분석

```jsx
import React, { useState } from 'react';

function Example() {
  const [count, setCount] = useState(0);  // ✅ 상태 변수 선언

  return (
    <div>
      <p>You clicked {count} times</p>     // ✅ 상태 변수 사용
      <button onClick={() => setCount(count + 1)}>Click me</button>  // ✅ 상태 갱신
    </div>
  );
}
```

* `useState(0)` → `count`를 0으로 초기화
* `setCount()` → `count` 값 갱신 및 리렌더링 유도
* `count` → 현재 값 참조

---

## 🧩 마무리

`useState`는 React의 함수형 컴포넌트에서 **상태를 선언하고 제어**할 수 있게 해주는 가장 기본적인 Hook입니다.
아직 Hook이 낯설게 느껴지더라도, 위 예제처럼 **작은 실습부터 천천히 시도해보는 것**이 가장 좋은 학습 방법입니다.

