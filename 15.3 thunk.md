# 1⃣ Thunk 한 문장 정의

> **Thunk란, “action을 바로 reducer로 보내지 않고,
> 그 전에 실행되는 ‘함수 형태의 action’으로
> 비동기·조건·여러 dispatch를 처리하는 중간 계층”입니다.**

핵심 키워드:

* 함수 action
* dispatch를 지연/제어
* reducer 앞단에서 실행

---

# 2⃣ Redux에서 왜 Thunk가 필요했을까?

Redux의 기본 규칙부터 다시 보면:

### X Reducer에서는 할 수 없는 것

* API 호출
* setTimeout / setInterval
* 조건 분기 (state에 따라 dispatch 결정)
* 여러 action 순차 dispatch

왜냐하면 reducer는 **순수 함수**여야 하기 때문입니다.

```js
(state, action) => nextState
```

 입력이 같으면 출력이 항상 같아야 함
 부작용(side effect) 금지

---

## 그런데 현실은?

```txt
버튼 클릭 →
  서버 요청 →
    성공이면 state 갱신
    실패면 에러 state 갱신
```

이 흐름은:

* 비동기
* 조건 분기
* 여러 단계

X reducer에는 절대 못 넣음

→ 그래서 **reducer 앞에 “중간 실행자”가 필요**했고
그게 바로 **Thunk (미들웨어)** 입니다.

---

## 설치/설정: 지금은 아무것도 안 해도 됩니다

```js
// ⚠️ 레거시 (RTK 이전)
import thunk from 'redux-thunk'
const store = createStore(rootReducer, applyMiddleware(thunk))
```

```js
// ✅ 현재 (Redux Toolkit 2.x)
const store = configureStore({ reducer: { todos: todosReducer } })
// 끝. thunk 미들웨어가 기본 미들웨어에 이미 포함되어 있습니다.
```

* `redux-thunk`는 **RTK 안에 포함**되어 있어 별도 설치가 필요 없습니다.
* 또한 redux-thunk 3.x부터는 default export가 사라져 `import { thunk } from 'redux-thunk'`처럼 **named import**를 써야 합니다. 위 레거시 코드의 `import thunk from 'redux-thunk'`는 더 이상 동작하지 않습니다.
* 미들웨어를 추가할 일이 있어도 기본값을 **덮어쓰지 말고 이어붙이세요**.

```js
configureStore({
  reducer,
  middleware: (getDefaultMiddleware) => getDefaultMiddleware().concat(myMiddleware),
})
```

---

# 3⃣ Thunk의 위치 (Redux 파이프라인)

Redux 흐름을 정확히 그리면 이렇습니다:

```
dispatch(action)
   ↓
[ middleware ]  ← Thunk가 여기
   ↓
reducer
   ↓
new state
```

Thunk는:

* `dispatch`를 가로채서
* action이 **함수인지 아닌지**를 검사합니다.

---

# 4⃣ Thunk의 핵심 아이디어 (진짜 중요)

### 일반 action

```js
dispatch({ type: 'todo/added', payload: 'Study Redux' })
```

### Thunk action (함수!)

```js
dispatch((dispatch, getState) => {
  // 여기서 마음껏 비동기/조건 로직
})
```

 Redux는 원래 **객체만 dispatch** 가능
 Thunk 미들웨어가 설치되면 **함수도 허용**

---

# 5⃣ Thunk의 기본 형태 (해부)

```js
const myThunk = (arg) => {
  return async (dispatch, getState) => {
    // 1. 현재 상태 조회
    const state = getState()

    // 2. 비동기 작업
    const result = await fetchSomething(arg)

    // 3. 여러 action dispatch
    dispatch(actionA(result))
    dispatch(actionB())
  }
}
```

### 여기서 각 역할

| 요소       | 역할              |
| -------- | --------------- |
| thunk 함수 | 유스케이스(use case) |
| dispatch | 다음 단계로 상태 변경 요청 |
| getState | 현재 상태 기반 조건 분기  |
| reducer  | 실제 상태 변경        |

 **Thunk = 도메인 유스케이스 실행기**

---

# 6⃣ Redux Toolkit의 `createAsyncThunk`는 뭐가 다른가?

RTK는 Thunk를 “표준화”했습니다.

### X 순수 Thunk의 문제

* pending / success / error 직접 관리
* action type 중복
* 상태 플래그 관리 난잡

---

### O createAsyncThunk의 해결책

```js
export const fetchTodos = createAsyncThunk(
  'todos/fetchTodos',
  async (_, { rejectWithValue }) => {
    const res = await fetch('/api/todos')
    if (!res.ok) {
      // ✅ 에러를 "직렬화 가능한 값"으로 바꿔 rejected.payload에 담는다
      return rejectWithValue({ status: res.status, message: '목록 조회 실패' })
    }
    return await res.json()
  },
  {
    // ✅ 이미 로딩 중이면 아예 dispatch 자체를 취소 (중복 요청 방지)
    condition: (_, { getState }) => getState().todos.status !== 'loading',
  }
)
```

> `throw new Error(...)`로 던지면 RTK가 에러를 직렬화해 `action.error`에 넣습니다(메시지/스택 문자열).
> 상태 코드나 서버 응답 본문처럼 **구조화된 에러**가 필요하면 `rejectWithValue`를 써서 `action.payload`로 받으세요.

자동으로 생성되는 것들:

```
todos/fetchTodos/pending
todos/fetchTodos/fulfilled
todos/fetchTodos/rejected
```

 **비동기 상태 머신을 자동 제공**

---

# 7⃣ createAsyncThunk 내부 구조 (정확히)

```js
dispatch(fetchTodos())
```

실제로는:

1. `pending` dispatch
2. payloadCreator 실행 (async)
3. 성공 → `fulfilled(payload)`
4. 실패 → `rejected(error | rejectWithValue)`

Reducer에서는 (RTK 2.x에서는 **builder 콜백 형태만** 지원합니다):

```js
extraReducers: (builder) => {
  builder
    .addCase(fetchTodos.pending, (state) => { state.status = 'loading' })
    .addCase(fetchTodos.fulfilled, (state, action) => {
      state.status = 'idle'
      state.list = action.payload
    })
    .addCase(fetchTodos.rejected, (state, action) => {
      state.status = 'failed'
      // rejectWithValue를 썼다면 action.payload, 아니면 action.error
      state.error = action.payload ?? action.error.message
    })
}
```

### 컴포넌트에서 결과를 직접 다루고 싶다면 `unwrap()`

`dispatch(thunk())`가 반환하는 promise는 **성공/실패 모두 resolve** 됩니다(액션 객체를 돌려줌).
`try/catch`로 다루고 싶으면 `.unwrap()`을 붙이세요.

```js
try {
  const todos = await dispatch(fetchTodos()).unwrap()
} catch (err) {
  // rejected일 때만 여기로 들어옵니다
}
```

`dispatch(...)`의 반환값에는 `abort()`도 있어 진행 중인 요청을 취소할 수 있고,
payloadCreator 안에서는 `{ signal }`로 `AbortSignal`을 받아 `fetch`에 넘길 수 있습니다.

---

# 8⃣ Thunk는 “도메인 로직인가, 인프라 로직인가?”

이 질문, **굉장히 수준 높은 질문**입니다.

### 결론부터:

> **Thunk는 “도메인 유스케이스(use case)” 계층입니다.**

* reducer → 도메인 규칙 (what changes)
* thunk → 도메인 흐름 (when / how / in what order)

### 예

```txt
주문 요청 →
  재고 확인 →
    결제 요청 →
      성공 시 주문 확정
      실패 시 롤백
```

이 흐름은:

* 단일 reducer X
* 단일 action X
* **Thunk**

---

# 9⃣ Thunk에서 `getState()`는 언제 쓰는가?

### O 조건부 dispatch

```js
if (getState().auth.isLoggedIn) {
  dispatch(fetchPrivateData())
}
```

### O 중복 요청 방지

```js
if (getState().todos.status === 'loading') return
```

### O 캐시 판단

```js
if (Date.now() - lastFetched < 60000) return
```

---

# Thunk에서 절대 하면 안 되는 것들

### X reducer 역할 침범

```js
dispatch({ type: 'SET_STATE', payload: bigObject }) // ❌ 설계 악취
```

### X UI 로직 과도 포함

```js
alert('성공') // ⚠️ 지양
```

Thunk는 **비즈니스 흐름**,
UI 반응은 **컴포넌트**가 담당하는 게 이상적입니다.

---

# 11⃣ Thunk vs 다른 미들웨어

| 구분     | Thunk       | Saga      |
| ------ | ----------- | --------- |
| 난이도    | 낮음          | 높음        |
| 러닝커브   | 완만          | 가파름       |
| 비동기    | async/await | generator |
| 실무 채택 | | |
| RTK 권장 | O | X (필요 시만) |

 **대부분의 앱은 Thunk로 충분**

### 단, "서버 데이터 조회"라면 RTK Query를 먼저 고려하세요

`createAsyncThunk` + `loading/error` 플래그 조합은 slice마다 거의 같은 코드가 반복됩니다.
RTK에 함께 들어있는 **RTK Query**(`createApi`)는 이 반복을 없애줍니다.

* 캐싱 / 중복 요청 제거 / 재검증(invalidation) / 폴링 자동 처리
* `useGetTodosQuery()` 같은 훅 자동 생성
* 로딩·에러 상태 자동 관리

> 공식 권장: **서버 상태(server state)** 는 RTK Query,
> **클라이언트 상태**와 그 밖의 복잡한 흐름(순서 있는 다단계 작업, 조건 분기)은 thunk.

---

# 12⃣ 한 문장 최종 정리

> **Thunk는 Redux에서 비동기·조건·여러 dispatch를 담당하는
> “도메인 유스케이스 실행 계층”이며,
> reducer가 순수성을 유지하도록 보호하는 방패입니다.**

