# Vite : 차세대 프론트엔드 빌드 도구

---

## 1. 개요: Vite란 무엇인가?

**Vite**는 Evan You(Vue의 창시자)가 만든 **차세대 프론트엔드 개발 도구**로, **빠른 개발 서버**와 **최적화된 번들링**을 목표로 설계된 **Modern Build Toolchain**입니다.

> "Vite"는 불어로 "빠르다(Fast)"를 의미하며, Vite의 철학은 단순합니다:
> *개발 서버는 빠르게, 프로덕션 빌드는 효율적으로.*

---

## 2. 전통적인 번들러(Webpack 등) 문제점

| 항목         | 설명                                           |
| ---------- | -------------------------------------------- |
| Cold Start | 전체 소스코드를 **초기 번들링**해야 하므로 `npm run dev` 시 느림 |
| HMR 비용     | 번들 단위로 핫 리로드 → 의존성 체인이 길수록 느림                |
| 복잡한 설정     | Loader, Plugin 조합 설정이 어려움                    |
| 느린 빌드      | 수천 개 모듈을 하나로 묶는 작업이 병목 발생                    |

---

## 3. Vite의 핵심 아키텍처

### 개발 시(Dev Server)

* **Native ESM (ES Modules)** 지원

  * **애플리케이션 소스 코드는 번들링하지 않고** 브라우저가 직접 `import` 실행
  * 브라우저가 요청한 모듈만 서버가 그때그때 변환해서 제공 → 매우 빠른 HMR 가능
* **고속 트랜스파일러 기반 on-demand 변환**

  * Vite 5\~7: Go로 작성된 **esbuild**
  * Vite 8부터: Rust로 작성된 **Oxc**
  * TypeScript, JSX 등을 요청 시점에 **실시간 변환**

> Vite는 `.ts`, `.jsx` 같은 확장자도 **on-demand 변환**하여 `import` 응답에만 반응합니다.

#### "개발 시엔 번들링을 전혀 안 한다"는 오해

정확히는 **소스 코드만** 번들링하지 않습니다. `node_modules`의 의존성은 개발 서버가 뜰 때 한 번 **사전 번들링(Dependency Pre-Bundling)** 합니다.

* **결과물 위치**: `node_modules/.vite/deps`
* **담당**: Vite 5\~7은 esbuild, Vite 8부터는 Rolldown
* **왜 필요한가?**

  1. **CommonJS/UMD → ESM 변환**: `react`, `react-dom` 같은 패키지는 CJS로도 배포되는데, 브라우저는 CJS를 이해하지 못합니다.
  2. **요청 폭증(request waterfall) 방지**: `lodash-es`처럼 내부 모듈이 600개가 넘는 패키지를 그대로 두면 브라우저가 600번 요청합니다. 이를 하나의 모듈로 합쳐 요청 1번으로 줄입니다.

> 그래서 **처음 `npm run dev` 하거나 의존성을 새로 설치했을 때만** "Pre-bundling dependencies..." 로그와 함께 잠깐 느리고, 이후에는 캐시를 재사용해 즉시 뜹니다.

### 프로덕션 빌드 시

* **번들러로 한 번에 묶어서 출력**

  * Vite 5\~7: **Rollup** 기반. 모듈 스코프 기반 트리셰이킹(tree-shaking)과 코드 스플리팅이 강력함
  * Vite 8부터: **Rolldown**(Rust로 다시 작성한 Rollup 호환 번들러) 기반
  * Vite는 번들러 설정을 기반으로 `vite build` 실행 시 최적화 수행

> **왜 개발과 빌드에서 도구가 다른가?**
> 개발은 "요청받은 파일 1개를 최대한 빨리 변환"이 목표라 트랜스파일러(esbuild/Oxc)가 맞고,
> 빌드는 "전체 그래프를 보고 트리셰이킹·청크 분할·해시 파일명·CSS 추출"이 목표라 번들러(Rollup/Rolldown)가 맞기 때문입니다.
> 이 이중 구조 때문에 **개발에서 잘 되던 것이 빌드에서 깨지는** 경우가 생길 수 있어, 배포 전 `npm run build && npm run preview` 확인이 필요합니다.

---

## 3-1. 버전별 주요 변화 (Vite 6 / 7 / 8)

| 버전                  | 핵심 변화                                                                                             |
| ------------------- | ------------------------------------------------------------------------------------------------- |
| **Vite 6** (2024.11) | **Environment API** 도입. client/ssr 등 여러 실행 환경을 1급 개념으로 다룰 수 있게 함 (주로 프레임워크 제작자용, 앱 개발자는 몰라도 무방)      |
| **Vite 7** (2025.06) | Node.js 20.19+ / 22.12+ 요구, 기본 브라우저 타깃이 `baseline-widely-available`로 변경                              |
| **Vite 8** (2026.03) | 번들러가 **Rolldown**, 트랜스파일러가 **Oxc**로 통합 교체. esbuild·Rollup 의존 제거. `build.rollupOptions` → `build.rolldownOptions` |

> Vite 8에서도 **`build.rollupOptions` 같은 옛 옵션명은 자동 변환 계층**이 처리해 주므로 기존 설정이 바로 깨지지는 않습니다. 다만 새로 배울 때는 `rolldownOptions`를 쓰세요.
> 반대로 "Rolldown이 esbuild보다 무조건 빠르다"처럼 단정하는 설명은 과장입니다. **빌드**가 크게 빨라진 것이고, 개발 서버의 체감 시작 속도는 원래도 이미 빨랐습니다.

---

## 4. 개발 서버(HMR) 동작 원리

```plaintext
브라우저 → dev server: import 요청
           ↳ esbuild(Vite 8부터는 Oxc)로 실시간 변환 후 응답 (module 단위)

파일 변경 발생 시:
Vite → 모듈 그래프에서 영향 범위(HMR 경계) 계산
Vite → WebSocket → 브라우저에 "이 모듈을 갱신하라" 메시지 전송
브라우저 → 해당 모듈만 다시 fetch 후 교체
React라면 react-refresh가 컴포넌트만 갈아끼우고 useState 값은 유지
```

### O 결과: 즉시 반응하는 HMR, 빠른 피드백 루프

---

## 5. 기본 디렉토리 구조

```bash
vite-project/
├── index.html         ← 엔트리포인트
├── vite.config.js     ← 설정 파일
└── src/
    ├── main.jsx       ← 진입점 (React 기준)
    └── App.jsx
```

---

## 6. 설치 및 기본 사용법

```bash
# 1. 프로젝트 생성
npm create vite@latest my-vite-app

# 2. 프레임워크 선택 (React, Vue, Svelte 등)

# 3. 의존성 설치
cd my-vite-app
npm install

# 4. 개발 서버 실행
npm run dev
```

### 주요 명령어

| 명령어               | 설명                                        |
| ----------------- | ----------------------------------------- |
| `npm run dev`     | 개발 서버 실행 (Native ESM + esbuild/Oxc on-demand 변환) |
| `npm run build`   | 프로덕션 번들 생성 (Vite 7까지 Rollup, Vite 8부터 Rolldown) |
| `npm run preview` | 빌드된 정적 파일을 로컬에서 미리보기                      |

---

## 7. Vite vs Webpack 성능 비교

| 항목            | Webpack            | Vite                         |
| ------------- | ------------------ | ---------------------------- |
| Cold Start    | 느림 (전체 번들링 필요)     | **즉시 시작 (ESM 요청만 변환)**       |
| HMR 속도        | 느림 (전체 모듈 그래프 탐색)  | **빠름 (부분적 ESM 모듈만 변경)**      |
| TypeScript 처리 | ts-loader or babel | **esbuild/Oxc 기반 20\~30배 빠름** |
| 설정 복잡도        | 복잡                 | 단순                           |
| 빌드 성능         | 중간 (Terser 사용)     | **빠름 (기본 minifier도 esbuild/Oxc)** |

> **TypeScript 처리에 대한 중요한 단서**
> Vite의 TS 처리는 **타입을 지우기만 하는 트랜스파일(transpile-only)** 입니다. **타입 검사를 하지 않습니다.**
> 즉 타입 에러가 있어도 `npm run dev`와 `npm run build`가 그냥 통과할 수 있습니다.
> 그래서 TS 템플릿의 build 스크립트는 `"build": "tsc -b && vite build"` 처럼 **`tsc`를 먼저 돌리도록** 되어 있습니다. 이 부분을 지우면 타입 안전성이 사라집니다.

---

## 8. 플러그인 생태계

Vite는 Rollup 플러그인을 그대로 사용 가능하며, **Vite 전용 플러그인**도 존재합니다.

```js
// vite.config.js
import vue from '@vitejs/plugin-vue'

export default {
  plugins: [vue()]
}
```

대표 플러그인:

* `@vitejs/plugin-react`
* `@vitejs/plugin-vue`
* `vite-plugin-pwa`
* `vite-plugin-pages` (파일 기반 라우팅)

---

## 9. 고급 설정: vite.config.ts

```ts
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  server: {
    port: 3000,
    open: true,
    proxy: {
      '/api': 'http://localhost:8080'
    }
  },
  build: {
    outDir: 'dist',
    sourcemap: true
  }
})
```

---

## 10. 실전 프로젝트 구조 예시 (React 기반)

```
vite-react-app/
├── public/
│   └── favicon.ico
├── src/
│   ├── assets/
│   ├── components/
│   ├── pages/
│   ├── App.tsx
│   └── main.tsx
├── index.html
├── vite.config.ts
└── tsconfig.json
```

---

## 11. 결론: 왜 Vite를 선택해야 하는가?

### O 추천 대상

* 빠른 HMR과 개발 환경이 필요한 **SPA(React/Vue/Svelte) 프로젝트**
* Webpack 설정에 지친 팀
* TypeScript와 JSX를 동시에 사용하는 경우
* Micro Frontend와 같이 모듈화가 중요한 시스템

### O 장점 정리

| 특징       | 설명                      |
| -------- | ----------------------- |
| 빠른 개발 서버 | ESM 기반으로 on-demand 변환   |
| 즉각적인 HMR | WebSocket을 통한 모듈 단위 리로드 |
| 빠른 빌드    | 네이티브 번들러 사용 (Vite 7까지 esbuild+Rollup, Vite 8부터 Oxc+Rolldown) |
| 간편한 설정   | zero-config 기반          |



