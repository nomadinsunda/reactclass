### ✅ 1. **패키지란?**

자바스크립트에서 **패키지**는 재사용 가능한 **코드 묶음**으로, 보통 특정 기능이나 라이브러리를 하나의 모듈로 구성해 배포 가능한 형태로 만든 것입니다.

* 예시: `lodash`, `axios`, `react`, `express`, `jsonwebtoken` 등
* 흔히 `.js` 파일 여러 개로 구성되어 있으며, 메타데이터를 포함한 디렉토리 구조를 가짐
* **npm (Node Package Manager)** 또는 **yarn** 같은 패키지 매니저를 통해 설치하고 관리할 수 있음
* https://www.npmjs.com/

---

### ✅ 2. **패키지의 구성 요소**

자바스크립트의 패키지는 보통 다음과 같은 구성 요소를 가짐:

| 파일/디렉토리         | 설명                                   |
| --------------- | ------------------------------------ |
| `package.json`  | 패키지의 메타데이터(이름, 버전, 의존성 등)를 포함한 핵심 파일 |
| `index.js`      | 기본 진입점(entry point) 파일               |
| `node_modules/` | 설치된 외부 패키지들이 저장되는 디렉토리 (루트에 위치)      |
| 기타 파일들          | 기능 구현 파일들 (JS, CSS, README 등)        |

---

### ✅ 3. **패키지 예시**

```bash
npm install axios
```

```js
// axios 패키지 사용 예시
import axios from 'axios';

axios.get('https://api.example.com/data')
     .then(response => console.log(response.data))
     .catch(error => console.error(error));
```

* 여기서 `axios`는 **패키지 이름**이며, `npm`을 통해 설치한 후 코드에서 `import` 또는 `require`로 사용할 수 있음

---

### ✅ 4. **로컬 패키지 vs 외부 패키지**

| 구분         | 설명                                          |
| ---------- | ------------------------------------------- |
| **로컬 패키지** | 사용자가 직접 만든 모듈 또는 패키지 (`myUtils.js` 등)       |
| **외부 패키지** | npm registry에 등록되어 있는 패키지 (lodash, react 등) |

---

### ✅ 5. **npm과 패키지 관계**

`npm`은 자바스크립트의 **공식 패키지 레지스트리와 관리 도구**입니다.
`npm`을 통해 패키지를:

* 설치 (`npm install`)
* 배포 (`npm publish`)
* 의존성 관리 (`package.json`, `package-lock.json`)
* 버전 관리 (SemVer: `1.2.3`)

할 수 있습니다.

---

### ✅ 6. **자바스크립트 모듈 vs 패키지**

| 용어                | 설명                                         |
| ----------------- | ------------------------------------------ |
| **모듈 (Module)**   | 개별 `.js` 파일로 특정 기능을 캡슐화한 단위 (예: `math.js`) |
| **패키지 (Package)** | 여러 모듈과 메타데이터를 포함하는 배포 가능한 단위 (예: `lodash`) |

---

### 🔎 요약

* 자바스크립트의 **패키지**는 특정 기능을 수행하는 코드 묶음이며, `npm`을 통해 설치하고 사용하는 **모듈 단위의 배포 가능 라이브러리**입니다.
* 일반적으로 `package.json` 파일이 포함되어 있으며, 재사용성과 공유를 목적으로 합니다.

