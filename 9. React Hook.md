# **React Hook이란?**

 **함수형 컴포넌트에 “상태(state)”와 “side-effect” 기능을 추가하기 위해, React가 제공하는 특별한 함수입니다.**

---

## 더 직관적인 설명

원래 함수형 컴포넌트는 단순히 “props → UI”만 만드는 **순수 함수**였습니다.

```jsx
function Hello() {
  return <h1>Hello</h1>;
}
```

여기서는

* 상태 없음
* 컴포넌트 생명주기 없음
* DOM 접근 불가
* 비동기 처리 없음

즉 **UI 출력만 가능한 순수한 함수**였죠.

---

## 그런데 앱을 만들면 이런 게 필요합니다:

* 클릭하면 숫자 증가 → **상태 필요**
* API 호출 → **사이드이펙트 필요**
* DOM 크기 측정 → **렌더 직후 작업 필요**
* 전역 데이터 공유 → **컨텍스트 필요**

이걸 **함수형 컴포넌트에서도 가능하게 만든 것**이 바로 **Hooks**입니다.

---

## 즉, Hook의 본질은 아래와 같습니다:

### O **React 내부 상태 저장소에 접근할 수 있게 해주는 인터페이스**

예:<br>
`useState()` → 상태를 React에 저장<br>
`useReducer()` → 복잡한 상태 로직을 React에 저장<br>

### O **렌더링 이후 특정 시점에 코드를 실행할 수 있게 하는 메커니즘**

예: <br>
`useEffect()` → 렌더 후 실행 <br>
`useLayoutEffect()` → DOM 반영 직후 실행 <br>

### O **React 렌더링 엔진(Fiber)과 연결되는 특수한 함수**

Hooks는 React 렌더러가 관리하는 “Hook 메모리 슬롯”과 매칭됩니다.
그래서 **매번 같은 순서로 호출되어야 합니다.**

---

## 1. Hooks의 규칙 (Rules of Hooks)

React는 Hooks 사용 시 두 가지 규칙을 강력히 강제합니다.

#### 1⃣ 반드시 **컴포넌트 최상위**에서만 호출해야 한다.

잘못된 예 X:

```js
if (isLogin) {
  const [count, setCount] = useState(0); // ❌ 오류 가능
}
```

React는 Hook을 **이름이 아니라 “호출된 순서”로 식별**합니다.
호출된 Hook들은 Fiber의 `memoizedState`에 매달린 **연결 리스트(Linked List)** 의
1번째, 2번째, 3번째… 노드에 순서대로 매핑됩니다.
즉 `useState`라는 이름이나 변수명 `count`는 아무 정보도 주지 않고,
**“이번 렌더에서 몇 번째로 불렸는가”만이 그 Hook의 신원**입니다.

그래서 조건문/반복문/`return` 이후에서 호출하면
렌더마다 호출 개수와 순서가 달라져 이전 렌더의 슬롯과 어긋납니다.

* 개수가 줄면 → `Rendered fewer hooks than expected` 오류로 렌더가 중단됩니다.
* 개수는 같고 종류만 바뀌면 → **오류 없이 엉뚱한 슬롯의 값을 읽는** 더 위험한 상황이 됩니다.

(자세한 내부 구조는 “9.1 Hook Memory Slot” 문서를 참고하세요.)

#### 2⃣ 반드시 **React 함수**(컴포넌트 또는 Custom Hook)에서만 호출해야 한다.

잘못된 예 X:

```js
function doSomething() {
  const [count, setCount] = useState(0); // ❌
}
```

Hook은 “지금 렌더링 중인 Fiber”라는 **암묵적 컨텍스트**에 의존합니다.
React는 컴포넌트를 호출하기 직전에 내부 전역 변수(`currentlyRenderingFiber`)와
**디스패처(dispatcher)** 를 세팅하는데, 렌더링 밖에서 Hook을 부르면
그 디스패처가 비어 있어 `Invalid hook call` 오류가 납니다.
그래서 Hook을 부를 수 있는 곳은 딱 두 군데뿐입니다.

* 함수형 컴포넌트 본문
* `use`로 시작하는 **Custom Hook** 본문

#### 이 규칙은 도구가 강제합니다

* **`eslint-plugin-react-hooks`**

  * `rules-of-hooks`: 조건문·반복문·중첩 함수·일반 함수 안의 Hook 호출을 에러로 잡습니다.
  * `exhaustive-deps`: 의존성 배열 누락을 경고합니다.
  * 대소문자 규칙이 판정 기준입니다 — **대문자로 시작하면 컴포넌트, `use`로 시작하면 Hook**으로 간주하므로 네이밍을 지켜야 린트가 동작합니다.
* **React Compiler (React 19에 내장된 것이 아니라 별도 opt-in 빌드 도구)**

  * 컴파일러는 컴포넌트가 규칙을 지킨다는 전제 위에서 자동 메모이제이션을 수행합니다.
  * 규칙 위반(조건부 Hook, 렌더 중 변형 등)이 감지되면 그 컴포넌트의 최적화를 **건너뜁니다**.
  * 즉 규칙을 지키는 것이 곧 성능 최적화의 전제 조건이 됩니다.

---

## 2. React Hooks의 내부 동작 구조

React는 컴포넌트 렌더링 시 다음과 같은 “Hook Memory Slot”을 관리합니다.

```
FiberNode {
  memoizedState: Hook {
    memoizedState: any,       // 저장된 state 값
    queue: UpdateQueue,       // setState로 쌓인 업데이트 큐
    next: Hook | null         // 다음 Hook
  }
}
```

즉, 한 컴포넌트 안의 Hooks는 **연결 리스트(Linked List)** 형태로 저장됩니다.

렌더링이 일어날 때 React는 *Hook 호출 순서대로* 이 리스트를 읽어가며:

1. 이전 렌더링에서의 Hook 값 복구
2. 업데이트 큐 처리
3. 새 상태 계산
4. 다음 Hook 이동

바로 이것이 “Hook은 반드시 같은 순서”여야 하는 이유입니다.

---

## 3. 핵심 Hooks

---

### useState — 상태 관리의 최소 단위

#### 특징

* 초기 렌더링(mount) 시에만 `initialState`가 상태로 채택됨
* 이후 렌더에서는 항상 슬롯에 저장된 `memoizedState` 사용
* 상태 업데이트는 **배치(batch)** 처리되며, `setState` 직후에도 그 렌더의 변수 값은 그대로(스냅샷)

 `useState(expensiveInit())` 로 쓰면 **매 렌더마다 `expensiveInit()`이 호출되고 결과는 버려집니다.**
초기값 계산 비용이 크다면 **lazy initializer(함수 자체)** 를 넘기세요.

```js
const [state, setState] = useState(() => expensiveInit()); // 마운트 시 1회만 호출
```

#### 예시

```js
const [count, setCount] = useState(0);
```

#### 업데이트 방식

##### 1) 값 업데이트

```js
setCount(3);
```

##### 2) 함수 업데이트 (recommended)

```js
setCount(prev => prev + 1);
```

배치 업데이트에서 안전하며 여러 이벤트가 한꺼번에 처리될 때 레이스 컨디션 방지.

---

### useEffect — 사이드 이펙트 처리

#### 사이드 이펙트란?

* DOM 조작
* 서버 데이터 fetch
* 이벤트 리스너 등록
* 타이머 setTimeout

#### useEffect 동작 순서

1. 렌더 → 커밋(DOM 반영) → **브라우저 페인트**
2. 페인트가 끝난 뒤 **비동기로** effect 실행
3. deps가 바뀐 렌더에서는 **이전 effect의 cleanup 실행 → 새 effect 실행** 순서
4. 컴포넌트가 언마운트될 때 마지막 cleanup 실행

 cleanup은 “다음 렌더마다 무조건” 도는 것이 아닙니다.
**그 effect가 다시 실행되기 직전**, 또는 **언마운트 시점**에만 실행됩니다.
deps가 그대로면 effect도 cleanup도 실행되지 않습니다.

#### 예시

```js
useEffect(() => {
  const id = setInterval(() => console.log('tick'), 1000);
  return () => clearInterval(id);
}, []);
```

#### 의존성 배열(deps)의 의미

* `[]` : 마운트 시 1회 실행, 언마운트 시 cleanup
* `[value]` : `Object.is`로 비교해 `value`가 바뀐 렌더에서만 재실행
* 생략 : 모든 렌더링마다 실행

 **개발 모드 + StrictMode**에서는 마운트 시 effect가
`실행 → cleanup → 재실행` 으로 **두 번 실행**됩니다(프로덕션 빌드는 1회).
버그가 아니라, cleanup이 제대로 작성되었는지 검사하기 위한 의도된 동작입니다.

 deps는 “언제 실행할지 고르는 스위치”가 아니라
**effect 안에서 참조하는 모든 반응형 값(props·state·그로부터 파생된 값)의 목록**입니다.
실행 횟수를 줄이려고 값을 일부러 빼면 stale closure(오래된 값 참조) 버그가 생기므로,
`exhaustive-deps` 경고를 끄지 말고 **effect 자체를 다시 설계**해야 합니다.

---

### useLayoutEffect — 렌더 직후 DOM 계산 (동기)

#### useEffect 와의 차이

| Hook              | 시점                | 특징                   |
| ----------------- | ----------------- | -------------------- |
| useEffect | 화면이 그려진 후 | 비동기, UI 깜빡임 X |
| useLayoutEffect | DOM 계산 직후, 페인트 이전 | 동기 실행 → layout 측정 용도 |

DOM 사이즈 측정이 필요할 때 꼭 사용합니다.

---

### useRef — 컴포넌트 렌더링과 독립적인 값 저장

#### 주요 사용 사례

1. DOM 요소 레퍼런스
2. 렌더링을 발생시키지 않는 상태 저장
3. setTimeout id 저장
4. 스크롤 위치 저장

```js
const inputRef = useRef(null);
```

---

### useMemo — expensive 계산 결과 캐싱

```js
const value = useMemo(() => computeHeavy(a, b), [a, b]);
```

#### 사용 대상

* 연산 비용이 큰 함수
* 객체/배열 레퍼런스 유지 (memoized 컴포넌트 props 최적화)

---

### useCallback — 함수 메모이제이션

```js
const onClick = useCallback(() => {
  console.log('clicked');
}, []);
```

#### 사용 목적

* 불필요한 자식 re-render 방지
* dependency 기반으로 함수 identity 유지

---

### useReducer — 복잡한 상태 관리

Redux의 reducer 패턴을 컴포넌트 안에 내장한 구조.

```js
const [state, dispatch] = useReducer(reducer, initialState);
```

#### 장점

* 다단계 상태 업데이트 로직 정리
* useState보다 예측 가능한 상태 흐름

---

### useContext — 전역 수준의 의존성 주입

#### 사용 방식

```js
const ThemeContext = createContext("light"); // 기본값을 주는 습관을 들이세요

function App() {
  return (
    // React 19: <ThemeContext> 자체를 Provider로 사용할 수 있습니다.
    // (React 18까지는 <ThemeContext.Provider value="dark">)
    <ThemeContext value="dark">
      <Page />
    </ThemeContext>
  );
}

function Page() {
  const theme = useContext(ThemeContext);
  // React 19에서는 const theme = use(ThemeContext); 도 가능합니다.
}
```

#### 특징

* 상태를 props drilling 없이 전달
* 단, 과도한 재렌더링 이슈 존재 → `memo + useMemo` 조합이 필요

---

## 4. Custom Hook — Hooks의 진짜 힘

Custom Hook을 사용하면 “상태 + 효과 + 함수”를 **모듈화**할 수 있습니다.

예: 스크롤 위치 감지 Custom Hook

```js
function useScrollY() {
  const [y, setY] = useState(0);

  useEffect(() => {
    const onScroll = () => setY(window.scrollY);
    window.addEventListener('scroll', onScroll);
    return () => window.removeEventListener('scroll', onScroll);
  }, []);

  return y;
}
```

사용:

```js
const y = useScrollY();
```

---

## 5. 실제 대규모 React 프로젝트에서 Hooks는 어떻게 쓰이나?

### O 상태 로직 분리(SRP)

* 데이터 fetch → `useFetch`
* 폼 관리 → `useForm`
* WebSocket → `useWebSocket`
* IntersectionObserver → `useIntersection`

### O 성능 최적화

* `useMemo`로 연산 최적화
* `useCallback`으로 핸들러 안정화
* `React.memo`로 자식 렌더 비용 절약

### O 전역 상태 관리와 결합

* 리덕스 + useSelector (React Hooks)
* Zustand, Jotai 같은 Hook 기반 전역 상태 라이브러리

---

## 6. React Hooks와 렌더링 메커니즘 (심화)

### React는 다음 조건에서 렌더링을 발생

* state 변경
* parent props 변경
* context value 변경
* key 변경

Hooks들은 이 렌더링과 깊이 연결되어 있으며,
특히 **useEffect는 렌더링 이후에 실행되므로 UI = 렌더링 결과**가 먼저입니다.

---

## 7. Hooks의 잘못된 사용 패턴 (주의)

### 1) 불필요한 useEffect

```js
useEffect(() => {
  setValue(props.value);
}, [props.value]);
```

→ 이런 패턴은 대부분 잘못된 코드입니다. props를 state로 복사하는 것은 anti-pattern.

### 2) useMemo 남발

* 비용이 거의 없는 연산에 useMemo 쓰기
* deps가 매번 바뀌면 캐싱 자체가 의미 없음

### 3) useCallback 무조건 사용

* 자식 컴포넌트가 memoized 되어 있지 않다면 효과 없음

---

## 8. 최신 버전의 Hook 동작 변화

### React 18에서 들어온 것

* 자동 배치 (Automatic Batching) — 이벤트 핸들러 밖(`setTimeout`, `Promise.then` 등)에서도 배치
* StrictMode의 double invocation (개발 모드 한정)
* Concurrent rendering 기본 지원
* `useTransition`, `useDeferredValue`, `useId`, `useSyncExternalStore` 등장

### React 19에서 달라진 것

* **`ref`가 일반 prop** → 함수 컴포넌트에서 `forwardRef` 없이 `({ ref })`로 수령 가능
* **`use`** — Hook이 아니라 **새로운 API**. Promise나 Context를 읽으며,
  Hook 규칙과 달리 **조건문·반복문 안에서도 호출할 수 있습니다.**

  ```jsx
  const data = use(promise);      // Suspense와 연동
  const theme = use(ThemeContext); // useContext 대체 가능
  ```
* **폼/액션 관련 Hook 추가**: `useActionState`, `useFormStatus`, `useOptimistic`
* **`<Context.Provider>` 대신 `<Context>` 를 직접 사용** 가능
* 함수형 컴포넌트의 **`defaultProps` 제거**, **`propTypes` 제거**
* `useTransition`의 액션 함수로 **async 함수**를 넘길 수 있음
* **React Compiler** — 규칙을 지킨 컴포넌트에 대해 `useMemo`/`useCallback`/`memo`를 자동 삽입
  (React 19에 포함되어 자동 동작하는 기능이 아니라, Babel/Vite 플러그인으로 **별도 설치·설정해야 하는 opt-in 도구**입니다)

---

## 결론: Hooks는 React의 핵심 러닝 모델이다

React Hooks는 단순한 API가 아니라 React 앱의 **렌더링, 상태, 부수효과, 최적화** 전체를 아우르는 근본 개념입니다.

* 상태 관리 → useState, useReducer
* 부수효과 관리 → useEffect
* DOM 연동 → useRef, useLayoutEffect
* 최적화 → useMemo, useCallback
* 전역 의존성 → useContext
* 재사용 모듈화 → Custom Hooks

Hooks를 이해하면 React의 렌더링 메커니즘 전체가 보이게 됩니다.



