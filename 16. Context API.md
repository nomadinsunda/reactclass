# React Context API

## — Props Drilling 없이 컴포넌트 트리에 값을 공유하는 방법

React의 데이터 흐름은 기본적으로 **부모 → 자식 방향의 props 전달**입니다.

하지만 로그인 사용자, 테마, 언어 설정처럼 여러 컴포넌트가 공통으로 사용하는 값을 깊은 트리까지 전달하다 보면 문제가 생깁니다.

```text
App
 │
 │ user
 ▼
Layout
 │
 │ user
 ▼
Header
 │
 │ user
 ▼
UserMenu
    ↑
    실제로 user를 사용하는 컴포넌트
```

`Layout`과 `Header`는 `user`를 사용하지 않습니다.

그런데 `UserMenu`에 전달하기 위해 중간에서 계속 받아서 넘겨야 합니다.

이 현상을 **Props Drilling**이라고 합니다.

React의 **Context API**는 바로 이런 문제를 해결하기 위한 공식적인 값 전달 메커니즘입니다.

> **Context API는 컴포넌트 트리의 특정 영역에 값을 공급하고, 그 아래 컴포넌트들이 중간 props 전달 없이 해당 값을 직접 읽을 수 있도록 하는 React의 공식 메커니즘입니다.**

---

# 1. 왜 Context가 필요한가?

다음과 같은 컴포넌트 구조가 있다고 해봅시다.

```jsx
<App>
  <Layout>
    <Header>
      <UserMenu />
    </Header>
  </Layout>
</App>
```

`App`이 로그인 사용자 정보를 가지고 있습니다.

```jsx
function App() {
  const user = {
    name: '홍길동',
    role: 'admin'
  };

  return <Layout user={user} />;
}
```

그런데 실제로 `user`를 사용하는 곳은 `UserMenu`입니다.

그러면 다음처럼 전달해야 합니다.

```jsx
function Layout({ user }) {
  return <Header user={user} />;
}

function Header({ user }) {
  return <UserMenu user={user} />;
}

function UserMenu({ user }) {
  return <div>{user.name}님 안녕하세요.</div>;
}
```

데이터 흐름은 다음과 같습니다.

```text
App
 │
 │ props
 ▼
Layout
 │
 │ props
 ▼
Header
 │
 │ props
 ▼
UserMenu
```

문제는 `Layout`, `Header`가 `user`를 사용하지 않는다는 것입니다.

단지 아래 컴포넌트로 전달하기 위해 존재합니다.

트리가 깊어질수록 이런 코드가 계속 증가합니다.

```text
App
 ↓
Layout
 ↓
Header
 ↓
Navigation
 ↓
UserArea
 ↓
UserMenu
```

그래서 React는 다른 통로를 제공합니다.

```text
                  Context
        ┌──────────────────────────┐
        │                          │
App ─── Provider                   │
        │                          ▼
        └───────────────────── UserMenu
```

중간 컴포넌트가 값을 전달하지 않아도 됩니다.

---

# 2. Context의 핵심 구조

Context는 크게 세 가지 개념만 이해하면 됩니다.

```text
createContext
     │
     ▼
Context 객체
     │
     ▼
Provider
     │
     │ value 공급
     ▼
컴포넌트 트리
     │
     ▼
useContext(Context)
     │
     ▼
value 사용
```

즉,

> **Context 객체를 만들고 → Provider가 값을 공급하고 → 필요한 컴포넌트가 그 값을 구독한다.**

이것이 Context API의 핵심입니다.

---

# 3. createContext — 공유할 통로 만들기

먼저 Context 객체를 생성합니다.

```jsx
import { createContext } from 'react';

export const ThemeContext = createContext('light');
```

여기서

```jsx
createContext('light')
```

는 state를 만드는 것이 아닙니다.

`ThemeContext`라는 **값 전달 통로(Context 객체)**를 만드는 것입니다.

```text
createContext('light')
        │
        ▼
┌─────────────────────┐
│    ThemeContext     │
│                     │
│ defaultValue: light │
└─────────────────────┘
```

`'light'`는 Provider가 존재하지 않을 때 사용할 **default value**입니다.

---

# 4. Provider — Context에 실제 값 공급하기

Context 객체만 만들어서는 실제 값이 전달되지 않습니다.

Provider가 값을 공급해야 합니다.

React 19에서는 Context 객체 자체를 Provider로 사용할 수 있습니다.

```jsx
import { useState } from 'react';
import { ThemeContext } from './theme-context';
import Page from './Page';

function App() {
  const [theme, setTheme] = useState('dark');

  const toggleTheme = () => {
    setTheme(prev =>
      prev === 'light' ? 'dark' : 'light'
    );
  };

  return (
    <ThemeContext value={{ theme, toggleTheme }}>
      <Page />
    </ThemeContext>
  );
}

export default App;
```

구조는 다음과 같습니다.

```text
App
 │
 │ state
 │ theme = "dark"
 │
 ▼
ThemeContext
value = {
  theme,
  toggleTheme
}
 │
 ├──────── Page
 │          │
 │          ├── Header
 │          │
 │          └── Content
 │
 └──────── Context 범위
```

Provider 아래에 있는 컴포넌트들은 필요한 경우 `ThemeContext`의 값을 읽을 수 있습니다.

### React 18 이하 코드

기존 코드에서는 다음 문법을 많이 볼 수 있습니다.

```jsx
<ThemeContext.Provider value={{ theme, toggleTheme }}>
  <Page />
</ThemeContext.Provider>
```

React 19에서는 다음처럼 단순화할 수 있습니다.

```jsx
<ThemeContext value={{ theme, toggleTheme }}>
  <Page />
</ThemeContext>
```

따라서 기존 프로젝트를 읽기 위해 `.Provider` 문법도 알아둘 필요가 있습니다.

---

# 5. useContext — Context 값 읽기

이제 하위 컴포넌트에서 값을 읽습니다.

```jsx
import { useContext } from 'react';
import { ThemeContext } from './theme-context';

function Page() {
  const { theme, toggleTheme } =
    useContext(ThemeContext);

  return (
    <div>
      <p>현재 테마: {theme}</p>

      <button onClick={toggleTheme}>
        테마 변경
      </button>
    </div>
  );
}
```

개념적으로 다음과 같습니다.

```text
ThemeContext Provider
value = { theme: "dark", ... }
          │
          │
          ▼
        Page
          │
          │ useContext(ThemeContext)
          ▼
   { theme: "dark", ... }
```

`Page`가 더 이상 `App → Layout → Header → Page` 같은 props 전달 경로를 신경 쓸 필요가 없습니다.

---

# 6. 가장 가까운 Provider의 값을 읽는다

Context에서 매우 중요한 규칙입니다.

> `useContext(Context)`는 현재 컴포넌트 위쪽에 존재하는 **가장 가까운 해당 Context Provider의 value**를 사용합니다.

예를 들어:

```jsx
<ThemeContext value="light">

  <Header />

  <ThemeContext value="dark">
    <Sidebar />
  </ThemeContext>

</ThemeContext>
```

구조적으로 보면:

```text
ThemeContext = "light"
│
├── Header
│     └─ "light"
│
└── ThemeContext = "dark"
      │
      └── Sidebar
            └─ "dark"
```

따라서 Context는 JavaScript의 진짜 전역 변수와 다릅니다.

**컴포넌트 트리 구조에 따라 값의 범위가 결정됩니다.**

---

# 7. Provider가 없으면 어떻게 될까?

`createContext`에 전달한 default value가 사용됩니다.

```jsx
const LocaleContext = createContext('ko');
```

Provider 없이:

```jsx
function ShowLocale() {
  const locale = useContext(LocaleContext);

  return <div>{locale}</div>;
}
```

결과:

```text
ko
```

반면 Provider가 있다면:

```jsx
<LocaleContext value="en">
  <ShowLocale />
</LocaleContext>
```

결과:

```text
en
```

즉,

```text
Provider 있음
    │
    └── Provider value 사용

Provider 없음
    │
    └── createContext(defaultValue) 사용
```

---

# 8. Context와 state의 관계

여기서 가장 많이 생기는 오해가 있습니다.

Context 자체가 state를 저장한다고 생각하기 쉽습니다.

하지만 역할이 다릅니다.

```text
useState / useReducer
        │
        │ 상태 보관
        ▼
      state
        │
        │ 전달
        ▼
     Context
        │
        │ 공유
        ▼
 여러 컴포넌트
```

예:

```jsx
const [user, setUser] = useState(null);

<UserContext value={{ user, setUser }}>
  {children}
</UserContext>
```

여기서 역할은:

```text
useState
   ↓
user 상태를 보관

Context
   ↓
user를 트리 아래로 전달
```

따라서 Context를 다음처럼 이해하는 것이 정확합니다.

> **Context는 상태 저장소라기보다 값 전달 메커니즘이다.**

---

# 9. Context 값이 변경되면 어떻게 될까?

Context의 중요한 특징은 **구독(subscription)**입니다.

```jsx
const theme = useContext(ThemeContext);
```

이 코드를 실행한 컴포넌트는 `ThemeContext`의 소비자가 됩니다.

```text
ThemeContext Provider
       │
       ├── Header
       │
       ├── Sidebar ── useContext(ThemeContext)
       │
       └── Content ── useContext(ThemeContext)
```

Provider의 `value`가 변경되면:

```text
ThemeContext
value 변경
    │
    ├─────────────┐
    ▼             ▼
 Sidebar       Content
 re-render     re-render
```

Context를 읽지 않는 컴포넌트는 **Context 변경 자체 때문에** 구독 업데이트를 받는 것은 아닙니다.

---

# 10. React.memo로 Context 업데이트를 막을 수 있을까?

다음 코드를 생각해봅시다.

```jsx
const Heavy = React.memo(function Heavy() {
  const { theme } = useContext(ThemeContext);

  return <div>{theme}</div>;
});
```

`React.memo`로 감쌌으므로 Context 업데이트도 막힐 것 같지만 그렇지 않습니다.

```text
React.memo
    │
    └── props 변경 최적화

useContext
    │
    └── Context 구독
```

Context 값이 변경되면:

```text
ThemeContext value 변경
        │
        ▼
      Heavy
        │
        ▼
    re-render
```

`React.memo`는 **props 비교를 통한 부모발 리렌더 최적화**이고, Context 구독 업데이트를 차단하는 기능이 아닙니다.

---

# 11. Context의 대표적인 성능 함정

다음 코드는 매우 흔합니다.

```jsx
<ThemeContext value={{ theme, setTheme }}>
  {children}
</ThemeContext>
```

문제는 객체입니다.

JavaScript에서:

```jsx
{ theme, setTheme }
```

는 렌더링될 때마다 새로운 객체가 만들어질 수 있습니다.

```text
Render #1

value ──▶ Object A


Render #2

value ──▶ Object B
```

내용이 같더라도:

```js
Object A !== Object B
```

입니다.

따라서 Provider가 다른 이유로 리렌더되면 새로운 `value` 참조가 만들어져 Context 소비자 업데이트를 유발할 수 있습니다.

---

# 12. useMemo로 value 참조 안정화하기

이럴 때 `useMemo`를 사용할 수 있습니다.

```jsx
function ThemeProvider({ children }) {
  const [theme, setTheme] = useState('light');

  const value = useMemo(
    () => ({ theme, setTheme }),
    [theme]
  );

  return (
    <ThemeContext value={value}>
      {children}
    </ThemeContext>
  );
}

// 위 코드중에 useMemo의 첫번째 아규먼트로 전달된 화살표 함수의 바디는
// 자바스크립트 객체를 만들어서 리턴하고 있음
// 자바스크립트 객체 리터럴을 사용할 때,
// 속성의 key와 value가 동일하다면, 다음과 같은 객체 리터럴을 사요할 수 있음
// { theme, setTheme }
// 이것은 다음 코드와 동일함
{
  theme: theme,
  setTheme: setTheme
}
```

이제:

```text
theme 변경 X

Render #1 ──▶ Object A
Render #2 ──▶ Object A


theme 변경 O

Render #3 ──▶ Object B
```

즉, Provider가 다른 이유로 다시 렌더되더라도 `theme`이 같다면 `value`의 참조를 재사용할 수 있습니다.

단, `useMemo`는 무조건 넣어야 하는 규칙이 아니라 **Provider가 자주 리렌더되고 객체형 value의 불필요한 변경이 실제 문제가 될 때 사용하는 최적화 수단**으로 보는 것이 좋습니다.

---

# 13. Context를 역할별로 분리하기

다음처럼 모든 상태를 하나의 Context에 넣을 수도 있습니다.

```jsx
const AppContext = createContext({
  user: null,
  theme: 'light',
  locale: 'ko'
});
```

그러면:

```text
AppContext
│
├── user
├── theme
└── locale
```

`user`만 바뀌어도 `AppContext`의 value가 바뀌므로 이 Context를 구독하는 컴포넌트들이 영향을 받습니다.

따라서 필요하다면 역할별로 분리합니다.

```jsx
const UserContext = createContext(null);
const ThemeContext = createContext('light');
const LocaleContext = createContext('ko');
```

```text
UserContext      ThemeContext      LocaleContext
    │                 │                 │
    ▼                 ▼                 ▼
 User UI           Theme UI         Locale UI
```

이렇게 하면 업데이트 범위를 더 명확하게 관리할 수 있습니다.

---

# 14. State와 Dispatch를 분리하는 패턴

`useReducer`와 Context를 조합할 때 유용한 패턴입니다.

```jsx
const CounterStateContext = createContext(null);
const CounterDispatchContext = createContext(null);
```

Provider:

```jsx
function CounterProvider({ children }) {
  const [count, dispatch] =
    useReducer(counterReducer, 0);

  return (
    <CounterStateContext value={count}>
      <CounterDispatchContext value={dispatch}>
        {children}
      </CounterDispatchContext>
    </CounterStateContext>
  );
}
```

구조는:

```text
CounterProvider
│
├── StateContext
│      count
│
└── DispatchContext
       dispatch
```

State를 읽는 컴포넌트:

```jsx
const count = useContext(CounterStateContext);
```

Dispatch만 필요한 컴포넌트:

```jsx
const dispatch =
  useContext(CounterDispatchContext);
```

`dispatch` 함수 자체는 안정적인 참조이므로 state 변경 때문에 Dispatch Context의 값까지 변경되지 않습니다.

따라서 **state를 읽지 않고 action만 발생시키는 컴포넌트의 불필요한 Context 업데이트를 줄이는 데 유용한 패턴**입니다.

---

# 15. 실전에서는 커스텀 훅으로 감싼다

실무에서는 컴포넌트마다 직접:

```jsx
useContext(AuthContext)
```

를 호출하기보다 커스텀 훅으로 추상화하는 경우가 많습니다.

```jsx
const AuthContext = createContext(null);

export function useAuth() {
  const context = useContext(AuthContext);

  if (context === null) {
    throw new Error(
      'useAuth must be used within AuthProvider'
    );
  }

  return context;
}
```

컴포넌트에서는:

```jsx
function UserInfo() {
  const {
    user,
    isLoggedIn,
    logout
  } = useAuth();

  // ...
}
```

이 패턴의 장점은 명확합니다.

```text
컴포넌트
   │
   ▼
useAuth()
   │
   ▼
AuthContext
   │
   ▼
Provider value
```

컴포넌트는 Context 구현 세부사항을 알 필요가 없고, Provider 밖에서 잘못 사용하면 즉시 에러를 발생시킬 수도 있습니다.

---

# 16. React 19의 use(Context)

React 19에서는 `useContext` 외에 `use` API로도 Context를 읽을 수 있습니다.

```jsx
import { use } from 'react';

function Panel({ compact }) {
  if (compact) {
    const { theme } = use(ThemeContext);

    return <span>{theme}</span>;
  }

  return <FullPanel />;
}
```

가장 큰 차이는 호출 위치입니다.

| 구분             | `useContext(C)` | `use(C)` |
| -------------- | --------------- | -------- |
| Context 읽기     | O               | O        |
| 조건문 안 호출       | X               | O        |
| 반복문 안 호출       | X               | O        |
| 컴포넌트/Hook 본문 밖 | X               | X        |
| Promise 읽기     | X               | O        |

`useContext`는 일반적인 Hook 호출 규칙을 따라야 합니다.

반면 `use`는 조건문이나 반복문 안에서도 사용할 수 있습니다.

다만:

```jsx
button.onclick = () => {
  use(ThemeContext); // X
};
```

처럼 이벤트 핸들러나 일반 함수에서 마음대로 호출할 수 있다는 의미는 아닙니다.

---

# 17. Context는 내부적으로 어떻게 동작할까?

이제 Fiber 관점에서 한 단계 더 깊게 들어가 보겠습니다.

컴포넌트가 렌더링되는 동안 React는 현재 Context 값을 관리합니다.

개념적으로:

```text
ThemeContext Provider
value = "light"
        │
        │ push
        ▼
Context Stack
        │
        ▼
   Child Render
        │
        │ useContext
        ▼
현재 Context 값 읽기
```

중첩 Provider가 등장하면:

```text
Provider "light"
    │
    │ push light
    ▼
Header
    │
    ▼
Provider "dark"
    │
    │ push dark
    ▼
Sidebar
```

`Sidebar`가 Context를 읽는 시점의 현재 값은 `"dark"`입니다.

서브트리 렌더링이 끝나면 이전 Context 값으로 복원됩니다.

```text
push "light"
      │
      ▼
push "dark"
      │
      ▼
Sidebar render
      │
      ▼
pop
      │
      ▼
"light" 복원
```

이 메커니즘 때문에 결과적으로 **가장 가까운 Provider의 값**을 읽게 됩니다.

---

# 18. useContext를 호출하면 Fiber에는 무엇이 기록될까?

여기서 중요한 내부 구현 포인트가 있습니다.

다음 코드가 실행되었다고 해봅시다.

```jsx
const theme = useContext(ThemeContext);
```

React는 단순히 값을 반환하는 것으로 끝나지 않습니다.

현재 Fiber가 이 Context를 사용하고 있다는 **dependency 정보**도 기록합니다.

개념적으로:

```text
Page Fiber
│
├── memoizedState
│      └── useState / useEffect 등의 Hook 정보
│
└── dependencies
       │
       └── ThemeContext 구독
```

즉,

> `useContext`의 Context 구독 정보는 `useState`처럼 Hook의 `memoizedState` 슬롯에 저장되는 것이 아니라 Fiber의 Context dependency 정보로 관리됩니다.

그래서 Context 값이 변경되면 React는 해당 Context에 의존하는 Fiber들을 찾아 업데이트 대상으로 표시할 수 있습니다.

```text
Provider value 변경
        │
        ▼
Context dependency 확인
        │
        ├── Fiber A
        ├── Fiber B
        └── Fiber C
              │
              ▼
        업데이트 스케줄링
```

---

# 19. Context 전체 동작 흐름

지금까지 내용을 하나로 연결하면 다음과 같습니다.

```text
① createContext
      │
      ▼
Context 객체 생성
      │
      ▼
② Provider
      │
      │ value
      ▼
컴포넌트 서브트리에 값 공급
      │
      ▼
③ useContext / use
      │
      ├── 현재 Context 값 읽기
      │
      └── Fiber에 dependency 등록
      │
      ▼
④ Provider value 변경
      │
      ▼
Context 변경 감지
      │
      ▼
⑤ Context를 구독하는 Fiber
   업데이트 스케줄링
      │
      ▼
⑥ 컴포넌트 리렌더
      │
      ▼
새 Context value 사용
```

이 흐름이 Context API의 핵심 내부 구조입니다.

---

# 20. Context는 언제 사용하면 좋은가?

Context가 특히 잘 맞는 값은 다음과 같습니다.

| 데이터                | Context 적합도 |
| ------------------ | ----------: |
| Theme              |       매우 좋음 |
| Locale             |       매우 좋음 |
| 로그인 사용자            |          좋음 |
| 권한 정보              |          좋음 |
| Router 관련 정보       |          좋음 |
| API Client         |          좋음 |
| 실시간 마우스 좌표         |       좋지 않음 |
| 대형 Form의 모든 입력값    |          주의 |
| 매우 빈번하게 변경되는 전역 상태 |          주의 |

핵심 판단 기준은 단순히 **“전역 데이터인가?”**가 아닙니다.

다음 두 가지를 같이 봐야 합니다.

```text
얼마나 넓게 공유되는가?
        +
얼마나 자주 변경되는가?
```

---

# 21. Context는 상태 관리 라이브러리인가?

엄밀하게 역할을 분리하면 다음과 같습니다.

```text
┌─────────────────────┐
│ useState/useReducer │
│                     │
│   상태 저장/변경     │
└─────────┬───────────┘
          │
          ▼
┌─────────────────────┐
│       Context       │
│                     │
│     값 전달/공유     │
└─────────┬───────────┘
          │
          ▼
┌─────────────────────┐
│     Components      │
│                     │
│      값 소비         │
└─────────────────────┘
```

따라서 Context의 본질을 한 문장으로 표현하면:

> **Context는 상태 자체를 관리하는 도구라기보다, 컴포넌트 트리의 특정 범위에 값을 공급하고 필요한 컴포넌트가 그 값을 구독할 수 있게 만드는 값 전달 메커니즘이다.**

---

# 22. Context vs 다른 상태 관리 방법

| 도구              | 주요 역할               |
| --------------- | ------------------- |
| `props`         | 부모 → 자식의 명시적 데이터 전달 |
| `useState`      | 로컬 상태 관리            |
| `useReducer`    | 복잡한 상태 변경 로직 관리     |
| Context         | 트리 범위의 값 전달         |
| Redux / Zustand | 복잡한 전역 클라이언트 상태 관리  |
| TanStack Query  | 서버 상태 관리 및 캐싱       |

특히 서버에서 가져온 데이터를 모두 Context에 넣는 방식은 일반적으로 좋은 선택이 아닙니다.

```text
Client State
   │
   ├── useState
   ├── useReducer
   ├── Context
   ├── Redux
   └── Zustand


Server State
   │
   └── TanStack Query 등
```

상태의 성격에 따라 도구를 구분하는 것이 중요합니다.

---

# 23. Context API 핵심 정리

Context API를 처음 배울 때는 다음 다섯 단계만 기억하면 됩니다.

```text
① createContext()
        │
        ▼
② Context 생성
        │
        ▼
③ Provider에서 value 공급
        │
        ▼
④ useContext(Context)
   또는 use(Context)
        │
        ▼
⑤ Provider value 변경
        │
        ▼
Context 소비 컴포넌트 업데이트
```

그리고 내부적으로는:

```text
Provider
   │
   │ Context value
   ▼
Context Stack
   │
   ▼
Consumer Fiber
   │
   └── dependencies
          │
          └── Context 구독 정보
```

마지막으로 가장 중요한 개념을 정리하면:

> **Context API는 props drilling을 피하기 위한 단순한 편의 기능을 넘어, React 컴포넌트 트리의 특정 서브트리에 값을 공급하고 필요한 Fiber가 그 Context를 구독하도록 만드는 React의 공식적인 값 전달 메커니즘입니다.**

그리고 역할을 반드시 구분해야 합니다.

```text
useState / useReducer
        ↓
     상태 관리

     Context
        ↓
      값 전달

useContext / use
        ↓
      값 소비
```

이 세 역할을 구분하면 Context API의 구조가 훨씬 명확해집니다.
