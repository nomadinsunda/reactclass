# 1️⃣ Thunk 한 문장 정의

> **Thunk란, “action을 바로 reducer로 보내지 않고,
> 그 전에 실행되는 ‘함수 형태의 action’으로
> 비동기·조건·여러 dispatch를 처리하는 중간 계층”입니다.**

핵심 키워드:

* 함수 action
* dispatch를 지연/제어
* reducer 앞단에서 실행

---

# 2️⃣ Redux에서 왜 Thunk가 필요했을까?

Redux의 기본 규칙부터 다시 보면:

### ❌ Reducer에서는 할 수 없는 것

* API 호출
* setTimeout / setInterval
* 조건 분기 (state에 따라 dispatch 결정)
* 여러 action 순차 dispatch

왜냐하면 reducer는 **순수 함수**여야 하기 때문입니다.

```js
(state, action) => nextState
```

👉 입력이 같으면 출력이 항상 같아야 함
👉 부작용(side effect) 금지

---

## 그런데 현실은?

```txt
버튼 클릭 →
  서버 요청 →
    성공이면 state 갱신
    실패면 에러 state 갱신
```

이 흐름은:

* 비동기
* 조건 분기
* 여러 단계

❌ reducer에는 절대 못 넣음

➡️ 그래서 **reducer 앞에 “중간 실행자”가 필요**했고
그게 바로 **Thunk (미들웨어)** 입니다.

---

# 3️⃣ Thunk의 위치 (Redux 파이프라인)

Redux 흐름을 정확히 그리면 이렇습니다:

```
dispatch(action)
   ↓
[ middleware ]  ← Thunk가 여기
   ↓
reducer
   ↓
new state
```

Thunk는:

* `dispatch`를 가로채서
* action이 **함수인지 아닌지**를 검사합니다.

---

# 4️⃣ Thunk의 핵심 아이디어 (진짜 중요)

### 일반 action

```js
dispatch({ type: 'todo/added', payload: 'Study Redux' })
```

### Thunk action (함수!)

```js
dispatch((dispatch, getState) => {
  // 여기서 마음껏 비동기/조건 로직
})
```

👉 Redux는 원래 **객체만 dispatch** 가능
👉 Thunk 미들웨어가 설치되면 **함수도 허용**

---

# 5️⃣ Thunk의 기본 형태 (해부)

```js
const myThunk = (arg) => {
  return async (dispatch, getState) => {
    // 1. 현재 상태 조회
    const state = getState()

    // 2. 비동기 작업
    const result = await fetchSomething(arg)

    // 3. 여러 action dispatch
    dispatch(actionA(result))
    dispatch(actionB())
  }
}
```

### 여기서 각 역할

| 요소       | 역할              |
| -------- | --------------- |
| thunk 함수 | 유스케이스(use case) |
| dispatch | 다음 단계로 상태 변경 요청 |
| getState | 현재 상태 기반 조건 분기  |
| reducer  | 실제 상태 변경        |

👉 **Thunk = 도메인 유스케이스 실행기**

---

# 6️⃣ Redux Toolkit의 `createAsyncThunk`는 뭐가 다른가?

RTK는 Thunk를 “표준화”했습니다.

### ❌ 순수 Thunk의 문제

* pending / success / error 직접 관리
* action type 중복
* 상태 플래그 관리 난잡

---

### ✅ createAsyncThunk의 해결책

```js
export const fetchTodos = createAsyncThunk(
  'todos/fetchTodos',
  async (_, { rejectWithValue }) => {
    const res = await fetch('/api/todos')
    if (!res.ok) throw new Error('fail')
    return await res.json()
  }
)
```

자동으로 생성되는 것들:

```
todos/fetchTodos/pending
todos/fetchTodos/fulfilled
todos/fetchTodos/rejected
```

👉 **비동기 상태 머신을 자동 제공**

---

# 7️⃣ createAsyncThunk 내부 구조 (정확히)

```js
dispatch(fetchTodos())
```

실제로는:

1. `pending` dispatch
2. payloadCreator 실행 (async)
3. 성공 → `fulfilled(payload)`
4. 실패 → `rejected(error | rejectWithValue)`

Reducer에서는:

```js
extraReducers(builder) {
  builder
    .addCase(fetchTodos.pending, ...)
    .addCase(fetchTodos.fulfilled, ...)
    .addCase(fetchTodos.rejected, ...)
}
```

---

# 8️⃣ Thunk는 “도메인 로직인가, 인프라 로직인가?”

이 질문, **굉장히 수준 높은 질문**입니다.

### 결론부터:

> **Thunk는 “도메인 유스케이스(use case)” 계층입니다.**

* reducer → 도메인 규칙 (what changes)
* thunk → 도메인 흐름 (when / how / in what order)

### 예

```txt
주문 요청 →
  재고 확인 →
    결제 요청 →
      성공 시 주문 확정
      실패 시 롤백
```

이 흐름은:

* 단일 reducer ❌
* 단일 action ❌
* **Thunk ⭕**

---

# 9️⃣ Thunk에서 `getState()`는 언제 쓰는가?

### ✅ 조건부 dispatch

```js
if (getState().auth.isLoggedIn) {
  dispatch(fetchPrivateData())
}
```

### ✅ 중복 요청 방지

```js
if (getState().todos.status === 'loading') return
```

### ✅ 캐시 판단

```js
if (Date.now() - lastFetched < 60000) return
```

---

# 🔟 Thunk에서 절대 하면 안 되는 것들

### ❌ reducer 역할 침범

```js
dispatch({ type: 'SET_STATE', payload: bigObject }) // ❌ 설계 악취
```

### ❌ UI 로직 과도 포함

```js
alert('성공') // ⚠️ 지양
```

Thunk는 **비즈니스 흐름**,
UI 반응은 **컴포넌트**가 담당하는 게 이상적입니다.

---

# 11️⃣ Thunk vs 다른 미들웨어

| 구분     | Thunk       | Saga      |
| ------ | ----------- | --------- |
| 난이도    | 낮음          | 높음        |
| 러닝커브   | 완만          | 가파름       |
| 비동기    | async/await | generator |
| 실무 채택  | ⭐⭐⭐⭐        | ⭐⭐        |
| RTK 권장 | ✅           | ❌ (필요 시만) |

👉 **대부분의 앱은 Thunk로 충분**

---

# 12️⃣ 한 문장 최종 정리

> **Thunk는 Redux에서 비동기·조건·여러 dispatch를 담당하는
> “도메인 유스케이스 실행 계층”이며,
> reducer가 순수성을 유지하도록 보호하는 방패입니다.**

