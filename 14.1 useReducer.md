

# 🧠 `useReducer` 

---

## ✅ 1. `useReducer`란?

```tsx
const [state, dispatch] = useReducer(reducer, initialState);
```

`useReducer`는 상태(state)를 업데이트할 때, 단순한 `setState`가 아닌 **"액션 기반 로직 분기 처리"** 를 통해 **더 구조화된 상태 관리**를 가능하게 해주는 Hook입니다.

> React의 상태 관리를 더 **명확하고 선언적으로** 만들고 싶을 때 사용합니다.
> Redux의 핵심 개념을 React 내부로 가져온 것과 유사합니다.

---

## 🧩 2. 기본 사용법

```tsx
const initialState = { count: 0 };

function reducer(state, action) {
  switch (action.type) {
    case 'increment':
      return { count: state.count + 1 };
    case 'decrement':
      return { count: state.count - 1 };
    default:
      throw new Error('알 수 없는 액션');
  }
}

const [state, dispatch] = useReducer(reducer, initialState);
```

### 📌 구조 설명

| 요소             | 설명                                      |
| -------------- | --------------------------------------- |
| `state`        | 현재 상태 객체                                |
| `dispatch`     | 상태를 업데이트하기 위한 함수                        |
| `reducer`      | 상태 전이 함수: `(state, action) => newState` |
| `initialState` | 초기 상태                                   |

---

## ✅ 3. `useState` vs `useReducer` 비교

| 항목       | `useState`               | `useReducer`          |
| -------- | ------------------------ | --------------------- |
| 상태 형태    | 간단한 값 (number, string 등) | 복잡한 구조 (객체, 배열 등)     |
| 업데이트 방식  | 직접 `setState`            | `dispatch(action)`    |
| 상태 변화 추적 | 어렵다 (로직 분산됨)             | 명확하다 (`reducer`에 집중됨) |
| 테스트/디버깅  | 상대적으로 어렵다                | reducer 함수 단위로 테스트 쉬움 |
| 용도       | 단순 상태                    | 복잡한 상태, 여러 값 조작 필요 시  |

---

## 🔄 4. 실전 예제: `useState`로는 힘든 상태 관리

```tsx
const initialState = {
  users: [],
  selectedUserId: null,
  loading: false,
  error: null
};

function reducer(state, action) {
  switch (action.type) {
    case 'LOAD_START':
      return { ...state, loading: true, error: null };
    case 'LOAD_SUCCESS':
      return { ...state, loading: false, users: action.payload };
    case 'LOAD_ERROR':
      return { ...state, loading: false, error: action.payload };
    case 'SELECT_USER':
      return { ...state, selectedUserId: action.payload };
    default:
      return state;
  }
}
```

이런 복합 상태를 `useState`로 관리하면 `setUsers`, `setLoading`, `setError`, `setSelectedUserId` 등 상태 관리 코드가 **흩어지고 복잡**해집니다.

✅ `useReducer`는 한 곳에서 모든 상태 전이를 관리 → **가독성, 유지보수성 증가**

---

## 🧠 5. 타입 세이프티와 `action` 패턴

```ts
type State = { count: number };
type Action = { type: 'increment' } | { type: 'decrement' } | { type: 'set'; payload: number };
```

```tsx
function reducer(state: State, action: Action): State {
  switch (action.type) {
    case 'increment': return { count: state.count + 1 };
    case 'decrement': return { count: state.count - 1 };
    case 'set':       return { count: action.payload };
  }
}
```

✅ `action` 객체는 `type` 기반으로 분기되며, 필요 시 `payload`를 통해 동적 데이터 전달 가능
✅ **Redux 패턴과 동일**한 액션 기반 구조를 갖습니다.

---

## ⚙️ 6. `dispatch` 예시

```tsx
dispatch({ type: 'increment' });
dispatch({ type: 'set', payload: 100 });
```

* `dispatch`는 `action`을 **trigger**하는 트리거 함수입니다.
* 상태 업데이트는 오직 `reducer`를 통해서만 이루어집니다.

---

## 🔍 7. 성능 최적화 측면

* `useReducer`는 **불필요한 setState 분할을 줄이고** 컴포넌트 리렌더 트리거를 제어할 수 있어 **성능에 유리**
* 특히 `React.memo`와 함께 쓰면 더 큰 효과를 봅니다.

---

## 🧠 8. 고급 패턴

### ✅ `useReducer` + `Context` → 글로벌 상태관리 (Mini Redux)

```tsx
// context.js
export const AppStateContext = createContext();
export const AppDispatchContext = createContext();
```

```tsx
export function AppProvider({ children }) {
  const [state, dispatch] = useReducer(appReducer, initialState);

  return (
    <AppStateContext.Provider value={state}>
      <AppDispatchContext.Provider value={dispatch}>
        {children}
      </AppDispatchContext.Provider>
    </AppStateContext.Provider>
  );
}
```

* 규모가 커질수록 상태를 전역에서 관리 가능
* Redux를 대체할 수 있는 구조

---

## ✳️ 언제 `useReducer`를 쓰면 좋을까?

| 상황                          | 추천 |
| --------------------------- | -- |
| 상태가 객체/배열 구조로 복잡할 때         | ✅  |
| 상태 업데이트가 다양한 분기로 나뉠 때       | ✅  |
| 여러 상태가 하나의 액션에 따라 바뀌어야 할 때  | ✅  |
| 상태 전이 로직을 테스트하거나 추상화하고 싶을 때 | ✅  |

---

## 📦 요약

| 항목             | 내용                                                      |
| -------------- | ------------------------------------------------------- |
| `useReducer`란? | 상태 전이를 함수(reducer)로 분리하는 상태 관리 방식                       |
| 구조             | `[state, dispatch] = useReducer(reducer, initialState)` |
| 장점             | 복잡한 상태를 한 곳에서 명확히 관리, 로직 테스트 쉬움                         |
| 사용 예           | form 상태, 비동기 처리 상태, 다단계 로직 등                            |
| 단점             | 단순 상태엔 오히려 복잡할 수 있음                                     |

