## useSelector란?

```ts
const value = useSelector(selectorFn)
```

> **Redux Store 전체 상태(state) 중에서
> 내가 필요한 조각만 “선택(select)”해서 React 컴포넌트로 가져오는 Hook**입니다.

O **읽기 전용**
O **자동 구독(subscribe)**
O **변경될 때만 리렌더링**

---

## Redux 상태 구조 먼저 보기

Redux Store는 보통 이렇게 생겼습니다:

```ts
{
  auth: {
    user: { id: 1, name: "Kim" },
    isLoggedIn: true
  },
  todos: {
    list: [...],
    loading: false
  }
}
```

이 전체 객체가 **단 하나의 Store state**입니다.

---

## useSelector의 핵심 역할

### X 나쁜 방식 (전체 state를 다 가져오기)

```ts
const state = useSelector(state => state)
```

* 모든 state 변경 → 무조건 리렌더링
* 성능 최악 X
* react-redux 9는 **개발 모드에서 이 패턴을 경고**합니다 (`identityFunctionCheck`)

```txt
Selector unknown returned the root state when called. This can lead to unnecessary rerenders.
Selectors that return the entire state are almost certainly a mistake, as they will cause a rerender whenever *anything* in state changes.
```

---

### O 올바른 방식 (필요한 것만 선택)

```ts
const isLoggedIn = useSelector(state => state.auth.isLoggedIn)
```

 **auth.isLoggedIn 이 바뀔 때만 리렌더링**

---

## 내부 동작 원리 (중요)

useSelector는 내부적으로 다음을 수행합니다:

1⃣ Redux Store를 **구독** — react-redux 8부터는 직접 `store.subscribe`를 다루지 않고 React 18의 **`useSyncExternalStore`** 위에서 동작합니다. (동시성 렌더링에서의 tearing 방지)
2⃣ 상태가 바뀌면 selector 함수 실행
3⃣ **이전 값 vs 새 값 비교**
4⃣ 다를 때만 컴포넌트 리렌더링

> selector는 **store가 바뀔 때만이 아니라, 컴포넌트가 리렌더링될 때마다도** 실행됩니다.
> 그래서 selector 안에서 무거운 계산을 하면 안 되고, 계산이 필요하면 메모이제이션이 필요합니다.

---

### 값 비교 방식 (아주 중요)

기본 비교 함수는 **참조 동등성(reference equality)** 입니다.

```ts
// react-redux 내부의 기본 equalityFn
const refEquality = (a, b) => a === b
```

즉,

| selector 반환값                        | 리렌더링 여부  |
| ----------------------------------- | -------- |
| primitive (number, string, boolean) | 안전       |
| object / array | 주의 필요 |

**핵심**: “값이 같은가”가 아니라 **“같은 객체인가”** 를 봅니다.
`{a:1}`과 `{a:1}`은 내용이 같아도 `===`가 `false`이므로 리렌더링이 발생합니다.

---

## 가장 흔한 실수

### X 매번 새 객체를 반환하는 selector

```ts
const userInfo = useSelector(state => ({
  id: state.auth.user.id,
  name: state.auth.user.name
}))
```

 매번 `{}` 새 객체 생성
 `===` 비교 실패
 **상태 안 바뀌어도 리렌더링**

---

### O 해결 방법 ①: 값 하나씩 선택

```ts
const userId = useSelector(state => state.auth.user.id)
const userName = useSelector(state => state.auth.user.name)
```

---

### O 해결 방법 ②: memoized selector (reselect)

```ts
// reselect는 Redux Toolkit에 기본 포함 → 별도 설치 불필요
import { createSelector } from '@reduxjs/toolkit'

const selectUserInfo = createSelector(
  [state => state.auth.user],
  user => ({
    id: user.id,
    name: user.name
  })
)

const userInfo = useSelector(selectUserInfo)
```

 **진짜로 user가 바뀔 때만 새 객체 생성**

* RTK 2.x가 사용하는 reselect 5의 기본 메모이저는 `weakMapMemoize`라서,
  예전(`defaultMemoize`, 캐시 크기 1)처럼 **인자가 번갈아 들어올 때 캐시가 깨지는 문제**가 크게 줄었습니다.
* 그래도 `selectItemById(state, id)`처럼 **인자를 받는 selector**를 여러 컴포넌트가 서로 다른 id로 쓴다면
  컴포넌트별 selector 인스턴스를 만드는 패턴(`useMemo(() => makeSelectItemById(), [])`)을 기억해두세요.

---

### O 해결 방법 ③: equalityFn 지정 (`shallowEqual`)

`useSelector`는 **두 번째 인자로 비교 함수**를 받습니다.

```ts
import { useSelector, shallowEqual } from 'react-redux'

const userInfo = useSelector(
  state => ({ id: state.auth.user.id, name: state.auth.user.name }),
  shallowEqual // 1단계 깊이의 값들을 === 로 비교
)
```

* 새 객체를 만들더라도 **내부 필드가 같으면 리렌더링하지 않습니다.**
* 다만 매번 selector는 실행되고 객체도 새로 만들어집니다(비교만 얕게 할 뿐).
  계산 비용까지 아끼려면 `createSelector`를 쓰세요.

> 선택 기준: **필드 단위 선택 > `createSelector` > `shallowEqual`** 순으로 검토하면 대체로 맞습니다.

---

## useSelector vs useState

| 구분    | useState | useSelector      |
| ----- | -------- | ---------------- |
| 상태 위치 | 컴포넌트 내부  | Redux Store      |
| 범위    | 로컬       | 전역               |
| 변경 방식 | setState | dispatch(action) |
| 읽기    | 직접 접근    | selector 함수      |

 **useSelector는 “전역 상태 읽기” 전용**

---

## useSelector 한 줄 요약

> **useSelector는
> Redux Store 전체 상태에서
> 컴포넌트가 필요한 “최소 단위”만 골라서
> 변경될 때만 리렌더링되게 해주는 Hook입니다.**

---

## 언제 잘 쓰고 있는 걸까?

O selector가 **작다**
O 반환값이 **primitive 위주**
O object/array는 **memoized selector 사용**
O 컴포넌트가 state 구조를 **직접 몰라도 됨**

---

## TypeScript: 타입이 입혀진 훅 만들기

매번 `useSelector((state: RootState) => ...)`라고 쓰지 말고,
react-redux 9의 `withTypes`로 한 번만 만들어 두는 것이 공식 권장입니다.

```ts
// hooks.ts
import { useDispatch, useSelector } from 'react-redux'
import type { RootState, AppDispatch } from './store'

export const useAppDispatch = useDispatch.withTypes<AppDispatch>()
export const useAppSelector = useSelector.withTypes<RootState>()
```

```ts
const isLoggedIn = useAppSelector(state => state.auth.isLoggedIn) // state 타입 자동 추론
```
