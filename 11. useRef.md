# 📘 React `useRef` 

---

## ✅ 목차

1. `useRef`란 무엇인가?
2. `useRef` vs `useState` vs 일반 변수
3. DOM 요소 접근 예제
4. 상태 저장용 Ref 사용 (렌더링 방지용)
5. 타이머 제어: `setInterval`, `setTimeout`에 활용
6. 외부 라이브러리 참조 보관 (canvas, chart 등)
7. React 컴포넌트 간 Ref 전달 (forwardRef, useImperativeHandle)
8. 리렌더링 최적화 전략
9. 실전 예제 정리

---

## 1️⃣ `useRef`란 무엇인가?

* React의 **기본 Hook** 중 하나
* **렌더링 사이에도 유지되는** **mutable 객체** 생성
* `useRef(initialValue)`는 `{ current: initialValue }` 형태의 객체 반환
* 리렌더링을 **일으키지 않음** ← `state`와의 가장 큰 차이

```jsx
const myRef = useRef(0);
myRef.current = myRef.current + 1;
```

---

## 2️⃣ `useRef` vs `useState` vs 일반 변수

| 구분          | 유지 범위         | 렌더링 유발 | 사용 목적        |
| ----------- | ------------- | ------ | ------------ |
| `let x = 0` | 함수 호출 시마다 초기화 | ❌      | 지역 변수        |
| `useState`  | 렌더 간 유지       | ✅      | UI 상태 표현     |
| `useRef`    | 렌더 간 유지       | ❌      | 값 보존, DOM 접근 |

🔎 요약:

* **렌더링 사이 값을 유지하지만 리렌더링이 필요 없다** → `useRef` 선택

---

## 3️⃣ 📌 DOM 요소 접근 예제

```jsx
import { useRef } from 'react';

function FocusInput() {
  const inputRef = useRef();

  const handleFocus = () => {
    inputRef.current.focus(); // DOM 접근
  };

  return (
    <>
      <input ref={inputRef} placeholder="이름을 입력하세요" />
      <button onClick={handleFocus}>포커스 이동</button>
    </>
  );
}
```

🧠 렌더링과 무관하게 input 엘리먼트에 직접 접근할 수 있음

---

## 4️⃣ 🔁 상태 저장용 Ref (렌더링 없는 값 저장)

```jsx
import { useRef, useState } from 'react';

function Counter() {
  const [count, setCount] = useState(0);
  const renderCount = useRef(1);

  const handleClick = () => {
    setCount(count + 1);
    renderCount.current += 1;
  };

  return (
    <>
      <p>Count: {count}</p>
      <p>Render Count: {renderCount.current}</p>
      <button onClick={handleClick}>+1</button>
    </>
  );
}
```

🧠 `renderCount.current`는 **리렌더링마다 증가**하지만 **화면에 영향 X**

---

## 5️⃣ ⏱ 타이머 참조 보관

```jsx
import { useRef, useState } from 'react';

function Timer() {
  const [time, setTime] = useState(0);
  const timerRef = useRef(null);

  const start = () => {
    if (timerRef.current) return; // 중복 방지
    timerRef.current = setInterval(() => setTime(t => t + 1), 1000);
  };

  const stop = () => {
    clearInterval(timerRef.current);
    timerRef.current = null;
  };

  return (
    <>
      <h2>{time}s</h2>
      <button onClick={start}>시작</button>
      <button onClick={stop}>중지</button>
    </>
  );
}
```

🧠 `useRef`는 **setInterval의 ID를 보관**하는 데 유용하며, 클린업 로직에도 자주 쓰임

---

## 6️⃣ 📊 외부 라이브러리 참조 (Canvas 등)

```jsx
import { useRef, useEffect } from 'react';

function Canvas() {
  const canvasRef = useRef();

  useEffect(() => {
    const ctx = canvasRef.current.getContext('2d');
    ctx.fillStyle = 'skyblue';
    ctx.fillRect(10, 10, 100, 100);
  }, []);

  return <canvas ref={canvasRef} width={300} height={200} />;
}
```

🧠 `canvas`, `chart.js`, `video`, `iframe` 등에서 DOM 접근이 필요한 경우 최적

---

## 7️⃣ 🧩 컴포넌트 간 Ref 전달 (forwardRef, useImperativeHandle)

```jsx
// 📁 MyInput.jsx
import React, { forwardRef, useImperativeHandle, useRef } from 'react';

const MyInput = forwardRef((props, ref) => {
  const inputRef = useRef();

  useImperativeHandle(ref, () => ({
    focus: () => inputRef.current.focus()
  }));

  return <input ref={inputRef} {...props} />;
});

export default MyInput;
```

```jsx
// 📁 App.jsx
import React, { useRef } from 'react';
import MyInput from './MyInput';

function App() {
  const inputRef = useRef();

  return (
    <>
      <MyInput ref={inputRef} />
      <button onClick={() => inputRef.current.focus()}>포커스</button>
    </>
  );
}
```

✅ `forwardRef` + `useImperativeHandle` = 외부에 안전한 인터페이스만 노출

---

## 8️⃣ 🎯 성능 최적화 전략

### `useRef`를 활용한 렌더링 방지

```jsx
const countRef = useRef(0);

useEffect(() => {
  countRef.current += 1; // 화면 영향 X
}, [someProp]); // prop이 변경될 때마다 추적 가능
```

### 불필요한 상태 관리를 제거하고 메모리 절약 가능

---

## 9️⃣ 🧪 실전 예제 정리

| 예제          | 설명                                  |
| ----------- | ----------------------------------- |
| 포커스 이동      | input, textarea 등 직접 제어             |
| setInterval | 타이머 ID 보관 및 중지                      |
| 외부 라이브러리    | canvas/chart 제어                     |
| 렌더링 횟수 측정   | 디버깅 용도                              |
| 컴포넌트 제어     | forwardRef + useImperativeHandle 사용 |

---

## 🧠 마무리 요약

| 항목                                  | 핵심 요점                                  |
| ----------------------------------- | -------------------------------------- |
| `useRef`는 객체 `{ current: ... }`를 반환 | 값 변경해도 리렌더링 발생 안 함                     |
| 주 용도 1                              | DOM 접근 (input, canvas 등)               |
| 주 용도 2                              | 값 저장소 (렌더링 필요 없는 상태)                   |
| 주 용도 3                              | 외부 컨트롤 (`setInterval`, 컴포넌트 제어 등)      |
| 보너스                                 | `useImperativeHandle`로 안전한 외부 인터페이스 제공 |



