`useRef`는 “돔을 잡는 Hook” 정도로만 알고 계시면 **반만** 아신 겁니다 😎
실제로는 **React 컴포넌트 안에서 “리렌더링 없이 계속 유지되는 박스”** 를 하나 만드는 Hook이라고 보시면 됩니다.

---

## 1. useRef 한 줄 정의 ✨

```
const ref = useRef<T>(initialValue);
// ref의 타입: { current: T }
```

```
function useRef(initialValue) {
  const hook = updateWorkInProgressHook(); // 해당 Fiber 노드의 Hook 슬롯 가져오기

  if (hook.memoizedState === null) {
    // 첫 렌더 → ref 객체 생성
    const refObject = { current: initialValue };
    hook.memoizedState = refObject;
    return refObject;
  } else {
    // 리리렌더 → 기존 객체 그대로 반환
    return hook.memoizedState;
  }
}
```

* **입력**: 초기 값 (`initialValue`)
* **출력**: `{ current: ... }`라는 **객체(Ref 객체)**
* 이 객체는 **컴포넌트가 리렌더링 되어도 절대 새로 만들어지지 않고, 같은 객체가 계속 재사용**됩니다.
* `ref.current` 값을 바꿔도 **리렌더링이 발생하지 않습니다.** ← 핵심

> 🧠 즉, `useRef`는 **“리렌더링과는 독립적으로 기억되는 한 칸짜리 메모리 박스”** 를 만드는 Hook입니다.

---

## 2. useRef는 언제 쓰나요? (3가지 대표 용도) 🎯

1. **DOM 요소에 직접 접근할 때**

   * 포커스 주기, 스크롤, 크기 측정 등
2. **리렌더링 없이 계속 유지되는 값 저장**

   * 이전 값, 타이머 ID, 외부 라이브러리 인스턴스, 플래그(isMounted 등)
3. **자식 컴포넌트에 “명령형 API”를 제공할 때**

   * `forwardRef`, `useImperativeHandle`과 함께

각 용도별로 구체적으로 보겠습니다.

---

## 3. 가장 많이 쓰는 패턴: DOM 접근 🧱

### 3-1. 기본 예제: 입력창에 자동 포커스 주기

```jsx
import { useRef, useEffect } from "react";

function AutoFocusInput() {
  const inputRef = useRef(null); // 초기값: null

  useEffect(() => {
    // 컴포넌트가 화면에 “커밋”된 후 실행
    if (inputRef.current) {
      inputRef.current.focus();
    }
  }, []);

  return <input ref={inputRef} placeholder="자동 포커스!" />;
}
```

여기서 중요한 점 ✅

* `ref={inputRef}` 라고 쓰면

  * React가 **렌더+커밋 이후에** 실제 DOM 요소를 `inputRef.current`에 넣어줍니다.
* `inputRef.current`의 타입은 `HTMLInputElement | null`
* `useEffect` 안에서 사용하는 이유:

  * 렌더 중에는 아직 실제 DOM이 없고, **DOM이 완성된 뒤 커밋 단계에서 ref가 채워지기 때문**입니다.

### 3-2. 스크롤 제어 예제

```jsx
function ChatMessages({ messages }) {
  const bottomRef = useRef(null);

  useEffect(() => {
    if (bottomRef.current) {
      bottomRef.current.scrollIntoView({ behavior: "smooth" });
    }
  }, [messages.length]); // 새 메시지가 추가될 때마다

  return (
    <div style={{ maxHeight: 300, overflowY: "auto" }}>
      {messages.map((m) => (
        <div key={m.id}>{m.text}</div>
      ))}
      <div ref={bottomRef} />
    </div>
  );
}
```

* `bottomRef`가 가리키는 더미 요소까지 스크롤을 항상 내려주는 패턴입니다.
* DOM 조작은 대부분 **useRef + useEffect** 조합으로 처리한다고 보시면 됩니다.

---

## 4. "리렌더 없이 기억되는 값" 저장하기 🧠

`useState`와 가장 큰 차이점은:

* `useState`:

  * 값이 바뀌면 → 컴포넌트 **리렌더링 발생**
* `useRef`:

  * `ref.current` 바뀌어도 → **리렌더링 없음**

그래서 “UI에 직접적으로 표시하지 않을 값”을 저장할 때 `useRef`가 딱입니다.

### 4-1. 이전 값(prev value) 기억하기

```jsx
import { useState, useRef, useEffect } from "react";

function Counter() {
  const [count, setCount] = useState(0);
  const prevCountRef = useRef(count); // 초기값: 현재 count

  useEffect(() => {
    // 이 이펙트가 실행될 때 이미 count는 최신 값
    console.log("현재:", count, "이전:", prevCountRef.current);

    // 다음 렌더에서 쓸 "이전 값" 저장
    prevCountRef.current = count;
  }, [count]);

  return (
    <div>
      <p>현재 값: {count}</p>
      <p>이전 값: {prevCountRef.current}</p>
      <button onClick={() => setCount((c) => c + 1)}>+1</button>
    </div>
  );
}
```

* `prevCountRef.current`는 **리렌더 사이에 살아남는 값**입니다.
* `useState`로 `prevCount`를 관리하면 불필요한 리렌더가 더 발생할 수 있습니다.

### 4-2. 타이머 ID 저장 (setTimeout / setInterval)

```jsx
function Timer() {
  const [seconds, setSeconds] = useState(0);
  const intervalIdRef = useRef(null);

  const start = () => {
    if (intervalIdRef.current != null) return; // 이미 동작 중이면 무시

    const id = setInterval(() => {
      setSeconds((s) => s + 1);
    }, 1000);

    intervalIdRef.current = id; // 리렌더 없이 ID 저장
  };

  const stop = () => {
    if (intervalIdRef.current != null) {
      clearInterval(intervalIdRef.current);
      intervalIdRef.current = null;
    }
  };

  useEffect(() => {
    return () => {
      // 컴포넌트 언마운트 시 타이머 정리
      if (intervalIdRef.current != null) {
        clearInterval(intervalIdRef.current);
      }
    };
  }, []);

  return (
    <div>
      <p>{seconds}초</p>
      <button onClick={start}>시작</button>
      <button onClick={stop}>정지</button>
    </div>
  );
}
```

여기서 `intervalId`는 **UI에 보여줄 값이 아니고, 단지 정리(cleanup)용**이므로
굳이 `useState`로 관리하여 리렌더를 트리거할 필요가 없습니다 → `useRef`가 더 적합 ✅

### 4-3. “인스턴스 변수”처럼 쓰기 (클래스 컴포넌트의 this.* 대체)

클래스 컴포넌트 시절에는 이런 코드 많이 보셨을 겁니다:

```js
class MyComponent extends React.Component {
  instanceValue = 0;
}
```

함수 컴포넌트에서는 이걸 `useRef`로 대체합니다.

```jsx
function MyComponent() {
  const instanceValueRef = useRef(0);

  const increase = () => {
    instanceValueRef.current += 1;
    console.log("현재 instanceValue:", instanceValueRef.current);
    // UI에 영향을 주지 않는 단순 값 저장이므로 setState 불필요
  };

  return <button onClick={increase}>로그만 증가</button>;
}
```

> 🧠 요약: **“UI에 안 보여도 되지만, 렌더 사이에 계속 유지되어야 하는 값” → useRef**

---

## 5. useRef의 내부 동작 & Fiber/Hook 슬롯 관점 🔬

이전에 여쭤보셨던 것처럼, React는 각 함수 컴포넌트를 **하나의 Fiber 노드**로 관리합니다.
각 Fiber에는 **Hook들의 “슬롯 배열”**(linked list / 배열 비슷한 구조)이 있고,
`useState`, `useEffect`, `useRef` 등이 이 슬롯을 차지합니다.

대략적인 개념적 구조:

```ts
// 매우 단순화한 개념적 표현
type Hook = {
  memoizedState: any; // useState: [value, dispatch], useRef: { current: ... }
  next: Hook | null;
};

type Fiber = {
  memoizedState: Hook | null; // 첫 번째 hook
  // ...
};
```

* `useRef(initialValue)`를 호출하면

  * 현재 Fiber의 hook 슬롯 중 하나를 사용해서
  * `memoizedState`에 `{ current: 초기값 }`을 저장합니다.
* 이후 리렌더에서는

  * **같은 위치의 hook**을 찾아서 이전 `memoizedState`를 그대로 재사용
  * 그래서 **ref 객체의 identity(===)는 렌더 사이에 유지**됩니다.
* `ref.current`를 바꾸면:

  * 이건 단순히 JavaScript 객체 속성 변경이기 때문에
  * React에게 “렌더 요청”을 하지 않습니다 → 리렌더 없음

> 그래서 **Hook의 호출 순서**가 바뀌면 안 되는 이유가 여기에도 적용됩니다.
> `if` 문 안에서 `useRef`를 conditionally 호출하면 Fiber의 hook 슬롯 연결이 꼬여버립니다.

---

## 6. createRef와 useRef의 차이 ⚔️

### 6-1. createRef

```jsx
import { createRef } from "react";

function MyComponent() {
  const inputRef = createRef(); // ❌ 함수 컴포넌트에서 이렇게 쓰면 매 렌더마다 새 객체

  // ...
}
```

* `createRef()`는 **호출할 때마다 새로운 ref 객체를 생성**합니다.
* 함수 컴포넌트에서는 렌더마다 다시 실행되므로, ref가 매번 새로 만들어져 버립니다.
* 그래서 **함수 컴포넌트에서는 createRef 대신 useRef를 사용**해야 합니다.

### 6-2. useRef

```jsx
function MyComponent() {
  const inputRef = useRef(null); // ✅ 렌더 사이에도 동일한 객체 유지
  // ...
}
```

* `useRef`는 내부적으로 Fiber의 hook 슬롯에 저장되기 때문에

  * 한 번 만들어진 ref 객체를 계속 재사용합니다.

> 결론:
>
> * 클래스 컴포넌트: `createRef`
> * 함수 컴포넌트: **`useRef`**

---

## 7. forwardRef + useRef: 자식에게 DOM/메서드 넘기기 📦

부모가 자식 컴포넌트의 DOM에 직접 접근하고 싶을 수도 있습니다.

### 7-1. 단순 DOM 전달 (forwardRef)

```jsx
import { forwardRef, useRef } from "react";

const MyInput = forwardRef(function MyInput(props, ref) {
  return <input {...props} ref={ref} />;
});

function Form() {
  const inputRef = useRef(null);

  const handleClick = () => {
    if (inputRef.current) {
      inputRef.current.focus();
    }
  };

  return (
    <>
      <MyInput ref={inputRef} placeholder="forwardRef 예제" />
      <button onClick={handleClick}>포커스</button>
    </>
  );
}
```

* `forwardRef`는 부모에서 넘긴 `ref`를 자식이 다시 DOM에 붙일 수 있게 해주는 고차 컴포넌트입니다.
* 이 방식으로 **컴포넌트를 감싸는 래퍼 컴포넌트에도 ref를 전달**할 수 있습니다.

### 7-2. 명령형 API 만들기 (useImperativeHandle과 함께)

조금 더 진화된 형태:

```jsx
import { forwardRef, useImperativeHandle, useRef } from "react";

const FancyInput = forwardRef(function FancyInput(props, ref) {
  const inputRef = useRef(null);

  useImperativeHandle(ref, () => ({
    focus() {
      inputRef.current?.focus();
    },
    clear() {
      if (inputRef.current) {
        inputRef.current.value = "";
      }
    },
  }));

  return <input ref={inputRef} {...props} />;
});

function Parent() {
  const fancyRef = useRef(null);

  return (
    <>
      <FancyInput ref={fancyRef} />
      <button onClick={() => fancyRef.current?.focus()}>포커스</button>
      <button onClick={() => fancyRef.current?.clear()}>지우기</button>
    </>
  );
}
```

* 부모는 이제 `fancyRef.current.focus()` / `clear()` 같은 **명령형 메서드**를 사용할 수 있습니다.
* 이때도 내부적으로는 결국 `useRef`로 DOM을 잡아두고 있습니다.

---

## 8. useRef vs useState vs useMemo/useCallback 비교 🧩

| 목적                 | Hook             | 값 변경 시 리렌더?  | 주 사용처                     |
| ------------------ | ------------- | ------------ | ------------------------- |
| UI에 직접 반영되는 상태     | `useState`    | ✅ 예          | 카운터, 입력값 등                |
| 값은 기억하되, 리렌더 필요 없음 | `useRef`      | ❌ 아니요        | 타이머 ID, 이전 값, 플래그, DOM 참조 |
| 계산 비용이 큰 값 캐싱      | `useMemo`     | (의존성 변경 시) ✅ | 무거운 계산 결과                 |
| 함수를 메모이제이션         | `useCallback` | (의존성 변경 시) ✅ | 콜백 재생성 방지                 |

> 간단히:
> **“화면에 보이는 값”은 `useState`,
> “화면에는 안 보이지만 기억해야 하는 값”은 `useRef`.**

---

## 9. useRef 사용할 때 자주 하는 실수들 ⚠️

### 9-1. `ref.current` 바꾸면 리렌더 된다고 착각하기

```jsx
const countRef = useRef(0);

const handleClick = () => {
  countRef.current += 1;
  console.log(countRef.current);
  // 여기서 UI에 countRef.current를 보여주고 있다면, 화면은 그대로입니다.
};
```

* 화면에 보여주고 싶다면 반드시 `useState`와 함께 사용해야 합니다.

### 9-2. 렌더 중에 `ref.current` 값을 믿어버리기

```jsx
function Component() {
  const divRef = useRef(null);

  console.log(divRef.current); // 여기서는 보통 null (렌더 중)

  return <div ref={divRef} />;
}
```

* 렌더 중에는 아직 DOM이 연결되지 않았습니다.
* **DOM을 사용한 로직은 반드시 `useEffect` / `useLayoutEffect`에서 수행**해야 합니다.

### 9-3. Strict Mode에서 “두 번 호출” 관련 혼란

React 18의 StrictMode에서는 개발 모드에서

* 일부 컴포넌트가 **의도적으로 두 번 마운트-언마운트** 되었다가 다시 마운트됩니다.
* 이때 `useEffect` / `useRef` 초기화/정리 로직이 여러 번 호출될 수 있어 혼란이 있을 수 있습니다.
* 따라서 타이머나 외부 라이브러리 초기화 시

  * 정리(cleanup) 로직을 반드시 정확하게 구현해야 합니다.

### 9-4. Controlled vs Uncontrolled 혼합

`ref`로 DOM의 value를 직접 조작하면서 동시에 `value` props로 제어하려 하면 혼란이 생깁니다.

```jsx
// 안 좋은 패턴 예시
<input
  ref={inputRef}
  value={text}
  onChange={(e) => {
    setText(e.target.value);
    inputRef.current.value = "뭔가 덮어쓰기"; // 이런 건 보통 피하는 게 좋습니다.
/>
```

* “상태는 React가 관리한다” vs “DOM을 직접 관리한다”를 섞으면 디버깅이 힘들어집니다.
* 일반적으로:

  * 폼을 **React 상태로 제어**할지 (controlled)
  * **DOM에 맡길지(uncontrolled + ref로 읽기)** 를 처음부터 전략적으로 선택하는 것이 좋습니다.

---

## 10. callback ref와 useRef의 차이 🔄

`ref`에는 단순히 객체 말고 **함수(콜백)**도 넣을 수 있습니다.

```jsx
function Component() {
  const handleRef = (node) => {
    if (node) {
      // mount 시
      console.log("DOM 연결:", node);
    } else {
      // unmount 시
      console.log("DOM 해제");
    }
  };

  return <div ref={handleRef} />;
}
```

* callback ref는 **DOM이 바뀔 때마다 함수가 호출**됩니다.
* `useRef`는 단순히 객체에 넣어두는 반면,

  * callback ref는 훨씬 세밀한 제어가 가능합니다.
* 다만, 대부분의 경우 `useRef`로 충분하고,

  * 아주 특수한 상황(예: ref 변경 이벤트를 바로 감지해야 하는 경우)에 callback ref를 씁니다.

---

## 11. 정리: useRef를 쓸 때 떠올려야 할 체크리스트 ✅

1. **이 값이 바뀌어도 리렌더가 필요 없는가?**

   * 예: 타이머 ID, 이전 값, 외부 라이브러리 인스턴스 → `useRef`
2. **DOM 요소에 포커스/스크롤/측정 등 직접 접근해야 하는가?**

   * 예: `input`, `video`, `div` 등의 DOM → `useRef + ref 속성 + useEffect`
3. **부모 컴포넌트에 명령형 API를 주고 싶은가?**

   * 예: `focus()`, `open()`, `reset()` → `forwardRef + useRef (+ useImperativeHandle)`
4. **UI에 보여줄 값인가?**

   * 그러면 `useState` 또는 그 외 상태 관리가 우선입니다.

---


