# React `useContext`

“props 지옥(prop drilling)”에서 탈출하는 법

---

## 0. 한 줄 요약

> `useContext`는 **“깊게 중첩된 컴포넌트들 사이에서 공통 데이터를 쉽게 공유하기 위해”** 만든 리액트 훅입니다.
> `createContext`로 “공통 저장소”를 만들고, `Provider`로 값을 공급하고, 자식 컴포넌트에서 `useContext`로 값을 바로 꺼내 쓰는 패턴입니다.

---

## 1. 왜 useContext가 필요한가? – Prop Drilling 문제

### 1-1. 전형적인 문제 상황

전역 테마 설정(다크 모드 / 라이트 모드), 로그인 사용자 정보, 언어 설정 같은 값은 **여러 컴포넌트에서 공통으로 사용**됩니다.

```tsx
// App → Layout → Header → UserMenu → UserAvatar
// 이런 구조일 때, user 정보를 App에서 최하단까지 계속 내려줘야 하는 상황
<App user={user}>
  <Layout user={user}>
    <Header user={user}>
      <UserMenu user={user}>
        <UserAvatar user={user} />
      </UserMenu>
    </Header>
  </Layout>
</App>
```

* 모든 중간 컴포넌트들이 **user를 직접 쓰지도 않는데** `props`로 계속 받아서 아래로 넘겨야 합니다.
* 구조가 복잡해질수록 **“props가 연결된 배관”** 을 유지하는 게 고통스럽습니다.
* 컴포넌트 리팩터링(위치 이동, 분리, 합치기)을 할 때마다 props 체인을 다시 손봐야 해서 실수도 많아집니다.

이게 흔히 말하는 **prop drilling(프로퍼티 뚫기 / 관통하기)** 문제입니다.

### 1-2. 이 문제를 해결하는 발상

> “중간 컴포넌트들을 거치지 말고,
> **공통 저장소(Context)** 에 값을 올려두고,
> **필요한 컴포넌트만 바로 연결해서 꺼내 쓰면 안 될까?”**

→ 이 아이디어를 구현한 것이 **React Context API**이고,
함수형 컴포넌트에서 이 Context를 편하게 사용하는 훅이 **`useContext`**입니다.

---

## 2. Context API 개념 정리

Context는 세 가지 축으로 이해하시면 좋습니다.

1. **Context 객체 만들기** – `createContext(defaultValue)`
2. **값을 공급하는 Provider** – `<MyContext value={...}>` *(React 19. 이전 문법은 `<MyContext.Provider value={...}>`)*
3. **값을 소비하는 소비자(consumer)** – `useContext(MyContext)` 또는 React 19의 `use(MyContext)`

간단 비유:

* `createContext` → **“주제(Topic)를 만드는 것”** (예: ThemeContext, AuthContext)
* `Provider` → **“이 주제의 실제 내용(value)을 방송하는 역할”**
* `useContext` → **“특정 주제 방송을 수신해서 value를 받는 라디오”**

---

## 3. useContext 기본 사용법

### 3-1. 1단계: Context 생성하기

```tsx
// theme-context.tsx (또는 themeContext.js)
import { createContext } from 'react'

// Context 타입 정의 (TS가 아니면 생략 가능)
export type Theme = 'light' | 'dark'

// defaultValue는 Provider가 없을 때만 사용
export const ThemeContext = createContext<Theme>('light')
```

* `createContext(defaultValue)`는 **Context 객체**를 만들고,
* 이 객체는 나중에

  * `<ThemeContext>`로 값 공급
  * `useContext(ThemeContext)`로 값 소비에 사용됩니다.
* `defaultValue`는 **주로 테스트용/비상용**이라 생각하시면 됩니다. 실제 앱에서는 거의 항상 Provider로 값을 주입합니다.

### 3-2. 2단계: Provider로 값 공급하기

```tsx
// App.tsx
import { useState } from 'react'
import { ThemeContext, Theme } from './theme-context'
import Page from './Page'

export default function App() {
  const [theme, setTheme] = useState<Theme>('light')

  const toggleTheme = () => {
    setTheme(prev => (prev === 'light' ? 'dark' : 'light'))
  }

  return (
    // React 19: Context 객체 자체가 Provider입니다. 여기에서 theme 값을 "방송"합니다.
    <ThemeContext value={theme}>
      <Page onToggleTheme={toggleTheme} />
    </ThemeContext>
  )
}
```

> React 18까지는 `<ThemeContext.Provider value={theme}>` 처럼 `.Provider`를 붙여야 했습니다.
> 지금도 동작하지만 **향후 릴리스에서 deprecated 예정**이니, 옛 코드를 읽을 때만 알아두시면 됩니다.
> 참고로 render prop 방식인 `<ThemeContext.Consumer>` 역시 레거시입니다.

* `value`에 전달한 값이 **트리 아래 모든 자식 컴포넌트에서 공유**됩니다.
* 이 트리 안 어디에서든 `useContext(ThemeContext)` 하면 `theme` 값을 바로 얻습니다.
* React 19에서는 `use(ThemeContext)`로도 읽을 수 있습니다. `useContext`와 달리 **조건문·반복문 안에서도 호출 가능**하지만,
  둘 다 “컴포넌트나 커스텀 훅의 본문 안”에서만 호출할 수 있다는 점은 같습니다. (`use`는 `try` 블록 안 호출도 금지)

> 중요: Context는 **Provider 기준으로 “아래 Subtree”에만 적용**됩니다.
> Provider 밖의 컴포넌트에서는 이 값을 받을 수 없습니다.

### 3-3. 3단계: 자식에서 useContext로 값 소비하기

```tsx
// ThemeButton.tsx
import { useContext } from 'react'
import { ThemeContext } from './theme-context'

export default function ThemeButton() {
  const theme = useContext(ThemeContext)

  return (
    <button
      style={{
        backgroundColor: theme === 'dark' ? '#333' : '#eee',
        color: theme === 'dark' ? '#fff' : '#000',
      }}
    >
      Current theme: {theme}
    </button>
  )
}
```

요약하면:

1. **Context 생성**: `const ThemeContext = createContext('light')`
2. **Provider로 공유 값 설정**: `<ThemeContext value={theme}>`
3. **useContext로 값 읽기**: `const theme = useContext(ThemeContext)`

---

## 4. 조금 더 현실적인 예제: AuthContext

테마보다 더 실전적인 예로, **로그인 정보**를 Context로 관리해보겠습니다.

### 4-1. AuthContext 정의

```tsx
// AuthContext.tsx
import { createContext } from 'react'

export type User = {
  id: number
  name: string
} | null

export type AuthContextValue = {
  user: User
  login: (name: string) => void
  logout: () => void
}

export const AuthContext = createContext<AuthContextValue | undefined>(undefined)
```

### 4-2. Provider 컴포넌트 분리 (추천 패턴)

```tsx
// AuthProvider.tsx
import { ReactNode, useState, useCallback } from 'react'
import { AuthContext, User } from './AuthContext'

type Props = {
  children: ReactNode
}

export function AuthProvider({ children }: Props) {
  const [user, setUser] = useState<User>(null)

  const login = useCallback((name: string) => {
    setUser({ id: Date.now(), name })
  }, [])

  const logout = useCallback(() => {
    setUser(null)
  }, [])

  // ⚠️ 여기서 { user, login, logout } 을 그대로 쓰면 AuthProvider가 리렌더될 때마다
  //    새 객체가 만들어져서 모든 소비자가 리렌더됩니다. 반드시 useMemo로 고정하세요.
  const value = useMemo(
    () => ({ user, login, logout }),
    [user, login, logout]   // login/logout은 useCallback 덕분에 참조가 고정됨
  )

  // React 19: <AuthContext.Provider> 대신 Context 객체를 그대로 렌더링
  return <AuthContext value={value}>{children}</AuthContext>
}
```

> 이런 식으로 Context + Provider를 세트로 묶어두면, **App 루트에서 깔끔하게 재사용**할 수 있습니다.
> `useMemo`의 import(`import { ReactNode, useState, useCallback, useMemo } from 'react'`)도 잊지 마세요.

### 4-3. App 루트에서 사용

```tsx
// App.tsx
import { AuthProvider } from './AuthProvider'
import Header from './Header'
import Dashboard from './Dashboard'

export default function App() {
  return (
    <AuthProvider>
      <Header />
      <Dashboard />
    </AuthProvider>
  )
}
```

### 4-4. 아무 자식이나 useContext로 로그인 정보 쓰기

```tsx
// Header.tsx
import { useContext } from 'react'
import { AuthContext } from './AuthContext'

export default function Header() {
  const auth = useContext(AuthContext)

  if (!auth) throw new Error('AuthContext is not provided')

  const { user, logout } = auth

  return (
    <header>
      {user ? (
        <>
          <span>안녕하세요, {user.name}님 👋</span>
          <button onClick={logout}>로그아웃</button>
        </>
      ) : (
        <span>로그인이 필요합니다</span>
      )}
    </header>
  )
}
```

```tsx
// LoginForm.tsx
import { useContext, useState } from 'react'
import { AuthContext } from './AuthContext'

export default function LoginForm() {
  const auth = useContext(AuthContext)
  if (!auth) throw new Error('AuthContext is not provided')

  const { login } = auth
  const [name, setName] = useState('')

  const handleSubmit = (e: React.FormEvent) => {
    e.preventDefault()
    login(name)
  }

  return (
    <form onSubmit={handleSubmit}>
      <input value={name} onChange={e => setName(e.target.value)} placeholder="이름" />
      <button type="submit">로그인</button>
    </form>
  )
}
```

이제 `Header`, `LoginForm`, `Dashboard` 등 어느 컴포넌트에서든 **props 전달 없이** 로그인 상태를 쉽게 공유할 수 있습니다.

---

## 5. useContext 내부 동작 이해하기 (조금 깊이)

사용자 관점에서는 `useContext`가 단순히 “전역 값 가져오기”처럼 보이지만, 내부적으로는 이런 일이 벌어집니다.

1. `createContext`를 호출하면 React는 **“특정 Context를 식별하는 객체”** 를 하나 만듭니다.
2. JSX를 렌더링하면서 `<MyContext value={...}>`를 만나면,

   * 해당 Provider가 감싸고 있는 모든 후손 노드에게 “이 Context는 이 value를 사용한다”는 정보를 연결합니다.
3. 어떤 컴포넌트에서 `useContext(MyContext)`를 호출하면,

   * React는 **현재 컴포넌트가 속한 트리 상에서 가장 가까운 Provider**를 찾고,
   * 그 Provider의 `value`를 리턴합니다.
4. 나중에 Provider의 `value`가 바뀌면,

   * React는 이 Context를 구독한 모든 소비자(useContext 사용하는 컴포넌트)를 다시 렌더링합니다.

> 핵심 포인트:
>
> * Context는 **“객체 그 자체”가 ID** 역할을 합니다.
>   같은 파일에서 import한 `ThemeContext` 객체를 사용해야 같은 Context로 인식합니다.
> * Provider 하나가 value를 바꾸면, 해당 Context를 구독하는 **모든 소비자가 리렌더링**됩니다.

---

## 6. 커스텀 훅으로 감싸기 – 실무에서의 패턴

실전 코드에서는 `useContext(AuthContext)`를 여기저기 직접 쓰기보다는, **커스텀 훅으로 감싸는 패턴**을 많이 사용합니다.

```tsx
// useAuth.ts
import { useContext } from 'react'
import { AuthContext } from './AuthContext'

export function useAuth() {
  const ctx = useContext(AuthContext)
  if (!ctx) {
    throw new Error('useAuth must be used within <AuthProvider>')
  }
  return ctx
}
```

사용 측:

```tsx
// Header.tsx
import { useAuth } from './useAuth'

export default function Header() {
  const { user, logout } = useAuth()

  return (
    <header>
      {user ? (
        <>
          <span>{user.name}님 환영합니다 🎉</span>
          <button onClick={logout}>로그아웃</button>
        </>
      ) : (
        <span>로그인 해주세요</span>
      )}
    </header>
  )
}
```

장점:

* `AuthContext` import를 여기저기 하지 않아도 됩니다.
* Provider 누락 시 에러 메시지를 **한 곳에서** 통제할 수 있습니다.
* 나중에 내부 구현을 바꿔도 (`Redux`로 교체 등) 외부 컴포넌트 코드 변경을 최소화할 수 있습니다.

---

## 7. 성능 관점에서의 useContext

### 7-1. Provider value가 바뀌면 모두 리렌더링

```tsx
<AuthContext value={{ user, login, logout }}>
  <Header />
  <Sidebar />
  <Main />
</AuthContext>
```

위 코드에서 `user`가 바뀌거나, `login`/`logout` 함수 참조가 바뀌면,
**이 Context를 구독하는 모든 컴포넌트가 리렌더링**됩니다.

그래서 보통은:

1. **value 객체를 그대로 inline으로 만들지 말고**

   * `useMemo`, `useCallback`, `useReducer` 등을 활용해서 **참조 안정성**을 확보하거나
2. **Context를 분리**해서 자주 바뀌는 값과 덜 바뀌는 값을 나누기도 합니다.

예시:

```tsx
const value = useMemo(
  () => ({
    user,
    login,
    logout,
  }),
  [user, login, logout]
)

return <AuthContext value={value}>{children}</AuthContext>
```

> 단, `login`/`logout`도 `useCallback`으로 감싸서 의존성 배열이 안정적으로 유지되도록 하는 것이 좋습니다.

### 7-2. `React.memo`로는 못 막습니다

가장 많이 하는 착각입니다.

```tsx
const Sidebar = React.memo(function Sidebar() {
  const { user } = useAuth()   // 👈 컨텍스트 구독
  return <aside>{user?.name}</aside>
})
```

* `memo`는 **props를 얕게 비교**해서 리렌더를 건너뛰는 도구입니다.
* 그런데 컨텍스트 구독은 props 경로가 아니라 **Fiber에 직접 등록된 의존성**이라, `memo`의 비교 대상이 아닙니다.
* 결론: **컨텍스트를 읽는 컴포넌트는 memo로 감싸도 value가 바뀌면 무조건 리렌더**됩니다.

그래서 실제 최적화 수단은 두 가지뿐입니다.

1. `value` 참조를 안정화해서 **애초에 value가 안 바뀌게** 하기 (`useMemo` / `useCallback`)
2. **구독 범위를 줄이기** — Context를 쪼개거나, 컨텍스트를 읽는 얇은 래퍼를 만들고 무거운 트리는 `children`으로 통과시키기

```tsx
function AuthProvider({ children }: Props) {
  // children은 이 컴포넌트가 만든 엘리먼트가 아니라 "위에서 받은 것"이므로,
  // user가 바뀌어도 children 엘리먼트 자체는 재생성되지 않습니다.
  return <AuthContext value={value}>{children}</AuthContext>
}
```

### 7-3. Context 분리 전략

사용자 정보와 UI 상태(예: 사이드바 열림 여부)를 **같은 Context에 넣으면**, 사소한 UI 상태 변경에도 로그인 관련 컴포넌트가 함께 리렌더링될 수 있습니다.

그래서:

* `AuthContext` – 로그인/사용자 정보
* `UIContext` – UI 관련 상태
* `ThemeContext` – 테마

이렇게 나누는 것이 성능 + 구조 면에서 더 좋습니다.

> 단, Context를 아무리 쪼개도 **“값의 일부만 구독”은 불가능**합니다.
> React 19에도 공식 `useContextSelector`는 없습니다. 자주 바뀌고 소비자가 많은 상태라면
> Context 대신 Zustand / Redux 같은 selector 기반 store를 쓰는 게 맞습니다.

---

## 8. useContext vs Redux / useReducer + Context

### 8-1. useContext의 역할은 “전달 통로”일 뿐

중요한 포인트:

> `useContext`는 **상태 관리 도구가 아니라, “값을 전달하는 메커니즘”** 입니다.

* 상태를 **어디서** 관리할지는 `useState`, `useReducer` 등으로 결정합니다.
* 그 상태를 **어디까지** 공유할지는 `Context + useContext`로 결정합니다.

실무에서 구조를 잡을 때는 보통 이렇게 씁니다:

```tsx
// AuthProvider 내부
const [state, dispatch] = useReducer(authReducer, initialAuthState)

const value = useMemo(
  () => ({ state, dispatch }),
  [state] // dispatch는 안정적
)

return <AuthContext value={value}>{children}</AuthContext>
```

그리고 소비자는:

```tsx
const { state, dispatch } = useAuth()
```

이 패턴은 **간단한 규모**에서는 Redux 없이도 꽤 강력합니다.

### 8-2. Redux와 비교

* Redux

  * 전역 Store, DevTools, 미들웨어, 타임 트래블, 구조화된 액션/리듀서 등 **큰 스케일용 인프라** 제공
  * “애플리케이션 전체 상태 트리”를 하나로 관리하는 철학
* Context + useReducer

  * 특정 도메인(예: Auth, Cart)에 대해 **가벼운 Store**를 직접 구현
  * 도메인별로 Context를 나누어 관리

> 교육/국비 과정에서는
>
> * “작은 프로젝트” → `Context + useReducer`
> * “현업에 가까운 중~대형 프로젝트” → Redux Toolkit
> 이런 식으로 계단식으로 설명하시면 좋습니다.

---

## 9. 흔한 실수 정리

1. **Provider를 빼먹은 경우**

   ```tsx
   const theme = useContext(ThemeContext) // 항상 'light'만 나옴
   ```

   * Provider 없이 `useContext`를 호출하면 `createContext`의 `defaultValue`만 받습니다.
   * 개발 중에는 **커스텀 훅에서 에러 던지기 패턴**으로 조기에 잡는 게 좋습니다.

2. **Context 객체를 잘못 import한 경우**

   ```tsx
   // 다른 파일에서 또 createContext()를 호출해버린 경우
   export const ThemeContext = createContext('light')
   ```

   * A파일에서 만든 `ThemeContext`와 B파일에서 만든 `ThemeContext`는 **서로 다른 Context**입니다.
   * Provider는 A의 Context, 소비자는 B의 Context를 쓰면, **값이 절대 전달되지 않습니다.**

3. **value에 매번 새로운 객체/함수를 만들어 넣는 경우**

   ```tsx
   <AuthContext value={{ user, login: () => {...} }}>
   ```

   * 렌더링마다 `value`의 참조가 바뀌어, 하위 컴포넌트가 **불필요하게 자주 리렌더링**됩니다.
   * `useCallback`, `useMemo`를 적절히 사용해서 참조를 안정시키는 것이 좋습니다.

4. **모든 상태를 Context로 전역화하는 경우**

   * Context는 어디서나 쓸 수 있다고 해서, 모든 state를 Context로 올리면

     * 구조가 더 복잡해지고
     * 리렌더링 범위도 커지고
     * 로컬 상태(Local State)의 장점(캡슐화)을 잃게 됩니다.
   * 원칙:

     > “**정말 여러 곳에서 필요**한 데이터만 Context로 올린다.”

5. **`createContext`의 기본값을 “Provider가 값을 안 줄 때의 fallback”으로 오해하는 경우**

   ```tsx
   const ThemeContext = createContext<Theme | undefined>('light')

   // 🚨 기대: 'light'  /  실제: undefined
   <ThemeContext value={undefined}>
     <Child />
   </ThemeContext>
   ```

   * `defaultValue`는 **트리에 Provider가 하나도 없을 때만** 사용됩니다.
   * Provider가 있으면 그 `value`가 무엇이든(`undefined`, `null` 포함) **그대로** 내려갑니다.
   * 그래서 “Provider를 깜빡했는지”를 잡으려면 기본값을 `undefined`/`null`로 두고 **커스텀 훅에서 throw**하는 편이 안전합니다.

6. **`React.memo`로 컨텍스트 리렌더를 막을 수 있다고 오해하는 경우**

   * `memo`는 **props만 비교**합니다. 컨텍스트 구독은 props가 아닙니다.
   * `useContext`(또는 `use`)를 호출하는 컴포넌트는 **memo로 감싸도** 컨텍스트 값이 바뀌면 반드시 리렌더됩니다.
   * 막으려면 “구독 자체를 옮겨야” 합니다. 컨텍스트를 읽는 얇은 컴포넌트를 따로 두고, 무거운 부분은 `children`으로 받아 넘기세요.

---

## 10. 마무리 정리

### useContext를 이해하는 키워드

* **Prop drilling 해소**: 중첩된 컴포넌트 사이에서 공통 데이터를 쉽게 공유
* **Context 생성**: `const MyContext = createContext(defaultValue)`
* **값 공급**: `<MyContext value={...}>` (React 19. 구 문법 `<MyContext.Provider>`는 향후 deprecated)
* **값 소비**: `const value = useContext(MyContext)` / 조건부로 읽어야 하면 `use(MyContext)`
* **Provider 기준 하위 트리**에만 영향을 미침
* 상태 관리 도구가 아니라, **“전달 통로”**

### 실무 팁 요약

* Context + Provider + 커스텀 훅(`useAuth`, `useTheme`…) 세트로 설계하기
* 자주 바뀌는 값은 Context 분리 or `useReducer`+Context 조합 고려
* value 객체는 `useMemo`, 함수는 `useCallback`으로 참조 안정화
* 글로벌 남용 주의 – “여러 곳에서 필요할 때만 Context로 올리기”

