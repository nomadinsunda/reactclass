
# 📌 `useMemo`

---

## 🧠 1. `useMemo`란?

```ts
const memoizedValue = useMemo(() => computeExpensiveValue(a, b), [a, b]);
```

`useMemo`는 React에서 **값의 메모이제이션(Memoization)** 을 수행하는 Hook입니다.
즉, **"이전 계산 값을 기억해서, 불필요한 재계산을 방지"** 하는 목적입니다.

> "함수를 실행하지 않고, 이전 값을 재사용할 수 있을까?" → 이때 쓰는 것이 `useMemo`

---

## ⚙️ 2. 내부 동작 원리

React는 매 렌더링마다 **컴포넌트 함수를 다시 실행**합니다.
이 과정에서 계산 비용이 큰 함수들도 매번 실행되면 비효율적입니다.

`useMemo`는 다음과 같은 캐시 메커니즘을 사용합니다:

* `deps` 배열을 기억
* 렌더링 시 `deps`가 **바뀌지 않았다면**, 이전 결과를 **재사용**
* `deps`가 바뀌었다면, **함수 다시 실행 → 새로운 값 캐싱**

이렇게 동작하여 **불필요한 계산을 회피**합니다.

---

## ⚡ 3. 주요 사용 목적

| 목적                     | 설명                                    |
| ---------------------- | ------------------------------------- |
| ✅ 무거운 계산 피하기           | CPU-intensive 연산 메모이제이션               |
| ✅ 렌더링 최적화              | 자식에게 전달하는 **객체, 배열, 함수**의 identity 유지 |
| ✅ Virtual DOM diff 최소화 | props가 매번 바뀌는 것처럼 보이지 않게 하기 위함        |

---

## 🧮 4. 사용 예시: 비용이 큰 계산

```jsx
const App = ({ count }) => {
  const expensiveValue = useMemo(() => {
    let sum = 0;
    for (let i = 0; i < 1e9; i++) {
      sum += i;
    }
    return sum + count;
  }, [count]);

  return <div>값: {expensiveValue}</div>;
};
```

👉 `count`가 바뀌지 않으면 이 어마어마한 반복문은 실행되지 않음
👉 성능 최적화 효과 극대화

---

## 📦 5. 참조값 안정화 (객체/배열 identity 보존)

```jsx
const App = () => {
  const [filter, setFilter] = useState('');

  const options = useMemo(() => {
    return { pageSize: 10, filter };
  }, [filter]);

  return <ChildComponent options={options} />;
};
```

📌 `useMemo` 없이 `options`는 렌더링마다 새 객체 → `ChildComponent`도 불필요하게 리렌더
✅ `useMemo` 사용 시 객체 참조 유지 → 불필요한 리렌더 방지

---

## 🧩 6. `React.memo`와의 시너지

```jsx
const List = React.memo(({ items }) => {
  console.log('List 리렌더링!');
  return items.map(item => <div key={item.id}>{item.text}</div>);
});

const App = () => {
  const [count, setCount] = useState(0);

  const items = useMemo(() => [{ id: 1, text: 'Item1' }], []);

  return (
    <>
      <List items={items} />
      <button onClick={() => setCount(c => c + 1)}>증가</button>
    </>
  );
};
```

👉 `useMemo` 없이 `items`는 매번 새로운 배열 → `React.memo` 무의미
✅ `useMemo` + `React.memo` = **최적화 콤보**

---

## ⚠️ 7. 주의사항 & 오해

### ❌ 1. *무조건* 사용하는 것은 오히려 역효과

`useMemo`도 **계산 & 메모리 비용**이 있음.
**계산이 복잡하지 않다면 그냥 계산하는 게 낫다.**

> "성능 문제 없는데 `useMemo` 남용하면 성능 저하도 발생할 수 있음."

### ❌ 2. 의존성 배열을 잘못 설정하면 버그 유발

```jsx
const result = useMemo(() => doSomething(obj), []);
```

* 위 코드에서 `obj`가 외부 상태인데 의존성에 넣지 않음 → stale value 문제 발생

✅ 항상 **의존성 배열 정확히 설정**해야 안전

---

## 🔍 8. 비교: `useMemo` vs `useCallback`

| Hook          | 반환값    | 사용 목적                      |
| ------------- | ------ | -------------------------- |
| `useMemo`     | **값**  | 계산된 값을 기억                  |
| `useCallback` | **함수** | 함수 정의를 기억 (함수 identity 유지) |

```jsx
const memoizedCallback = useCallback(() => { doSomething(a, b); }, [a, b]);
const memoizedValue = useMemo(() => compute(a, b), [a, b]);
```

---

## 🧪 9. 메모이제이션 전략 비교

| 전략             | 사용 위치             | 목적                 |
| -------------- | ----------------- | ------------------ |
| `useMemo`      | React 내부 값 메모이제이션 | 계산 성능 향상           |
| `memo`         | 컴포넌트 메모이제이션       | 리렌더링 방지            |
| `useCallback`  | 함수 메모이제이션         | 불필요한 함수 재생성 방지     |
| `React.useRef` | 영구 참조값 저장         | 컴포넌트 사이클과 무관한 값 저장 |

---

## ✅ 10. 정리

| 항목     | 설명                          |
| ------ | --------------------------- |
| 역할     | 계산 결과를 캐싱하여 불필요한 재계산 방지     |
| 언제 사용? | 비용이 큰 계산 / 객체 참조 유지가 필요한 경우 |
| 의존성 배열 | 매우 중요. 잘못되면 버그 발생 가능        |
| 주의사항   | 남용 시 오히려 성능 저하 가능           |

---

## 📘 추가 실전 예제가 필요하면?

* `useMemo`로 정렬된 리스트 캐시
* `useMemo` + `filter`/`map` 조합
* Redux나 Context 상태 기반 계산 최적화



# 📌 `useMemo`를 **정확하고 효과적으로 사용하는 예제들**

---

## ✅ 1. `useMemo`로 정렬된 리스트 캐시

### 📌 문제

```jsx
const sortedUsers = users.sort((a, b) => a.name.localeCompare(b.name));
```

> `users`가 바뀌지 않아도 정렬 함수는 **렌더링마다 호출**됨 → 불필요한 계산

---

### ✅ 개선: `useMemo` 적용

```jsx
const sortedUsers = useMemo(() => {
  return [...users].sort((a, b) => a.name.localeCompare(b.name));
}, [users]);
```

✅ `users`가 바뀌지 않으면 `sortedUsers`도 **재계산되지 않음**
✅ `users`를 직접 정렬하지 않고 복사 (`...users`) → 원본 불변성 보장

---

## ✅ 2. `useMemo` + `filter`/`map` 조합

### 📌 시나리오

```jsx
const filtered = users.filter(u => u.country === filter);
const names = filtered.map(u => u.name);
```

> 필터링 + 매핑이 매 렌더링마다 호출되면 성능 저하 가능
> 특히 리스트 크기가 크면 필터와 map 모두 비용이 큼

---

### ✅ 개선: 단계별 `useMemo` 최적화

```jsx
const filteredUsers = useMemo(() => {
  return users.filter(u => u.country === filter);
}, [users, filter]);

const userNames = useMemo(() => {
  return filteredUsers.map(u => u.name);
}, [filteredUsers]);
```

✅ 두 연산 모두 메모이제이션
✅ 복합 연산을 쪼개어 관리하면 **의존성 추적 및 디버깅이 쉬움**

---

### ✳️ 통합형 예시

```jsx
const visibleNames = useMemo(() => {
  return users
    .filter(u => u.country === filter)
    .map(u => u.name);
}, [users, filter]);
```

* 한 줄 처리도 가능하지만, **debug 관점에서 단계 분리**가 더 안전한 경우도 많음

---

## ✅ 3. Redux 또는 Context 상태 기반 계산 최적화

### 📌 시나리오

```jsx
const items = useSelector(state => state.cart.items);
const totalPrice = items.reduce((sum, item) => sum + item.price * item.qty, 0);
```

> `items`가 안 바뀌었는데도 `reduce`는 매번 호출됨
> → 메모이제이션 필요

---

### ✅ 개선: `useMemo`로 계산 비용 최소화

```jsx
const totalPrice = useMemo(() => {
  return items.reduce((sum, item) => sum + item.price * item.qty, 0);
}, [items]);
```

---

### 📘 `Context` 기반 예시

```jsx
const { todos } = useContext(TodoContext);

const completedCount = useMemo(() => {
  return todos.filter(t => t.completed).length;
}, [todos]);
```

✅ `todos`가 바뀔 때만 계산됨
✅ 컨텍스트나 셀렉터 값이 **immutable하게 관리될 경우** 매우 효과적

---

## ⚠️ 주의: `useMemo` 오남용 방지 팁

* 연산 비용이 **거의 없는 계산**은 굳이 `useMemo` 사용하지 않기
* 객체/배열 비교는 참조기반이므로, 의존성 배열을 **정확히 명시**
* `Redux selector`는 자체적으로 memoization (`reselect`)이 가능함 → 이중 적용 주의

---

## 🧠 요약

| 패턴                      | 설명                                           |
| ----------------------- | -------------------------------------------- |
| `sort`, `filter`, `map` | 연산량 많거나 반복 호출되는 경우 `useMemo` 적용              |
| Redux state 계산          | `useSelector()` 결과에 대해 계산이 있을 경우 메모이제이션      |
| Context value 계산        | `.filter`, `.reduce` 등 복잡한 파생값은 `useMemo` 처리 |
| 배열/객체 identity 유지       | 자식 컴포넌트의 불필요한 리렌더 방지                         |



