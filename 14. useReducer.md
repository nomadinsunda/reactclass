# 🧠 Redux 

---

## ✅ 1. Redux란?

> **Redux**는 JavaScript 앱의 상태를 **예측 가능하고 중앙 집중적으로 관리**할 수 있게 해주는 **State 관리 라이브러리**입니다.

> “모든 **state** 를 하나의 단일 저장소(**store**)에서 관리하며, state 변경은 오직 액션(action)을 통해서만 일어난다.”

React와는 독립적이며, **Vanilla JS**, **Vue**, **Angular**, **React Native**에서도 사용 가능하지만, 주로 **React와 함께** 사용됩니다.

---

## 🎯 Redux를 왜 사용하는가?

### 리액트 앱이 커질수록 생기는 문제:

| 문제             | 설명                                  |
| -------------- | ----------------------------------- |
| state가 여기저기 흩어짐   | 여러 컴포넌트에서 `useState`를 사용해 state를 나눠 관리 |
| props drilling | 데이터를 깊은 자식 컴포넌트까지 계속 전달해야 함         |
| state 일관성 어려움     | 서로 다른 컴포넌트가 동일한 state를 다르게 표현하거나 수정    |

### 👉 Redux는 이를 **단일 저장소(Store)** 로 해결합니다.

---

## 🧩 2. 핵심 개념 구조

### Redux의 핵심은 아래 3가지입니다:

```
Action → Reducer → Store → View
```

| 요소          | 설명                                                          |
| ----------- | ----------------------------------------------------------- |
| **Action**  | 어떤 일이 발생했는지를 설명하는 **객체** (`{ type: 'INCREMENT' }`)          |
| **Reducer** | 현재 state와 action을 받아 **새로운 state를 반환**하는 **순수 함수**                    |
| **Store**   | 앱의 전체 state를 보유하며, **`dispatch`**, **`subscribe`**, **`getState`** 메서드를 가짐 |

---

## 🔁 3. Redux 상태 흐름 요약

1. UI에서 **사용자 이벤트 발생**
2. 해당 이벤트가 **Action 객체로 표현**
3. Action은 `dispatch(action)`을 통해 **Store에 전달**
4. Store는 내부의 `reducer`를 호출 → 새로운 state 계산
5. state가 바뀌면 → UI가 **자동으로 다시 그려짐**

---

## 🧠 4. 코드로 보는 기본 예시

```js
// 1. 초기 상태 정의
const initialState = { count: 0 };

// 2. 리듀서 함수
function counterReducer(state = initialState, action) {
  switch (action.type) {
    case 'INCREMENT':
      return { count: state.count + 1 };
    case 'DECREMENT':
      return { count: state.count - 1 };
    default:
      return state;
  }
}

// 3. Store 생성
const store = Redux.createStore(counterReducer);

// 4. Action 보내기
store.dispatch({ type: 'INCREMENT' });
console.log(store.getState()); // { count: 1 }
```

---

## 🧑‍🔧 5. React에서 Redux 사용하기

> `react-redux`라는 공식 바인딩 라이브러리를 사용합니다.

```bash
npm install redux react-redux
```

### 기본 사용 흐름:

1. `store` 생성
2. `<Provider store={store}>`로 앱 감싸기
3. `useSelector()`로 state 구독
4. `useDispatch()`로 action 발생

```jsx
// counterSlice.js
const initialState = { count: 0 };
export const counterReducer = (state = initialState, action) => {
  switch (action.type) {
    case 'INC': return { count: state.count + 1 };
    case 'DEC': return { count: state.count - 1 };
    default: return state;
  }
};
```

```jsx
// store.js
import { createStore } from 'redux';
import { counterReducer } from './counterSlice';
export const store = createStore(counterReducer);
```

```jsx
// App.jsx
import { Provider, useSelector, useDispatch } from 'react-redux';
import { store } from './store';

const Counter = () => {
  const count = useSelector(state => state.count);
  const dispatch = useDispatch();

  return (
    <>
      <p>Count: {count}</p>
      <button onClick={() => dispatch({ type: 'INC' })}>+1</button>
    </>
  );
};

export default () => (
  <Provider store={store}>
    <Counter />
  </Provider>
);
```

---

## 💡 6. Redux의 장점

| 장점        | 설명                                            |
| --------- | --------------------------------------------- |
| 단일 진실의 원천 | 앱 state가 오직 하나의 Store에 존재하여 예측 가능함               |
| 디버깅 용이    | state 전이 추적, undo/redo, time-travel debugging 가능 |
| 미들웨어 확장성  | Redux-thunk, Redux-saga 등으로 비동기 처리 분리 가능      |
| 테스트 용이    | 순수 함수인 reducer는 테스트가 쉬움                       |

---

## ⚠️ 7. Redux의 단점과 비판

| 단점             | 설명                                     |
| -------------- | -------------------------------------- |
| 보일러플레이트 코드가 많음 | 액션/리듀서 작성이 장황함                         |
| 작은 앱에는 과한 구조   | 간단한 상태엔 `useState`, `useReducer`가 더 간결 |
| 학습 난이도         | 미들웨어, thunk, saga 등 이해할 것이 많음          |

✅ 이 문제를 해결하기 위해 등장한 것이 바로 `Redux Toolkit`

---

## 🚀 8. Redux Toolkit

> Redux 팀이 만든 **권장 사용 방법**, 코드량을 대폭 줄임

* `configureStore()`, `createSlice()`, `createAsyncThunk()` 등 유틸 제공
* Immer, devtools, thunk 내장
* 실무에서는 거의 무조건 **Redux Toolkit을 사용**

예시:

```tsx
import { createSlice, configureStore } from '@reduxjs/toolkit';

const counterSlice = createSlice({
  name: 'counter',
  initialState: { value: 0 },
  reducers: {
    increment: (state) => { state.value += 1 },
    decrement: (state) => { state.value -= 1 },
  }
});

export const { increment, decrement } = counterSlice.actions;

const store = configureStore({
  reducer: { counter: counterSlice.reducer }
});
```

---

## 🧠 마무리 요약

| 항목       | 설명                                                      |
| -------- | ------------------------------------------------------- |
| Redux란?  | state를 전역에서 예측 가능하게 관리하는 라이브러리                             |
| 핵심 구성    | Action, Reducer, Store                                  |
| React 연동 | `react-redux`의 `Provider`, `useSelector`, `useDispatch` |
| 장점       | 상태 추적 용이, 예측 가능한 앱, 미들웨어 확장                             |
| 단점       | 코드가 복잡해짐, 작은 앱엔 과함                                      |
| 실무 권장    | ✅ Redux Toolkit 사용                                      |


