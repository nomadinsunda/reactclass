Redux는 “state 관리 라이브러리”라고만 들으면 너무 밋밋하죠.
조금 과장해서 말하면,

> **“복잡한 프론트엔드 앱을 그래도 사람 머리로 이해 가능한 수준으로 만들어주는 규칙 세트”** 입니다.

이 글에서는 **전통적인 Redux 개념 → 왜 예측 가능해지는지 → React와의 연결 → 현대 Redux(=Redux Toolkit 중심)** 까지, 깊게 정리해볼게요.

---

## 1. Redux 한 줄 정의와 핵심 키워드

**한 줄 정의**

> Redux는 **단일 Store**에 앱의 state를 모으고,
> state 변경을 **Action → Reducer → 새로운 State** 라는
> **엄격한 단방향 데이터 흐름**으로만 허용해서
> state 변화를 **예측 가능(predictable)** 하게 만드는 state 관리 라이브러리입니다.

여기서 핵심 키워드는:

1. **Single Source of Truth (단일 진실의 근원)**
2. **State is Read-Only (state는 읽기 전용)**
3. **Changes are Made with Pure Functions (변화는 순수 함수로만)**

이 세 가지 원칙이 Redux의 모든 것을 지배합니다.

---

## 2. 왜 Redux가 등장했나? “state 지옥” 이야기

초기 React 앱을 떠올려 봅시다.

* 상위 컴포넌트가 `props`로 state를 자식에게 계속 내려줌 (props drilling)
* 형제 컴포넌트끼리 state를 공유하려면 공통 부모로 끌어올림(lifting state up)
* 페이지가 많아지고, 모달, 알림, 유저 정보, 필터 state, 페이지네이션, 폼 값 …
  이런 것들이 여기저기 흩어지면서 **어디에서 state가 변경되는지 추적이 지옥**이 됩니다.

문제점들:

1. **state가 여기저기 흩어짐**

   * 어떤 데이터는 A 컴포넌트 내부 `useState`
   * 어떤 데이터는 B 컴포넌트의 `useState`
   * 어떤 데이터는 Context
   * 코드 읽을 때 “도대체 이 값은 어디에서 오고, 누가 바꾸는 거야?”가 됩니다.

2. **암묵적인 state 변경**

   * 여러 컴포넌트가 같은 데이터를 다르게 해석하거나,
   * 서로 다른 곳에서 state를 조작해서 **버그 재현이 어렵고, 디버깅이 힘듦**.

3. **타이밍·순서 문제**

   * 비동기 요청, 타이머, 이벤트 핸들러가 뒤섞이면서
   * “이 버튼을 누르면, 어떤 순서로 무슨 state가 바뀌는지” 머릿속에서 시뮬레이션하기가 매우 힘듭니다.

Redux는 이 문제에 이렇게 답합니다:

> “**state는 한 군데(Store)에 모아두고,
> state를 바꾸는 방법은 오직 Action → Reducer 뿐이다.
> 그 외의 경로는 모두 금지!**”

이렇게 하면 **state 흐름이 규약으로 고정**되기 때문에,
코드를 읽을 때 훨씬 예측 가능해집니다.

---

## 3. Redux의 3대 원칙

### 3.1 Single Source of Truth

> 앱 전체 state는 **하나의 Store 객체**에 트리 형태로 저장된다.

```ts
// 예: Redux Store 안의 state 구조 예시
{
  user: {
    id: 1,
    name: '홍길동',
    role: 'admin'
  },
  cart: {
    items: [
      { id: 'p1', name: '노트북', price: 1500000, quantity: 1 },
      { id: 'p2', name: '마우스', price: 30000, quantity: 2 },
    ],
    totalPrice: 1560000
  },
  ui: {
    loading: false,
    modal: {
      isOpen: true,
      content: '로그인이 필요합니다.'
    }
  }
}
```

* 앱에서 필요한 모든 state가 **한 눈에 구조적으로** 보입니다.
* “현재 앱의 전체 state”를 하나의 JSON 스냅샷으로 저장/로깅/전송할 수 있습니다.
* 테스트, 디버깅, 서버 사이드 렌더링에 유리합니다.

---

### 3.2 State is Read-Only

> state를 직접 수정하는 것은 **절대 안 된다.**
> state 변경은 반드시 **Action**을 통해서만 일어난다.

```ts
// ❌ 이렇게 하면 안 됨
store.state.cart.items.push(newItem);  // 직접 수정 금지
store.state.user.name = '이몽룡';      // 직접 수정 금지

// ✅ 항상 Action 디스패치
store.dispatch({ type: 'cart/addItem', payload: newItem });
store.dispatch({ type: 'user/changeName', payload: '이몽룡' });
```

이 규칙 덕분에:

* “이 state가 언제, 어떤 이유로 바뀌었나?” → 항상 **어떤 Action이 디스패치 되었는지**를 보면 됩니다.
* Action은 보통 **평범한 객체(Plain Object)** 이므로,
  이를 로깅하거나 기록하기가 매우 쉽습니다.

---

### 3.3 Changes are Made with Pure Functions

> **Reducer**는 (현재 state, Action)을 받아서 **새로운 state**를 반환하는 **순수 함수**여야 합니다.

순수 함수(pure function) 특징:

* 같은 아규먼트 → 항상 같은 결과
* 외부 state를 직접 변경하지 않음 (no side effect)
* 아규먼트를 변경하지 않고, 항상 새로운 값을 만들어서 반환

```ts
// (state, action) => newState
function cartReducer(state, action) {
  switch (action.type) {
    case 'cart/addItem':
      // ❌ state.items.push(...) X
      // ✅ 새로운 배열로 복사해서 반환
      return {
        ...state,
        items: [...state.items, action.payload],
      };

    case 'cart/clear':
      return {
        ...state,
        items: [],
      };

    default:
      return state;
  }
}
```

이렇게 하면:

* Reducer는 **데이터 변환 로직만** 담고,
* 네트워크 호출, 로깅 등 사이드 이펙트는 **별도의 계층(Thunk, Saga 등)**로 분리할 수 있습니다.
* 결과적으로 **“state 변경 로직”이 순수 함수들로 모듈화**되어 테스트와 이해가 쉬워집니다.

---

## 4. Redux의 핵심 구성 요소 4가지

1. **Store**
2. **Action**
3. **Reducer**
4. **Dispatch & Subscribe**

### 4.1 Store: state를 보관하는 객체

Store는 **앱의 모든 state를 담고 있는 객체**이며, 세 가지 주요 메서드를 제공합니다.

* `getState()` : 현재 state 조회
* `dispatch(action)` : 액션을 보내서 state 변경 요청
* `subscribe(listener)` : state 변경을 감지하는 리스너 등록

전통적인 Redux(순수 JS)에서 Store 생성 예시입니다.

> **레거시 주의**: `createStore`는 Redux 4.2부터 **deprecated**이며, Redux 5 / Redux Toolkit 2.x에서는 `legacy_createStore`라는 별칭으로만 남아 있습니다.
> 아래 코드는 **원리 이해용**이고, 새 프로젝트에서 Store를 만드는 공식 방법은 RTK의 `configureStore` 하나뿐입니다. (→ `15.4 configureStore`)

```js
// ⚠️ 레거시(개념 설명용). 실제 프로젝트에서는 configureStore를 사용하세요.
import { createStore } from 'redux'; // deprecated
import rootReducer from './reducers';

const store = createStore(rootReducer);

// 현재 state 조회
console.log(store.getState());

// state 변경 구독
const unsubscribe = store.subscribe(() => {
  console.log('state 변경됨:', store.getState());
});

// 액션 디스패치
store.dispatch({ type: 'counter/increment' });
store.dispatch({ type: 'counter/decrement' });

// 구독 해제
unsubscribe();
```

React와 결합할 땐 `Provider`를 통해 컴포넌트 트리에 Store를 주입합니다.

---

### 4.2 Action: “무슨 일이 일어났는지”를 설명하는 객체

Action은 단순한 JS 객체입니다.

```ts
{
  type: 'cart/addItem',
  payload: { id: 'p1', name: '노트북', price: 1500000 }
}
```

* `type`: **문자열 상수**, 꼭 있어야 합니다.
* `payload`: (선택) 실제 데이터

Action은 **“어떤 의도가 있는 이벤트”**를 표현합니다.

* `'user/loginRequested'`
* `'user/loginSucceeded'`
* `'user/loginFailed'`
* `'cart/addItem'`
* `'cart/removeItem'`

이렇게 타입을 분리해두면, 나중에 로그를 볼 때:

> “아, 이 시간에 loginFailed가 여러 번 발생했구나 → 인증 서버 문제인가?”

처럼 **도메인 이벤트 흐름** 자체를 추적할 수 있습니다.

그래서 Redux 액션은 거의 **도메인 이벤트 로그**에 가까운 역할을 합니다.

---

### 4.3 Reducer: state 변경 로직을 담은 순수 함수

Reducer는 보통 **도메인별로 쪼개서** 관리합니다.

```ts
const initialUserState = {
  id: null,
  name: null,
  isLoggedIn: false,
};

function userReducer(state = initialUserState, action) {
  switch (action.type) {
    case 'user/loginSucceeded':
      return {
        ...state,
        ...action.payload,
        isLoggedIn: true,
      };

    case 'user/logout':
      return initialUserState;

    default:
      return state;
  }
}
```

여러 Reducer를 **combineReducers**로 합쳐서 `rootReducer`를 만듭니다.
(아래 `switch`문 리듀서 + 수동 `combineReducers` 조합은 **레거시 스타일**입니다.
RTK에서는 `createSlice`가 리듀서를 만들고, `configureStore({ reducer: { ... } })`가 `combineReducers`를 자동으로 호출합니다.)

```ts
import { combineReducers } from 'redux';
import userReducer from './userReducer';
import cartReducer from './cartReducer';
import uiReducer from './uiReducer';

const rootReducer = combineReducers({
  user: userReducer,
  cart: cartReducer,
  ui: uiReducer,
});

export default rootReducer;
```

이 구조 덕분에 **도메인별로 state와 로직이 잘 분리**됩니다.

---

### 4.4 Dispatch & Subscribe

* **Dispatch**: Action을 Store에 보내는 것
* **Subscribe**: state가 바뀔 때마다 호출될 콜백을 등록

React와 함께 쓸 때는 직접 `subscribe`를 쓰기보다는
`react-redux`의 `useSelector`, `useDispatch` 훅이 이를 추상화합니다.

---

## 5. Redux가 “예측 가능하다”는 건 실제로 무슨 의미?

“state가 예측 가능하다”는 말이 많이 쓰이는데, 좀 더 구체적으로 풀어봅시다.

### 5.1 입력이 같으면 출력도 같다 (Deterministic)

> **같은 Initial State + 같은 Action 시퀀스 → 항상 같은 최종 State**

이게 가능한 이유:

* state는 **순수 함수인 Reducer**를 통해서만 변경됩니다.
* Reducer는 같은 인자에 대해 같은 결과를 반환합니다.

즉,

```ts
const initialState = {...};

const finalState1 = actions.reduce(rootReducer, initialState);
const finalState2 = actions.reduce(rootReducer, initialState);

// 항상 finalState1 === finalState2
```

이게 보장됩니다.
그래서:

* **Replay**가 가능합니다. (Action들을 기록해두고 다시 재생)
* **Time Travel Debugging**이 가능합니다.
  (DevTools에서 “이 시점으로 되돌리기” 같은 기능)

---

### 5.2 “state 변경의 역사”를 기록할 수 있다

Action들은 단순 객체라서:

```ts
[
  { type: 'user/loginRequested', payload: {...} },
  { type: 'user/loginSucceeded', payload: {...} },
  { type: 'cart/addItem', payload: {...} },
  { type: 'cart/addItem', payload: {...} },
  { type: 'cart/removeItem', payload: {...} },
]
```

이렇게 **순서대로 쌓여 있는 “이력”**이 됩니다.

이걸 로그로 남기면:

* 특정 버그가 발생한 시점의 state,
* 그 직전에 어떤 Action들이 있었는지,
* 동일한 Action 시퀀스를 재현해서 디버깅할 수 있습니다.

이게 바로 “예측 가능성 + 재현 가능성”의 핵심입니다.

---

## 6. Redux와 React의 연결

전통적인 연결 방식 (지금도 개념 이해용으로 좋음):

1. `Provider`로 Store를 React 컴포넌트 트리에 주입
2. `useSelector` 혹은 `connect`로 state 구독
3. `useDispatch` 혹은 `mapDispatchToProps`로 액션 디스패치

### 6.1 기본 세팅 예시

> 아래 6.1의 store 코드는 **레거시(원리 설명용)** 입니다.
> 실제 프로젝트는 8장의 RTK 방식(`@reduxjs/toolkit` + `configureStore`)으로 시작하세요.
> 또한 `connect` / `mapStateToProps` / `mapDispatchToProps` HOC 방식은 **클래스 컴포넌트 시절의 레거시 API**이며(지금도 동작하지만 신규 코드에는 권장하지 않음), 함수 컴포넌트에서는 `useSelector` / `useDispatch` 훅을 사용합니다.

```bash
# 레거시 방식
npm install redux react-redux

# 현재 권장 방식 (redux는 RTK의 의존성으로 함께 설치됨)
npm install @reduxjs/toolkit react-redux
```

```tsx
// store.ts — ⚠️ 레거시(개념 설명용)
import { legacy_createStore as createStore } from 'redux';
import rootReducer from './reducers';

export const store = createStore(rootReducer);
```

```tsx
// index.tsx (Vite + React 기준)
import React from 'react';
import ReactDOM from 'react-dom/client';
import { Provider } from 'react-redux';
import { store } from './store';
import App from './App';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <Provider store={store}>
    <App />
  </Provider>
);
```

### 6.2 컴포넌트에서 사용

```tsx
// Counter.tsx
import React from 'react';
import { useSelector, useDispatch } from 'react-redux';

function Counter() {
  const count = useSelector((state: any) => state.counter.value);
  const dispatch = useDispatch();

  return (
    <div>
      <p>현재 카운트: {count}</p>
      <button onClick={() => dispatch({ type: 'counter/increment' })}>+1</button>
      <button onClick={() => dispatch({ type: 'counter/decrement' })}>-1</button>
    </div>
  );
}

export default Counter;
```

이렇게 하면:

* 이 컴포넌트는 **Redux Store의 `counter.value` state를 구독**하게 되고,
* 해당 값이 바뀔 때만 리렌더링합니다.

---

## 7. Middleware: 비동기와 사이드 이펙트 분리하기

Reducer는 순수 함수여야 하므로:

* API 호출
* 로깅
* 타이머
* 기타 사이드 이펙트

를 Reducer 안에서 하면 안 됩니다.

그래서 Redux는 **Middleware** 개념을 도입합니다.

### 7.1 Middleware란?

> `store.dispatch(action)`이 Reducer에 도달하기 전에
> 중간에서 Action을 가로채어 **무언가를 할 수 있는 레이어**입니다.

대표적인 미들웨어:

* `redux-thunk` : 함수(Action Creator가 함수를 반환) 기반 비동기 처리
* `redux-saga` : Generator 기반 사이드 이펙트 관리
* `logger` : Action/State 로깅

> **현재 기준(RTK 2.x)**: thunk는 **따로 설치·적용하지 않습니다.**
> `redux-thunk`가 RTK 안에 포함되어 있고, `configureStore`의 기본 미들웨어에 이미 들어 있습니다.
> 즉 `npm i redux-thunk` + `applyMiddleware(thunk)`는 **레거시 절차**입니다.

예: Thunk

```ts
// thunk action creator 예시
function fetchUser() {
  return async (dispatch) => {
    dispatch({ type: 'user/fetchStarted' });

    try {
      const res = await fetch('/api/user');
      const data = await res.json();
      dispatch({ type: 'user/fetchSucceeded', payload: data });
    } catch (error) {
      dispatch({ type: 'user/fetchFailed', error });
    }
  };
}
```

이렇게 하면:

* 컴포넌트에서 `dispatch(fetchUser())`를 호출
* 실제로는 함수가 실행되면서 비동기 로직 수행
* 중간에 여러 Action을 분리해서 디스패치 가능

결론적으로:

> **비동기/사이드 이펙트 로직**은 미들웨어 층으로 밀어 올리고,
> **state 변경 로직**은 Reducer에서만 처리 → 관심사 분리(Cross-cutting Concern 분리)

---

## 8. 현대 Redux: “Redux Toolkit”이 표준이다

기존 Redux는:

* `createStore`, `combineReducers`, `switch(action.type)` 패턴
* Action Type, Action Creator, Reducer 파일 분리 등 보일러플레이트가 많음
* Immer 없이 직접 불변성을 지켜야 해서 코드가 장황함

그래서 공식 문서에서도 이제는 **Redux Toolkit(RTK)** 사용을 권장합니다.

```bash
npm install @reduxjs/toolkit react-redux
```

### 8.1 Redux Toolkit의 핵심 기능

1. `configureStore()` : 미들웨어, DevTools, Reducer 통합 설정
   → 기본 미들웨어에 **thunk + 불변성 검사(immutableCheck) + 직렬화 검사(serializableCheck)** 포함.
   이 중 두 검사 미들웨어는 **개발 모드에서만** 동작하고 프로덕션 빌드에서는 빠집니다.
2. `createSlice()` : Action Type + Action Creator + Reducer를 한 번에 정의
   → action type이 `slice이름/리듀서이름` 형태로 **자동 생성**되고, `prepare` 콜백으로 payload를 가공할 수 있습니다.
3. `createAsyncThunk()` : 비동기 로직 템플릿 제공 (`pending/fulfilled/rejected` 자동 생성)
4. Immer 기반 Draft 사용 → **가변처럼 작성해도 내부적으로 불변성 유지**
   (단, Immer가 적용되는 곳은 `createSlice` / `createReducer` **내부뿐**입니다. → `15.2 Immer`)
5. `createSelector()` : reselect가 **RTK에 기본 포함**되어 있어 별도 설치 없이 메모이제이션 selector를 만들 수 있습니다.
6. **RTK Query** (`createApi`) : 서버 데이터 캐싱·재요청·무효화까지 다루는 데이터 페칭 레이어.
   "서버에서 받아온 데이터"를 다루는 일이라면 `createAsyncThunk`로 로딩/에러 플래그를 직접 관리하기 전에 RTK Query를 먼저 검토하는 것이 공식 권장입니다.

### 8.2 RTK 예시: Counter Slice

```ts
// features/counterSlice.ts
import { createSlice } from '@reduxjs/toolkit';
import type { PayloadAction } from '@reduxjs/toolkit';

interface CounterState {
  value: number;
}

const initialState: CounterState = {
  value: 0,
};

const counterSlice = createSlice({
  name: 'counter', // action type prefix
  initialState,
  reducers: {
    increment(state) {
      // 여기서 state를 "직접 변경"하는 것처럼 보이지만,
      // 실제로는 Immer가 불변성을 유지해줌
      state.value += 1;
    },
    decrement(state) {
      state.value -= 1;
    },
    // payload 타입은 PayloadAction<T>로 명시해야 any가 되지 않습니다.
    incrementByAmount(state, action: PayloadAction<number>) {
      state.value += action.payload;
    },
  },
});

export const { increment, decrement, incrementByAmount } = counterSlice.actions;
export default counterSlice.reducer;
```

### 8.3 Store 설정 (RTK)

```ts
// store.ts
import { configureStore } from '@reduxjs/toolkit';
import counterReducer from './features/counterSlice';

export const store = configureStore({
  reducer: {
    counter: counterReducer,
  },
});

export type RootState = ReturnType<typeof store.getState>;
export type AppDispatch = typeof store.dispatch;
```

### 8.4 컴포넌트에서 사용 (RTK + React)

```tsx
// Counter.tsx
import React from 'react';
import { useSelector, useDispatch } from 'react-redux';
import type { RootState, AppDispatch } from '../store';
import { increment, decrement, incrementByAmount } from '../features/counterSlice';

function Counter() {
  const count = useSelector((state: RootState) => state.counter.value);
  // react-redux 9에서는 매번 제네릭을 붙이는 대신
  // `export const useAppDispatch = useDispatch.withTypes<AppDispatch>()` 처럼
  // 미리 타입이 입혀진 훅을 만들어 쓰는 방식이 공식 권장입니다.
  const dispatch = useDispatch<AppDispatch>();

  return (
    <div>
      <p>현재 카운트: {count}</p>
      <button onClick={() => dispatch(increment())}>+1</button>
      <button onClick={() => dispatch(decrement())}>-1</button>
      <button onClick={() => dispatch(incrementByAmount(5))}>+5</button>
    </div>
  );
}

export default Counter;
```

이렇게 하면:

* Action Type 문자열 따로 정의할 필요 X
* Action Creator 따로 만들 필요 X
* Reducer도 switch문 없이 깔끔
* 불변성은 Immer가 자동 처리

> 실무/강의 모두에서 **“Redux = Redux Toolkit”**으로 이해시키는 게 요즘은 훨씬 좋습니다.
> (전통 Redux는 “역사/원리 설명용”으로)

---

## 9. Redux vs React Context/useReducer 비교

리액트만으로도 state 관리를 할 수 있습니다:

* `useState` + 상위 컴포넌트 lifting
* `useReducer` + `useContext`

그럼에도 Redux를 쓰는 이유는?

### Redux가 유리한 상황

1. **state가 많고 복잡한 대규모 앱**
2. **여러 도메인이 섞여있는 경우**

   * 인증, 권한, UI state, 도메인 모델, 알림, 폼 등
3. **강력한 DevTools + Time Travel 디버깅이 필요**
4. **복잡한 비동기 흐름이 많고, 명시적으로 관리하고 싶을 때**
5. **팀/조직 차원에서 패턴을 표준화하고 싶을 때**
   (onboarding, 코드 리뷰, 테스트, 디버깅이 쉬워짐)

### Context/useReducer가 충분한 경우

* 작은 앱, 단순 state 공유
* 로그인 여부 정도만 Context로 관리
* Redux의 도입 비용이 오버스펙인 상황

---

## 10. Redux를 쓸 때의 패턴 & 안티패턴

### 좋은 패턴 O

* **Slice 단위 설계**

  * `userSlice`, `authSlice`, `cartSlice`, `uiSlice` …
* **state를 너무 거대하게 한 곳에 몰아넣지 말고, 도메인별로 나누기**
* **Action 타입은 “무슨 일이 일어났는지”를 잘 설명하는 이름으로**

  * `user/loginSucceeded`, `cart/itemAdded`, `modal/opened`
* **Thunk/비동기 로직은 Slice 바깥 또는 `extraReducers`에서 관리**
* **Selector를 따로 정의해서 재사용**

  * `selectIsLoggedIn(state)`, `selectCartTotal(state)`

### 안 좋은 패턴 X

* Reducer 안에서 **DOM 조작, fetch, console.log 남발**
* Reducer에서 **날짜/랜덤 생성** (비결정적)
* state 트리 구조를 아무렇게나 설계
* Redux에 모든 state를 다 넣기

  * 로컬 UI state(모달 내부 인풋, hover state 등)는 그냥 `useState`가 더 나음

---

## 11. 마무리: Redux를 “도메인 이벤트와 state 이력 관리 시스템”으로 바라보기

Redux를 단순히 “전역 state 관리 도구”로만 보면 매력이 반만 보입니다.

조금 더 넓게 보면,

> * **Action**: 도메인에서 일어나는 이벤트들의 로그
> * **Reducer**: 이벤트가 state에 어떤 영향을 미치는지를 정의한 순수 함수 집합
> * **Store**: 현재 state + 이력을 재생할 수 있는 공간
> * **Middleware**: 이벤트 흐름 중간에서 부가 작업을 하는 레이어
> * **DevTools**: 이 모든 것을 시간 축에서 시각화하는 디버거

즉, Redux는

> **“복잡한 프론트엔드 앱의 state를
> 시간축을 포함해서 ‘논리적으로 이해 가능하게’ 만들기 위한 아키텍처 도구”**

라고 볼 수 있습니다.

