**React가 Side Effect로 간주하는, 즉 “렌더 함수 안에서 실행하면 안 되는” 실전 사례** 들을 소개합니다.

> **먼저 짚고 갈 것**: 아래 목록은 “**렌더 중에 하면 안 되는 것**”의 목록이지
> “**전부 `useEffect`에 넣어야 하는 것**”의 목록이 **아닙니다.**
> 렌더 밖으로 나가는 출구는 두 개이고, 둘 중 **이벤트 핸들러가 기본값**입니다.
>
> | 이 동작이 실행되는 이유는?                 | 어디에 둘 것인가                        |
> | ------------------------------- | ------------------------------- |
> | 사용자가 **무언가를 했기 때문에** (클릭·입력·제출) | **이벤트 핸들러** ← 대부분 여기            |
> | 컴포넌트가 **화면에 보이는 동안 유지되어야** 하므로  | **`useEffect`** (외부 시스템과의 동기화)  |
> | 페인트 전에 **DOM 측정/보정**이 필요하므로     | `useLayoutEffect`               |
>
> 예를 들어 아래 목록의 `localStorage.setItem`은
> “저장 버튼을 눌렀기 때문”이라면 **이벤트 핸들러**가 맞고,
> “테마 state를 브라우저 저장소와 계속 맞춰야 하기 때문”이라면 **Effect**가 맞습니다.
> (자세한 판단 기준은 “9.2 React Side Effect”의 *You Might Not Need an Effect* 절 참고)

# React에서 발생하는 **실제 Side Effect 리스트**

## — 렌더 함수에서 절대 수행하면 안 되는 모든 작업들 —

React의 Side Effect는 크게 세 부류로 나뉩니다:

---

# 1⃣ **브라우저 환경 변경(Browser Environment Mutation)**

렌더 외부의 브라우저 상태를 변경하는 모든 행위
→ 무조건 Side Effect

## O 1. 문서 관련

* `document.title = ...`
* `document.body.style = ...`
* `<meta>` 태그 변경
* `document.cookie = ...`

## O 2. Storage 관련

* `localStorage.setItem(key, value)`
* `sessionStorage.setItem(key, value)`
* `indexedDB` read/write

## O 3. URL / History 변경

* `window.history.pushState(...)`
* `history.replaceState(...)`
* `window.location = ...`
* `window.location.hash = ...`

## O 4. 브라우저 전역 속성

* `navigator.clipboard.writeText(...)`
* `Notification.requestPermission()`
* `matchMedia` 등록 & 제거
* `window.scrollTo(...)`

---

# 2⃣ **DOM 조작(DOM Manipulation)**

React의 관리 밖에서 DOM을 직접 건드리는 모든 작업
→ 100% Side Effect

## O 1. 직접 DOM 갱신

* `document.getElementById(...)`
* `element.innerHTML = ...`
* `element.style.xxx = ...`

## O 2. 서드파티 라이브러리 DOM 조작

* jQuery(`$('#id').fadeIn()`)
* D3.js 직접 DOM 조작
* chart.js의 canvas 초기화 / destroy
* ApexCharts, Swiper.js 등 UI lib 초기화

## O 3. 포커스 & 스크롤 위치 변경

* `element.focus()`
* `element.scrollIntoView()`
* `window.scrollTo()`

---

# 3⃣ **네트워크 관련(Networking & I/O)**

외부 리소스를 읽거나 쓰는 모든 작업은 Side Effect

## O 1. HTTP 요청

* `fetch('/api/...')`
* `axios.get(...)`, `axios.post(...)`
* GraphQL 클라이언트 요청(Apollo, Relay 등)
* 이미지 프리로드(new Image())

## O 2. WebSocket / SSE / Streaming

* `new WebSocket(url)`
* `webSocket.send(...)`
* `EventSource` 구독(SSE)
* gRPC(streaming) 요청

## O 3. 파일 I/O

* File API (input file)
* FileReader
* Blob 생성 및 다운로드 트리거

---

# 4⃣ **타이머 & 비동기 핸들링(Timers / Async Scheduling)**

React 렌더링과 무관한 시간 기반 모듈은 모두 Side Effect

## O 1. 타이머

* `setTimeout(...)`
* `setInterval(...)`
* `requestAnimationFrame(...)`
* `setImmediate(...)`
* `queueMicrotask(...)`

## O 2. 외부 async 작업

* 백그라운드 async 함수 실행
* debounce/throttle (lodash)

  * 내부적으로 `setTimeout`을 사용하므로 Side Effect

---

# 5⃣ **이벤트 리스너(Event Listeners)**

React가 관리하지 않는 DOM, window, document 이벤트는 모두 Side Effect

## O 1. DOM 전역 이벤트

* `window.addEventListener('resize', ...)`
* `window.addEventListener('scroll', ...)`
* `document.addEventListener('keydown', ...)`
* 전역 마우스/터치 이벤트

## O 2. 브라우저 생명주기 이벤트

* `visibilitychange`
* `beforeunload`
* `popstate`
* `online/offline`

## O 3. 외부 라이브러리 이벤트 핸들러

* WebSocket.onmessage
* Firebase onSnapshot
* EventEmitter.on(...)

*모두 cleanup이 반드시 필요합니다.*

---

# 6⃣ **애니메이션(Animation / Transition)**

DOM 조작이나 API 호출 기반의 애니메이션 초기화는 Side Effect

## O 1. CSS Transition 시작하기

* 클래스 추가/제거로 transition triggering

## O 2. JavaScript 애니메이션

* `element.animate(...)`
* D3 Transition
* GreenSock(GSAP) 애니메이션

---

# 7⃣ **전역 객체(Global Mutable State) 변경**

React state가 아닌 외부 전역 객체 변경은 전부 Side Effect

## O 1. 글로벌 싱글톤 변경

* 외부 예측 불가능한 cache 객체 조작
* window/global 스코프 변수 변경

## O 2. 외부 스토어에 쓰기 (dispatch / set)

* `store.dispatch(...)`, `useStore.setState(...)` 를 **렌더 중에 호출하는 것은 금지**입니다.
  렌더가 중단·재시도될 수 있으므로 dispatch가 중복되고, 렌더 도중 다른 컴포넌트의 상태를 바꾸는 셈이 됩니다.
* 이벤트 핸들러에서의 `dispatch`는 **Side Effect가 아니라 정상적인 사용법**입니다.
  (“사용자가 눌렀기 때문에 일어나는 일”이므로 핸들러가 제자리입니다.)

## O 3. 외부 스토어 **구독/읽기**는 `useSyncExternalStore`로

* `store.subscribe(...)`를 직접 호출해 구독하는 것은 Side Effect이며 cleanup(구독 해제)이 필요합니다.
* 하지만 **직접 `useEffect` + `useState`로 구독을 구현하지 마세요.**
  Concurrent 렌더링에서는 렌더 도중 외부 값이 바뀌면
  한 화면 안에서 서로 다른 값이 섞이는 **tearing(찢어짐)** 이 발생합니다.
* React 18+ 정답은 **`useSyncExternalStore`** 입니다.
  React가 렌더 시점에 안전하게 스냅샷을 읽고 구독을 관리해 줍니다.

```jsx
const state = useSyncExternalStore(
  store.subscribe,     // 구독 등록 → cleanup 반환
  store.getSnapshot,   // 현재 값 (동일 입력이면 동일 참조를 반환해야 함)
  store.getServerSnapshot // SSR용 (선택)
);
```

* Redux(react-redux v8+), Zustand, Jotai 등은 **내부적으로 이미 이 Hook을 사용**하므로,
  `useSelector` / `useStore(selector)` 를 쓰는 한 직접 신경 쓸 일은 없습니다.

---

# 8⃣ **리소스 할당 및 해제(Resource Allocation)**

리소스를 생성/파괴/연결하는 작업

## O 1. WebGL context 생성

* canvas.getContext("webgl")
* Three.js scene 초기화

## O 2. Media API

* getUserMedia
* 오디오 스트림
* 비디오 스트림

## O 3. MutationObserver / IntersectionObserver

* 관찰 시작
* 언마운트 시 disconnect 필요

---

# 9⃣ **로그 및 모니터링(Log/Analytics)**

렌더링은 여러 번 실행될 수 있으므로, 로그/집계 전송도 렌더 중에 하면 **중복 집계**가 됩니다.

## O 1. 로깅

* Sentry 에러 로깅
* Firebase Analytics
* GA(Google Analytics) 이벤트

> **`console.log()`는 예외적으로 관대하게 봅니다.**
> 엄밀히는 순수하지 않지만, React 공식 문서도 디버깅용 `console.log`는 렌더 중 호출을 허용합니다.
> 다만 개발 모드 StrictMode에서는 컴포넌트가 두 번 호출되어 **로그가 두 번 찍힌다**는 점만 기억하세요.
>
> 반면 **분석 이벤트 전송은 다릅니다.**
> “버튼을 눌렀다” 같은 사용자 행동 로그는 **이벤트 핸들러**에서 보내야 하고,
> “이 페이지가 표시되었다” 같은 노출 로그만 Effect에 둡니다.
> 렌더 중에 전송하면 리렌더마다 카운트가 부풀려집니다.

---

# **React 외부 환경과 동기화(Synchronization with external environment)**

UI 상태를 React 외부(브라우저/서버/전역)에 맞추는 것

## O 1. UI ↔ 서버 동기화

* push 알림 상태 sync
* 실시간 채팅 read 상태 sync

## O 2. UI ↔ localStorage sync

* 테마 저장
* 최근 열람 목록 저장

## O 3. UI ↔ URL sync

* search params 관리
* modal open 여부를 URL에 반영

---

# 특별 카테고리: **“렌더링을 오염시키는 모든 동작” = 무조건 Side Effect**

React가 렌더링을 여러 번 실행할 수 있기 때문에
**아래 행동은 모두 “렌더 단계에서 실행하면 위험한 Side Effect”입니다.**

### X 렌더 단계에서 하면 절대 안 되는 것들

1. DOM 조작
2. 타이머 생성
3. fetch 호출
4. 파일 읽기
5. 전역 이벤트 등록
6. 전역 변수 변경
7. localStorage read/write
8. **호출할 때마다 값이 달라지는 함수** — `Math.random()`, `crypto.randomUUID()`, `Date.now()`, `new Date()`

   * 렌더는 “같은 입력 → 같은 출력”이어야 하는데 이들은 그 전제를 깨뜨립니다.
   * StrictMode 이중 호출 시 값이 달라지고, SSR에서는 서버/클라이언트 값이 어긋나
     **하이드레이션 불일치(hydration mismatch)** 오류가 납니다.
   * 대안:

     * DOM `id`·`aria-*` 연결용 고유값 → **`useId()`**
     * 컴포넌트 수명 동안 고정할 값 → `useState(() => crypto.randomUUID())` (초기화 1회)
     * “현재 시각”을 표시해야 한다면 → state로 두고 Effect/이벤트에서 갱신

---

# React가 공식적으로 인정한 Side Effect 카테고리

React docs 기준 Side Effect는 다음 3개로 요약됩니다:

### 1. **서버에서 데이터 가져오기(fetching data)**

### 2. **브라우저 API 사용(localStorage, title, scroll 등)**

### 3. **서드파티 라이브러리 연동(DOM 조작 포함)**

공식 문서의 가이드는 “이 동작들을 **Effect에서만** 실행하라”가 아니라,
**“렌더 함수 안에서는 실행하지 말라”** 입니다. Effect는 그중 한쪽 출구일 뿐입니다.

## 최종 판별 플로우

```
이 코드는 렌더 함수 본문 안에 있는가?
        │
        ├─ 아니오 → 이벤트 핸들러 안이다 → ✅ 그대로 두세요 (대부분 정답)
        │
        └─ 예 → 렌더 밖으로 빼야 합니다. 어디로?
                 │
                 ├─ props/state만으로 계산 가능한 값인가?
                 │     → ❌ effect 금지. 렌더 중 계산 (필요 시 useMemo)
                 │
                 ├─ 사용자의 특정 행동(클릭/제출)의 결과인가?
                 │     → ✅ 이벤트 핸들러로
                 │
                 ├─ 컴포넌트가 보이는 동안 외부 시스템과 계속 맞춰야 하는가?
                 │     → ✅ useEffect (+ 반드시 cleanup)
                 │
                 ├─ 외부 스토어를 "읽는" 것인가?
                 │     → ✅ useSyncExternalStore
                 │
                 └─ 페인트 전에 DOM을 측정/보정해야 하는가?
                       → ✅ useLayoutEffect
```

### O cleanup 체크리스트

Effect에 두기로 했다면, 아래는 **cleanup이 없으면 반드시 새는(leak) 항목**들입니다.

* `addEventListener` → `removeEventListener`
* `setInterval` / `setTimeout` → `clearInterval` / `clearTimeout`
* `requestAnimationFrame` → `cancelAnimationFrame`
* `new WebSocket` / `EventSource` → `close()`
* `IntersectionObserver` / `MutationObserver` / `ResizeObserver` → `disconnect()`
* 외부 라이브러리 인스턴스(chart, map, editor) → `destroy()` / `dispose()`
* 진행 중인 `fetch` → `AbortController.abort()` 또는 결과 무시 플래그
* 구독(Firebase `onSnapshot`, store `subscribe`) → 반환된 unsubscribe 호출

> 개발 모드 StrictMode의 이중 실행은 위 cleanup 누락을 **개발 중에 드러내기 위한 장치**입니다.
> 두 번 실행해서 문제가 생긴다면, StrictMode가 아니라 cleanup이 잘못된 것입니다.

---

# 결론: React Side Effect 전체 정리

> **React의 Side Effect =
> UI 렌더링이라는 “순수 계산” 외부에서
> 브라우저/서버/전역환경/시간/리소스를 건드리는
> 모든 작업 전체를 의미한다.**


