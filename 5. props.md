

# 🚀 React의 `props` — 왜 존재하고, 언제, 어떻게, 무엇을 위해 쓰이는가?

---

## ✅ 1. **왜 props가 필요한가?** (이해의 시작)

> 컴포넌트는 단순히 UI를 "그리는 함수"다.
> 하지만 같은 UI 컴포넌트를 **다른 데이터로 반복해서 쓰고 싶다**면 어떻게 해야 할까?

```jsx
<Button text="확인" />
<Button text="취소" />
```

* 위처럼 `text`만 바꿔가며 같은 버튼을 재사용하려면…
* 결국 **"컴포넌트에 외부 값을 주입"할 방법이 필요**하다
  → 이게 바로 props다.

### 💡 React는 컴포넌트를 "순수 함수처럼" 사용하기 위해

> 외부에서 값을 넘겨주는 파라미터 개념을 `props`로 만든 것

---

## 🧠 2. props의 진짜 정체

```jsx
function Greeting(props) {
  return <h1>Hello, {props.name}</h1>
}
```

위 코드는 사실상 다음과 같다:

```js
function Greeting(props) {
  return React.createElement('h1', null, `Hello, ${props.name}`);
}
```

즉,

```jsx
<Greeting name="홍길동" />
```

는 **JavaScript 함수에 객체 인자를 넘기는 것**과 같다:

```js
Greeting({ name: '홍길동' });
```

> ✅ props는 단지 **컴포넌트 함수에 전달되는 첫 번째 매개변수**다.
> 하지만 그 의미는 단순한 파라미터 이상으로, **UI 구성의 단위 정보**이자 **React 철학의 핵심**이다.

---

## ⚖️ 3. props의 3가지 핵심 속성

| 개념         | 설명                                    |
| ---------- | ------------------------------------- |
| **불변성**    | 자식은 props를 절대 바꾸지 못함. → 일관된 데이터 흐름 보장 |
| **단방향**    | 항상 부모 → 자식. 역방향은 없음.                  |
| **설명 가능성** | props만 보면 해당 컴포넌트가 어떻게 동작할지 예측 가능     |

### → 이 모든 특성은 결국 "**디버깅 가능한 UI**"를 만들기 위함이다.

---

## 🔬 4. props는 '상태'가 아니다

> 상태(state)는 "변할 수 있는 값",
> props는 "외부에서 들어오는 값"

* 예시:

```jsx
<UserCard name="홍길동" />
```

* 여기서 `name`은 외부에서 전달된 고정값 → props
* 반면, 버튼 클릭에 따라 값이 바뀐다면 → 그건 state

> 📌 **props = 외부 세계**, **state = 내부 세계**

---

## 🧩 5. props로 무엇을 전달할 수 있을까?

| 타입            | 예시                            |
| ------------- | ----------------------------- |
| 문자열           | `name="홍길동"`                  |
| 숫자            | `age={30}`                    |
| 불리언           | `isActive={true}`             |
| 배열            | `tags={["JS", "React"]}`      |
| 객체            | `user={{ id: 1, name: "홍" }}` |
| 함수            | `onClick={handleClick}`       |
| JSX/ReactNode | `children={<div>내용</div>}`    |

> JSX 문법은 결국 JavaScript이므로, **모든 표현식이 props로 전달 가능**

---

## ⚙️ 6. 고급 패턴 요약

| 패턴          | 설명                               |
| ----------- | -------------------------------- |
| 구조 분해 할당    | `function Comp({ name, age })`   |
| 스프레드 연산자    | `<Card {...user} />`             |
| 조건부 props   | `isVisible && <Box />`           |
| children 활용 | `<Modal>내용</Modal>`              |
| 함수 전달       | `onClick={() => setState(true)}` |

---

## 🎯 요약: props는 React 컴포넌트가 진정으로 “재사용 가능한 UI 단위”가 되기 위한 입구

### 그래서…

```js
function Component(props) {
  return <div>{props.value}</div>
}
```

은 단지 값을 출력하는 코드가 아니라,

> 🔥 "이 컴포넌트는 외부에서 어떤 값이 들어오든 같은 방식으로 동작하겠다"는
> **UI 컴포넌트 설계 원칙 그 자체를 코드화한 것**이다.

---

## ✅ 한 줄 요약:

> **props는 React 컴포넌트를 "재사용 가능한 UI 함수"로 만들기 위한 매개변수 시스템이며, UI 구성의 핵심 단위다.**


