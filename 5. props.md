# ✅ React의 `props`

---

## 📘 1. props란?

> **`props`는 "properties"의 줄임말로, 부모 컴포넌트가 자식 컴포넌트를 인스턴스화하면서 전달하는 읽기 전용 데이터입니다.**

```jsx
<MyComponent title="Hello" />
```

* 이때 `title="Hello"`는 `MyComponent` 함수 내부에서 `props.title`로 접근 가능
* JSX에서 컴포넌트를 함수처럼 호출하되 **HTML 태그처럼 속성을 부여**할 수 있는 이유는 Babel이 이를 다음처럼 변환하기 때문입니다:

```js
React.createElement(MyComponent, { title: "Hello" })
```

---

## 🧭 2. props가 왜 필요한가?

### ✅ 핵심 목적은 "컴포넌트의 재사용성과 독립성 확보"

* 하드코딩된 내부 상태가 아닌 외부로부터 동적으로 값을 전달
* 같은 컴포넌트를 다양한 데이터로 재사용 가능

```jsx
<User name="홍길동" />
<User name="김코딩" />
```

→ 하나의 컴포넌트로 두 명의 사용자 정보를 표현 가능

---

## 🔐 3. props의 **불변성**은 왜 중요한가?

* React는 **불변성(immutability)** 기반으로 Virtual DOM을 비교(diff)하고, 변경 사항만 실제 DOM에 반영합니다.
* 자식 컴포넌트 내부에서 props를 변경하면 예상치 못한 동작, 성능 저하, 디버깅 어려움 발생

❌ 잘못된 예:

```js
props.name = "이름변경"; // 불변성 위반 (오류는 나지 않지만 절대 하지 말 것)
```

✅ 권장: props를 바꾸고 싶다면 **부모 상태를 바꾸고 다시 전달**해야 함

---

## 🔄 4. props 전달 방식 다양화 (고급 실습 포함)

### ✅ 구조 분해 할당

```jsx
function Welcome({ name }) {
  return <h2>{name}님 반갑습니다!</h2>;
}
```

### ✅ 객체 통째로 전달

```jsx
const user = { name: "홍길동", age: 30 };
<UserCard {...user} />
```

> \*\*스프레드 연산자(`...`)\*\*를 활용해 props를 깔끔하게 전달

### ✅ 함수형 props 전달

```jsx
<Counter onClick={() => setCount(count + 1)} />
```

자식은 이렇게 사용:

```js
<button onClick={onClick}>+1</button>
```

### ✅ 조건부 렌더링

```jsx
<Banner isLoggedIn={true} />

function Banner({ isLoggedIn }) {
  return isLoggedIn ? <p>환영합니다</p> : <p>로그인해주세요</p>;
}
```

---

## 🧠 5. props vs state (구조적 비교)

| 항목     | `props`       | `state`        |
| ------ | ------------- | -------------- |
| 정의     | 외부로부터 전달      | 내부에서 생성, 관리    |
| 수정 가능성 | ❌ 읽기 전용       | ✅ setState로 수정 |
| 주 목적   | 컴포넌트 간 데이터 전달 | UI 상태 저장       |
| 사용 대상  | 함수의 파라미터처럼 전달 | 로컬 변수처럼 보관     |
| 소유자    | 부모            | 자신(컴포넌트 자신)    |

---

## ⚙️ 6. React 내부에서 props 처리 흐름 (Babel 이후)

```jsx
<Welcome name="홍길동" />
```

Babel 변환 결과:

```js
React.createElement(Welcome, { name: "홍길동" });
```

→ React는 컴포넌트를 호출할 때, 해당 함수에 `{ name: "홍길동" }` 객체를 첫 번째 인자로 전달함

---

## 💬 7. props로 무엇이든 전달 가능

| 데이터 타입 | 예시                               |
| ------ | -------------------------------- |
| 문자열    | `name="홍길동"`                     |
| 숫자     | `age={30}`                       |
| 불리언    | `isActive={true}`                |
| 배열     | `hobbies={['영화', '축구']}`         |
| 객체     | `user={{ id: 1, name: 'Jane' }}` |
| 함수     | `onClick={handleClick}`          |
| JSX    | `children={<strong>강조</strong>}` |

---



## ✅ 요약

| 개념      | 요약 설명                        |
| ------- | ---------------------------- |
| props란? | 부모가 자식에게 전달하는 **읽기 전용 데이터**  |
| 목적      | **컴포넌트 재사용성**과 **UI 외부화** 실현 |
| 특징      | 단방향 흐름, 불변성, 다양한 데이터 전달      |
| 고급 사용   | 구조 분해, 스프레드 전달, 함수/JSX 전달    |
| 비교      | state는 내부 데이터, props는 외부 전달용 |

---

## 🧠 한 줄 요약:

> **props는 React 컴포넌트를 함수처럼 외부에서 주어진 입력값으로 제어하는 수단이며, 컴포넌트의 재사용성과 예측 가능한 렌더링을 가능케 한다.**


