

# `useImperativeHandle`

> “컴포넌트의 내부 로직을 외부에게 **선택적으로 노출**하는 Hook”

> **문서 안내**: 이 문서는 `useImperativeHandle`의 **압축 요약판**입니다.
> 개념 설명·내부 동작·의존성 배열·TypeScript 타이핑 등 **자세한 내용은 `12.3 useImperativeHandle.md`** 를 참고하세요.
> (두 문서는 같은 주제를 다루며, 이 문서는 빠른 참조용입니다.)

> **React 19 기준 안내**
> **React 19부터 함수 컴포넌트는 `ref`를 일반 prop으로 받습니다.** → `forwardRef` **불필요** (향후 deprecated 예정)
> 아래 예제는 **React 19 방식(prop)** 을 기본으로 하고, React 18 이하 방식은 비교용으로 함께 표기합니다.

---

## O 1. 무엇인가?

```tsx
useImperativeHandle(ref, createHandle, deps)
```

> `useImperativeHandle`은 부모가 **ref를 통해 자식 컴포넌트의 메서드/값에 직접 접근**하도록
> **“공개 API”를 직접 정의**하는 Hook입니다.

### 인자 3개의 의미

| 인자             | 의미                                                                          |
| -------------- | --------------------------------------------------------------------------- |
| `ref`          | 부모가 넘긴 ref. **React 19**: `props.ref` / **React 18 이하**: `forwardRef`의 두 번째 인자 |
| `createHandle` | **팩토리 함수**. 이 함수의 **반환값이 곧 `ref.current`** 가 됩니다. (객체가 아니라 함수를 넘겨야 함)        |
| `deps`         | `useEffect`와 동일. **이 값들이 바뀔 때만** 팩토리를 다시 실행해 핸들을 교체합니다.                     |

> `deps`를 **생략하면 매 렌더마다** 핸들 객체가 새로 만들어집니다.
> 보통 `[]`(DOM ref만 참조할 때) 또는 **핸들 내부에서 읽는 state/props**를 넣습니다.
> `[]`로 두고 내부에서 state를 읽으면 **stale closure**(옛날 값)에 걸립니다.

### ⏱ 적용 타이밍

layout effect와 같은 타이밍(커밋 단계, **paint 이전**)에 적용됩니다.

```txt
렌더 → DOM 커밋 → ref 연결 / useImperativeHandle 적용 → useLayoutEffect → paint → useEffect
```

* 부모의 **렌더 중에는 `ref.current`가 `null`** 입니다. 이벤트 핸들러나 effect에서 접근하세요.

---

## 2. 일반 `ref`와의 차이

### 일반적인 `ref`는 DOM 접근 용도

```tsx
const inputRef = useRef(null);
<input ref={inputRef} />;
inputRef.current.value = 'hello';
```

### `useImperativeHandle`은 **컴포넌트의 커스텀 API** 노출 용도

```tsx
inputRef.current.focusInput(); // focusInput은 자식 컴포넌트에서 제공한 메서드
```

---

## 3. 실전 예제: CustomInput 컴포넌트

### 자식 컴포넌트 (`CustomInput.jsx`) — O React 19

```jsx
import { useImperativeHandle, useRef } from 'react';

function CustomInput({ ref, ...props }) {   // 👈 ref를 그냥 prop으로 받습니다
  const inputRef = useRef(null);

  useImperativeHandle(ref, () => ({
    focus: () => inputRef.current?.focus(),
    clear: () => {
      if (inputRef.current) inputRef.current.value = '';
    },
  }), []);                                   // 👈 deps를 명시하세요

  return <input ref={inputRef} {...props} />;
}

export default CustomInput;
```

### 같은 컴포넌트 — React 18 이하 (레거시)

```jsx
import { forwardRef, useImperativeHandle, useRef } from 'react';

const CustomInput = forwardRef((props, ref) => {   // 👈 forwardRef 필수
  const inputRef = useRef(null);

  useImperativeHandle(ref, () => ({
    focus: () => inputRef.current?.focus(),
    clear: () => {
      if (inputRef.current) inputRef.current.value = '';
    },
  }), []);

  return <input ref={inputRef} />;
});

export default CustomInput;
```

> 두 코드에서 **`useImperativeHandle` 호출부는 완전히 동일**합니다.
> 바뀐 것은 “ref를 받는 껍데기”뿐입니다.

### 부모 컴포넌트 (`App.jsx`) — 버전 무관 동일

```jsx
function App() {
  const inputRef = useRef(null);

  return (
    <>
      <CustomInput ref={inputRef} />
      <button onClick={() => inputRef.current.focus()}>포커스</button>
      <button onClick={() => inputRef.current.clear()}>지우기</button>
    </>
  );
}
```

O `ref.current`는 단순 DOM이 아니라 `{ focus, clear }` 같은 사용자 정의 API가 됨

---

## 4. 매개변수 설명

| 매개변수           | 설명                                                                 |
| -------------- | ------------------------------------------------------------------ |
| `ref`          | 부모가 넘긴 ref — **React 19**: `props.ref` / **React 18 이하**: `forwardRef`의 두 번째 인자 |
| `createHandle` | 노출할 객체를 **생성하는 함수(팩토리)**. 반환값이 `ref.current`가 됩니다.                  |
| `deps`         | 의존성 배열. **문법상 선택이지만 사실상 필수** — 생략하면 매 렌더마다 핸들이 재생성되고, `[]`인데 내부에서 state를 읽으면 **stale closure**가 발생합니다. |

---

## 5. 주요 사용 시점

| 상황                               | 설명                                              |
| -------------------------------- | ----------------------------------------------- |
| O 자식의 내부 메서드/상태를 외부에서 제어하고 싶을 때 | 모달 열기/닫기, 입력 초기화 등 |
| O 자식이 DOM 외에 커스텀 인터페이스를 제공해야 할 때 | `resetForm()`, `validate()`, `scrollToBottom()` |
| O 리팩토링 시 컴포넌트를 캡슐화하면서 제어권 유지 | 내부는 숨기고 필요한 것만 열어줌 |

---

## 주의할 점

| 항목                       | 설명                                                                                     |
| ------------------------ | -------------------------------------------------------------------------------------- |
| **ref를 받을 통로가 필요함**      | **React 18 이하**: `forwardRef`로 감싸야 함 / **React 19**: `props`에서 `ref`를 **꺼내 써야** 함 (안 꺼내면 조용히 무동작) |
| `createHandle`은 **함수**여야 함 | `useImperativeHandle(ref, { focus })` X → `useImperativeHandle(ref, () => ({ focus }), [])` O |
| `deps` 누락/오용             | 생략 시 매 렌더 재생성, `[]`인데 state 참조 시 **stale closure**                                     |
| 내부 상태/메서드는 숨겨짐           | 명시적으로 `useImperativeHandle`에서 노출한 것만 외부에서 접근 가능                                        |
| 렌더 중 `ref.current` 접근 금지 | 커밋 이후에 채워지므로 렌더 중에는 `null`. 이벤트 핸들러/effect에서 접근할 것                                     |
| **너무 많은 제어는 안티패턴**       | `useImperativeHandle`은 **명령형 탈출구(escape hatch)** 입니다. 지나치면 React의 선언적 흐름이 무너지고, 부모–자식이 호출 순서로 얽혀 디버깅이 어려워집니다. |

> **쓰기 전에 반드시 물어볼 질문**
> “이걸 **props / state / context**로 표현할 수는 없나?”
> 대부분은 가능합니다. 불가능한 경우(포커스, 스크롤, 미디어 재생, 캔버스/차트 등 **DOM의 물리적 동작**)에만 쓰세요.

---

## 내부 원리 요약

```tsx
// 부모 → ref를 자식에게 전달
<Child ref={ref} />

// 자식 → ref.current에 커스텀 객체를 명시적으로 설정
useImperativeHandle(ref, () => ({ focus, clear }))
```

* `ref.current = {...}` 대신 React가 Hook 내부에서 **제어된 할당**을 수행

---

## 고급 예제: Modal 제어

### Modal.jsx

```jsx
// ✅ React 19
import { useImperativeHandle, useState } from 'react';

function Modal({ ref }) {
  const [open, setOpen] = useState(false);

  useImperativeHandle(ref, () => ({
    show: () => setOpen(true),
    hide: () => setOpen(false),
  }), []);   // setter는 안정적이므로 [] 로 충분합니다

  if (!open) return null;

  return (
    <div className="modal">
      <p>Modal Content</p>
      <button onClick={() => setOpen(false)}>Close</button>
    </div>
  );
}

export default Modal;
```

> React 18 이하라면 `const Modal = forwardRef((props, ref) => { ... });` 로 감싸면 됩니다.
> 나머지 코드는 동일합니다.

### App.jsx

```jsx
const modalRef = useRef(null);

return (
  <>
    <button onClick={() => modalRef.current.show()}>Open Modal</button>
    <Modal ref={modalRef} />
  </>
);
```

O `Modal`은 내부 상태 `open`을 은닉하고, `show()` / `hide()`만 외부에 노출

---

## 캡슐화의 이점

> 자식 컴포넌트는 **어떻게 작동하는지**를 숨기고,
> 부모에게는 **무엇을 할 수 있는지**만 제공

 내부 구현 숨기기 + 필요한 기능만 노출 → React의 **컴포넌트 캡슐화 철학** 실현

---

## `useImperativeHandle`이 꼭 필요할까?

| 상황                              | Hook 필요 여부                                     |
| ------------------------------- | ---------------------------------------------- |
| 자식의 DOM 노드 자체가 필요 | X **ref 전달만으로 충분** (React 19: `ref` prop) |
| 단순 포커스/초기화 — 자식이 DOM을 그대로 노출해도 됨 | X 불필요. DOM ref를 그대로 넘기는 편이 단순합니다 |
| **내부 DOM 구조를 숨기고** 최소 API만 열고 싶음 | O `useImperativeHandle` 적합 |
| 자식의 **내부 state**를 부모가 트리거해야 함 | 먼저 props/state 승격(lifting)을 검토 → 안 되면 사용 |
| 데이터/값 전달 | X **props / context**로 해결하세요 |

---

## O 정리 요약

| 항목      | 설명                                                                     |
| ------- | ---------------------------------------------------------------------- |
| Hook 이름 | `useImperativeHandle(ref, () => ({...}), deps)`                        |
| 사용 조건   | **React 19**: `function C({ ref })` — `forwardRef` 불필요 / **React 18 이하**: `forwardRef` 필수 |
| 인자      | ① 부모의 ref ② **팩토리 함수**(반환값 = `ref.current`) ③ **의존성 배열**               |
| 적용 시점   | 커밋 단계, **paint 이전** (`useLayoutEffect`와 같은 타이밍)                        |
| 목적      | ref를 통해 자식의 메서드/상태를 부모에게 노출                                            |
| 핵심 가치   | 내부 구현 은닉 + 제어 포인트 선택적 공개                                               |
| 사용 예    | Form API, Modal, Carousel, Canvas, 비디오 플레이어 등                          |
| 주의점     | **명령형 탈출구** — 선언형 패턴을 훼손하지 않도록 최소한으로 사용, `deps` 정확히 명시                |
| 더 자세히   | `12.3 useImperativeHandle.md` (동일 주제의 상세판)                             |


