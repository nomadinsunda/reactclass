`window`는 단순히 `alert()`을 호출하는 객체가 아닙니다. 브라우저, 자바스크립트, 렌더링 엔진, 보안 모델 등과 **직접 연결된 핵심 인터페이스**입니다.

---

# O `window`

---

## 1. `window` 객체란?

> `window`는 브라우저 탭 하나를 대표하는 **전역 컨텍스트(Global Context)** 객체이며,
> 자바스크립트가 웹 브라우저와 상호작용할 수 있도록 브라우저에서 제공하는 **최상위 전역 객체**입니다.

### 브라우저 기반 JavaScript의 진입점이자 루트 환경

```js
console.log(window === globalThis); // true (브라우저 메인 스레드에서)
console.log(window === self);       // true (브라우저 메인 스레드에서)
```

### `window`는 어느 표준에 정의되어 있나?

* `window`는 **ECMAScript(JavaScript 언어 명세)에 없습니다.** `Window` 인터페이스는 **WHATWG HTML Living Standard**가 정의하고, 브라우저가 구현해 전역에 노출합니다.
* 반대로 `globalThis`, `Object`, `Array`, `Promise` 같은 **빌트인**은 ECMAScript 명세 소속입니다.
  → 그래서 `window === globalThis`는 "같은 객체를 두 표준이 각각의 이름으로 가리키고 있다"는 뜻입니다.
* 강의/블로그에서 흔히 쓰는 **BOM(Browser Object Model)은 표준 용어가 아닙니다.** 어떤 명세에도 "BOM"이라는 규격은 없고,
  `location` / `history` / `navigator` / `screen`처럼 문서(DOM)가 아닌 **브라우저 창 자체를 다루는 API 묶음**을 편의상 부르는 관용어일 뿐입니다. 실제 규격은 HTML Standard에 흩어져 있습니다.

> 한 걸음 더: 코드에서 만지는 `window`는 엄밀히는 `Window`가 아니라 **`WindowProxy`** 라는 얇은 래퍼입니다.
> 같은 탭에서 다른 문서로 이동하면 내부의 `Window` 객체는 **새것으로 교체**되지만 `WindowProxy`의 동일성은 유지되므로,
> iframe을 잡아 둔 참조(`const w = frame.contentWindow`)가 페이지 이동 후에도 끊기지 않고 새 문서를 가리키게 됩니다.

---

## 2. 구조적 위치

```
window
 ├── document        → 현재 로드된 HTML 문서
 ├── location        → 현재 페이지 URL 정보 (window.location.href 등)
 ├── navigator       → 사용자 브라우저 정보 (User Agent, 브라우저 종류 등)
 ├── history         → 브라우저 방문 히스토리 제어
 ├── screen          → 디스플레이 정보 (해상도 등)
 ├── localStorage    → 출처(origin) 단위 영구 저장소 (같은 출처의 모든 탭이 공유)
 ├── sessionStorage  → 출처 + 탭(브라우징 컨텍스트) 단위 저장소 (탭을 닫으면 소멸)
 ├── console         → 콘솔 API
 ├── alert, prompt   → 기본 대화 상자
 ├── setTimeout      → 타이머
 └── [기타 수백 개의 API]
```

---

## 3. 핵심적인 역할 5가지

| 역할                     | 설명                                                          |
| ---------------------- | ----------------------------------------------------------- |
| ① 전역 객체(Global Object) | 브라우저에서 JS가 실행되는 최상위 스코프                                     |
| ② 브라우저 인터페이스           | location, history, screen 등 브라우저 기능 접근                      |
| ③ 이벤트 핸들러 컨테이너         | `onload`, `onresize`, `onscroll`, `onbeforeunload` 등        |
| ④ 글로벌 변수 저장소           | `var`, `function`으로 선언한 전역 식별자는 window에 바인딩                 |
| ⑤ 타이머 및 Web API 접점     | `setTimeout`, `requestAnimationFrame`, `fetch` 등 비동기 API 포함 |

---

## 4. 전역 객체로서의 특징

```js
var a = 123;
console.log(window.a); // 123

let b = 456;
console.log(window.b); // undefined (let은 window에 등록되지 않음)
```

* `var`, `function`은 `window` 객체에 등록됩니다.
* `let`, `const`, `class`는 전역 스코프에 선언되지만 **`window`의 프로퍼티가 되지는 않습니다.**

> 흔한 오해: "`let`/`const`는 **블록 스코프라서** window에 안 붙는다"는 설명은 원리가 틀렸습니다.
> 최상위 `let`은 블록 안이 아니라 전역에 선언된 것이 맞고, 스크립트 전체에서 유효합니다.
> 진짜 이유는 **기록되는 저장소가 다르기 때문**입니다. ECMAScript의 전역 환경(Global Environment Record)은 두 칸으로 나뉩니다.
>
> * **Object Environment Record** = `window` 그 자체 → `var`, `function` 선언이 여기에 기록되므로 `window.a`로 보입니다.
> * **Declarative Environment Record** = `window`와 분리된 내부 저장소 → `let`, `const`, `class`가 여기에 기록되므로 `window.b`는 `undefined`입니다.

```js
var a = 1;
delete window.a;   // false → var로 만든 전역은 configurable: false 라서 삭제 불가

window.c = 3;
delete window.c;   // true  → 직접 대입한 프로퍼티는 삭제 가능
```

> **Vite/React 환경에서는 이 규칙조차 적용되지 않습니다.**
> `<script type="module">`(= Vite가 로드하는 모든 `.jsx`)의 최상위 코드는 전역이 아니라 **모듈 스코프**에서 실행되므로,
> 모듈 안의 `var`, `function`도 `window`에 등록되지 않습니다. 전역에 노출하려면 `window.foo = foo`처럼 **명시적으로 대입**해야 합니다.

---

## 5. 주요 프로퍼티

| 프로퍼티                              | 설명                     |
| --------------------------------- | ---------------------- |
| `document`                        | 현재 로드된 DOM 트리          |
| `location`                        | 현재 주소, 이동, 리다이렉트       |
| `history`                         | 브라우저 히스토리 컨트롤 (뒤로가기 등) |
| `navigator`                       | 브라우저 종류/기기/버전 정보       |
| `screen`                          | 모니터 해상도, 색상 등 정보       |
| `localStorage`, `sessionStorage`  | 클라이언트 저장소              |
| `frames`, `self`, `top`, `parent` | 창 간 접근 (iframe 등)      |
| `innerWidth`, `innerHeight`       | 뷰포트 크기                 |
| `devicePixelRatio`                | 화면 해상도 배율 (HiDPI 대응)   |

---

## 6. 주요 메서드

### 사용자 인터페이스

| 메서드            | 설명             |
| -------------- | -------------- |
| `alert(msg)`   | 경고창 표시         |
| `prompt(msg)`  | 입력창 표시         |
| `confirm(msg)` | 확인창 표시 (확인/취소) |

### 타이머

| 메서드                         | 설명                        |
| --------------------------- | ------------------------- |
| `setTimeout(fn, ms)`        | 일정 시간 후 함수 실행             |
| `setInterval(fn, ms)`       | 일정 간격마다 함수 실행             |
| `requestAnimationFrame(fn)` | **다음 리페인트 "직전"에 딱 1회** 실행 (프레임/VSync 동기) |

> `requestAnimationFrame`은 "리페인트 시점"이 아니라 **리페인트 직전**에 호출됩니다.
> 브라우저는 프레임을 갱신하기로 결정하면 `rAF 콜백 실행 → Style → Layout → Paint → Composite` 순으로 진행하므로,
> rAF 안에서 바꾼 DOM/스타일은 **같은 프레임에 반영**됩니다. `setTimeout(fn, 16)`으로 흉내 내면 프레임 경계와 어긋나 프레임 드랍/떨림이 생깁니다.
>
> * 콜백은 **1회용**입니다. 애니메이션을 이어가려면 콜백 안에서 다시 `requestAnimationFrame`을 호출해야 합니다.
> * 탭이 백그라운드이거나 문서가 숨겨지면(`document.visibilityState === "hidden"`) rAF는 **호출되지 않습니다.** (`setInterval`은 스로틀링될 뿐 계속 돕니다 → 백그라운드 애니메이션 누적 방지에 rAF가 유리)
> * 인자로 받는 타임스탬프는 `Date.now()`가 아니라 **`performance.now()` 기준의 고해상도 시각**입니다.

### 윈도우 제어

| 메서드              | 설명                 |
| ---------------- | ------------------ |
| `open(url)`      | 새 창 또는 탭 열기        |
| `close()`        | 현재 창 닫기 (조건 제한 있음) |
| `print()`        | 프린트 다이얼로그 띄우기      |
| `scrollTo(x, y)` | 문서 스크롤 이동          |

---

## 7. `window`는 실제로 여러 "스레드/컨텍스트"를 가질 수 있다

| 환경             | 설명                               |
| -------------- | -------------------------------- |
| 메인 스레드         | 일반적인 브라우저 JS 실행                  |
| iframe         | 각각 독립적인 window 객체                |
| Web Worker     | 별도의 전역 객체 (`self`) → `window` 없음 |
| Service Worker | `self` 존재, `window`는 없음          |

---

## 8. 보안과 window (Same-Origin Policy)

* `window.parent`, `window.top`, `window.frames[0]`으로 **다른 프레임의 window 객체 자체는 언제나 얻을 수 있습니다.**
* 다만 **교차 출처(cross-origin)** 라면 그 window의 거의 모든 멤버 접근이 차단됩니다.

> 정정 포인트 2가지
>
> 1. 이 차단은 **CORS가 아니라 동일 출처 정책(Same-Origin Policy)** 입니다. CORS는 `fetch`/`XHR`의 **네트워크 응답을 읽을 수 있는지**를 정하는 별개의 규칙입니다.
> 2. 기준은 "도메인"이 아니라 **출처(origin) = 스킴 + 호스트 + 포트** 입니다. `https://a.com`, `http://a.com`, `https://a.com:8443`은 **모두 서로 다른 출처**입니다.

```js
// A.com에서 B.com iframe을 접근하려 하면?
window.frames[0].document;       // ❌ SecurityError
window.frames[0].location.href;  // ❌ 읽기 불가 (단, 쓰기=이동시키기는 허용)

// 반면 아래는 교차 출처에서도 허용됩니다 (cross-origin 허용 목록)
window.frames[0].postMessage("hi", "https://b.com"); // ✅ 정식 통신 수단
window.frames[0].closed;                             // ✅
window.frames.length, window.top, window.opener;     // ✅
```

* 즉 교차 출처 프레임과 데이터를 주고받는 **유일한 정식 수단은 `postMessage` + `message` 이벤트**이며,
  수신 측은 반드시 `event.origin`을 검증한 뒤 데이터를 사용해야 합니다.

---

## 9. window vs globalThis vs self

| 객체           | 브라우저         | Web Worker | 의미                  |
| ------------ | ------------ | ---------- | ------------------- |
| `window` | O 있음 | X 없음 | 브라우저 전역 |
| `self` | O window와 동일 | O 전역 객체 | 범용 전역 식별자 |
| `globalThis` | O | O | ECMAScript 표준 전역 객체 |

> `globalThis`는 `window`, `self`, `global`을 추상화한 **ECMAScript 표준 전역 객체**입니다.

---

## O 요약

| 항목        | 설명                                                     |
| --------- | ------------------------------------------------------ |
| 정체        | 브라우저의 전역 객체 (탭 단위)                                     |
| 포함 관계     | `window.document`, `window.location`, `window.alert` 등 |
| 역할        | 브라우저 기능 + DOM 진입점 + 비동기 API 노출                         |
| 접근 방식     | 대부분의 전역 식별자는 window에 귀속됨 (`window.alert()`, `alert()`) |
| 다른 환경과 차이 | Node.js에서는 `global`, Web Worker에서는 `self`              |

---



## O window와 자바스크립트 관계:

> **`window` 객체는 자바스크립트가 웹 브라우저 환경의 리소스(DOM, 위치 정보, 저장소, UI 기능 등)에 접근하고 제어할 수 있도록 브라우저가 제공하는 API(인터페이스)이다.**

---

## 더 구체적으로 말하면…

* **JavaScript는 원래 브라우저와 무관한 언어**입니다.
  즉, JS 자체만으로는 DOM도 없고, alert도 없고, 화면도 없고, 쿠키도 없어요.

* 하지만 브라우저는 JavaScript를 실행할 수 있는 **실행 환경(runtime environment)** 을 제공합니다.
  그 환경이 바로 `window` 객체를 포함한 브라우저 API입니다.

---

## JavaScript vs 브라우저의 역할

| 항목     | JavaScript 자체       | 브라우저(window 등)                 |
| ------ | ------------------- | ------------------------------ |
| 연산 | 숫자 계산, 함수 호출, 반복문 등 | X |
| DOM 조작 | X | O document.getElementById |
| UI 제어 | X | O alert, confirm, prompt |
| 네트워크 | X | O fetch, XMLHttpRequest |
| 시간 | X | O setTimeout, setInterval |
| 저장소 | X | O localStorage, sessionStorage |

---

## 결론적으로:

### O "JavaScript는 문법과 계산의 도구"

### O "브라우저(`window`)는 그 JS에게 현실 세계(화면, 주소, 쿠키, 이벤트 등)를 열어주는 창구"

---

## O 한 줄 요약:

> **`window`는 자바스크립트가 브라우저 세계를 "터치"할 수 있게 해주는 브라우저 측 API 객체이다.**



