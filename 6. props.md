# 🌟 React Props: “UI 구성의 최소 단위”를 설계하는 가장 핵심 개념

React를 깊이 이해하는 개발자들은 **props를 단순히 ‘부모가 자식에게 주는 값’이 아니라**
**컴포넌트를 ‘순수 UI 함수로 만드는 핵심 메커니즘’** 으로 해석합니다.

React 팀도 여러 문서에서 반복해 말합니다.

> “컴포넌트는 `props`를 입력받아 UI를 반환하는 함수다.”

여기서 props는 단순한 옵션 값이 아니라
React 렌더링 엔진의 전체 동작 원리를 결정하는 **근본적인 abstraction**입니다.


---

# 1️⃣ Props의 본질: “Virtual DOM을 생성하는 함수의 파라미터”

React에서 모든 컴포넌트는 **함수** 입니다.

```tsx
function Button(props) {
  return <button>{props.label}</button>;
}
```

하지만 이 함수는 일반 JS 함수가 아니라
**React Reconciler(조정자)의 관리 하에 동작하는 UI 함수**입니다.

즉:

* 입력값(props)이 바뀌면
* React는 해당 컴포넌트를 다시 호출하여
* 새로운 **Virtual DOM(VDOM)** 을 생성하고
* 이전 VDOM과 비교(diffing)하여 필요한 부분만 실제 DOM에 반영합니다.

📌 결론
**props는 UI 함수의 입력값이며, React가 리렌더링을 수행할지 여부를 판단하는 기준이다.**

---

# 2️⃣ Props는 “불변(Immutable)”이어야 하는 이유 — 이것을 이해하면 렌더링이 보인다

React의 핵심 최적화는 **얕은 비교(shallow compare)** 입니다.

React.memo, PureComponent, shouldComponentUpdate 등
모든 “렌더링 최적화”는 다음 규칙에 의존합니다.

```
같은 props → 같은 UI가 나온다
```

이를 위해 props는 반드시 다음을 충족해야 합니다.

### ✔ 변경되면 **새 객체**여야 한다

리렌더링 판단 기준이 객체의 참조(reference)이기 때문입니다.

### ❌ 내부 값을 직접 바꾸는 순간 전체가 깨진다

```jsx
function User({ data }) {
  data.age++; // ❌ props 직접 변경 → 부모 state까지 오염 가능
  return <div>{data.age}</div>;
}
```

왜 문제가 되나?

* 부모가 갖고 있는 state 객체를 그대로 mutate할 수 있다.
* React.memo는 `prev.data === next.data`를 비교하는데
  내부만 바뀌면 “변경 없음”으로 판단한다.
* 최적화 로직이 모두 무너진다 → 예측 불가능한 렌더링

📌 결론
**props는 절대 변하면 안 된다.
변경이 필요하면 부모의 state에서 새로운 객체를 만들어 내려보내야 한다.**

---

# 3️⃣ React Rendering Cycle에서 props가 어떤 역할을 하는가

React는 렌더링을 다음 순서로 진행합니다.

---

## 🔹 (1) Reconciliation 단계

부모가 렌더링되면 React는 자식에게 다음과 같이 동작합니다.

1. 이전 렌더에서의 props를 저장해둔다.
2. 새 렌더에서의 props를 새로 계산한다.
3. 얕은 비교(shallow equal)를 한다.

비교 규칙:

* 기본 데이터 값(문자열, 숫자, boolean)은 값 비교
* 객체/함수/배열은 “참조” 비교

---

## 🔹 (2) Commit 단계

props가 바뀐 경우에만 컴포넌트를 다시 호출하여 새로운 VDOM을 만든다.
그 후 실제 DOM 반영(diff → patch)을 한다.

---

📌 의미
**props는 React의 렌더링 파이프라인 전체에서 변경 여부를 판단하는 핵심 값이다.
state와 다르게 자식 입장에서는 외부 입력이며, 이는 Pure Function Model을 유지한다.**

---

# 4️⃣ props.children — React가 “컴포지션을 선호하는 이유”

React의 가장 큰 철학 중 하나는 **상속 대신 컴포지션**입니다.

그 핵심 도구가 바로 `props.children` 입니다.

---

## 🔹 예: 레이아웃 컴포넌트

```
function Card({ title, children }) {
  return (
    <div className="card">
      <h3>{title}</h3>
      {children}
    </div>
  );
}
```

사용:

```
<Card title="Profile">
  <UserProfile />
  <ActionButtons />
</Card>
```

이는 다음을 의미합니다.

* Card는 단지 틀(레이아웃)만 제공
* 내부 내용은 외부에서 주입
* UI 구성이 매우 유연함
* 컴포넌트를 작은 단위로 분해하여 재사용성이 극대화됨

📌 결론
**children은 React의 진짜 강점인 “컴포넌트 조합(Composition)”을 가능하게 하는 핵심 props이다.**

---

# 5️⃣ props를 설계할 때 고려하는 4가지 기준

## ✔ 1. 불변성 유지

새로운 객체를 생성해 전달.

```jsx
<Child config={{ theme }} /> // ❌ 매번 새로운 객체
<Child config={memoizedConfig} /> // ✔ useMemo로 고정
```

## ✔ 2. 명확한 타입 정의

TypeScript로 인터페이스 설계.

```ts
interface ButtonProps {
  label: string;
  onClick?: () => void;
  variant?: "primary" | "secondary";
}
```

## ✔ 3. 역할 분리 (Presentation vs Container)

데이터를 다루는 컴포넌트와 UI만 보여주는 컴포넌트를 분리.

* Presentational: props만 받음
* Container: state + 비즈니스 로직 + props로 전달

## ✔ 4. props는 조합될 수 있어야 한다

Flexbox처럼 “조합 가능성”을 우선으로 설계.

예:

* children
* render props
* controlled/uncontrolled props

---

# 6️⃣ Render Props: 고급 컴포넌트 패턴의 핵심

다음은 props의 진정한 확장 형태입니다.

```jsx
<DataProvider url="/api/users">
  {(users) => <UserList users={users} />}
</DataProvider>
```

DataProvider는 다음처럼 동작하죠.

```jsx
function DataProvider({ url, children }) {
  const [data, setData] = useState([]);

  useEffect(() => {
    fetch(url).then((r) => r.json()).then(setData);
  }, [url]);

  return children(data); // children이 함수!
}
```

장점:

* 외부 UI 구성 방식을 0% 제한
* 로직과 UI의 완벽한 분리
* 재사용성 극대화

📌 props는 단순한 값이 아니라
**함수, 컴포넌트, 객체 등 “행동(behavior)”까지 전달하는 확장 가능한 인터페이스 역할을 한다.**

---

# 7️⃣ props와 성능 최적화: “참조 안정성(Reference Stability)”이 핵심

React는 props를 기반으로 리렌더링 여부를 판단합니다.
여기서 가장 중요한 개념은 **참조 안정성**입니다.

```jsx
const handleClick = () => console.log("clicked"); // ❌ 매 렌더마다 새 함수
```

React.memo + useCallback을 쓰면:

```jsx
const handleClick = useCallback(() => {
  console.log("clicked");
}, []);
```

* 참조가 유지됨
* 자식이 불필요하게 리렌더링되지 않음

React 애플리케이션의 성능을 잘 내는 팀들은 공통점이 있습니다.

> “props로 내려가는 함수/객체는 반드시 memoized한다.”

---

# 8️⃣ props vs context — 왜 props가 기본이고 context는 예외인가?

### ❌ 많은 초보자가 context를 props의 대체품이라고 착각

하지만 이는 명백한 오해입니다.

### ✔ context는 전역적인 Ambient Value

* 인증 정보
* 언어 설정
* 테마
* 라우터 정보

### ✔ props는 명시적, 선언적 계약

* 해당 컴포넌트가 어떤 데이터를 사용하는지 매우 명확히 드러남
* 변경 흐름도 파악 쉬움
* 테스트 가능성이 높음

React 팀이 계속 강조하는 말:

> **“기본 데이터 전달은 props, 예외적인 환경 설정은 context.”**

---

# 9️⃣ “props를 깊게 이해하면 React 전체가 보인다”

왜냐하면 props는 다음 모두와 밀접하게 연결되기 때문입니다.

* 리렌더링 조건
* React.memo
* useCallback, useMemo
* Controlled Component
* 컴포지션 패턴(children, slot-like 구조)
* 상태 끌어올리기(Lifting State Up)
* Context API
* Presentational vs Container 패턴
* React Fiber의 업데이트 스케줄링

즉, **props는 React의 철학을 가장 잘 드러내는 추상화(Abstraction)** 입니다.

---

# 🔥 결론 — 한 문장으로 요약

### 💡 **React의 props는 “UI 순수 함수를 만들기 위한 입력값”이며, **

**React 전체 렌더링 모델과 최적화 전략의 중심에 있는 핵심 개념이다.** 

