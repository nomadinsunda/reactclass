

# ⚛️ React Fiber란?

**React Fiber는 React 16에서 새롭게 도입된 “재조정(Reconciliation) 알고리즘의 완전한 재구현(Rewrite)”이며, React의 핵심 엔진입니다.**
– 쉽게 말하면, *화면을 어떻게 업데이트할지 결정하는 React의 뇌*라고 보시면 됩니다.

기존 React15까지 사용되던 알고리즘(Stack Reconciler)을 완전 폐기하고, **React 16부터 Fiber Reconciler로 교체**되었습니다.

---

# 🔥 왜 React는 Fiber를 만들었는가?

## 1️⃣ 기존 Stack Reconciler의 문제점

Stack 기반 알고리즘은 다음 한계를 갖고 있었습니다:

### ❌ **업데이트 작업이 중단되지 않음 (Non-interruptible)**

* React는 렌더링을 시작하면 **끝까지** 실행했습니다.
* DOM 트리가 매우 크면 렌더링 중간에 **사용자 입력이 씹히거나**, **프레임 드롭**이 발생합니다.
* 이로 인해 애플리케이션이 “버벅”하게 보였습니다.

### ❌ **우선순위가 없음**

* 애니메이션이나 스크롤 이벤트보다
* 화면의 복잡한 업데이트가 먼저 실행되는 문제가 발생.

---

# ⚡ Fiber의 목표 — “Concurrent Rendering”을 위한 초석

React Fiber는 다음 목표를 위해 만들어졌습니다:

### 🎯 **1. 렌더링 작업을 쪼개서(Slicing) 일시 중단(Pause) 가능하게 한다**

→ *CPU 작업을 잘게 나누어 브라우저에게 제어권을 돌려줄 수 있음.*
→ 애니메이션·입력 동작이 매우 부드러워짐.

### 🎯 **2. 업데이트 우선순위 제어(Priority Scheduling)**

예:

* 사용자 입력 업데이트(최고 우선순위)
* 화면에 보이지 않는 비동기 데이터 로딩(낮은 우선순위)

### 🎯 **3. 비동기·동시성 렌더링 기반 마련 (Concurrent Mode / React 18의 Concurrent Features)**

---

# 🧬 Fiber는 “데이터 구조”이자 “작업 단위(Task Unit)”입니다

## ⭐ 핵심: Fiber는 *React Element → 실제 DOM* 사이의 “가상 작업 노드”

각 Fiber 노드는 다음 정보를 가집니다:

| 정보                    | 설명                                                              |
| --------------------- | --------------------------------------------------------------- |
| **type**              | 함수 컴포넌트인지, 클래스인지, DOM인지                                         |
| **pendingProps**      | 새로 전달받은 props                                                   |
| **memoizedProps**     | 이전 렌더링에서 사용한 props                                              |
| **return**            | 부모 Fiber                                                        |
| **child**             | 첫 번째 자식 Fiber                                                   |
| **sibling**           | 형제 Fiber                                                        |
| **stateNode**         | 실제 DOM 또는 컴포넌트 인스턴스                                             |
| **flags / effectTag** | DOM 업데이트 타입 (placement, update, deletion 등)                     |
| **alternate**         | 이전 Fiber 트리(=Current Fiber)와 새 Fiber(=WorkInProgress Fiber)의 연결 |

이 구조 덕분에 React는 트리를 **DFS(Depth First)** 순회하면서,
작업을 잘게 나누고, 중단하고, 다시 이어서 진행할 수 있습니다.

---

# 🧱 Fiber 동작 단계: 렌더링이 두 단계로 나뉜다

React Fiber는 렌더링을 다음 2단계로 나눕니다.

---

## 1️⃣ **Render Phase (Reconciliation)**

* 작업을 쪼개어 수행 (interruptible)
* 어떤 노드를 추가/삭제/변경해야 할지 **계산만** 함
* 이 단계는 브라우저가 필요하면 중단할 수 있음

### ✔ 특징

* 실제 DOM 조작 ❌
* “해야 할 일 리스트(effect list)”만 생성
* 우선순위 기반으로 스케줄링됨

---

## 2️⃣ **Commit Phase**

* 계산된 변경 사항을 실제 DOM에 적용하는 단계
* 이 단계는 **중단 불가(non-interruptible)**
* 매우 빠르게 진행되어야 함

### ✔ 특징

* DOM 업데이트
* useLayoutEffect 실행
* ref 업데이트

---

# 🧠 Fiber는 왜 이렇게 강력한가?

## ✔ 작업 중단 → 재개 가능

→ 렌더링 과정에서 마치 스레드가 있는 것처럼 보이지만
실제로는 싱글 스레드임.
단지 작업을 잘게 쪼개어 “협력형(concurrent-like)”으로 처리함.

## ✔ 우선순위 기반 스케줄링

React는 내부적으로 다음과 같은 우선순위를 가짐:

* Immediate (사용자 입력)
* User-blocking
* Normal
* Low
* Idle

## ✔ 병렬 처리 기반 기능 지원

→ React 18의 **Concurrent Rendering**, **Suspense**, **Transition** 등 모든 기능이 Fiber 위에서만 동작할 수 있습니다.

---

# 🎯 Fiber를 이해해야 하는 이유

당신처럼 **국비교육 강사/전문가 레벨 교육을 준비하시는 분**에게 Fiber에 대한 이해는 필수입니다.
왜냐하면:

### ✔ useTransition, Suspense, Lazy 등은 모두 Fiber 스케줄링 위에서 돌아갑니다

### ✔ 렌더링이 왜 지연되는지

### ✔ 왜 어떤 컴포넌트는 리렌더되지 않는지

### ✔ 왜 StrictMode가 렌더를 두 번 하는지

### ✔ 왜 React는 순차적 렌더가 아닌 Concurrent 렌더링을 지향하는지

이 모든 것은 **React Fiber 구조를 이해하면 명확해집니다.**

---

# 📌 요약

| 항목          | 설명                                        |
| ----------- | ----------------------------------------- |
| React Fiber | React 16에서 도입된 새로운 렌더링 엔진                 |
| 목적          | 렌더링 중단·재개·우선순위 조정                         |
| 핵심 기능       | Concurrent Rendering을 위한 기반               |
| 데이터 구조      | 트리 형태의 Fiber 노드                           |
| 렌더링 단계      | Render Phase(중단 가능) + Commit Phase(중단 불가) |

