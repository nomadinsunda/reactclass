
# 🔍 React Hook 소개: 클래스 없이도 상태 관리를!

---

## 🧠 Hook이란 무엇인가요?

React Hook은 **React 16.8** 버전에서 도입된 기능으로, 클래스 컴포넌트를 작성하지 않고도 **state**와 **lifecycle 기능**을 함수형 컴포넌트에서 사용할 수 있도록 해줍니다.

이전까지는 다음과 같은 코드로 상태 관리를 했습니다:

```jsx
class Counter extends React.Component {
  state = { count: 0 };

  render() {
    return (
      <button onClick={() => this.setState({ count: this.state.count + 1 })}>
        Count: {this.state.count}
      </button>
    );
  }
}
```

그러나 Hook이 등장하면서 아래처럼 **훨씬 간결하고 가독성 좋은 코드**로 대체할 수 있게 되었습니다:

```jsx
import { useState } from 'react';

function Counter() {
  const [count, setCount] = useState(0);
  return (
    <button onClick={() => setCount(count + 1)}>
      Count: {count}
    </button>
  );
}
```

---

## 🎯 왜 Hook이 필요한가요?

Hook의 등장 배경에는 다음과 같은 이유가 있습니다:

1. **복잡한 클래스 구조 탈피**

   * 클래스는 `this` 바인딩, 메서드 정의 등 초심자에게 진입 장벽이 높습니다.
   * Hook은 함수형 컴포넌트만으로도 동일한 기능 구현이 가능합니다.

2. **로직 재사용의 어려움**

   * 클래스 컴포넌트에서는 로직 재사용을 위해 HOC(Higher-Order Component), Render Props 등 복잡한 패턴을 써야 했습니다.
   * Hook은 `Custom Hook`으로 로직을 재사용할 수 있습니다.

3. **관심사의 분리(Separation of Concerns)**

   * 하나의 lifecycle 메서드 안에서 여러 로직이 섞이는 문제가 있었습니다.
   * Hook은 `useEffect` 등을 통해 효과를 기능 단위로 분리할 수 있습니다.

---

## 🧩 Hook의 종류 (대표적인 것만 소개)

| Hook 이름       | 설명                                 |
| ------------- | ---------------------------------- |
| `useState`    | 컴포넌트 내부에 상태를 추가합니다.                |
| `useEffect`   | 컴포넌트가 렌더링될 때 부수 효과(Effect)를 처리합니다. |
| `useRef`      | DOM 요소나 변경 가능한 값을 참조할 때 사용합니다.     |
| `useContext`  | 전역 컨텍스트 값을 구독할 때 사용합니다.            |
| `useReducer`  | 복잡한 상태 로직 관리를 위해 사용합니다.            |
| `useMemo`     | 계산 결과를 메모이제이션하여 렌더링 성능 최적화         |
| `useCallback` | 함수를 메모이제이션하여 불필요한 렌더링 방지           |

---

## 💡 Hook은 어떤 철학을 가지고 있을까?

Hook의 디자인은 다음과 같은 철학에 기반합니다:

* **단순한 API로 강력한 기능 제공**
* **불변성과 선언적 UI 강조**
* **클래스를 대체할 수 있는 완전한 함수형 접근**
* **로직의 명확한 분리와 재사용성 증가**

---

## 📝 결론: Hook은 React의 새로운 표준

React Hook은 단순히 "클래스를 대체"하는 도구가 아닙니다. 그것은 **함수형 프로그래밍 스타일**을 지향하는 React의 철학을 반영한 진화의 결과입니다.

이제부터 React를 시작하는 개발자라면, 클래스보다는 **함수형 컴포넌트 + Hook 조합**으로 애플리케이션을 구성하는 것이 가장 표준적이고 현대적인 방법입니다.


