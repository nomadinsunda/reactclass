[React Hooks](https://ko.legacy.reactjs.org/docs/hooks-overview.html)



# 🎣 React Hook

React 개발을 하다 보면 한 번쯤은 들어봤을 이름, **Hook**. 2019년 초, React 16.8 버전부터 공식적으로 도입된 이 개념은 기존의 **Class 컴포넌트** 기반 개발에서 **함수형 컴포넌트 중심**으로 흐름을 전환시키는 큰 변화를 만들어냈습니다.

---

## ✅ Hook이란?

> **Hook은 React 16.8에서 도입된 새로운 기능으로, 함수형 컴포넌트에서 상태(state)와 생명주기(lifecycle) 기능을 사용할 수 있게 해줍니다.**

기존에는 상태를 관리하거나 생명주기를 제어하려면 반드시 **Class 컴포넌트**를 사용해야 했습니다. 하지만 Hook을 이용하면 **함수형 컴포넌트만으로도 복잡한 로직을 구현**할 수 있습니다.

### 예시: `useState` 사용

```jsx
import React, { useState } from 'react';

function Example() {
  const [count, setCount] = useState(0); // 상태 변수 선언

  return (
    <div>
      <p>You clicked {count} times</p>
      <button onClick={() => setCount(count + 1)}>Click me</button>
    </div>
  );
}
```

---

## 🎯 Hook의 핵심 특징

| 특징                 | 설명                                           |
| ------------------ | -------------------------------------------- |
| **선택적 도입 가능**      | 기존 코드(Class 기반)를 리팩토링하지 않고도 일부 컴포넌트에서만 사용 가능 |
| **100% 호환성**       | 기존 Class 컴포넌트와 병행 사용 가능. React의 동작을 변경하지 않음  |
| **현재 사용 가능**       | React 16.8 이상 버전이면 바로 사용 가능                  |
| **Class 제거 계획 없음** | Class 컴포넌트는 여전히 공식적으로 지원되며 제거할 계획은 없음        |

---

## 🧠 왜 Hook이 등장했는가? (동기)

Hook은 단순히 문법의 변화가 아닌, **React를 사용하며 생기는 구조적 문제들을 해결하기 위한 철학적 접근**입니다.

### 1. 상태 로직의 재사용 어려움

* 기존에는 **render props**나 \*\*HOC(Higher-Order Component)\*\*를 사용해야 상태 로직을 재사용할 수 있었습니다.
* 하지만 이러한 방식은 **컴포넌트 트리를 복잡하게 만들고**, **디버깅을 어렵게 하며**, \*\*"Wrapper Hell"\*\*을 유발합니다.

#### ✅ Hook의 해결책

→ 상태 로직을 독립적인 **커스텀 Hook**으로 추출하여 계층 구조에 영향을 주지 않고 재사용 가능하게 함.

```js
// 예: useAuth.js
function useAuth() {
  const [user, setUser] = useState(null);
  useEffect(() => { /* 로그인 처리 */ }, []);
  return user;
}
```

---

### 2. 복잡한 컴포넌트는 이해하기 어렵다

* 상태(state)와 부수효과(side effect)가 뒤섞여 있는 Class 컴포넌트는 유지보수하기 어렵습니다.
* 같은 `componentDidMount()` 안에 **이벤트 리스너 등록**과 **데이터 요청**이 함께 들어가 있는 경우가 흔합니다.

#### ✅ Hook의 해결책

→ `useEffect`, `useReducer` 등을 활용해 관련 로직을 **기능 단위로 분리**할 수 있습니다.

---

### 3. Class는 this 바인딩 등 학습 난이도가 높음

* `this` 키워드의 동작은 JavaScript 초심자에게 혼란을 주기 쉽습니다.
* 이벤트 핸들러에 대한 바인딩, 생명주기 메서드의 사용 시점 등 Class 문법 자체가 **진입 장벽**이었습니다.

#### ✅ Hook의 해결책

→ 순수 함수형 컴포넌트에서 상태와 기능을 **직관적이고 일관된 방식**으로 구현할 수 있습니다.

---

### 4. 최적화와 도구 호환성 문제

* Class는 **코드 압축(minify)**, **핫 리로딩**, **컴파일 최적화** 등에 불리합니다.
* 예컨대 Prepack이나 컴파일 기반 최적화 도구들은 함수형 코드에 더 적합합니다.

#### ✅ Hook의 해결책

→ 함수형 스타일은 이러한 최적화 도구들과 호환성이 뛰어납니다.

---

## 🧭 Hook 도입 전략: 점진적 적용

> **"모든 컴포넌트를 Hook으로 바꾸지 않아도 괜찮습니다."**

* 기존 Class 컴포넌트를 Hook으로 **일괄 변환하는 것은 권장하지 않습니다.**
* 새로 작성하는 **작은 컴포넌트부터 점진적으로 Hook을 도입**하는 것이 바람직합니다.
* 모든 팀원이 Hook에 대해 이해하고 있을 때 적용 범위를 확장하는 것이 좋습니다.

---

## 🔚 마치며

React Hook은 단순한 문법 변화가 아닌, **React 앱을 더 읽기 쉽고, 유지보수 가능하며, 재사용성 높은 코드로 이끄는 방향성**입니다.

Hook을 통해:

* 상태 로직을 더 명확하게 구조화하고,
* 부수 효과를 더 통제할 수 있으며,
* 클래스 없는 코드 스타일을 지향할 수 있습니다.

---

### 📌 참고 자료

* [React 공식 Hook 문서](https://reactjs.org/docs/hooks-intro.html)
* [Why Hooks? RFC 원문](https://github.com/reactjs/rfcs/pull/68)

