# 📘 React State

---

## ✅ 목차

1. [State란 무엇인가?](#1)
2. [함수형 컴포넌트에서의 state](#2)
3. [setState의 비동기성과 배치 처리](#3)
4. [state의 구조화와 깊은 복사 이슈](#4)
5. [복수의 state vs 단일 state 객체](#5)
6. [Derived state: 계산된 상태](#6)
7. [상태 끌어올리기 (Lifting State Up)](#7)
8. [불변성(immutability)과 state 관리 전략](#8)
9. [state와 props 차이 완벽 정리](#9)
10. [성능 최적화: React.memo, useMemo, useCallback](#10)
11. [Custom Hook으로 상태 로직 추상화](#11)
12. [비동기 state: useEffect와 통합](#12)

---

<a name="1"></a>

## 1. State란 무엇인가?

* 컴포넌트의 렌더링 결과에 영향을 주는 **동적인 데이터**
* React는 state가 변경되면 해당 컴포넌트를 **자동으로 리렌더링**함
* class형과 function형 컴포넌트 모두 state를 가질 수 있음

```jsx
import { useState } from 'react';

function Counter() {
  const [count, setCount] = useState(0); // 초기값: 0

  return (
    <div>
      <p>현재 카운트: {count}</p>
      <button onClick={() => setCount(count + 1)}>+1</button>
    </div>
  );
}
```

---

<a name="2"></a>

## 2. 함수형 컴포넌트에서의 state

* `useState(initialValue)`는 배열 `[value, setter]`를 반환
* setter 함수는 **비동기**로 작동
* 여러 state를 가질 수 있음

```jsx
const [username, setUsername] = useState('');
const [age, setAge] = useState(0);
```

---

<a name="3"></a>

## 3. setState의 비동기성과 배치 처리

```jsx
const [count, setCount] = useState(0);

// ❌ 이렇게 하면 상태가 잘못될 수 있음
setCount(count + 1);
setCount(count + 1); // 여전히 이전 count 기준

// ✅ 함수형 업데이트 사용
setCount(prev => prev + 1);
setCount(prev => prev + 1); // prev는 최신값을 반영
```

### ⚠️ React 18+에서는 자동으로 여러 setState가 batch 처리됨 (동기/비동기 구분 없이)

---

<a name="4"></a>

## 4. state의 구조화와 깊은 복사 이슈

```jsx
const [user, setUser] = useState({ name: 'Alice', age: 25 });

// ❌ 이렇게 하면 얕은 복사로 참조 문제 발생 가능
user.age = 30;
setUser(user); // React는 변경을 감지하지 못함

// ✅ 새로운 객체로 복사해야 변경 감지됨
setUser(prev => ({ ...prev, age: 30 }));
```

---

<a name="5"></a>

## 5. 복수의 state vs 단일 객체 state

| 복수의 state (`useState` 여러 개) | 하나의 객체 state          |
| --------------------------- | --------------------- |
| 개별 책임 분리                    | 관련 상태를 한 묶음으로 관리      |
| 리렌더링 최소화 가능                 | 구조 분해/복사 비용 있음        |
| `setX()` 개별 제어              | `setState({...})`로 관리 |

> **고급 설계 팁:** 상태 간 종속성이 있다면 객체화하는 것이 좋음. 독립적이라면 분리.

---

<a name="6"></a>

## 6. Derived state: 계산된 상태

```jsx
const [firstName, setFirstName] = useState('');
const [lastName, setLastName] = useState('');

const fullName = `${firstName} ${lastName}`; // derived state
```

* derived state는 가능하면 `useMemo()`나 함수 내부에서 계산하여 저장하지 말 것

---

<a name="7"></a>

## 7. 상태 끌어올리기 (Lifting State Up)

* 공통 부모가 하위 컴포넌트의 state를 소유
* props로 setter를 전달

```jsx
function Parent() {
  const [value, setValue] = useState('');
  return (
    <>
      <ChildInput value={value} onChange={setValue} />
      <ChildDisplay value={value} />
    </>
  );
}
```

---

<a name="8"></a>

## 8. 불변성(immutability)과 state 관리 전략

* React는 객체 비교를 `===`(얕은 비교)로 수행
* 성능 최적화를 위해 반드시 새로운 참조로 교체해야 함

```jsx
setArray([...array, newItem]);
setObject({ ...object, newProp: value });
```

---

<a name="9"></a>

## 9. props와 state 차이

| 항목     | props                   | state                      |
| ------ | ----------------------- | -------------------------- |
| 소유자    | 부모 컴포넌트                 | 자신                         |
| 변경 가능성 | 읽기 전용                   | 변경 가능                      |
| 목적     | 외부에서 주입                 | 내부 동작 제어                   |
| 사용 방법  | `<Comp prop="value" />` | `useState()`, `setState()` |

---

<a name="10"></a>

## 10. 성능 최적화: React.memo, useMemo, useCallback

```jsx
const memoizedValue = useMemo(() => compute(value), [value]);
const handleClick = useCallback(() => { ... }, [deps]);
```

* **React.memo(Component)** → props 변경 없으면 재렌더링 방지
* **useMemo/useCallback** → 불필요한 계산/재생성 방지

---

<a name="11"></a>

## 11. Custom Hook으로 상태 로직 추상화

```jsx
function useCounter(initial = 0) {
  const [count, setCount] = useState(initial);
  const inc = () => setCount(c => c + 1);
  const dec = () => setCount(c => c - 1);
  return { count, inc, dec };
}

function App() {
  const { count, inc, dec } = useCounter(5);
}
```

* **Hook을 통해 재사용성과 가독성 향상**

---

<a name="12"></a>

## 12. 비동기 state 처리와 useEffect

```jsx
useEffect(() => {
  fetch('/api/data')
    .then(res => res.json())
    .then(data => setData(data));
}, []); // 컴포넌트 마운트 시 한 번만 실행
```

* state는 비동기 데이터를 받아 저장하는 데 자주 사용됨
* `useEffect`와 함께 사용해야 올바른 시점에 동작

---

## 🧪 실전 연습 과제

1. 버튼 2개를 눌러 각각 독립적인 카운트 증가/감소 기능 구현
2. `useState`로 객체 상태 관리 + input을 통해 사용자 정보 업데이트
3. 부모 → 자식으로 상태 변경 함수 전달 (props로)
4. custom hook으로 `useToggle` 구현 (`true/false` 전환)

---

## 📌 마무리 요약

| 키워드        | 핵심 요약                                        |
| ---------- | -------------------------------------------- |
| `useState` | React 함수형 컴포넌트의 핵심 Hook                      |
| `setState` | 비동기이며 상태 변경 시 리렌더링 발생                        |
| 불변성        | `setState({...})`, `setState([...])` 형태 필수   |
| 성능         | `useMemo`, `React.memo`, `useCallback`으로 최적화 |
| 재사용성       | Custom Hook으로 로직 분리 가능                       |


