![Image](https://ko.redux.js.org/assets/images/ReduxDataFlowDiagram-49fa8c3968371d9ef6f2a1486bd40a26.gif)

![Image](https://redux.js.org/assets/images/ReduxAsyncDataFlowDiagram-d97ff38a0f4da0f327163170ccc13e80.gif)

![Image](https://media2.dev.to/dynamic/image/width%3D800%2Cheight%3D%2Cfit%3Dscale-down%2Cgravity%3Dauto%2Cformat%3Dauto/https%3A%2F%2Fdev-to-uploads.s3.amazonaws.com%2Fuploads%2Farticles%2F83uixo3rjc0u8x0qmf6e.png)

---

## 1⃣ configureStore란?

`configureStore`는 **Redux Toolkit**이 제공하는
 **Redux Store를 ‘실무 표준 설정’으로 한 번에 만들어 주는 함수**입니다.

```js
import { configureStore } from '@reduxjs/toolkit'

export const store = configureStore({
  reducer: {
    cardEntities,
    listidCardids
  }
})
```

핵심 요약:

> X createStore + applyMiddleware + compose
> X 미들웨어 수동 설정
> X DevTools 조건 분기
>
> O configureStore 한 줄

---

## 2⃣ configureStore가 등장한 이유 (역사적 배경)

### 예전 Redux (RTK 이전)

```js
import { createStore, applyMiddleware, compose } from 'redux'
import thunk from 'redux-thunk'

const store = createStore(
  rootReducer,
  compose(
    applyMiddleware(thunk),
    window.__REDUX_DEVTOOLS_EXTENSION__ &&
      window.__REDUX_DEVTOOLS_EXTENSION__()
  )
)
```

문제점:

* 보일러플레이트 과다
* DevTools 설정 실수 빈번
* 미들웨어 순서 실수
* 초보자 진입장벽

 이 문제를 **Redux 팀 스스로가 인정**하고 만든 해법이 `configureStore`입니다.

---

## 3⃣ configureStore의 정확한 정체

아주 중요합니다.

> **configureStore는 “새로운 상태 관리 시스템”이 아닙니다.**
> **Redux의 store 생성 로직을 감싼 ‘공식 프리셋 함수’입니다.**

결과물은 100% 표준 Redux Store이고, 동작 원리도 동일합니다.
다만 표현은 정확히 해두겠습니다.

* Redux 4.2부터 `createStore`는 **deprecated**로 표시되었고, Redux 5 / RTK 2.x에서는 `legacy_createStore`라는 별칭으로만 남아 있습니다.
* 그래서 “configureStore가 createStore를 감싼다”는 건 **개념적 설명**이고,
  실제로는 **`configureStore`가 Store를 만드는 유일한 공개 경로**라고 보는 편이 현재 기준에 맞습니다.
* deprecated라고 해서 동작이 멈추는 건 아닙니다. 다만 **새 코드에서 쓰지 말라는 공식 신호**입니다.

내부적으로 하는 일은 다음과 같습니다

---

## 4⃣ 내부에서 자동으로 해주는 것들 (핵심)

### O 1. Redux Store 생성

```txt
createStore() + combineReducers()
```

```js
configureStore({
  reducer: { a, b }
})
```

↓ 내부적으로

```js
combineReducers({ a, b })
```

---

### O 2. thunk 미들웨어 자동 포함

```js
dispatch(thunkFn)
```

이게 가능한 이유는:

```txt
redux-thunk 가 기본 포함
```

 그래서 **별도 설정 없이 비동기 dispatch 가능**

---

### O 3. Redux DevTools 자동 연결

* 개발 환경 → ON
* 운영 환경 → OFF

 조건 분기 코드 필요 X

---

### O 4. 개발용 안전장치 미들웨어 기본 포함

기본적으로 이런 것들이 들어 있습니다:

| 미들웨어              | 역할             | 동작 환경    |
| ----------------- | -------------- | -------- |
| thunk             | 비동기 action 지원  | 개발 + 운영  |
| immutableCheck    | state 직접 변경 감지 | **개발 전용** |
| serializableCheck | 직렬화 불가 값(Date, Map, Promise, 클래스 인스턴스 등) 감지 | **개발 전용** |
| actionCreatorCheck | action creator를 `dispatch(myAction)`처럼 **호출하지 않고** 넘긴 실수 감지 | **개발 전용** |

 실무에서 터질 버그를 **초기에 차단**

> 중요한 오해 방지: 검사 미들웨어들은 **개발 모드(`NODE_ENV !== 'production'`)에서만** 추가됩니다.
> 즉 **프로덕션 번들에는 들어가지 않으므로 런타임 성능 부담이 없습니다.**
> 반대로 말하면 “개발에서만 느려지는” 현상이 생길 수 있는데, state가 매우 큰 경우
> 아래처럼 임계값을 조정하거나 특정 검사를 꺼서 대응합니다.

```js
configureStore({
  reducer,
  middleware: (getDefaultMiddleware) =>
    getDefaultMiddleware({
      serializableCheck: { ignoredActions: ['some/action'] },
      immutableCheck: { warnAfter: 128 },
    }),
})
```

### O 5. 미들웨어/enhancer 커스터마이즈 (RTK 2.x 문법)

기본값을 **덮어쓰지 말고 이어붙이는 것**이 원칙입니다.

```js
configureStore({
  reducer,
  // 반드시 콜백을 쓰고, getDefaultMiddleware()의 결과에 concat/prepend
  middleware: (getDefaultMiddleware) =>
    getDefaultMiddleware().concat(logger),
  // RTK 2.0부터 enhancers도 콜백 형태만 지원
  enhancers: (getDefaultEnhancers) => getDefaultEnhancers().concat(myEnhancer),
  preloadedState,          // SSR/로컬스토리지 복원용 초기 state
  devTools: import.meta.env.DEV, // 기본값도 개발에서만 ON
})
```

```js
// ❌ 이렇게 배열을 그냥 넘기면 thunk와 검사 미들웨어가 통째로 사라집니다
middleware: [logger]
```

---

## 5⃣ configureStore vs createStore (한 눈 정리)

| 항목          | createStore | configureStore |
| ----------- | ----------- | -------------- |
| Redux 공식 추천 | X | O |
| thunk 설정    | 수동          | 자동             |
| DevTools    | 수동          | 자동             |
| 안전 검사       | 없음          | 기본 포함          |
| 실무 사용성      | 낮음          | 매우 높음          |

 **Redux 팀 공식 입장**

> “새 프로젝트에서는 createStore를 쓰지 마세요”

---

## 6⃣ 왜 `useDispatch()` / `useSelector()`가 그대로 작동하는가?

아주 중요한 연결 포인트입니다.

```js
const store = configureStore(...)
```

이 결과물은:

```txt
정통 Redux Store 객체
```

즉:

```js
store.dispatch
store.getState
store.subscribe
```

모두 그대로 존재합니다.

그래서:

```js
import { useDispatch, useSelector } from 'react-redux'
```

이 훅들이 **아무 문제 없이 작동**합니다
(react-redux는 “store 형태”만 요구합니다).

---

## 7⃣ configureStore는 “아키텍처 강제 장치”다

이게 정말 중요한 포인트입니다.

configureStore는 단순 편의 함수가 아니라:

* slice 중심 설계 유도
* thunk 기반 비동기 흐름 유도
* action 직렬화 강제
* devtools 기반 디버깅 습관 강제

 **Redux를 ‘이상적으로 쓰게 만드는 가드레일’**

---

## 8⃣ 지금 사용 중인 코드와 정확히 연결하면

```js
const store = configureStore({
  reducer: {
    cardEntities,
    listidCardids
  }
})
```

이 구조 덕분에:

```js
useSelector(({ cardEntities, listidCardids }) => ...)
```

가 **정확히 도메인 단위로 분리된 상태 접근**이 됩니다.

> 단, 이런 구조 분해 selector에서 **새 객체/배열을 만들어 반환하면** 매 렌더마다 참조가 달라져
> 불필요한 리렌더링이 발생합니다. 반환값은 원시값 위주로 하거나 `createSelector`로 메모이제이션하세요. (→ `15.6 useSelector`)

 configureStore는
**“store 구조 = 도메인 구조”를 강제**합니다.

---

## 강의용 한 줄 정의

> **configureStore는 Redux Store를
> 실무에서 안전하고 일관되게 사용하도록
> 모든 필수 설정을 기본값으로 묶어 제공하는
> Redux Toolkit의 핵심 API이다.**

