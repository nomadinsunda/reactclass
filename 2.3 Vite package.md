# Vite에 의해 생성되는 `package.json` 구성 요소 상세 설명

```json
{
  "name": "vite-react-js",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "lint": "oxlint",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.0",
    "oxlint": "^1.75.0",
    "vite": "^8.0.0"
  }
}
```

> 버전 숫자는 생성 시점의 최신값이라 계속 바뀝니다. 중요한 것은 **조합**입니다.
> `react`와 `react-dom`은 **항상 같은 메이저 버전**이어야 하고(19 + 18 섞으면 런타임 에러), `@vitejs/plugin-react`는 Vite 메이저 버전과 호환되는 것을 써야 합니다.
> 또 `"private": true`는 실수로 `npm publish`가 되는 것을 막는 안전장치이므로 지우지 마세요.
> `"type": "module"` 역시 필수입니다. 이것이 있어야 `vite.config.js`와 설정 파일들이 CommonJS가 아닌 **ESM(`import`/`export`)** 으로 해석됩니다.

---

## 1. `scripts` 섹션

| 스크립트      | 설명                                        |
| --------- | ----------------------------------------- |
| `dev`     | 개발 서버 실행 (`vite` 명령)                      |
| `build`   | 프로덕션용 정적 파일 번들 생성 (`vite build`)          |
| `lint`    | 코드 품질 검사 (`oxlint`)                        |
| `preview` | `build` 결과물을 로컬 서버로 미리보기 (`vite preview`) |

---

## 2. `dependencies`

### O `react`

* React 핵심 라이브러리
* 컴포넌트, Hooks, 상태 관리 등을 제공

### O `react-dom`

* React 컴포넌트를 실제 DOM에 렌더링하는 브릿지 역할
* `ReactDOM.createRoot(...)` 등 제공

---

## 3. `devDependencies`

### O `vite`

* Vite 본체
* 개발 서버, HMR, 빌드 등 모든 CLI 기능의 핵심
* 내부 번들러가 `vite build` 시 프로덕션 번들 생성 (Vite 7까지 Rollup, **Vite 8부터 Rolldown**)

### O `@vitejs/plugin-react`

* React를 Vite와 통합하기 위한 플러그인
* 다음을 처리:

  * **React Fast Refresh 주입** (이 플러그인의 존재 이유)
  * JSX automatic runtime 설정 (`react/jsx-runtime` 자동 주입 → `import React` 불필요)
  * 개발용 디버그 정보(`__self`, `__source`) 주입

 **자주 오해하는 부분**: "이 플러그인이 없으면 JSX가 변환되지 않는다"는 정확하지 않습니다.
`.jsx` / `.tsx` 파일의 **JSX 문법 변환 자체는 Vite 내장 트랜스파일러**(Vite 7까지 esbuild, Vite 8부터 Oxc)가 이미 해 줍니다.
플러그인이 실제로 더해 주는 것은 **상태를 유지하는 Fast Refresh**와 React 전용 개발 편의 기능입니다. 빼면 코드는 돌아가지만 파일을 저장할 때마다 페이지가 통째로 새로고침됩니다.

 **Babel 사용 여부는 버전에 따라 다릅니다.**

* plugin-react v4\~v5: 개발 중 Fast Refresh 주입에 **Babel** 사용
* plugin-react **v6부터: Babel 의존성을 제거하고 Oxc로 대체** → 설치 용량과 변환 속도가 개선됨
* 예전에는 "더 빠른 대안"으로 `@vitejs/plugin-react-swc`(Rust 기반 SWC)를 쓰기도 했지만, **Vite 8에서는 권장되지 않습니다.** 기본 플러그인이 이미 Rust(Oxc) 기반이라 속도 이점이 사라졌고, 이 플러그인 자체도 Rolldown 기반 Vite를 감지하면 `@vitejs/plugin-react`로 옮기라고 안내합니다

 그리고 **`.js` 확장자 파일 안에는 JSX를 쓸 수 없습니다.** 확장자가 `.jsx`여야 JSX로 파싱합니다.
`.js`에 JSX를 넣으면 `Failed to parse source ... you can use extension .jsx` 같은 에러가 납니다.

---

# 생성된 디렉터리 구조와 역할

```bash
vite-react-js/
├── node_modules/        # 설치된 모든 패키지들
├── public/              # 정적 자산 위치 (favicon, 이미지 등)
├── src/                 # 소스 코드
│   ├── main.jsx         # 진입점
│   └── App.jsx          # 루트 컴포넌트
├── index.html           # 앱의 HTML 진입점 (브라우저 로드용)
├── package.json         # 프로젝트 의존성 및 스크립트
├── vite.config.js       # Vite 설정 파일 (필요 시)
└── .gitignore           # Git 제외 파일 목록
```

---

# Vite가 설치한 핵심 모듈 설명 (node\_modules 내부)

| 패키지명                                    | 설명                                                  |
| --------------------------------------- | --------------------------------------------------- |
| `vite`                                  | Vite의 CLI, 서버, 번들러 기능 포함                            |
| `@vitejs/plugin-react`                  | React JSX 트랜스파일 + HMR 플러그인                          |
| `esbuild`                               | Vite 7까지의 고속 트랜스파일러 (Go 작성). **Vite 8에서는 `oxc`로 대체됨**  |
| `rollup`                                | Vite 7까지의 프로덕션 번들러. **Vite 8에서는 `rolldown`으로 대체됨**       |
| `react` / `react-dom`                   | React 앱의 핵심 라이브러리                                   |
| `@babel/*`                              | plugin-react v5까지의 Fast Refresh 주입용 (v6부터는 설치되지 않음)   |
| `debug`, `picocolors`                   | CLI 로그 출력용 유틸 (내부 의존성)                              |
| `fsevents`                              | 파일 변경 감지용 (macOS 전용 선택적 의존성)                        |
| `postcss`, `lightningcss`               | CSS 처리 / CSS 최소화용                                   |

> `css-loader`, `style-loader`는 **Webpack 전용 로더라서 Vite 프로젝트에는 설치되지 않습니다.**
> Vite는 `.css` import를 **내장 기능으로** 처리하며(개발 시엔 `<style>` 주입, 빌드 시엔 별도 `.css` 파일로 추출), 로더 설정이 아예 필요 없습니다.

---

## 참고: Vite는 Webpack과 달리…

* 기본적으로 **zero-config (설정 거의 없음)** 구조를 가짐
* 설정이 필요하면 `vite.config.js`에 Rollup-style로 설정
* `webpack.config.js`, `babel.config.js`, `tsconfig.json` 없이도 작동함

---

## 개발자가 알아야 할 필수 Vite 내부 의존성 구조

```plaintext
[ Vite 5~7 ]
Vite
 ├─ esbuild (Go)
 │   ├─ 소스 on-demand 변환 (JSX/TS)
 │   ├─ 의존성 사전 번들링
 │   └─ minify (기본값)
 ├─ Rollup (JS)
 │   └─ 프로덕션 번들
 └─ Plugin System
     └─ @vitejs/plugin-react
         └─ Babel (Fast Refresh 주입)

[ Vite 8+ ]
Vite
 ├─ Oxc (Rust)
 │   ├─ 소스 on-demand 변환 (JSX/TS)
 │   └─ minify (기본값)
 ├─ Rolldown (Rust)
 │   ├─ 의존성 사전 번들링
 │   └─ 프로덕션 번들
 ├─ Lightning CSS (Rust)
 │   └─ CSS minify
 └─ Plugin System
     └─ @vitejs/plugin-react v6
         └─ Oxc (Babel 의존성 제거됨)
```

### `node_modules/.vite` 폴더의 정체

개발 서버를 처음 띄우면 `node_modules/.vite/deps/` 안에 `react.js`, `react-dom_client.js` 같은 파일이 생깁니다.
이것이 **의존성 사전 번들링(Dependency Pre-Bundling)** 결과물입니다.

* **왜 하는가**: ① `node_modules`의 CommonJS 패키지를 브라우저가 읽을 수 있는 ESM으로 변환 ② 내부 파일이 수백 개인 패키지를 하나로 합쳐 HTTP 요청 폭증을 방지
* **언제 다시 하는가**: `package.json`의 의존성, lock 파일, `vite.config.js`가 바뀌었을 때 자동 감지
* **꼬였을 때**: `npm run dev -- --force` 로 캐시를 버리고 다시 만들면 됩니다
* `.vite`는 캐시이므로 **Git에 커밋하지 않습니다**(`node_modules` 아래라 기본 `.gitignore`에 이미 포함)

---

## 마무리 정리

| 항목                     | 설명                                   |
| ---------------------- | ------------------------------------ |
| `vite`                 | 개발 서버 + 프로덕션 번들링 핵심                  |
| `@vitejs/plugin-react` | JSX 변환 + HMR 지원                      |
| `react`, `react-dom`   | 필수 UI 라이브러리                          |
| `scripts`              | `vite`, `vite build`, `vite preview` |
| `node_modules`         | 위 모든 패키지와 플러그인의 실제 코드 저장소            |

---



# 1. `vite.config.js` 커스터마이징 예제

```js
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import path from 'path';

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
      '@components': path.resolve(__dirname, './src/components'),
    }
  },
  server: {
    port: 5173,
    open: true,
    proxy: {
      '/api': {
        target: 'http://localhost:8080',
        changeOrigin: true,
        rewrite: (path) => path.replace(/^\/api/, '')
      }
    }
  },
  build: {
    outDir: 'dist',
    sourcemap: true,
    rollupOptions: {
      output: {
        manualChunks: {
          react: ['react', 'react-dom'],
          vendor: ['axios', 'lodash']
        }
      }
    }
  }
});
```

### 주요 설정 설명

| 항목                                  | 설명                                  |
| ----------------------------------- | ----------------------------------- |
| `plugins`                           | Vite 플러그인 배열 (여기선 React 지원 플러그인 사용) |
| `resolve.alias`                     | 경로 단축 설정 (ex: `@components/Button`) |
| `server.port`                       | 개발 서버 포트                            |
| `server.proxy`                      | API 프록시 설정 (CORS 회피 등)              |
| `build.outDir`                      | 빌드 결과물 경로                           |
| `sourcemap`                         | 디버깅용 소스맵 생성                         |
| `rollupOptions.output.manualChunks` | 코드 스플리팅 명시적 설정 (캐시 최적화 목적)          |

---

# 2. Vite 내부의 Rollup 설정 구조

## `vite build` 명령을 실행하면

1. 내부적으로 `vite`는 번들러를 import (Vite 7까지 Rollup, **Vite 8부터 Rolldown**)
2. 번들 실행 함수 호출
3. Vite 기본 설정 + 사용자의 `build.rollupOptions`(Vite 8: `build.rolldownOptions`)를 머지한 설정 객체 생성
4. **코드 분석 → 트리 셰이킹 → 청크 분할 → 번들링 → minify**

> **Vite 8 옵션 이름 변경**
> `build.rollupOptions` → `build.rolldownOptions`, `worker.rollupOptions` → `worker.rolldownOptions`,
> `esbuild` → `oxc`, `optimizeDeps.esbuildOptions` → `optimizeDeps.rolldownOptions` 로 바뀌었습니다.
> 다행히 **옛 이름을 자동 변환해 주는 호환 계층**이 있어 기존 설정 파일이 즉시 깨지지는 않지만, 새로 작성할 때는 새 이름을 쓰세요.

### 기본적인 Rollup 설정 예시 (Vite 내부)

```js
{
  input: '/src/main.jsx',
  plugins: [
    viteInternalPlugin,
    reactPlugin,
    esbuildTransformPlugin,
    cssPlugin,
    ...
  ],
  output: {
    dir: 'dist',
    format: 'es',
    entryFileNames: '[name].[hash].js',
    chunkFileNames: 'chunks/[name].[hash].js',
    assetFileNames: 'assets/[name].[ext]',
    manualChunks: { ... }
  }
}
```

---

# 3. Vite 플러그인 시스템 구조

Vite의 플러그인은 **Rollup 플러그인을 기반으로 설계되었지만**, **Vite 전용 Hook도 지원**합니다.

## O Rollup 플러그인 API

* `name`: 플러그인 이름
* `resolveId(source)`: 모듈 import 경로 분석
* `load(id)`: 모듈 로드 시 동작
* `transform(code, id)`: 코드 변환 시점

## O Vite 전용 Hook

| Hook                       | 설명                |
| -------------------------- | ----------------- |
| `configureServer(server)`  | 개발 서버 인스턴스에 직접 접근 |
| `transformIndexHtml(html)` | `index.html` 조작   |
| `handleHotUpdate(context)` | HMR 핸들링 커스터마이징    |

---

## 예: Vite 전용 플러그인 만들기

```js
export default function myLoggerPlugin() {
  return {
    name: 'vite:my-logger',
    transform(code, id) {
      if (id.endsWith('.jsx')) {
        console.log(`[Transform] ${id}`);
      }
      return null;
    },
    handleHotUpdate(ctx) {
      console.log(`[HMR] ${ctx.file}`);
    }
  };
}
```

* `transform`: 특정 파일 타입을 감지하여 로그 출력
* `handleHotUpdate`: 파일 변경 시 커스텀 동작 가능

---

# 정리: Vite 고급 구성 핵심 요약

| 항목                    | 설명                                   |
| --------------------- | ------------------------------------ |
| `vite.config.js`      | Vite의 모든 설정 엔트리포인트, Rollup 설정도 포함    |
| `build.rollupOptions` | Vite → Rollup으로 전달되는 옵션              |
| Vite Plugin           | Rollup 플러그인 기반 + 자체 Hook도 지원         |
| 수동 청크 설정              | 큰 라이브러리(react 등)를 별도 청크로 분리 → 캐싱 최적화 |
| 프록시 설정                | CORS 우회 또는 백엔드 API 프록시 필요 시 활용       |

