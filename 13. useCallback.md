# 🧠 `useCallback` 

---

## ✅ 1. 정의: `useCallback`이란?

```tsx
const memoizedCallback = useCallback(fn, deps);
```

React의 `useCallback`은 **콜백 함수의 레퍼런스(identity)를 메모이제이션**하여 **불필요한 함수 재생성을 방지**하는 Hook입니다.

> `useMemo`가 "값"을 기억한다면, `useCallback`은 **"함수 정의"** 를 기억합니다.

---

## 🔍 2. 왜 함수의 참조값을 기억해야 하나요?

리액트 컴포넌트는 매 렌더링 시 `function` 내부의 모든 함수를 **새로 생성**합니다.

```tsx
const handleClick = () => console.log('clicked');
```

위와 같은 코드도 렌더링마다 **새로운 함수 객체**가 만들어짐.

이로 인해:

* 자식 컴포넌트에 **props로 전달된 함수**는 매번 변경된 것으로 판단됨
* `React.memo`, `useEffect`, `useMemo` 등에서 **불필요한 실행 유발**

---

## ✅ 3. 사용 예제

### ❌ 문제 코드: 렌더링마다 함수 새로 생성됨

```tsx
const App = () => {
  const [count, setCount] = useState(0);

  const increment = () => setCount(prev => prev + 1);

  return <Child onClick={increment} />;
};
```

`Child` 컴포넌트가 `React.memo`로 감싸져 있어도, `increment`는 매번 새로운 함수 → 리렌더링 발생

---

### ✅ 해결: `useCallback` 사용

```tsx
const increment = useCallback(() => {
  setCount(prev => prev + 1);
}, []);
```

✅ 이제 `increment`는 **항상 동일한 함수 객체**를 유지
✅ `React.memo`된 자식 컴포넌트가 **불필요하게 리렌더링되지 않음**

---

## ⚡ 4. 내부 동작 원리

React는 `useCallback(fn, deps)`을 만나면 다음과 같이 처리합니다:

* 최초 실행 시 `fn`을 저장
* 다음 렌더링 때 `deps` 배열의 값이 모두 동일하면 → 이전 `fn` 재사용
* `deps` 값 중 하나라도 달라지면 → 새 `fn` 생성 및 저장

> 🔁 `deps`가 상태 추적에 핵심입니다. (정확하지 않으면 stale closure 문제 발생)

---

## 🔄 5. `useMemo`와의 비교

| 항목    | `useCallback` | `useMemo`        |
| ----- | ------------- | ---------------- |
| 반환값   | **함수**        | 값 (숫자, 배열, 객체 등) |
| 목적    | 함수 참조 보존      | 값 계산 결과 보존       |
| 사용 시기 | 함수 props 최적화  | 계산 비용 최적화        |

```tsx
const fn = useCallback(() => doSomething(a, b), [a, b]);
const value = useMemo(() => compute(a, b), [a, b]);
```

---

## 🧩 6. 실전 예제: `React.memo` 자식과 함께 사용

```tsx
const Child = React.memo(({ onClick }) => {
  console.log('Child 렌더링');
  return <button onClick={onClick}>+</button>;
});
```

```tsx
const App = () => {
  const [count, setCount] = useState(0);

  const increment = useCallback(() => {
    setCount(c => c + 1);
  }, []);

  return <Child onClick={increment} />;
};
```

✅ `Child`는 `onClick`이 동일하기 때문에 리렌더링되지 않음
✅ 성능 최적화 효과 발생

---

## 📎 7. 주의사항

| 항목               | 주의점                                  |
| ---------------- | ------------------------------------ |
| ❌ 의존성 누락         | 내부에서 사용하는 모든 상태/props를 `deps`에 넣어야 함 |
| ❌ 무조건 사용         | 연산량 적은 함수에 사용하면 성능 오히려 나쁠 수 있음       |
| ⚠️ stale closure | `deps`가 부정확하면 오래된 값 참조 → 버그 유발 가능    |

### 예시 (잘못된 의존성)

```tsx
const handle = useCallback(() => {
  console.log(count); // ❌ count 의존성 누락
}, []);
```

✅ 올바르게는 `[count]` 포함

---

## 🧠 8. 요약

| 항목    | 설명                                                          |
| ----- | ----------------------------------------------------------- |
| 역할    | 함수의 참조값(identity)을 고정                                       |
| 목적    | 불필요한 렌더링/재계산 방지                                             |
| 사용 시기 | 자식 컴포넌트에 콜백 전달할 때, 혹은 `useEffect`, `useMemo`에서 함수 의존성 필요할 때 |
| 주의사항  | 의존성 정확히 관리, 남용하지 않기                                         |

---

## ✅ 언제 꼭 쓰면 좋을까?

* `React.memo` 자식에게 **콜백 함수 props**를 넘길 때
* `useEffect`, `useMemo`, `useImperativeHandle` 등에서 함수가 **의존성으로 들어갈 때**
* `Context.Provider`에 고정된 콜백을 넘기고 싶을 때

