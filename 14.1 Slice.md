**“슬라이스(slice)”를 제대로 이해하면 Redux의 80%를 이해한 겁니다.**

---

## 1️⃣ 한 줄 정의부터

> **Slice란, _Redux 전체 상태(Store)의 한 조각_을 담당하는 모듈입니다.**
> 이 조각 안에는
> **① 해당 상태의 초기값 + ② 그 상태를 바꾸는 reducer들 + ③ 자동 생성된 action들**이 함께 들어 있습니다.

---

## 2️⃣ 왜 “slice(조각)”라는 이름일까? 🍰

Redux Store는 **하나의 큰 상태 객체**입니다.

```js
state = {
  auth: { ... },
  todos: { ... },
  ui: { ... }
}
```

여기서:

* `auth` → 인증 관련 상태 덩어리
* `todos` → 할 일 관련 상태 덩어리
* `ui` → 필터, 검색어 같은 UI 상태

👉 이 **각 덩어리 하나하나가 slice** 입니다.

### 비유하면:

* Store = 🍰 **케이크 전체**
* Slice = 🍰 **케이크 한 조각**

---

## 3️⃣ Slice가 생기기 전 (구식 Redux)

예전 Redux 방식에서는 파일이 이렇게 쪼개졌습니다:

```
todos.types.js
todos.actions.js
todos.reducer.js
```

```js
// action type
export const ADD_TODO = 'ADD_TODO'

// action creator
export const addTodo = (text) => ({
  type: ADD_TODO,
  payload: text,
})

// reducer
export function todosReducer(state = [], action) {
  switch (action.type) {
    case ADD_TODO:
      return [...state, action.payload]
    default:
      return state
  }
}
```

❌ 문제점:

* 관련 코드가 **흩어짐**
* 보일러플레이트 과다
* 실무에서 유지보수 지옥

---

## 4️⃣ Slice의 핵심 아이디어 (Redux Toolkit)

Redux Toolkit은 이렇게 말합니다:

> **“같은 도메인의 상태, 액션, 리듀서는 한 파일에 둬라.”**

그 결과가 **`createSlice()`** 입니다.

---

## 5️⃣ Slice의 실제 구조 (정확히 이것들로 구성됨)

```js
const slice = createSlice({
  name: 'todos',        // slice 이름 (action type prefix)
  initialState: {...},  // 이 slice가 담당하는 state
  reducers: {...},      // 동기 상태 변경 로직
  extraReducers: ...    // 비동기(thunk) 처리 결과
})
```

즉, **Slice = 하나의 작은 Redux 모듈**입니다.

---

## 6️⃣ Slice 코드 해부 (한 줄씩 뜯어보기)

```js
const todosSlice = createSlice({
  name: 'todos',
```

### 🔹 name

* action type의 prefix가 됩니다
* 예: `todos/todoAdded`

---

```js
  initialState: {
    ids: [],
    entities: {}
  },
```

### 🔹 initialState

* `state.todos`의 **초기값**
* 이 slice가 **책임지는 상태의 범위**

---

```js
  reducers: {
    todoAdded(state, action) {
      state.ids.push(action.payload.id)
      state.entities[action.payload.id] = action.payload
    },
```

### 🔹 reducers

* **“이 slice의 상태를 바꾸는 방법들”**
* 각 함수 하나가:

  * reducer + action creator **동시에 생성**

```js
todosSlice.actions.todoAdded()
// { type: 'todos/todoAdded', payload: ... }
```

> ⭐ 이게 slice의 핵심 마법입니다.

---

```js
  extraReducers: (builder) => {
    builder.addCase(fetchTodos.fulfilled, ...)
  }
```

### 🔹 extraReducers

* **다른 slice / thunk에서 발생한 action**을 처리
* 특히 `createAsyncThunk` 결과 처리용

---

## 7️⃣ Slice가 만들어주는 것들 (자동 생성 결과)

```js
const todosSlice = createSlice(...)
```

이 한 줄로 생성되는 것:

### 1️⃣ reducer (기본 export)

```js
export default todosSlice.reducer
```

→ Store에 등록

---

### 2️⃣ action creators

```js
export const { todoAdded, todoRemoved } = todosSlice.actions
```

→ 컴포넌트에서 `dispatch(todoAdded())`

---

### 3️⃣ action type 문자열 (자동)

```
todos/todoAdded
todos/todoRemoved
```

→ 사람이 직접 관리 안 해도 됨

---

## 8️⃣ Slice와 Store의 관계 (이게 진짜 중요)

```js
configureStore({
  reducer: {
    todos: todosReducer,
    auth: authReducer,
  }
})
```

여기서 의미는:

```js
state.todos === todosSlice.initialState
state.auth === authSlice.initialState
```

👉 **slice 이름이 곧 state의 한 레벨을 결정합니다.**

---

## 9️⃣ Slice는 “Reducer 하나”가 아닙니다 ❌

많이 헷갈리는 오해:

> ❌ “slice = reducer 아닌가요?”

### ❌ 틀림

### ✅ 정확한 정의

> **Slice = reducer + action + initialState + 도메인 경계**

Reducer는 slice의 **구성 요소 중 하나**일 뿐입니다.

---

## 🔟 언제 slice를 나누는가? (실무 기준)

### ✅ 도메인 기준으로 나눕니다

```
authSlice   → 로그인 / 로그아웃 / 토큰
userSlice   → 사용자 정보
boardSlice  → 게시판 / 보드
cardSlice   → 카드
uiSlice     → 필터 / 모달
```

### ❌ 이런 기준은 안 좋습니다

* 화면 기준
* 컴포넌트 기준
* 파일 크기 기준

---

## 11️⃣ slice vs useReducer (개념 연결)

| 구분       | useReducer | Redux Slice     |
| -------- | ---------- | --------------- |
| 범위       | 컴포넌트 로컬    | 앱 전역            |
| dispatch | 컴포넌트 내부    | 어디서든            |
| reducer  | 직접 작성      | createSlice로 생성 |
| 공유       | 불가         | 가능              |

👉 **Slice는 “전역 useReducer”의 정식 아키텍처 버전**입니다.

---

## 12️⃣ 한 문장으로 다시 정리

> **Slice란, Redux Store 전체 상태 중 “하나의 도메인 책임을 가진 상태 조각”이며,
> 그 상태를 바꾸는 규칙(reducer)과 액션(action)을 한 파일에 묶어둔 단위입니다.**

