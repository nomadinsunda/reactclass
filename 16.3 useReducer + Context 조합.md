`useReducer` + `Context` 조합은 **Redux 없이도 구조적이고 확장 가능한 상태관리 시스템**을 React 내부에서 구현할 수 있는 강력한 패턴입니다.

이번엔 이를 바탕으로 **실전 예제**를 하나 만들어드리겠습니다.

---

# 목표

> O 전역 상태로 사용자 목록 관리
> O `useReducer`로 액션 기반 상태 전이
> O `Context`로 어디서든 접근 가능
> O 컴포넌트는 `useContext`만으로 상태/디스패치 사용

---

# 프로젝트 구조

```
src/
├── context/
│   ├── UserContext.jsx     ← Context + Reducer 정의
├── components/
│   ├── UserList.jsx        ← 상태 읽기
│   ├── UserControls.jsx    ← 상태 수정 (추가/삭제)
├── App.jsx
```

---

## O 1. `UserContext.jsx`

```tsx
import { createContext, useContext, useReducer } from 'react';

// 🧩 상태 초기값
const initialState = {
  users: []
};

// 🧩 액션 기반 reducer
function userReducer(state, action) {
  switch (action.type) {
    case 'ADD_USER':
      return { users: [...state.users, action.payload] };
    case 'REMOVE_USER':
      return { users: state.users.filter(u => u.id !== action.payload) };
    default:
      throw new Error(`Unhandled action: ${action.type}`);
  }
}

// 🧩 Context 생성
// 기본값을 명시적으로 null로 → "Provider를 안 감쌌다"를 아래 커스텀 훅에서 잡아낼 수 있음
const UserStateContext = createContext(null);
const UserDispatchContext = createContext(null);

// 🧩 Provider 컴포넌트
export function UserProvider({ children }) {
  const [state, dispatch] = useReducer(userReducer, initialState);

  // React 19: Context 객체 자체를 Provider로 렌더링 (<X.Provider>는 향후 deprecated 예정)
  // state는 reducer가 만들어준 참조를 그대로, dispatch는 애초에 참조가 고정 → useMemo 불필요
  return (
    <UserStateContext value={state}>
      <UserDispatchContext value={dispatch}>
        {children}
      </UserDispatchContext>
    </UserStateContext>
  );
}

// 🧩 커스텀 훅: 상태 구독
export const useUserState = () => {
  const context = useContext(UserStateContext);
  // ⚠️ if (!context) 로 쓰면 state가 0, '', false 같은 falsy 값일 때 오작동합니다.
  //    "Provider 없음"만 걸러야 하므로 반드시 null 비교로!
  if (context === null) throw new Error('useUserState must be used within UserProvider');
  return context;
};

// 🧩 커스텀 훅: 디스패치 구독
export const useUserDispatch = () => {
  const context = useContext(UserDispatchContext);
  if (context === null) throw new Error('useUserDispatch must be used within UserProvider');
  return context;
};
```

> **`createContext()` 를 인자 없이 호출하면** 기본값은 `undefined`가 됩니다. 동작은 하지만
> “기본값을 의도적으로 비워뒀다”는 신호가 코드에 남지 않으니, 위처럼 `createContext(null)` 로 명시하는 편이 좋습니다.
> 그리고 기본값은 **Provider가 트리에 하나도 없을 때만** 쓰인다는 점을 기억하세요.
> Provider가 있으면 그 `value`가 `undefined`여도 그대로 내려갑니다.

---

## O 2. `UserList.jsx` (전역 상태 읽기)

```tsx
import { useUserState } from '../context/UserContext';

function UserList() {
  const { users } = useUserState();

  return (
    <ul>
      {users.map(user => (
        <li key={user.id}>{user.name}</li>
      ))}
    </ul>
  );
}

export default UserList;
```

---

## O 3. `UserControls.jsx` (전역 상태 수정)

```tsx
import { useUserDispatch } from '../context/UserContext';

function UserControls() {
  const dispatch = useUserDispatch();   // 참조가 고정되어 있음 (아래 설명 참고)

  const addUser = () => {
    const newUser = {
      // Date.now()는 빠르게 연타하면 같은 값이 나올 수 있어 key 중복 위험이 있습니다.
      id: crypto.randomUUID(),
      name: `User ${Math.floor(Math.random() * 100)}`
    };
    dispatch({ type: 'ADD_USER', payload: newUser });
  };

  const removeUser = () => {
    const id = prompt('삭제할 ID 입력');
    if (!id) return;                     // 취소(null) / 빈 문자열 방어
    dispatch({ type: 'REMOVE_USER', payload: id });
  };

  return (
    <div>
      <button onClick={addUser}>+ 사용자 추가</button>
      <button onClick={removeUser}>– 사용자 삭제</button>
    </div>
  );
}

export default UserControls;
```

> **타입 불일치 함정 (자주 터지는 버그)**
> `prompt()`의 반환값은 **항상 문자열**입니다.
> 만약 `id`를 `Date.now()`처럼 **숫자**로 만들어 두고 `payload: prompt(...)` 를 그대로 넘기면,
> reducer의 `u.id !== action.payload` 비교가 `123 !== '123'` → **항상 true**가 되어
> **아무것도 삭제되지 않습니다.** (에러도 안 나서 더 찾기 힘듭니다.)
> 위 코드는 `crypto.randomUUID()`로 id를 문자열로 통일해 이 문제를 없앴습니다.
> 숫자 id를 유지하고 싶다면 `payload: Number(id)` 로 변환해서 넘기세요.

> 실무에서는 `prompt`(브라우저 모달) 대신 `UserList`의 각 항목에 삭제 버튼을 두고
> `dispatch({ type: 'REMOVE_USER', payload: user.id })` 처럼 **id를 직접 전달**하는 편이 훨씬 안전합니다.

---

## O 4. `App.jsx`

```tsx
import { UserProvider } from './context/UserContext';
import UserList from './components/UserList';
import UserControls from './components/UserControls';

function App() {
  return (
    <UserProvider>
      <h1>👥 사용자 목록</h1>
      <UserControls />
      <UserList />
    </UserProvider>
  );
}

export default App;
```

---

# 왜 Context를 **두 개**로 쪼갰을까?

이 예제에서 가장 중요한 설계 결정입니다. 하나로 합치면 이렇게 됩니다.

```tsx
// ❌ 합쳐놓은 버전
<UserContext value={{ state, dispatch }}>
```

이러면 `UserControls`처럼 **dispatch만 필요한 컴포넌트**도 `state`를 구독하게 되어,
사용자가 추가될 때마다 같이 리렌더됩니다. 게다가 `{ state, dispatch }` 객체 리터럴은
`UserProvider`가 리렌더될 때마다 **새 참조**라서, 결국 모든 소비자가 리렌더됩니다.

쪼개면:

| 컨텍스트                  | 값          | 언제 바뀌나              | 소비자             |
| --------------------- | ---------- | ------------------- | --------------- |
| `UserStateContext`    | `state`    | 액션이 디스패치될 때마다       | `UserList` (읽기) |
| `UserDispatchContext` | `dispatch` | **절대 안 바뀜**         | `UserControls` (쓰기) |

### `dispatch`는 메모이제이션이 필요 없습니다

`useReducer`가 돌려주는 `dispatch`는 **컴포넌트가 살아있는 동안 참조가 절대 바뀌지 않습니다.**
(`useState`의 `setState`와 동일한 보장입니다.)

* 그래서 `useMemo`/`useCallback`으로 감쌀 필요가 **전혀 없습니다.**
* `UserDispatchContext`의 value는 항상 같은 참조 → `UserControls`는 사용자가 아무리 추가돼도 **리렌더되지 않습니다.**
* 반대로 `state`는 객체지만 `useReducer`가 만들어준 값이라 그대로 넘기면 됩니다.
  (`value={{ ...state }}` 처럼 **감싸지 마세요.** 매번 새 참조가 되어 최적화가 무너집니다.)

> 정리: “state와 dispatch를 별도 컨텍스트로 분리” = **읽기 전용 컴포넌트와 쓰기 전용 컴포넌트의 리렌더를 분리**하는 기법.

---

# O 장점 요약

| 항목               | 효과                     |
| ---------------- | ---------------------- |
| `useReducer`     | 상태 전이를 액션 중심으로 깔끔하게 구성 |
| `Context`        | 전역으로 상태/dispatch 공유    |
| state/dispatch 분리 | dispatch만 쓰는 컴포넌트는 state 변경에 리렌더되지 않음 |
| 분리된 커스텀 훅        | 코드 재사용, 테스트 용이         |
| Redux 없이도 구조적 관리 | 작은 앱에 매우 적합한 상태관리 솔루션  |

---

## 확장 가능성

* **복수 Context로 모듈화 가능** (`UserContext`, `ThemeContext`, 등)
* **비동기 작업 도입**: dispatch + `useEffect` or `custom hook`
* **TypeScript로 타입 안전한 액션/상태 정의**
* **React DevTools의 Context 디버깅도 가능**

---

## 이 패턴의 한계 (언제 Redux/Zustand로 넘어갈까?)

“Redux 없이도 된다”가 “Redux가 필요 없다”는 뜻은 아닙니다. 경계선을 정확히 알아두세요.

* **부분 구독이 안 됩니다.**
  `useUserState()`는 `state` 전체를 구독합니다. `users.length`만 쓰는 컴포넌트도 배열이 바뀌면 리렌더됩니다.
  React 19에도 공식 `useContextSelector`는 없습니다.
* **`React.memo`로 못 막습니다.**
  `memo`는 props만 비교하는데, 컨텍스트 구독은 props가 아닙니다.
  컨텍스트를 읽는 컴포넌트는 memo로 감싸도 value가 바뀌면 반드시 리렌더됩니다.
* **미들웨어/DevTools 타임트래블이 없습니다.**
  액션 로깅, 비동기 미들웨어, 상태 스냅샷 이동 같은 인프라는 직접 만들어야 합니다.

판단 기준을 한 줄로:

> Context는 **상태 관리 라이브러리가 아니라 의존성 주입(DI) 도구**입니다.
> “값을 어디에 담을까”가 아니라 “**어느 서브트리까지 내려보낼까**”를 정하는 장치죠.

| 상황                                    | 권장                     |
| ------------------------------------- | ---------------------- |
| 업데이트가 드문 도메인 상태 (auth, cart, 설정 폼) | O `useReducer` + Context |
| 소비자가 많고 **초당 여러 번 바뀌는** 상태 (드래그, 입력값) | X Zustand / Redux 등 selector 기반 store |
| 서버 데이터 캐싱·재검증 | X TanStack Query 등 |
| 미들웨어·DevTools·엄격한 액션 규약이 필요 | X Redux Toolkit |
