리액트의 **useReducer**는 사실상 “컴포넌트 안에 들어있는 미니 상태 머신(state machine) 훅”이라고 보시면 됩니다.
단순 카운터를 넘어서 **복잡한 상태 / 여러 액션 / 명확한 상태 전이**를 표현할 때 진가가 드러나요.

아래에서는

1. 왜 useState 말고 useReducer인가?
2. 기본 문법과 동작 원리
3. 대표 예제들 (카운터, 폼, Todo)
4. lazy init, 성능/구조적 장점
5. Context와 결합해 “미니 Redux” 만들기
6. 내부 구현( Fiber / hook.memoizedState 연관 )
7. Redux와 비교
8. 자주 하는 실수 & 팁

까지 파보겠습니다.

---

## 1. 왜 useState 말고 useReducer인가?

### 1-1. useState로는 점점 코드가 망가지는 순간

처음에는 이렇게 시작하죠:

```jsx
const [title, setTitle] = useState('');
const [content, setContent] = useState('');
const [isSubmitting, setIsSubmitting] = useState(false);
const [error, setError] = useState(null);
```

조금만 복잡해져도:

* 한 이벤트에서 **여러 state를 동시에** 업데이트
* 로딩 → 성공 → 실패와 같이 **“상태 전이”**가 명확한 흐름 필요
* “취소, 리셋, 되돌리기” 같은 **액션들이 많아질 때**

이때부터 `setXXX()`가 사방에서 난무하고,
“이 액션이 오면 어떤 state들이 어떻게 바뀌어야 하지?”를 추적하기가 어려워집니다.

### 1-2. useReducer가 해결하는 것

useReducer는 이런 상황에서:

* 상태를 하나의 **state 객체**로 묶고
* 상태 변경을 **reducer 함수** 한 곳에 모아서
* “액션 → 상태 전이”를 **명시적으로** 표현합니다.

즉,

> “이벤트 발생 시, dispatch로 액션을 던지고,
> 상태가 어떻게 바뀌는지는 reducer에서 체계적으로 관리한다”

라는 구조를 강제하는 훅입니다.
이게 구조적 설계와 유지보수 관점에서 굉장히 큰 장점이에요.

---

## 2. useReducer 기본 문법

기본 시그니처는 다음과 같습니다:

```ts
const [state, dispatch] = useReducer(reducer, initialArg, init?)
```

* **reducer**: `(state, action) => newState`
* **state**: 현재 상태 값 (숫자, 객체, 배열 다 가능)
* **dispatch**: 액션을 reducer로 전달하는 함수
* **initialArg**: 초기 상태 값 또는 초기화에 필요한 값
* **init?**: (선택) lazy 초기화를 위한 함수

예를 들어:

```jsx
function reducer(state, action) {
  switch (action.type) {
    case 'increment':
      return { count: state.count + 1 }
    case 'decrement':
      return { count: state.count - 1 }
    case 'reset':
      return { count: 0 }
    default:
      throw new Error(`Unhandled action type: ${action.type}`)
  }
}

const [state, dispatch] = useReducer(reducer, { count: 0 })
```

여기서 중요한 포인트:

1. **reducer는 순수 함수**여야 합니다.

   * 같은 `state`와 `action`이 들어오면 항상 같은 `newState`를 반환
   * 내부에서 `fetch`, `console.log`, DOM 조작 등을 하면 안 됨 (side effect 금지)
2. **state를 직접 mutate하면 안 됨**

   * `state.count++` 같은 건 금지
   * 항상 **새로운 객체/배열**을 만들어 반환해야 함
   * (Redux Toolkit의 `createSlice`와 달리 **React의 `useReducer`에는 Immer가 없습니다.**
     여기서는 스프레드 등으로 직접 불변 업데이트를 해야 합니다. → `15.2 Immer`)
3. **개발 모드 Strict Mode에서 reducer는 의도적으로 두 번 호출됩니다**

   * React 19의 `<StrictMode>`는 순수하지 않은 reducer를 찾아내려고 reducer(와 초기화 함수)를 **개발 환경에서만 두 번** 호출합니다.
   * 순수 함수라면 결과가 같으므로 아무 문제가 없고, "왜 로그가 두 번 찍히지?"는 대부분 이 때문입니다.
   * 프로덕션 빌드에서는 한 번만 호출됩니다.

---

## 3. 가장 기본적인 예제: 카운터

### 3-1. 코드

```jsx
import { useReducer } from 'react'

function counterReducer(state, action) {
  switch (action.type) {
    case 'increment':
      return { count: state.count + 1 }
    case 'decrement':
      return { count: state.count - 1 }
    case 'reset':
      return { count: 0 }
    default:
      throw new Error(`Unknown action type: ${action.type}`)
  }
}

function Counter() {
  const [state, dispatch] = useReducer(counterReducer, { count: 0 })

  return (
    <div>
      <p>현재 카운트: {state.count}</p>
      <button onClick={() => dispatch({ type: 'decrement' })}>-1</button>
      <button onClick={() => dispatch({ type: 'increment' })}>+1</button>
      <button onClick={() => dispatch({ type: 'reset' })}>리셋</button>
    </div>
  )
}

export default Counter
```

### 3-2. 동작 흐름

1. 처음 렌더링 시
   `useReducer(counterReducer, { count: 0 })`
   → state는 `{ count: 0 }`로 초기화, `dispatch` 함수 생성

2. 버튼 클릭 시

   ```js
   dispatch({ type: 'increment' })
   ```

   * React 내부의 `dispatch` 구현이

     * 현재 state와 action을 reducer에 전달
     * `const newState = reducer(prevState, action)`
     * `Object.is(prevState, newState)`가 false면 **re-render** 스케줄링

3. 컴포넌트 재실행

   * 같은 순서의 useReducer 훅을 다시 읽고
   * 새로운 state 값(`{ count: 1 }`)을 반환

이 패턴은 Redux와 거의 동일한 mental model이라,
나중에 Redux 학습/강의에도 자연스럽게 연결됩니다.

---

## 4. 액션 설계와 reducer 패턴

현업/강의에서 주로 사용하는 액션 형태는 다음과 같습니다:

```ts
type Action =
  | { type: 'add_todo'; payload: { text: string } }
  | { type: 'toggle_todo'; payload: { id: number } }
  | { type: 'remove_todo'; payload: { id: number } }
```

### 4-1. switch-case 패턴 (가장 일반적)

```jsx
function todoReducer(state, action) {
  switch (action.type) {
    case 'add_todo': {
      const newTodo = {
        id: Date.now(),
        text: action.payload.text,
        completed: false,
      }
      return { ...state, todos: [...state.todos, newTodo] }
    }
    case 'toggle_todo': {
      return {
        ...state,
        todos: state.todos.map((todo) =>
          todo.id === action.payload.id
            ? { ...todo, completed: !todo.completed }
            : todo
        ),
      }
    }
    case 'remove_todo': {
      return {
        ...state,
        todos: state.todos.filter(
          (todo) => todo.id !== action.payload.id
        ),
      }
    }
    default:
      throw new Error(`Unknown action type: ${action.type}`)
  }
}
```

### 4-2. 객체 매핑 패턴 (switch를 피하고 싶을 때)

```jsx
const handlers = {
  add_todo: (state, action) => {
    const newTodo = {
      id: Date.now(),
      text: action.payload.text,
      completed: false,
    }
    return { ...state, todos: [...state.todos, newTodo] }
  },
  toggle_todo: (state, action) => ({
    ...state,
    todos: state.todos.map((todo) =>
      todo.id === action.payload.id
        ? { ...todo, completed: !todo.completed }
        : todo
    ),
  }),
  remove_todo: (state, action) => ({
    ...state,
    todos: state.todos.filter((todo) => todo.id !== action.payload.id),
  }),
}

function todoReducer(state, action) {
  const handler = handlers[action.type]
  if (!handler) throw new Error(`Unknown action type: ${action.type}`)
  return handler(state, action)
}
```

---

## 5. 복잡한 폼 상태를 useReducer로 관리하기

### 5-1. 여러 필드를 가진 폼 + reset

```jsx
import { useReducer } from 'react'

const initialFormState = {
  name: '',
  email: '',
  age: '',
  isSubmitted: false,
}

function formReducer(state, action) {
  switch (action.type) {
    case 'change_field': {
      const { name, value } = action.payload
      return {
        ...state,
        [name]: value,
      }
    }
    case 'submit_success':
      return {
        ...state,
        isSubmitted: true,
      }
    case 'reset':
      return initialFormState
    default:
      throw new Error(`Unknown action type: ${action.type}`)
  }
}

function ProfileForm() {
  const [state, dispatch] = useReducer(formReducer, initialFormState)

  const handleChange = (e) => {
    dispatch({
      type: 'change_field',
      payload: { name: e.target.name, value: e.target.value },
    })
  }

  const handleSubmit = (e) => {
    e.preventDefault()
    // 서버 전송 등 로직...
    dispatch({ type: 'submit_success' })
  }

  return (
    <form onSubmit={handleSubmit}>
      <input
        name="name"
        placeholder="이름"
        value={state.name}
        onChange={handleChange}
      />
      <input
        name="email"
        placeholder="이메일"
        value={state.email}
        onChange={handleChange}
      />
      <input
        name="age"
        placeholder="나이"
        value={state.age}
        onChange={handleChange}
      />
      <button type="submit">제출</button>
      <button type="button" onClick={() => dispatch({ type: 'reset' })}>
        리셋
      </button>

      {state.isSubmitted && <p>제출 완료! 🎉</p>}
    </form>
  )
}

export default ProfileForm
```

여기서 핵심은:

* **하나의 change 핸들러**로 모든 input 관리
  (`change_field` 액션)
* 제출 성공 / 리셋 같은 “폼의 상태 전이”가
  reducer 안에서 **명확하게 표현**된다는 점입니다.

---

## 6. lazy initialization (init 함수)

초기 상태를 계산하는 비용이 큰 경우 (예: localStorage, 복잡한 계산)
`init` 함수를 사용해서 **초기 렌더링 시 한 번만** 실행하게 만들 수 있습니다.

### 6-1. 문법

```ts
const [state, dispatch] = useReducer(reducer, initialArg, init)
```

* `initialArg`: 초기화를 위한 “원시 값”
* `init(initialArg)`: 실제 초기 state를 계산하는 함수

### 6-2. 예제: localStorage에서 초기 값 읽기

```jsx
import { useReducer, useEffect } from 'react'

function init(initialCount) {
  // 예: 로컬스토리지에 저장된 값 우선 사용
  const stored = window.localStorage.getItem('count')
  if (stored !== null) {
    return { count: Number(stored) }
  }
  return { count: initialCount }
}

function reducer(state, action) {
  switch (action.type) {
    case 'increment':
      return { count: state.count + 1 }
    default:
      return state
  }
}

function CounterWithStorage({ initialCount }) {
  const [state, dispatch] = useReducer(reducer, initialCount, init)

  useEffect(() => {
    window.localStorage.setItem('count', state.count)
  }, [state.count])

  return (
    <div>
      <p>Count: {state.count}</p>
      <button onClick={() => dispatch({ type: 'increment' })}>+1</button>
    </div>
  )
}
```

`init`은 **초기 렌더링 때만** 호출되며,
이후 `dispatch`에 의해 재렌더링될 때는 다시 호출되지 않습니다.

---

## 7. useReducer의 구조적/성능적 장점

### 7-1. dispatch 함수는 안정적인 레퍼런스

React는 `dispatch` 함수를 렌더링마다 새로 만들지 않고,
컴포넌트가 살아 있는 동안 **같은 함수 레퍼런스**를 유지합니다. (React가 공식적으로 보장하는 성질입니다.)

장점:

* `dispatch`를 자식 컴포넌트에 props로 내려도
  (자식이 `memo`라면) 그 때문에 리렌더링이 발생하지 않음
* `useCallback`으로 감싸줄 필요가 없음
* **`useEffect` / `useCallback` / `useMemo`의 의존성 배열에 넣지 않아도 됩니다.**
  (넣어도 무해하지만 불필요합니다. `useState`의 `setState`도 동일합니다.)

```jsx
useEffect(() => {
  dispatch({ type: 'init' })
}, []) // dispatch는 안정적이므로 deps에 없어도 lint 경고가 나지 않습니다
```

### 7-1-1. dispatch는 “즉시” state를 바꾸지 않습니다

```jsx
dispatch({ type: 'increment' })
console.log(state.count) // ⚠️ 아직 이전 값
```

* `dispatch`는 **다음 렌더링을 예약**할 뿐, 현재 렌더의 `state` 변수를 바꾸지 않습니다.
* 또한 React 18부터는 이벤트 핸들러 안이든 `setTimeout`/`Promise` 안이든 **자동 배칭(automatic batching)** 이 적용되어,
  여러 번의 `dispatch`가 **한 번의 리렌더링**으로 묶입니다.
* 다만 reducer는 dispatch한 **횟수만큼** 순서대로 실행되므로, 연속 dispatch의 누적 계산은 정상적으로 반영됩니다.

### 7-2. 여러 state 대신 한 state 객체

복잡한 상태를 `useState` 여러 개로 쪼개면:

* “어떤 이벤트가 어떤 state들을 동시에 바꿨는지”를 파악하기 어려움
* 코드가 이벤트 핸들러 이곳저곳으로 흩어짐

useReducer는:

* **하나의 state 객체**로 묶고
* 모든 state 변경 로직을 **reducer 한 곳**으로 모아서
  “상태 전이의 전체 그림”을 보기 쉽게 합니다.

이건 유지보수/교육 관점에서 엄청 중요한 포인트입니다.

---

## 8. useReducer + Context = 미니 Redux

컴포넌트 트리 전체에서 공유하는 상태가 필요하면,
`useReducer`와 `useContext`를 조합해 간단한 상태 관리 라이브러리처럼 쓸 수 있습니다.

### 8-1. Store 만들기

```jsx
// store.js
import { createContext, useContext, useReducer } from 'react'

const initialState = {
  user: null,
  theme: 'light',
}

function appReducer(state, action) {
  switch (action.type) {
    case 'set_user':
      return { ...state, user: action.payload }
    case 'toggle_theme':
      return { ...state, theme: state.theme === 'light' ? 'dark' : 'light' }
    default:
      throw new Error(`Unknown action type: ${action.type}`)
  }
}

const AppStateContext = createContext(null)
const AppDispatchContext = createContext(null)

export function AppProvider({ children }) {
  const [state, dispatch] = useReducer(appReducer, initialState)

  return (
    // React 19부터는 <Context.Provider> 대신 <Context>를 직접 렌더링할 수 있습니다.
    // (기존 <Context.Provider> 문법도 그대로 동작합니다.)
    <AppStateContext value={state}>
      <AppDispatchContext value={dispatch}>
        {children}
      </AppDispatchContext>
    </AppStateContext>
  )
}

export function useAppState() {
  const ctx = useContext(AppStateContext)
  if (!ctx) throw new Error('useAppState must be used within AppProvider')
  return ctx
}

export function useAppDispatch() {
  const ctx = useContext(AppDispatchContext)
  if (!ctx) throw new Error('useAppDispatch must be used within AppProvider')
  return ctx
}
```

### 8-2. 사용 예시

```jsx
// App.jsx
import { AppProvider } from './store'
import HomePage from './HomePage'

function App() {
  return (
    <AppProvider>
      <HomePage />
    </AppProvider>
  )
}

// HomePage.jsx
import { useAppState, useAppDispatch } from './store'

function HomePage() {
  const { user, theme } = useAppState()
  const dispatch = useAppDispatch()

  return (
    <div data-theme={theme}>
      <p>{user ? `${user.name}님 안녕하세요` : '로그인하세요'}</p>
      <button
        onClick={() =>
          dispatch({ type: 'set_user', payload: { name: '홍길동' } })
        }
      >
        로그인
      </button>

      <button
        onClick={() => dispatch({ type: 'toggle_theme' })}
      >
        테마 토글
      </button>
    </div>
  )
}
```

> **state와 dispatch를 두 개의 Context로 분리한 이유**
> dispatch는 레퍼런스가 변하지 않으므로, 분리해두면 **dispatch만 쓰는 컴포넌트는 state가 바뀌어도 리렌더링되지 않습니다.**
> 반대로 `value={{ state, dispatch }}`처럼 객체 하나로 합치면 매 렌더마다 새 객체가 만들어져 **모든 소비자가 리렌더링**됩니다.
> (Context는 `useSelector`와 달리 "값의 일부만 구독"하는 기능이 없습니다.)

Redux의 패턴과 거의 동일하지만:

* 미들웨어, DevTools, Thunk 등은 없음
* **구독 단위 최적화가 없음** — Context 값이 바뀌면 그 값을 쓰는 모든 컴포넌트가 리렌더링됩니다.
  이 지점이 Redux(`useSelector`로 필요한 조각만 구독)와 갈리는 실질적인 분기점입니다.
* 대신 **가볍고, 프로젝트 의존성이 증가하지 않음**

---

## 9. 내부 동작 (고급): Fiber & hook.memoizedState와의 관계

이미 hook 내부를 궁금해하셔서 조금 더 깊게 보겠습니다.
(실제 구현은 React 버전마다 디테일이 달라질 수 있고, 여기서는 개념 위주입니다.)

각 함수 컴포넌트는 React Fiber 트리에서 **하나의 Fiber 노드**로 표현됩니다.

이 Fiber 노드에는 대략 이런 필드들이 있습니다 (개념적으로):

```ts
type Hook = {
  memoizedState: any
  baseState: any
  queue: UpdateQueue | null
  next: Hook | null
}
```

* **memoizedState**: 현재 훅의 state (useReducer라면 현재 state 객체)
* **queue**: 새로운 업데이트(action)가 쌓이는 큐
* **next**: 다음 훅을 가리키는 싱글 링크드 리스트

함수 컴포넌트 실행 시:

1. React는 “현재 실행 중인 Fiber”를 기억합니다.
2. 첫 번째 `useReducer` 호출 시, 그 Fiber의 훅 리스트에서 첫 번째 Hook을 사용.
3. 다음 `useReducer`/`useState` 호출 시, 그 다음 Hook을 사용.
4. 이런 식으로 **훅의 호출 순서가 곧 훅의 “슬롯 인덱스”**가 됩니다.

   * 그래서 “조건문 안에서 훅 호출하면 안 된다”는 규칙이 생긴 거죠.

`useReducer`의 Hook 구조는 대략:

```ts
hook.memoizedState = currentState
hook.baseState = baseState
hook.queue = {
  pending: Update | null,
  dispatch: function boundDispatch() { ... },
}
```

* **dispatch**는 내부적으로 `boundDispatch(fiber, hook.queue)`처럼
  특정 Fiber와 큐에 바인딩된 함수입니다.
* `dispatch(action)`을 호출하면:

  1. `queue.pending`에 새로운 업데이트 노드를 연결
  2. 해당 Fiber를 다시 렌더링 대상으로 스케줄링
  3. 다음 렌더링에서 `reducer`를 돌려 새 state 계산 후 `memoizedState` 업데이트

즉, `useReducer`의 state는 결국 **hook.memoizedState에 저장**되고,
dispatch는 그 Hook의 `queue`에 업데이트를 쌓는 역할을 합니다.

---

## 10. Redux와 useReducer 비교

둘 다 **reducer 패턴** + **action 기반**이라는 공통점이 있지만, 차이점도 많습니다.

| 항목       | useReducer               | Redux                             |
| -------- | ------------------------ | --------------------------------- |
| 저장 위치    | React 컴포넌트 Fiber 내부      | 외부 store 객체                       |
| 범위       | 특정 컴포넌트 (또는 Context로 공유) | 앱 전체 전역                           |
| DevTools | 기본 없음                    | Redux DevTools 지원                 |
| 미들웨어     | 직접 구현 필요                 | redux-thunk, redux-saga 등 풍부한 생태계 |
| 비동기 처리   | useEffect 등 React 훅으로 처리 | 미들웨어 패턴 활용                        |
| 의존성      | React 내장                 | Redux 패키지 추가 필요                   |

**정리하자면:**

* **useReducer**:

  * “컴포넌트 단위”의 미니 상태 머신
  * Context와 합치면 작은/중간 규모 앱의 전역 상태 관리에 충분

* **Redux**:

  * 복잡한 규모, DevTools, 미들웨어, time-travel 디버깅이 필요할 때

---

## 11. 자주 하는 실수 & 베스트 프랙티스 O

1. **reducer에서 side effect를 하는 경우** X

   * `fetch`, `console.log`, `localStorage` 같은 것은 `useEffect`로 빼야 합니다.

2. **state를 직접 mutate하는 경우** X

   * `state.todos.push(...)`
   * `state.user.name = '...'`
     → 항상 새로운 객체/배열을 만들어서 반환해야 합니다.

3. **action 타입 문자열 오타**

   * `'add_todo'` vs `'addTodo'` 같은 오타 방지를 위해
     상수/enum을 쓰거나, TypeScript로 강제하는 게 좋습니다.

4. 너무 단순한 경우에 useReducer를 사용하는 것

   * `count` 하나만 있는 정말 간단한 카운터는 `useState`가 더 읽기 쉽습니다.
   * “상태 전이 로직이 복잡해질 때” useReducer로 옮기는 게 좋습니다.

5. **dispatch를 남발하여 한 렌더링 사이클에 너무 많이 호출하는 경우**

   * React 18/19에서는 자동 배칭 덕분에 여러 dispatch가 **한 번의 리렌더링**으로 묶이므로 성능 문제는 거의 없습니다.
   * 진짜 문제는 성능이 아니라 **설계**입니다. 하나의 이벤트에 dispatch가 3~4개씩 나온다면
     “이건 사실 하나의 액션(예: `submit_succeeded`) 아닌가?”를 먼저 고민해보세요.
   * 액션 하나가 “사용자에게 실제로 일어난 사건 하나”에 대응하는 것이 좋은 설계입니다.

6. **reducer의 `default`에서 무조건 throw 하는 것**

   * 예제처럼 `throw new Error(...)`는 오타를 빨리 잡아주는 장점이 있지만,
     처리하지 않는 액션이 하나라도 들어오면 앱이 죽습니다.
   * Redux의 관례(모르는 액션은 `return state`)와도 다릅니다.
   * 학습·개발 단계에서는 throw, 공용 로직에서는 `return state`처럼 상황에 맞게 선택하세요.

---

## 12. 마무리: 언제 useReducer를 쓰면 좋은가?

정리해보면, 다음과 같은 경우에 **useReducer**를 고려하면 좋습니다.

* 상태가 **객체/배열**이고, 필드가 많아짐
* 특정 이벤트가 “여러 state를 동시에” 바꿔야 함
* 상태 전이가 **명확한 단계(로딩 → 성공 → 실패)**를 가짐
* “액션” 단위로 상태 변화를 모델링하고 싶음 (Redux와 유사한 mental model)
* Context로 전역 상태를 만들고 싶지만, Redux 도입은 오버킬일 때

반대로 **`useState`가 더 나은 경우**도 분명합니다.

* 상태가 1~2개이고 서로 독립적일 때
* 상태 전이 규칙이라고 할 만한 게 없을 때 (그냥 값 대입)
* reducer/action 이름을 짓는 비용이 얻는 이득보다 클 때

> **React 19 참고**: 폼 제출처럼 “제출 중 → 성공/실패”가 핵심인 흐름이라면
> `useReducer`로 로딩·에러 플래그를 직접 관리하는 대신 **`useActionState`**(+ `<form action>`, `useFormStatus`)를 먼저 검토해보세요.
> pending 상태와 결과 상태를 React가 대신 관리해 줍니다.


