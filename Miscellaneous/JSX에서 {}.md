
JSX 코드에서 중괄호 `{}`는 **JavaScript 표현식을 삽입**하기 위한 **문법적인 구문**입니다.

---

## ✅ JSX에서 `{}`의 역할

JSX는 JavaScript이기 때문에, JSX 내부에서 **JavaScript 표현식을 쓰고 싶을 때**는 반드시 중괄호 `{}`를 사용해야 합니다.

---

## 🎯 예제 설명

```jsx
const users = ['홍길동', '김코딩', '이자바'];
const isEmpty = users.length === 0;

return (
  <section>
    <h3>📋 사용자 목록</h3>

    {isEmpty ? (
      <p>사용자가 없습니다.</p>
    ) : (
      <ul>
        {users.map((user, index) => (
          <li key={index}>{user}</li>
        ))}
      </ul>
    )}
  </section>
);
```

### 여기서 사용된 중괄호 `{}`는 각각 다음을 의미합니다:

| 위치                          | 의미                                 |
| --------------------------- | ---------------------------------- |
| `{isEmpty ? (...) : (...)}` | **삼항 연산자**를 통해 조건부 렌더링             |
| `{users.map(...)}`          | 배열 반복을 위한 **JavaScript map 함수 실행** |
| `{user}`                    | 리스트 항목 안에 사용자 이름 출력                |
| `{index}`                   | key 속성에 JavaScript 값을 삽입           |

---

## ✅ JSX에서 가능한 `{}` 안 표현식

| 표현식 종류 | 예시                              |
| ------ | ------------------------------- |
| 변수     | `{title}`                       |
| 삼항 연산자 | `{isLoggedIn ? "로그아웃" : "로그인"}` |
| 함수 호출  | `{getUsername()}`               |
| 배열 반복  | `{items.map(...)}`              |
| 수학 연산  | `{a + b}`                       |

---

## ⚠️ 주의

* JSX에서 \*\*문(statement)\*\*는 `{}` 안에 넣을 수 없습니다.
  예: `if`, `for`, `while` 등은 직접 넣지 못함

  ❌ 오류 발생:

  ```jsx
  { if (isEmpty) { ... } } // 오류
  ```

  ✅ 대신 **삼항 연산자**나 **함수 호출**로 대체:

  ```jsx
  {isEmpty ? <p>비어 있음</p> : <p>값 있음</p>}
  ```

---

## 🧠 결론

JSX에서 `{}`는 HTML-like 태그 안에서 **동적으로 JavaScript 표현식을 넣기 위한 문법**입니다.
이 덕분에 React는 UI에 **로직을 자연스럽게 삽입**할 수 있습니다.

---



# ✅ JSX에서 `{}` 표현식 사용 가능 여부 정리표

| 구분             | 예시                                          | 설명                   | 사용 가능 여부                             |             |         |
| -------------- | ------------------------------------------- | -------------------- | ------------------------------------ | ----------- | ------- |
| **변수**         | `{name}`                                    | 변수 값을 삽입             | ✅ 사용 가능                              |             |         |
| **숫자/문자열 리터럴** | `{42}`, `{"Hello"}`                         | 리터럴 값 직접 삽입          | ✅ 사용 가능                              |             |         |
| **연산식**        | `{1 + 2}`, `{price * quantity}`             | 수학적 연산 가능            | ✅ 사용 가능                              |             |         |
| **삼항 연산자**     | `{isLoggedIn ? "Logout" : "Login"}`         | 조건부 표현 가능            | ✅ 사용 가능                              |             |         |
| **논리 연산자**     | `{isVisible && <p>보임</p>}`                  | `&&`, \`             |                                      | \`로 조건부 렌더링 | ✅ 사용 가능 |
| **함수 호출**      | `{getUserName()}`                           | 리턴값이 렌더링됨            | ✅ 사용 가능                              |             |         |
| **배열**         | `{[1, 2, 3]}`                               | 배열을 JSX로 렌더링         | ✅ 사용 가능                              |             |         |
| **map()**      | `{items.map(item => <li>{item}</li>)}`      | 반복 렌더링               | ✅ 사용 가능                              |             |         |
| **객체**         | `{ {name: "intheeast"} }`                   | 객체는 직접 렌더링 불가        | ❌ 오류 발생 (`[object Object]` 출력 또는 에러) |             |         |
| **조건문**        | `{ if (isEmpty) { return <p>비어 있음</p>; } }` | if문은 문(statement)    | ❌ 사용 불가                              |             |         |
| **for문**       | `{for (let i = 0; i < 5; i++) { ... }}`     | 반복문도 문(statement)    | ❌ 사용 불가                              |             |         |
| **while문**     | `{while (true) { ... }}`                    | 무한루프일 수도 있고, 문이므로    | ❌ 사용 불가                              |             |         |
| **try/catch**  | `{try { ... } catch { ... }}`               | 예외 처리문은 문(statement) | ❌ 사용 불가                              |             |         |
| **switch문**    | `{switch (value) { case 1: ... }}`          | 조건 분기 문(statement)   | ❌ 사용 불가                              |             |         |
| **return문**    | `{return "값";}`                             | 반환 문(statement)      | ❌ 사용 불가                              |             |         |

---

## 💡 사용 불가능한 문(statement)을 사용하고 싶다면?

➡️ **JSX 바깥에서 처리**하거나, **즉시 실행 함수(IIFE)** 혹은 **도우미 함수**로 분리해야 합니다.

### ✅ 예시 1: JSX 바깥에서 처리

```jsx
let content;
if (isEmpty) {
  content = <p>비어 있음</p>;
} else {
  content = <ul>{items.map(item => <li>{item}</li>)}</ul>;
}

return <div>{content}</div>;
```

### ✅ 예시 2: 즉시 실행 함수 (IIFE)

```jsx
{(() => {
  if (isEmpty) {
    return <p>비어 있음</p>;
  } else {
    return <ul>{items.map(item => <li>{item}</li>)}</ul>;
  }
})()}
```

---

## 📌 팁: JSX는 "표현식(Expression) 중심"

JSX 안에서는 \*\*문(statement)\*\*이 아닌, \*\*값을 리턴하는 표현식(expression)\*\*만 허용된다는 원칙을 기억하세요.

---

