

# 🧠 `useImperativeHandle` 

> “컴포넌트의 내부 로직을 외부에게 **선택적으로 노출**하는 Hook”

---

## ✅ 1. 무엇인가?

```tsx
useImperativeHandle(ref, createHandle, [deps])
```

> `useImperativeHandle`은 `forwardRef`와 함께 사용하여
> 부모가 **ref를 통해 자식 컴포넌트의 메서드/값에 직접 접근**하도록 만들 수 있는 Hook입니다.

---

## 🧩 2. 일반 `ref`와의 차이

### 🔸 일반적인 `ref`는 DOM 접근 용도

```tsx
const inputRef = useRef();
<input ref={inputRef} />;
inputRef.current.value = 'hello';
```

### 🔹 `useImperativeHandle`은 **컴포넌트의 커스텀 API** 노출 용도

```tsx
inputRef.current.focusInput(); // focusInput은 자식 컴포넌트에서 제공한 메서드
```

---

## 🧪 3. 실전 예제: CustomInput 컴포넌트

### 🔧 자식 컴포넌트 (`CustomInput.jsx`)

```tsx
import { forwardRef, useImperativeHandle, useRef } from 'react';

const CustomInput = forwardRef((props, ref) => {
  const inputRef = useRef();

  useImperativeHandle(ref, () => ({
    focus: () => inputRef.current.focus(),
    clear: () => (inputRef.current.value = ''),
  }));

  return <input ref={inputRef} />;
});

export default CustomInput;
```

### 🧑‍💻 부모 컴포넌트 (`App.jsx`)

```tsx
function App() {
  const inputRef = useRef();

  return (
    <>
      <CustomInput ref={inputRef} />
      <button onClick={() => inputRef.current.focus()}>포커스</button>
      <button onClick={() => inputRef.current.clear()}>지우기</button>
    </>
  );
}
```

✅ `ref.current`는 단순 DOM이 아니라 `{ focus, clear }` 같은 사용자 정의 API가 됨

---

## 📌 4. 매개변수 설명

| 매개변수           | 설명                                |
| -------------- | --------------------------------- |
| `ref`          | `forwardRef`로 받은 ref              |
| `createHandle` | 노출할 객체를 생성하는 함수                   |
| `deps`         | 의존성 배열 (선택 사항, memoization 위해 사용) |

---

## 🔥 5. 주요 사용 시점

| 상황                               | 설명                                              |
| -------------------------------- | ----------------------------------------------- |
| ✅ 자식의 내부 메서드/상태를 외부에서 제어하고 싶을 때  | 모달 열기/닫기, 입력 초기화 등                              |
| ✅ 자식이 DOM 외에 커스텀 인터페이스를 제공해야 할 때 | `resetForm()`, `validate()`, `scrollToBottom()` |
| ✅ 리팩토링 시 컴포넌트를 캡슐화하면서 제어권 유지     | 내부는 숨기고 필요한 것만 열어줌                              |

---

## ❗ 주의할 점

| 항목                      | 설명                                              |
| ----------------------- | ----------------------------------------------- |
| `forwardRef` 없으면 작동 안 함 | 반드시 `forwardRef`로 감싼 컴포넌트에만 사용 가능               |
| 내부 상태/메서드는 숨겨짐          | 명시적으로 `useImperativeHandle`에서 노출한 것만 외부에서 접근 가능 |
| 너무 많은 제어는 안티패턴          | 지나친 imperative 방식은 React의 선언적 흐름을 해침            |

---

## 🧠 내부 원리 요약

```tsx
// 부모 → ref를 자식에게 전달
<Child ref={ref} />

// 자식 → ref.current에 커스텀 객체를 명시적으로 설정
useImperativeHandle(ref, () => ({ focus, clear }))
```

* `ref.current = {...}` 대신 React가 Hook 내부에서 **제어된 할당**을 수행

---

## 🧪 고급 예제: Modal 제어

### 📦 Modal.jsx

```tsx
import { forwardRef, useImperativeHandle, useState } from 'react';

const Modal = forwardRef((_, ref) => {
  const [open, setOpen] = useState(false);

  useImperativeHandle(ref, () => ({
    show: () => setOpen(true),
    hide: () => setOpen(false),
  }));

  if (!open) return null;

  return (
    <div className="modal">
      <p>Modal Content</p>
      <button onClick={() => setOpen(false)}>Close</button>
    </div>
  );
});

export default Modal;
```

### 📄 App.jsx

```tsx
const modalRef = useRef();

return (
  <>
    <button onClick={() => modalRef.current.show()}>Open Modal</button>
    <Modal ref={modalRef} />
  </>
);
```

✅ `Modal`은 내부 상태 `open`을 은닉하고, `show()` / `hide()`만 외부에 노출

---

## 🧠 캡슐화의 이점

> 자식 컴포넌트는 **어떻게 작동하는지**를 숨기고,
> 부모에게는 **무엇을 할 수 있는지**만 제공

🔒 내부 구현 숨기기 + 🔓 필요한 기능만 노출 → React의 **컴포넌트 캡슐화 철학** 실현

---

## ❓ `useImperativeHandle`이 꼭 필요할까?

| 예                  | Hook 필요 여부                 |
| ------------------ | -------------------------- |
| DOM 접근             | ❌ `ref`만으로 충분              |
| input 초기화/포커스 등 조작 | ✅ 가능하면 사용                  |
| 외부에서 내부 상태 제어      | ✅ `useImperativeHandle` 적합 |

---

## ✅ 정리 요약

| 항목      | 설명                                        |
| ------- | ----------------------------------------- |
| Hook 이름 | `useImperativeHandle(ref, () => ({...}))` |
| 사용 조건   | 반드시 `forwardRef`와 함께 사용                   |
| 목적      | ref를 통해 자식의 메서드/상태를 부모에게 노출               |
| 핵심 가치   | 내부 구현 은닉 + 제어 포인트 선택적 공개                  |
| 사용 예    | Form API, Modal, Carousel, Canvas 등       |
| 주의점     | React의 선언적 패턴을 훼손하지 않도록 최소한 사용            |


