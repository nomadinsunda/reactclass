리액트에서 **useCallback + useRef 조합**은 사실상
“함수형 컴포넌트에서 *인스턴스 메서드 + 인스턴스 필드* 흉내 내기 패턴”이에요. 🤓
이걸 이해하면 **stale closure(오래된 값 잡는 문제)**, **불필요한 리렌더**, **외부 이벤트 핸들러 관리** 같은 걸 훨씬 깔끔하게 처리할 수 있습니다.

아래에서 **자주 쓰는 패턴들을 케이스별로** 정리해볼게요.

---

## 0. useCallback / useRef 역할부터 정리 🧱

### useRef의 핵심

* `const ref = useRef(initialValue)`
* `ref.current`에 **변하는 값을 저장**하지만
  👉 `ref.current = ...`를 바꿔도 **리렌더가 일어나지 않음**
* 그래서:

  * DOM 노드 잡기 (`<input ref={inputRef} />`)
  * 타이머 id, WebSocket 인스턴스, 외부 라이브러리 인스턴스 등 “**컴포넌트가 들고 있어야 하는 상태**”를 저장

### useCallback의 핵심

* `const fn = useCallback(callback, deps)`
* **함수의 “참조(identity)”를 메모이즈**해서

  * 자식 컴포넌트에 props로 넘길 때 매번 새 함수 생성되는 것을 방지
  * `useEffect` / `useMemo` 등의 의존성 배열에서 **불필요한 재실행**을 줄이기 위함

---

## 1. 가장 많이 쓰는 패턴: “항상 최신 state를 보는 콜백” 🔄

### 문제 상황: stale closure

```jsx
function Chat() {
  const [message, setMessage] = useState("");
  
  const send = useCallback(() => {
    // 이 안의 message는 "콜백이 만들어질 당시"의 message를 캡처함
    console.log("send:", message);
  }, [message]);

  useEffect(() => {
    const id = setInterval(() => {
      send(); // interval이 계속 send 호출
    }, 1000);
    return () => clearInterval(id);
  }, [send]); // send가 바뀔 때마다 interval 재등록
}
```

* `message`가 바뀔 때마다:

  * `send`가 새로 만들어짐
  * `useEffect`가 다시 실행 → interval 재등록
* 간단한 경우엔 괜찮지만, **외부 라이브러리에 핸들러 등록**하거나, **비싼 작업**이면 문제가 큼.

### 해결: useRef로 최신 값만 저장하고, useCallback은 의존성 없이 고정

```jsx
import { useState, useRef, useEffect, useCallback } from "react";

function Chat() {
  const [message, setMessage] = useState("");
  const messageRef = useRef(message);

  // 매 렌더마다 최신 message를 ref에 반영
  messageRef.current = message;

  const send = useCallback(() => {
    // 항상 최신 messageRef.current를 참조
    console.log("send:", messageRef.current);
  }, []); // 의존성 없음 → 함수 identity 고정

  useEffect(() => {
    const id = setInterval(() => {
      send();
    }, 1000);
    return () => clearInterval(id);
  }, [send]);

  return (
    <div>
      <input
        value={message}
        onChange={(e) => setMessage(e.target.value)}
        placeholder="메시지 입력"
      />
      <button onClick={send}>Send</button>
    </div>
  );
}
```

**포인트 🔍**

* `messageRef.current = message`
  → 리렌더마다 “최신 값”을 ref에 덮어씀
* `send`는 의존성이 `[]`라 **identity가 고정**됨
* 하지만 내부에서 **항상 최신 `messageRef.current`를 읽기 때문에**
  stale closure 문제가 사라짐

이 패턴은 특히:

* `addEventListener` / `removeEventListener`
* `setInterval`, `setTimeout`
* 외부 SDK에 콜백 등록 (예: WebSocket 메시지 핸들러, map 라이브러리 이벤트 등)

에서 **정석 패턴**으로 자주 쓰입니다. 🚀

---

## 2. “DOM 이벤트 + useRef + useCallback” 패턴 🧵

### 상황: window/document 이벤트 핸들러

```jsx
function useWindowResize(handler) {
  const handlerRef = useRef(handler);

  // handler가 바뀔 때마다 ref만 업데이트
  useEffect(() => {
    handlerRef.current = handler;
  }, [handler]);

  const stableCallback = useCallback((event) => {
    // 항상 최신 handler를 호출
    handlerRef.current(event);
  }, []);

  useEffect(() => {
    window.addEventListener("resize", stableCallback);
    return () => {
      window.removeEventListener("resize", stableCallback);
    };
  }, [stableCallback]);
}
```

사용 예시:

```jsx
function ResizeLogger() {
  useWindowResize(() => {
    console.log("window size:", window.innerWidth, window.innerHeight);
  });

  return <div>윈도우 리사이즈 시 콘솔 로그</div>;
}
```

**핵심 🧠**

* `stableCallback`은 **한 번만 만들어진 함수**
* 하지만 내부에서 `handlerRef.current`를 보기 때문에 **항상 최신 핸들러**를 실행
* 이벤트 리스너 등록/해제에서 **항상 같은 함수 참조**를 쓰므로 안전

---

## 3. “타이머 / 인스턴스 관리” 패턴 ⏱️

### 문제: setTimeout / setInterval id 보관

클래스 컴포넌트라면 `this.timerId`로 보관하던 걸,
함수 컴포넌트에선 `useRef`로 보관합니다.

```jsx
function AutoSaveEditor() {
  const [text, setText] = useState("");
  const timeoutIdRef = useRef(null);

  const scheduleSave = useCallback(() => {
    // 기존 타이머가 있다면 취소
    if (timeoutIdRef.current) {
      clearTimeout(timeoutIdRef.current);
    }

    timeoutIdRef.current = setTimeout(() => {
      console.log("Auto save:", text);
      // 실제로는 서버에 저장하는 로직
    }, 1000);
  }, [text]); // ❗ text를 deps에 넣으면 send와 마찬가지 문제(타이머 재생성) 발생

  useEffect(() => {
    return () => {
      // 언마운트 시 남은 타이머 정리
      if (timeoutIdRef.current) {
        clearTimeout(timeoutIdRef.current);
      }
    };
  }, []);

  return (
    <textarea
      value={text}
      onChange={(e) => {
        setText(e.target.value);
        scheduleSave();
      }}
    />
  );
}
```

위 코드도 사실 **stale closure**가 잠재적 문제죠.
완전히 깔끔히 하려면 **텍스트도 ref에 넣고 콜백은 고정**하는 패턴으로 가는 게 좋습니다.

```jsx
function AutoSaveEditor() {
  const [text, setText] = useState("");
  const textRef = useRef(text);
  const timeoutIdRef = useRef(null);

  textRef.current = text;

  const scheduleSave = useCallback(() => {
    if (timeoutIdRef.current) {
      clearTimeout(timeoutIdRef.current);
    }

    timeoutIdRef.current = setTimeout(() => {
      console.log("Auto save:", textRef.current);
    }, 1000);
  }, []); // 의존성 없음

  useEffect(() => {
    return () => {
      if (timeoutIdRef.current) {
        clearTimeout(timeoutIdRef.current);
      }
    };
  }, []);

  return (
    <textarea
      value={text}
      onChange={(e) => {
        setText(e.target.value);
        scheduleSave();
      }}
    />
  );
}
```

여기서 패턴을 정리하면:

* **useRef: 인스턴스 필드**

  * `timeoutIdRef.current`
  * `textRef.current`
  * `socketRef.current` 등
* **useCallback: 인스턴스 메서드**

  * `scheduleSave`, `connect`, `disconnect`, `sendMessage` 등

---

## 4. “자식 컴포넌트에 넘기는 콜백 + ref” 패턴 🧒➡️👨‍👩‍👦

자식 컴포넌트에 함수를 props로 넘길 때,
부모가 매번 새 함수를 만들면 자식이 불필요하게 리렌더될 수 있죠.

```jsx
function Parent() {
  const [count, setCount] = useState(0);

  const handleClick = useCallback(() => {
    setCount((c) => c + 1);
  }, []); // setState는 안정적인 함수라 deps에 안 넣어도 ok

  return <Child onClick={handleClick} />;
}
```

여기서는 `useRef`가 꼭 필요하진 않지만,
**자식이 전달한 어떤 값(예: DOM 노드)을 ref에 저장하고 콜백에서 같이 쓰는 패턴**도 있습니다.

```jsx
function Parent() {
  const buttonRef = useRef(null);

  const focusChildButton = useCallback(() => {
    if (buttonRef.current) {
      buttonRef.current.focus();
    }
  }, []);

  return (
    <>
      <ChildButton ref={buttonRef} />
      <button onClick={focusChildButton}>자식 버튼 포커스</button>
    </>
  );
}

const ChildButton = React.forwardRef(function ChildButton(props, ref) {
  return <button ref={ref}>Child</button>;
});
```

* `buttonRef` → 실제 DOM 노드 저장
* `focusChildButton` → useCallback으로 메모이즈된 함수
* 부모 렌더가 자주 일어나도 `focusChildButton`의 identity는 유지

---

## 5. “callback ref + useCallback” 패턴 (DOM mount/unmount 감지) 👀

`ref` prop에 **함수**를 넘기는 것도 가능하죠. 이걸 `callback ref`라고 합니다.

```jsx
function CanvasContainer() {
  const canvasRef = useRef(null);

  const setCanvasRef = useCallback((node) => {
    if (node) {
      // mount 시점: node는 실제 <canvas> DOM
      console.log("Canvas mounted", node);
      canvasRef.current = node;
      // 외부 라이브러리 초기화 (예: chart.js, 3D 렌더러 등)
    } else {
      // unmount 시점: node === null
      console.log("Canvas unmounted");
      canvasRef.current = null;
      // 정리 작업(디스트로이)
    }
  }, []);

  return <canvas ref={setCanvasRef} />;
}
```

**왜 useCallback을 쓰느냐?**

* `ref={함수}`에서 그 함수가 매 렌더마다 새로 생성되면:

  * React가 기존 ref를 `null`로 한 번 호출
  * 새 ref로 DOM을 다시 전달
  * 불필요한 mount/unmount 비슷한 작업이 반복됨
* `useCallback`으로 ref 함수를 고정하면:

  * DOM 노드가 실제로 바뀔 때만 ref가 호출됨
  * 외부 라이브러리 초기화/정리 로직이 안정적으로 한 번씩만 실행

---

## 6. “useEvent / useStableCallback” 커스텀 훅 패턴 🧪

요즘 많이 언급되는 패턴이 **“항상 최신 값을 보는 안정적인 콜백”**을 아예 훅으로 빼는 것.

```jsx
import { useRef, useCallback, useEffect } from "react";

function useStableCallback(fn) {
  const fnRef = useRef(fn);

  useEffect(() => {
    fnRef.current = fn;
  });
  // 의존성 배열 없음 → 매 렌더마다 fnRef 업데이트

  const stableFn = useCallback((...args) => {
    return fnRef.current(...args);
  }, []);

  return stableFn;
}
```

사용 예:

```jsx
function Timer({ onTick }) {
  const handleTick = useStableCallback(onTick);

  useEffect(() => {
    const id = setInterval(() => {
      handleTick();
    }, 1000);
    return () => clearInterval(id);
  }, [handleTick]);

  return <div>Ticking...</div>;
}
```

* 부모에서 `onTick`이 자주 바뀌어도:

  * `handleTick` identity는 고정
  * 내부에서 항상 최신 `onTick`을 호출

이게 사실 **useRef + useCallback 패턴의 정제된 형태**입니다.
(React 19의 `useEvent` 개념과 상당히 유사한 패턴이기도 해요. 😉)

---

## 7. 반대로, “이 조합은 과하다” 하는 경우들 ⚠️

### 1) 그냥 state만 보고, 외부 이벤트/타이머도 없는데 무조건 useRef + useCallback

```jsx
const [count, setCount] = useState(0);
const countRef = useRef(count);
countRef.current = count;

const handleClick = useCallback(() => {
  setCount(countRef.current + 1);
}, []);
```

→ 이건 굳이 이렇게까지 할 필요 없이:

```jsx
const handleClick = () => {
  setCount((c) => c + 1);
};
```

로 끝나요.
**“외부 이벤트/타이머/라이브러리와 상호작용”이 없다면**
useRef + useCallback 패턴은 오히려 코드를 복잡하게 만들 뿐입니다.

### 2) 모든 함수에 무조건 useCallback

* 자식 컴포넌트가 `React.memo`도 안 쓰고,
* 그 함수가 props로 깊게 내려가지도 않는데,

무조건 `useCallback` 쓰는 건 **불필요한 최적화**입니다.
실제로는 **deps 비교 비용**이 더 들 수도 있어요. 😅

---

## 8. 정리: 언제 useCallback + useRef 조합을 써야 하나? ✅

**아래 중 하나라도 해당하면** 진지하게 이 패턴을 고려할 만합니다:

1. **외부 이벤트 리스너 / 타이머 / 외부 라이브러리 콜백 사용**

   * `addEventListener`, `setInterval`, `setTimeout`
   * WebSocket, Map/Chart 라이브러리 핸들러
2. 콜백이 **항상 최신 state/props를 봐야 하는데**,
   그 콜백을 **의존성 배열에서 고정된 함수로 쓰고 싶을 때**
3. 컴포넌트가 사실상 **클래스 인스턴스처럼** 동작해야 할 때

   * `ref`에 인스턴스를 저장하고
   * `useCallback` 함수들이 그 인스턴스를 조작하는 “메서드” 역할을 할 때
4. `callback ref`로 DOM mount/unmount를 감지하면서
   외부 초기화/정리 작업을 하고 싶을 때

---

## 마무리 😎

요약하면:

> **useRef**는 “리렌더를 일으키지 않는 인스턴스 필드”
> **useCallback**은 “identity가 고정된 인스턴스 메서드”

라고 생각하시면 됩니다.

함수형 컴포넌트 안에서 이 둘을 같이 쓰는 순간,
우리가 익숙한 **“클래스 인스턴스 + 메서드” 패턴**을 안전하게 재현할 수 있어요.

