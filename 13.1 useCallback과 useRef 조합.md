`useCallback`과 `useRef`를 **조합해서 사용하는 패턴**은 실무에서 매우 강력하며, 특히 다음과 같은 목적에서 자주 사용됩니다:

---

## 🎯 조합하는 이유

| 상황                                                      | 해결 방법                                 |
| ------------------------------------------------------- | ------------------------------------- |
| ✅ 최신 상태(state)를 콜백 내부에서 사용하고 싶을 때                       | `useRef`로 상태를 추적                      |
| ✅ 함수 identity 유지 (`useCallback`) + 최신 데이터 접근 (`useRef`) | 두 훅을 **조합**                           |
| ✅ 불필요한 재렌더 없이 이벤트 핸들러 유지                                | `useRef`로 값 저장, `useCallback`으로 함수 고정 |

---

## 🧠 핵심 개념

### `useRef`:

* 값을 저장하지만 컴포넌트를 **리렌더링하지 않음**
* 최신 값을 유지하는 데 사용

### `useCallback`:

* **함수의 참조값을 고정**하여 렌더링 최적화

> 👉 `useCallback`은 **함수**를 고정하고,
> 👉 `useRef`는 \*\*데이터(상태)\*\*를 고정 없이 추적

---

## ✅ 예제 1: 최신 상태 값을 참조하는 콜백

```tsx
import { useCallback, useEffect, useRef, useState } from 'react';

function TimerComponent() {
  const [count, setCount] = useState(0);
  const countRef = useRef(count);

  // 최신 count 값 유지
  useEffect(() => {
    countRef.current = count;
  }, [count]);

  // 콜백 함수는 단 한 번만 생성
  const handleAlert = useCallback(() => {
    setTimeout(() => {
      alert(`현재 count 값: ${countRef.current}`); // 최신 값
    }, 1000);
  }, []);

  return (
    <div>
      <p>Count: {count}</p>
      <button onClick={() => setCount(c => c + 1)}>+1</button>
      <button onClick={handleAlert}>1초 후 alert</button>
    </div>
  );
}
```

### 🔍 핵심 설명

* `useCallback`은 빈 deps `[]`로 인해 **함수는 단 한 번만 생성**
* 하지만 내부에서 참조하는 `count`는 `useRef`로 관리하여 항상 **최신 상태 유지**
* `alert` 콜백이 오래된 클로저로부터 벗어나 최신 값을 씀 → 💯 **stale closure 방지**

---

## ✅ 예제 2: 스크롤, 마우스, 타이머 등의 이벤트 핸들러에서 최신 상태 유지

```tsx
function ScrollTracker() {
  const [y, setY] = useState(0);
  const handlerRef = useRef();

  // 최신 handler 유지
  handlerRef.current = () => {
    setY(window.scrollY);
  };

  useEffect(() => {
    const onScroll = () => handlerRef.current(); // 항상 최신 핸들러
    window.addEventListener('scroll', onScroll);
    return () => window.removeEventListener('scroll', onScroll);
  }, []);

  return <div>현재 스크롤 위치: {y}</div>;
}
```

✅ 위 코드는 `useCallback` 없이 `ref`만 쓰지만,
📌 아래처럼 `useCallback`으로 이벤트 핸들러 고정도 가능:

```tsx
const stableScrollHandler = useCallback(() => {
  setY(window.scrollY);
}, []);
```

그러나 `setY`가 상태를 직접 업데이트하기 때문에 `useRef` 버전이 더 안전한 경우도 많습니다.

---

## 🚨 주의할 점

| 항목                                      | 설명                                                              |
| --------------------------------------- | --------------------------------------------------------------- |
| ❗ `useCallback`만 쓰면 stale closure 발생 가능 | 내부에서 참조하는 상태가 오래된 값이 될 수 있음                                     |
| ✅ `useRef`로 최신 값 유지                     | 클로저 대신 `.current`로 최신 값 접근                                      |
| ✅ 두 훅 조합 시 렌더링 최적화 + 버그 방지              | 특히 **setTimeout**, **addEventListener**, **debounce** 등에서 매우 유용 |

---

## ✨ 정리

| 목적        | 사용 방법                                 |
| --------- | ------------------------------------- |
| 함수 재생성 막기 | `useCallback(fn, deps)`               |
| 최신 상태 유지  | `useRef` + `useEffect`로 `.current` 갱신 |
| 두 개 조합    | 콜백은 고정, 내부 참조는 유동적 (최신 값)             |

---

## 📦 요약 패턴

```tsx
const stateRef = useRef(state);
useEffect(() => {
  stateRef.current = state;
}, [state]);

const callback = useCallback(() => {
  // 항상 최신 state 참조
  doSomething(stateRef.current);
}, []);
```


