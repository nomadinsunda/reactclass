# ⚛️ React JSX

JSX는 단순히 “HTML처럼 보이는 문법”이 아닙니다.
**React 렌더링 모델을 완전히 바꿔놓은 선언적 UI DSL(Domain-Specific Language)** 입니다.
즉, JSX는 HTML이 아니라 **JavaScript의 확장 문법이며, 결국 JavaScript로 컴파일되는 표현식**입니다.

---

# 🎯 1. JSX란 무엇인가?

JSX(JavaScript XML)는 **JavaScript에서 UI 구조를 선언적으로 작성하기 위한 확장 문법**입니다.

```jsx
const element = <h1>Hello JSX</h1>;
```

하지만 중요한 사실은…

👉 **JSX는 브라우저가 직접 이해하지 못합니다.**
👉 **JSX는 React.createElement 또는 React의 새로운 JSX Transform으로 변환됩니다.**

즉, JSX는 **단지 개발자 경험(DX)을 위한 syntax sugar** 이고
실제 실행되는 것은 **순수 JavaScript 함수 호출**입니다.

---

# 🔥 2. JSX는 HTML이 아니다!

겉보기엔 HTML처럼 보이지만, **완전히 다른 규칙**을 가지고 있습니다.

| 항목   | HTML          | JSX                                |
| ---- | ------------- | ---------------------------------- |
| 문법   | 마크업 언어        | JavaScript 표현식                     |
| 속성   | 속성명 lowercase | camelCase (`className`, `onClick`) |
| 동적 값 | 없음            | `{}` 사용                            |
| 조건   | 불가능           | JS 표현식 사용                          |
| 반복   | 불가능           | map() 문법 사용                        |

### ❌ HTML

```html
<h1 onclick="alert(1)">Click</h1>
```

### ✅ JSX

```jsx
<h1 onClick={() => alert(1)}>Click</h1>
```

---

# 🧠 3. JSX는 결국 함수 호출로 변환된다

## 🔹 Babel 컴파일 결과 보기

### JSX

```jsx
const element = <h1>Hello</h1>;
```

### Babel 트랜스파일 결과 (React 17 이전 방식)

```js
const element = React.createElement("h1", null, "Hello");
```

### React 17+ 새로운 JSX Transform

```js
import { jsx as _jsx } from "react/jsx-runtime";

const element = _jsx("h1", { children: "Hello" });
```

📌 React는 이제 `React.createElement`를 직접 호출하지 않으며
컴파일러가 더 최적화된 런타임 함수를 사용합니다.

---

# 🏗️ 4. JSX는 "표현식"이다

JSX는 JavaScript **표현식(expression)** 입니다.
따라서 다음처럼 사용할 수 있습니다.

### ✔️ 변수에 할당 가능

```jsx
const result = <p>Text</p>;
```

### ✔️ 함수의 반환값으로 사용 가능

```jsx
function renderMessage() {
  return <p>Hello</p>;
}
```

### ✔️ 객체 내부에도 사용 가능

```jsx
const ui = {
  title: <h1>Title</h1>
};
```

### ✔️ 조건문 & 삼항 연산자

```jsx
{isLogin ? <Dashboard /> : <Login />}
```

### ✔️ 반복문(map)

```jsx
{items.map(item => <li key={item.id}>{item.name}</li>)}
```

👉 **즉 JSX는 어떤 위치에서도 사용 가능한 “값”이다.**

---

# 🎨 5. JSX 내부 규칙 정리

## ✔ 5-1. `{}`는 “JavaScript로 탈출하는 문법”

JSX 안에서 동적 값을 넣을 때 사용합니다.

```jsx
const name = "ChatGPT";
<p>Hello {name}</p>
```

가능한 것들:

* 변수
* 함수 호출
* 삼항 연산자
* 배열/객체
* map 결과

불가능한 것:

* if문
* for문
  (이들은 JS statement이므로 표현식이 아님)

---

## ✔ 5-2. 속성은 camelCase 사용

HTML과 달리 JSX는 JavaScript 객체이므로 camelCase가 기본 규칙입니다.

| HTML     | JSX       |
| -------- | --------- |
| onclick  | onClick   |
| class    | className |
| tabindex | tabIndex  |

---

## ✔ 5-3. 태그는 컴포넌트 판별에 사용된다

대문자: 컴포넌트
소문자: HTML 태그

```jsx
<div />     // HTML div
<MyButton /> // React Component
```

---

# ⚙️ 6. JSX → React Element → Fiber 구조

JSX는 렌더링까지 다음 단계를 거칩니다:

1. **JSX를 작성**
2. **Babel이 js/jsx-runtime 코드로 변환**
3. **React Element 객체 생성**
4. **React Fiber Node로 변환**
5. **DOM 업데이트(Reconciliation + Commit)**

React Element 형태 예시:

```js
{
  type: "h1",
  props: {
    children: "Hello"
  }
}
```

이는 **Virtual DOM 노드**이며
React Fiber는 이를 기반으로 Diffing/Reconciliation을 수행합니다.

---

# 🧩 7. JSX 컴파일 과정 깊이 살펴보기

아래 예제를 보겠습니다.

```jsx
const ui =
  <section className="box">
    <h2>{title}</h2>
    <ItemList items={items} />
  </section>;
```

### Babel → JS 변환

```js
import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";

const ui =
  _jsxs("section", {
    className: "box",
    children: [
      _jsx("h2", { children: title }),
      _jsx(ItemList, { items })
    ]
  });
```

JSX 인라인 구조는 **배열 기반 children**으로 컴파일됩니다.

---

# 🚀 8. JSX의 최적화

React는 다음 최적화를 수행합니다.

### ✔ 8-1. 정적 요소 hoisting (컴파일러 최적화)

```jsx
<div>Hello</div>
```

이런 요소는 매 렌더링마다 새로 생성되지 않고
컴파일 시 정적으로 보관할 수 있습니다.

### ✔ 8-2. 동일한 타입 요소의 diff 최적화

JSX는 Element의 최소 정보만 구조화하여
Fiber의 비교를 빠르게 합니다.

### ✔ 8-3. children 최적화

문자열, 숫자, 배열, 컴포넌트 모두
각기 다른 경로로 최적화됩니다.

---

# 🛠️ 9. JSX는 왜 생겨났는가?

● UI를 **선언적으로 작성**하기 위해
● HTML처럼 생긴 문법으로 **직관성을 제공**하기 위해
● JavaScript의 강력한 표현력을 그대로 사용하기 위해
● 템플릿 언어를 따로 만들지 않기 위해
● 컴파일러가 최적화하기 쉽도록 하기 위해

즉, **JSX는 템플릿 엔진이 아니라 JavaScript 자체의 확장 모델**입니다.

---

# 📚 10. 자주 발생하는 JSX 오류들

### ❌ 1. 여러 요소를 반환 (React는 하나만 허용)

```jsx
return (
  <h1>Hi</h1>
  <p>Hello</p>
); // 오류
```

### ✔ 해결

```jsx
return (
  <>
    <h1>Hi</h1>
    <p>Hello</p>
  </>
);
```

### ❌ 2. HTML 속성 그대로 사용

```jsx
<div class="box"></div>  // X
```

### ✔ 해결

```jsx
<div className="box"></div>  // O
```

---

# 🧩 11. 고급 JSX 패턴 예시

### ⭐ 조건부 렌더링 패턴

```jsx
{isLoading && <Spinner />}
```

### ⭐ 컴포넌트를 배열로 렌더링

```jsx
{items.map(item => (
  <Item key={item.id} {...item} />
))}
```

### ⭐ JSX 자체를 변수처럼 조립

```jsx
const Button = ({ primary }) =>
  primary
    ? <button className="primary">OK</button>
    : <button className="default">OK</button>;
```

---

# 🏁 총정리

| 개념       | 핵심 내용                                  |
| -------- | -------------------------------------- |
| JSX란?    | JavaScript 확장 문법 (HTML 아님)             |
| 무엇으로 변환? | `_jsx`, `_jsxs`, React.createElement`  |
| 내부 구조    | React Element → Fiber → DOM            |
| 장점       | 선언적 UI, 강력한 JS 표현력                     |
| 규칙       | camelCase, `{}` 표현식, 단일 부모 요소          |
| 최적화      | hoisting, children 구조화, fiber diff 최적화 |

JSX를 이해하면 React 렌더링 모델 자체를 이해한 것이며
이는 Hook, Reconciliation, Virtual DOM, Fiber 구조까지 연결되는 핵심 기초가 됩니다.

