React에서의 \*\*합성 이벤트(Synthetic Event)\*\*는 자바스크립트의 이벤트 시스템과 밀접한 관계가 있으며, 성능과 호환성, 일관성을 위해 고안된 핵심 기술입니다. 

---

# ⚙️ React의 합성 이벤트(Synthetic Event)란?

## ✅ 1. 정의

> **합성 이벤트(Synthetic Event)** 는
> React가 브라우저의 **네이티브 DOM 이벤트를 감싸서 만든 추상화된 이벤트 객체**입니다.

* `SyntheticEvent`는 **브라우저 간의 이벤트 동작 차이를 제거**하고,
* React 내부의 이벤트 시스템(이벤트 위임, 버블링, 최적화)을 구현하기 위해 사용됩니다.

---

## 🔍 2. 왜 SyntheticEvent를 쓰는가?

| 이유          | 설명                                                        |
| ----------- | --------------------------------------------------------- |
| ✅ 일관성       | 모든 브라우저에서 동일한 이벤트 인터페이스 제공                                |
| ✅ 퍼포먼스      | 이벤트 위임 + 재사용 가능한 이벤트 객체                                   |
| ✅ 캡처/버블링 지원 | 네이티브 이벤트처럼 동작 (단, React의 컨트롤 아래에 있음)                      |
| ✅ 메모리 관리    | 이벤트 객체는 **풀링(pooled)** 되며 자동 재사용됨 (단, React 17 이후 일부 변경됨) |

---

## 📦 3. 동작 구조

React는 이벤트 리스너를 **DOM 노드에 직접 등록하지 않고**,
최상위 루트(root) 요소에 **이벤트를 위임(delegate)** 합니다.

```html
<div id="root">
  <button>Click</button>
</div>
```

```jsx
<button onClick={handleClick}>Click</button>
```

⬇️ 내부적으로는 다음처럼 작동:

1. React는 `document` 또는 `root`에 한 번만 `click`, `change` 등 이벤트 리스너를 등록
2. 이벤트 발생 시 해당 리스너가 **버블링 이벤트를 가로채고**
3. 해당 컴포넌트에 매핑된 핸들러를 실행
4. 이벤트 객체는 `SyntheticEvent`로 전달됨

---

## 🧠 4. `SyntheticEvent` vs `DOM Event`

| 항목     | `SyntheticEvent`                        | 브라우저의 `Event`    |
| ------ | --------------------------------------- | ---------------- |
| 정의     | React가 만든 래퍼 이벤트                        | 브라우저가 생성         |
| 이벤트 풀링 | 있음 (React 17 이전까지)                      | 없음               |
| 일관성    | ✅ 모든 브라우저에서 동일                          | ❌ 브라우저마다 다를 수 있음 |
| 인터페이스  | 표준 DOM Event와 유사                        | 표준 또는 브라우저 고유 확장 |
| 사용법    | 동일 (`e.target`, `e.preventDefault()` 등) | 동일               |

> ✅ `SyntheticEvent`는 실제 `e.nativeEvent`에 원본 DOM 이벤트를 포함하고 있습니다.

---

## ✅ 5. 사용 예시

```jsx
function MyButton() {
  const handleClick = (e) => {
    console.log(e); // SyntheticEvent 객체
    console.log(e.nativeEvent); // 원본 DOM 이벤트 객체
  };

  return <button onClick={handleClick}>클릭</button>;
}
```

---

## 🔧 6. 이벤트 풀링(Pooling)

**React 17 이전**에는 `SyntheticEvent` 객체를 재사용(pooling)하므로,
비동기 작업에서 이벤트 객체를 참조하면 **null이 되거나 접근 오류**가 발생했습니다.

```jsx
function App() {
  const handleClick = (e) => {
    setTimeout(() => {
      // ❌ e.target은 null일 수 있음 (React 16 이하)
      console.log(e.target); 
    }, 1000);
  };
  return <button onClick={handleClick}>클릭</button>;
}
```

### ✅ 해결 방법

```jsx
// 1. 이벤트 정보 복사
const target = e.target;

// 2. 또는 이벤트 객체를 유지
e.persist();  // React 이벤트 풀링 해제
```

> React 17 이후는 자동으로 pooling이 비활성화되었기 때문에 `.persist()`는 대부분 불필요합니다.

---

## 🧩 7. 지원하는 Synthetic 이벤트 종류

React는 다음과 같은 **모든 표준 DOM 이벤트 타입**을 `SyntheticEvent`로 래핑합니다.

| 카테고리 | 이벤트                                                        |
| ---- | ---------------------------------------------------------- |
| 마우스  | `onClick`, `onDoubleClick`, `onMouseEnter`, `onMouseLeave` |
| 키보드  | `onKeyDown`, `onKeyUp`, `onKeyPress`                       |
| 폼    | `onChange`, `onSubmit`, `onInput`, `onFocus`, `onBlur`     |
| 포커스  | `onFocus`, `onBlur`                                        |
| 터치   | `onTouchStart`, `onTouchMove`, `onTouchEnd`                |
| 드래그  | `onDrag`, `onDrop`, `onDragOver`, `onDragStart`            |
| 기타   | `onScroll`, `onWheel`, `onCompositionStart`, ... 등 다수 지원   |

---

## 📌 8. 주의 사항

| 항목                    | 설명                           |
| --------------------- | ---------------------------- |
| `e.preventDefault()`  | 기본 동작 취소                     |
| `e.stopPropagation()` | 이벤트 버블링 중단                   |
| `e.nativeEvent`       | 원본 DOM 이벤트 참조                |
| `e.persist()`         | 이벤트 풀링 해제 (React 17 이하에서 필요) |

---

## ✅ 요약

| 항목   | 설명                                                |
| ---- | ------------------------------------------------- |
| 이름   | SyntheticEvent                                    |
| 정의   | React가 만든 래퍼 이벤트 객체                               |
| 목적   | 브라우저 간 일관성, 성능 최적화, 추상화                           |
| 특징   | pooling, 위임, 일관 API, 버블링 지원                       |
| 사용법  | DOM 이벤트와 거의 동일 (`e.target`, `e.preventDefault()`) |
| 실전 팁 | React 17 이상은 pooling 비활성화, `e.persist()` 거의 불필요   |

