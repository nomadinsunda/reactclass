![Image](https://ko.redux.js.org/assets/images/ReduxDataFlowDiagram-49fa8c3968371d9ef6f2a1486bd40a26.gif)

![Image](https://redux.js.org/assets/images/ReduxAsyncDataFlowDiagram-d97ff38a0f4da0f327163170ccc13e80.gif)

![Image](https://media2.dev.to/dynamic/image/width%3D800%2Cheight%3D%2Cfit%3Dscale-down%2Cgravity%3Dauto%2Cformat%3Dauto/https%3A%2F%2Fdev-to-uploads.s3.amazonaws.com%2Fuploads%2Farticles%2F83uixo3rjc0u8x0qmf6e.png)

---

## 1️⃣ configureStore란?

`configureStore`는 **Redux Toolkit**이 제공하는
👉 **Redux Store를 ‘실무 표준 설정’으로 한 번에 만들어 주는 함수**입니다.

```js
import { configureStore } from '@reduxjs/toolkit'

export const store = configureStore({
  reducer: {
    cardEntities,
    listidCardids
  }
})
```

핵심 요약:

> ❌ createStore + applyMiddleware + compose
> ❌ 미들웨어 수동 설정
> ❌ DevTools 조건 분기
>
> ✅ configureStore 한 줄

---

## 2️⃣ configureStore가 등장한 이유 (역사적 배경)

### 예전 Redux (RTK 이전)

```js
import { createStore, applyMiddleware, compose } from 'redux'
import thunk from 'redux-thunk'

const store = createStore(
  rootReducer,
  compose(
    applyMiddleware(thunk),
    window.__REDUX_DEVTOOLS_EXTENSION__ &&
      window.__REDUX_DEVTOOLS_EXTENSION__()
  )
)
```

문제점:

* 보일러플레이트 과다
* DevTools 설정 실수 빈번
* 미들웨어 순서 실수
* 초보자 진입장벽 🚧

👉 이 문제를 **Redux 팀 스스로가 인정**하고 만든 해법이 `configureStore`입니다.

---

## 3️⃣ configureStore의 정확한 정체

아주 중요합니다.

> **configureStore는 “새로운 상태 관리 시스템”이 아닙니다.**
> **Redux의 `createStore`를 감싼 ‘공식 프리셋 함수’입니다.**

내부적으로 하는 일은 다음과 같습니다 👇

---

## 4️⃣ 내부에서 자동으로 해주는 것들 (핵심)

### ✅ 1. Redux Store 생성

```txt
createStore() + combineReducers()
```

```js
configureStore({
  reducer: { a, b }
})
```

⬇️ 내부적으로

```js
combineReducers({ a, b })
```

---

### ✅ 2. thunk 미들웨어 자동 포함

```js
dispatch(thunkFn)
```

이게 가능한 이유는:

```txt
redux-thunk 가 기본 포함
```

👉 그래서 **별도 설정 없이 비동기 dispatch 가능**

---

### ✅ 3. Redux DevTools 자동 연결

* 개발 환경 → ON
* 운영 환경 → OFF

👉 조건 분기 코드 필요 ❌

---

### ✅ 4. 개발용 안전장치 미들웨어 기본 포함

기본적으로 이런 것들이 들어 있습니다:

| 미들웨어              | 역할             |
| ----------------- | -------------- |
| immutableCheck    | state 직접 변경 감지 |
| serializableCheck | 직렬화 불가 값 감지    |
| thunk             | 비동기 action 지원  |

👉 실무에서 터질 버그를 **초기에 차단**

---

## 5️⃣ configureStore vs createStore (한 눈 정리)

| 항목          | createStore | configureStore |
| ----------- | ----------- | -------------- |
| Redux 공식 추천 | ❌           | ✅              |
| thunk 설정    | 수동          | 자동             |
| DevTools    | 수동          | 자동             |
| 안전 검사       | 없음          | 기본 포함          |
| 실무 사용성      | 낮음          | 매우 높음          |

👉 **Redux 팀 공식 입장**

> “새 프로젝트에서는 createStore를 쓰지 마세요”

---

## 6️⃣ 왜 `useDispatch()` / `useSelector()`가 그대로 작동하는가?

아주 중요한 연결 포인트입니다.

```js
const store = configureStore(...)
```

이 결과물은:

```txt
정통 Redux Store 객체
```

즉:

```js
store.dispatch
store.getState
store.subscribe
```

모두 그대로 존재합니다.

그래서:

```js
import { useDispatch, useSelector } from 'react-redux'
```

이 훅들이 **아무 문제 없이 작동**합니다
(react-redux는 “store 형태”만 요구합니다).

---

## 7️⃣ configureStore는 “아키텍처 강제 장치”다

이게 정말 중요한 포인트입니다.

configureStore는 단순 편의 함수가 아니라:

* slice 중심 설계 유도
* thunk 기반 비동기 흐름 유도
* action 직렬화 강제
* devtools 기반 디버깅 습관 강제

👉 **Redux를 ‘이상적으로 쓰게 만드는 가드레일’**

---

## 8️⃣ 지금 사용 중인 코드와 정확히 연결하면

```js
const store = configureStore({
  reducer: {
    cardEntities,
    listidCardids
  }
})
```

이 구조 덕분에:

```js
useSelector(({ cardEntities, listidCardids }) => ...)
```

가 **정확히 도메인 단위로 분리된 상태 접근**이 됩니다.

👉 configureStore는
**“store 구조 = 도메인 구조”를 강제**합니다.

---

## 🧾 강의용 한 줄 정의

> **configureStore는 Redux Store를
> 실무에서 안전하고 일관되게 사용하도록
> 모든 필수 설정을 기본값으로 묶어 제공하는
> Redux Toolkit의 핵심 API이다.**

