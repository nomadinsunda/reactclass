

# 🌊 React Hook 완전 정복: **Effect Hook 사용하기 (`useEffect`)**


---

## 🧠 useEffect란?

React 16.8부터 도입된 `useEffect` Hook은 **함수형 컴포넌트에서 side effect(부수 효과)** 를 처리할 수 있게 해줍니다.

📌 **Side effect란?**

> 데이터를 가져오기, 구독(subscription), DOM 조작, 타이머 설정 등
> “컴포넌트 렌더링 외적인 모든 작업”을 의미합니다.

```jsx
import { useEffect } from 'react';

useEffect(() => {
  console.log('렌더링 이후 실행됨');
});
```

기존 클래스 컴포넌트에서 `componentDidMount`, `componentDidUpdate`, `componentWillUnmount`를 합쳐놓은 것과 유사하게 작동합니다.

---

## 🔍 기본 예제: 문서 타이틀 변경

```jsx
import React, { useState, useEffect } from 'react';

function Example() {
  const [count, setCount] = useState(0);

  useEffect(() => {
    document.title = `You clicked ${count} times`;
  });

  return (
    <div>
      <p>You clicked {count} times</p>
      <button onClick={() => setCount(count + 1)}>Click me</button>
    </div>
  );
}
```

✅ 이 코드는 다음과 같이 작동합니다:

* 처음 렌더링 + `count`가 업데이트될 때마다 `useEffect` 실행
* `document.title`을 최신 클릭 횟수로 갱신

---

## 🧩 클래스 컴포넌트와 비교

```jsx
class Example extends React.Component {
  componentDidMount() {
    document.title = `You clicked ${this.state.count} times`;
  }
  componentDidUpdate() {
    document.title = `You clicked ${this.state.count} times`;
  }
}
```

클래스에서는 생명주기 메서드마다 같은 로직을 중복해서 넣어야 했습니다.
**Hook은 이 로직을 하나의 `useEffect()`로 통합**합니다.

---

## ⚙️ 클린업 함수로 정리하기 (Clean-up)

구독이나 타이머처럼 컴포넌트가 사라질 때 해제해야 하는 작업은 **정리(clean-up) 함수**를 `useEffect` 내부에서 반환하여 처리합니다.

```jsx
useEffect(() => {
  const id = setInterval(() => console.log('Tick'), 1000);
  return () => clearInterval(id); // 정리
}, []);
```

🧹 정리 함수는 다음 시점에 호출됩니다:

* 컴포넌트가 언마운트될 때
* effect가 다시 실행되기 전에

---

## 📬 예시: 친구 상태 구독 관리

```jsx
function FriendStatus({ friend }) {
  const [isOnline, setIsOnline] = useState(null);

  useEffect(() => {
    function handleStatusChange(status) {
      setIsOnline(status.isOnline);
    }

    ChatAPI.subscribeToFriendStatus(friend.id, handleStatusChange);
    return () => {
      ChatAPI.unsubscribeFromFriendStatus(friend.id, handleStatusChange);
    };
  }, [friend.id]);
}
```

✅ `props.friend.id`가 바뀔 때만 effect가 다시 실행되며,
✅ 이전 구독은 정리(clean-up)되고 새로 설정됩니다.

---

## 🧠 왜 useEffect는 렌더링마다 실행될까?

React는 `useEffect`를 컴포넌트 렌더링 이후에 실행합니다.
렌더링 결과에 의존하는 작업(즉, side effect)은 렌더링 후에 실행되어야 안전하기 때문입니다.
각 `effect`는 **렌더링에 귀속된 독립적인 함수**로 취급되며, 최신 상태와 props를 사용할 수 있습니다.

---

## 🧪 성능 최적화: 의존성 배열

렌더링마다 매번 effect를 실행하지 않으려면 **두 번째 인자로 의존성 배열**을 전달합니다.

```jsx
useEffect(() => {
  console.log('count가 변경될 때만 실행됩니다.');
}, [count]);
```

* `[count]`: `count`가 변경될 때만 실행
* `[]`: 처음 한 번만 실행 (마운트 시)
* 생략: 매 렌더링마다 실행

---

## 🧷 주의: 의존성 배열 제대로 관리하기

* `useEffect`는 클로저를 통해 내부에서 사용한 모든 변수들을 기억합니다.
* 따라서 의존성 배열에 해당 변수들을 **빠짐없이** 명시해야 합니다.
* 이를 자동으로 검사하려면 `eslint-plugin-react-hooks`의 `exhaustive-deps` 규칙을 적용하세요.

```bash
npm install eslint-plugin-react-hooks --save-dev
```

---

## 🔥 다중 effect로 관심사 분리하기

`useEffect`는 여러 번 호출해도 됩니다.
→ 관련 없는 로직은 별도의 `effect`로 분리하는 것이 **가독성**과 **디버깅**에 도움이 됩니다.

```jsx
function FriendStatusWithCounter({ friend }) {
  const [count, setCount] = useState(0);
  const [isOnline, setIsOnline] = useState(null);

  useEffect(() => {
    document.title = `You clicked ${count} times`;
  }, [count]);

  useEffect(() => {
    function handleStatusChange(status) {
      setIsOnline(status.isOnline);
    }
    ChatAPI.subscribeToFriendStatus(friend.id, handleStatusChange);
    return () => {
      ChatAPI.unsubscribeFromFriendStatus(friend.id, handleStatusChange);
    };
  }, [friend.id]);
}
```

---

## 📌 요약

| 기능           | 클래스 방식                   | Hook 방식                       |
| ------------ | ------------------------ | ----------------------------- |
| 초기 실행        | `componentDidMount()`    | `useEffect(..., [])`          |
| 상태 업데이트 후 실행 | `componentDidUpdate()`   | `useEffect(..., [deps])`      |
| 언마운트 시 정리    | `componentWillUnmount()` | `useEffect → return () => {}` |
| 중복 로직 제거     | 어렵고 분산됨                  | 하나의 Hook 함수로 통합 가능            |
| 관심사 분리       | 복잡하고 결합도 높음              | `useEffect` 여러 개로 나눠서 구성 가능   |

---

## ✨ 마무리

`useEffect`는 React 함수형 컴포넌트에서 side effect를 안전하게 처리할 수 있는 **가장 핵심적인 Hook**입니다.

* `DOM 조작`, `데이터 fetch`, `타이머`, `구독 관리` 등 대부분의 비 UI 로직은 `useEffect`에서 처리하세요.
* **정리 함수 반환**, **의존성 배열 관리**, **effect 분리 구성**은 숙련된 React 개발자로 가는 첫걸음입니다.

---

📚 참고 링크:

* [공식 문서: useEffect](https://react.dev/reference/react/useEffect)
* [공식 문서: Synchronizing with Effects](https://react.dev/learn/synchronizing-with-effects)
* [Hook 규칙](https://react.dev/learn/rules-of-hooks)

