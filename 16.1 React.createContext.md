# React `createContext`와 다중 Context 관리

## — Context 객체의 정의부터 역할별 Context 분리까지

React의 Context API를 이해하려면 가장 먼저 알아야 할 함수가 있습니다.

```jsx
createContext()
```

많은 입문자가 `createContext()`를 처음 보면 다음처럼 생각합니다.

```text
createContext()
      ↓
전역 상태 저장소 생성?
```

하지만 정확히는 아닙니다.

---

# 1. `createContext()`란?

`createContext()`는 React가 제공하는 함수로,

> **컴포넌트 트리 안에서 특정 종류의 값을 공급하고 구독할 수 있도록 하는 Context 객체를 생성하는 함수입니다.**

기본 문법은 다음과 같습니다.

```jsx
import { createContext } from 'react';

const MyContext = createContext(defaultValue);
```

예를 들어:

```jsx
import { createContext } from 'react';

const ThemeContext = createContext('light');
```

이 코드를 실행하면 `ThemeContext`라는 **Context 객체**가 생성됩니다.

개념적으로 보면:

```text
createContext('light')
        │
        ▼
┌──────────────────────┐
│    ThemeContext      │
│                      │
│ React Context 객체    │
│ defaultValue: light  │
└──────────────────────┘
```

중요한 점은 `createContext()`가 `"light"`라는 값을 계속 저장하는 state 저장소를 만든 것이 아니라는 것입니다.

---

# 2. `createContext()`는 state를 만들지 않는다

다음 두 함수는 역할이 완전히 다릅니다.

```jsx
useState('light');
```

```jsx
createContext('light');
```

`useState()`는:

```text
useState()
    │
    ▼
상태를 생성하고 보관
    │
    ▼
상태가 바뀌면
컴포넌트 업데이트
```

역할을 합니다.

반면 `createContext()`는:

```text
createContext()
      │
      ▼
Context 객체 생성
      │
      ▼
값을 공급하고 구독할
통로를 정의
```

합니다.

따라서 다음처럼 역할을 구분해야 합니다.

```text
useState / useReducer
        │
        ▼
    상태 관리


createContext
        │
        ▼
 Context 통로 정의
```

---

# 3. `createContext()`가 반환하는 것은 Context 객체다

다음 코드를 보겠습니다.

```jsx
const ThemeContext = createContext('light');
```

`ThemeContext`는 단순 문자열도 아니고 state도 아닙니다.

React가 사용하는 **Context 객체**입니다.

이 Context 객체는 이후 두 가지 중요한 작업에 사용됩니다.

```text
                ThemeContext
                     │
          ┌──────────┴──────────┐
          ▼                     ▼
     Provider 역할          Consumer에서 사용
          │                     │
          ▼                     ▼
     value 공급        useContext(ThemeContext)
```

React 19에서는 다음처럼 Context 객체 자체를 Provider로 렌더링할 수 있습니다.

```jsx
<ThemeContext value="dark">
  <App />
</ThemeContext>
```

그리고 하위 컴포넌트에서는:

```jsx
const theme = useContext(ThemeContext);
```

로 값을 읽습니다.

즉 같은 `ThemeContext` 객체가 **공급자와 소비자를 연결하는 식별자** 역할을 합니다.

---

# 4. Context 객체 자체가 식별자다

다음 코드가 핵심입니다.

```jsx
const ThemeContext = createContext('light');
```

React 입장에서 중요한 것은 `"ThemeContext"`라는 변수 이름이 아닙니다.

`createContext()`가 반환한 **그 객체 자체**가 Context의 정체성입니다.

예를 들어:

```jsx
const ContextA = createContext('light');
const ContextB = createContext('light');
```

두 Context의 기본값은 동일하지만:

```js
ContextA !== ContextB
```

입니다.

즉:

```text
ContextA
   │
   └── 독립적인 Context


ContextB
   │
   └── 또 다른 독립적인 Context
```

입니다.

그래서 Provider가 `ContextA`를 사용하고 소비자가 `ContextB`를 사용한다면 값은 연결되지 않습니다.

```jsx
const ContextA = createContext('light');
const ContextB = createContext('light');

function App() {
  return (
    <ContextA value="dark">
      <Child />
    </ContextA>
  );
}

function Child() {
  const value = useContext(ContextB);

  console.log(value);
}
```

결과는:

```text
light
```

입니다.

`Child`는 `ContextA`가 아니라 전혀 다른 `ContextB`를 읽고 있기 때문입니다.

---

# 5. `defaultValue`는 무엇인가?

`createContext()`에는 기본값을 전달할 수 있습니다.

```jsx
const ThemeContext = createContext('light');
```

여기서:

```text
'light'
```

가 `defaultValue`입니다.

하지만 이 값의 의미를 정확히 이해해야 합니다.

> **`defaultValue`는 현재 컴포넌트 위쪽에 해당 Context Provider가 하나도 존재하지 않을 때 사용하는 값입니다.**

예를 들어:

```jsx
const ThemeContext = createContext('light');

function Button() {
  const theme = useContext(ThemeContext);

  return <button>{theme}</button>;
}
```

Provider가 없다면:

```text
Button
  │
  │ useContext(ThemeContext)
  ▼
Provider 찾기
  │
  └── 없음
       │
       ▼
 defaultValue 사용
       │
       ▼
     "light"
```

가 됩니다.

---

# 6. Provider가 있다면 `defaultValue`는 사용되지 않는다

다음과 같이 Provider를 추가하면:

```jsx
<ThemeContext value="dark">
  <Button />
</ThemeContext>
```

`Button`에서는:

```jsx
const theme = useContext(ThemeContext);
```

결과가:

```text
dark
```

가 됩니다.

구조적으로:

```text
ThemeContext
value = "dark"
│
└── Button
      │
      │ useContext(ThemeContext)
      ▼
    "dark"
```

입니다.

즉:

```text
Provider 없음
      │
      ▼
defaultValue


Provider 있음
      │
      ▼
Provider의 value
```

입니다.

---

# 7. Provider의 value가 `undefined`여도 기본값으로 돌아가지 않는다

여기서 흔히 오해하는 부분이 있습니다.

```jsx
const ThemeContext = createContext('light');

<ThemeContext value={undefined}>
  <Button />
</ThemeContext>
```

이 경우 결과는:

```js
undefined
```

입니다.

다음처럼 동작하지 않습니다.

```text
undefined
   ↓
기본값 light 사용
```

Provider가 존재하기 때문에 `defaultValue`는 사용되지 않습니다.

정확한 규칙은:

```text
Provider 없음
      │
      ▼
defaultValue


Provider 있음
      │
      ▼
Provider.value
      │
      ├── "dark"
      ├── null
      └── undefined
```

입니다.

---

# 8. `createContext → Provider → useContext` 관계

지금까지의 흐름을 연결하면 Context API의 기본 구조가 보입니다.

```text
① createContext()
       │
       ▼
Context 객체 생성
       │
       ▼
② Provider
       │
       │ value 공급
       ▼
Component Tree
       │
       ▼
③ useContext(Context)
       │
       ▼
현재 Context value 읽기
       +
Context 변경 구독
```

코드로 보면:

```jsx
import {
  createContext,
  useContext,
} from 'react';

const ThemeContext = createContext('light');
```

값 공급:

```jsx
<ThemeContext value="dark">
  <ThemeButton />
</ThemeContext>
```

값 소비:

```jsx
function ThemeButton() {
  const theme = useContext(ThemeContext);

  return <button>{theme}</button>;
}
```

이 세 단계가 Context API의 가장 기본적인 사용법입니다.

---

# 9. 그런데 왜 Context를 여러 개 만드는가?

이제 `createContext()`가 무엇인지 알았으니 **다중 Context**가 왜 필요한지도 자연스럽게 이해할 수 있습니다.

애플리케이션에는 서로 성격이 다른 공통 값들이 있습니다.

```text
로그인 사용자
테마
언어
권한
UI 설정
```

이것들을 하나의 Context에 모두 넣을 수도 있습니다.

```jsx
import { createContext } from 'react';

const AppContext = createContext({
  user: null,
  theme: 'light',
  language: 'ko',
});
```

구조적으로:

```text
AppContext
│
├── user
├── theme
└── language
```

입니다.

문제는 이 Context를 구독하면 **특정 필드가 아니라 AppContext 자체를 구독한다는 점**입니다.

---

# 10. 하나의 Context에 모든 값을 넣었을 때의 문제

Provider에서 다음처럼 공급한다고 해봅시다.

```jsx
<AppContext
  value={{
    user,
    theme,
    language,
  }}
>
  <App />
</AppContext>
```

여러 컴포넌트가 각각 다른 값을 사용합니다.

```text
AppContext
value = {
  user,
  theme,
  language
}
       │
       ├── Header
       │     └── user 사용
       │
       ├── ThemeButton
       │     └── theme 사용
       │
       └── LanguageMenu
             └── language 사용
```

이제 `theme`만 변경됩니다.

```text
theme

"light"
   ↓
"dark"
```

그러면 Provider의 전체 `value` 객체가 변경됩니다.

```text
기존 value
{
  user,
  theme: "light",
  language
}

        ↓

새 value
{
  user,
  theme: "dark",
  language
}
```

따라서 `AppContext`를 구독하는 컴포넌트들은 Context 업데이트의 영향을 받습니다.

```text
AppContext value 변경
          │
    ┌─────┼────────────┐
    ▼     ▼            ▼
 Header ThemeButton LanguageMenu
```

---

# 11. 구조분해는 부분 구독이 아니다

다음 코드가 있다고 해봅시다.

```jsx
const { user } = useContext(AppContext);
```

겉으로 보면 `user`만 구독하는 것처럼 보일 수 있습니다.

하지만 실제 의미는:

```text
useContext(AppContext)
        │
        ▼
AppContext value 전체 읽기
        │
        ▼
그 결과에서 user만 구조분해
```

입니다.

즉:

```text
AppContext.user 구독   X

AppContext 구독
      ↓
user 사용             O
```

입니다.

따라서 Context의 다른 필드가 바뀌더라도 Provider의 `value`가 변경되면 소비자는 Context 업데이트 대상이 됩니다.

---

# 12. 해결 — 역할별로 `createContext()`를 호출한다

서로 독립적인 관심사라면 Context 자체를 분리하면 됩니다.

```jsx
// contexts/UserContext.js

import { createContext } from 'react';

export const UserContext = createContext(null);
```

```jsx
// contexts/ThemeContext.js

import { createContext } from 'react';

export const ThemeContext = createContext('light');
```

```jsx
// contexts/LanguageContext.js

import { createContext } from 'react';

export const LanguageContext = createContext('ko');
```

이제 `createContext()`를 세 번 호출했으므로 서로 독립적인 세 Context 객체가 존재합니다.

```text
createContext(null)
        │
        ▼
   UserContext


createContext('light')
        │
        ▼
   ThemeContext


createContext('ko')
        │
        ▼
 LanguageContext
```

그리고:

```text
UserContext !== ThemeContext

ThemeContext !== LanguageContext

UserContext !== LanguageContext
```

입니다.

---

# 13. 다중 Context의 핵심 구조

Context를 분리하면 다음과 같이 됩니다.

```text
              Context 분리

┌──────────────────┐
│   UserContext    │
│                  │
│       user       │
└──────────────────┘


┌──────────────────┐
│  ThemeContext    │
│                  │
│      theme       │
└──────────────────┘


┌──────────────────┐
│ LanguageContext  │
│                  │
│    language      │
└──────────────────┘
```

각 Context는 완전히 독립적입니다.

따라서 컴포넌트가 자신에게 필요한 Context만 구독할 수 있습니다.

---

# 14. 여러 Provider를 중첩해서 공급하기

React 19에서는 Context 객체 자체를 Provider로 사용할 수 있습니다.

```jsx
import { UserContext } from './contexts/UserContext';
import { ThemeContext } from './contexts/ThemeContext';
import { LanguageContext } from './contexts/LanguageContext';

export default function App() {
  const user = {
    id: 1,
    name: '홍길동',
  };

  return (
    <UserContext value={user}>
      <ThemeContext value="dark">
        <LanguageContext value="ko">
          <MainApp />
        </LanguageContext>
      </ThemeContext>
    </UserContext>
  );
}
```

트리 구조는 다음과 같습니다.

```text
UserContext
value = user
│
└── ThemeContext
    value = "dark"
    │
    └── LanguageContext
        value = "ko"
        │
        └── MainApp
            │
            ├── Header
            ├── ThemeButton
            └── LanguageMenu
```

`MainApp` 아래에서는 세 Context를 모두 사용할 수 있습니다.

하지만 모든 컴포넌트가 세 Context를 자동으로 구독하는 것은 아닙니다.

---

# 15. 필요한 Context만 `useContext()`로 구독

`Header`는 사용자 정보만 사용합니다.

```jsx
import { useContext } from 'react';
import { UserContext } from '../contexts/UserContext';

export default function Header() {
  const user = useContext(UserContext);

  return (
    <header>
      {user ? `${user.name}님` : '로그인 필요'}
    </header>
  );
}
```

`ThemeButton`은 테마만 사용합니다.

```jsx
import { useContext } from 'react';
import { ThemeContext } from '../contexts/ThemeContext';

export default function ThemeButton() {
  const theme = useContext(ThemeContext);

  return (
    <button>
      현재 테마: {theme}
    </button>
  );
}
```

`LanguageMenu`는 언어만 사용합니다.

```jsx
import { useContext } from 'react';
import { LanguageContext } from '../contexts/LanguageContext';

export default function LanguageMenu() {
  const language = useContext(LanguageContext);

  return (
    <div>
      현재 언어: {language}
    </div>
  );
}
```

의존 관계는:

```text
UserContext
     │
     └──────────▶ Header


ThemeContext
     │
     └──────────▶ ThemeButton


LanguageContext
     │
     └──────────▶ LanguageMenu
```

가 됩니다.

---

# 16. ThemeContext만 변경되면?

예를 들어:

```text
ThemeContext

"light"
   │
   ▼
"dark"
```

로 변경되었다고 해봅시다.

Context 자체를 분리했기 때문에 직접적인 Context 업데이트 관계는:

```text
ThemeContext value 변경
          │
          ▼
     ThemeButton
          │
          ▼
      re-render
```

입니다.

`UserContext`만 구독하는 `Header`는 ThemeContext 변경 자체의 직접적인 업데이트 대상이 아닙니다.

```text
ThemeContext 변경

Header            X
ThemeButton       O
LanguageMenu      X
```

단, 부모 컴포넌트의 일반적인 리렌더로 인해 자식 컴포넌트가 다시 실행되는 문제는 Context 구독과 별개의 React 렌더링 규칙입니다.

---

# 17. 다중 Context를 사용하는 진짜 이유

여기까지 보면 다중 Context의 목적이 명확해집니다.

다음처럼 단순히:

```text
Context를 많이 만들면
성능이 좋아진다
```

가 아닙니다.

보다 정확하게는:

```text
관심사를 분리
      ↓
Context 객체 분리
      ↓
Context dependency 분리
      ↓
필요한 Context만 구독
      ↓
Context 변경의 직접적인
업데이트 범위 분리
```

입니다.

---

# 18. 여러 Provider는 `AppProviders`로 정리할 수 있다

Context가 늘어나면 다음 코드가 길어집니다.

```jsx
<UserContext value={user}>
  <ThemeContext value={theme}>
    <LanguageContext value={language}>
      <PermissionContext value={permission}>
        <App />
      </PermissionContext>
    </LanguageContext>
  </ThemeContext>
</UserContext>
```

Provider 구성을 별도의 컴포넌트로 분리하면 더 깔끔합니다.

```jsx
// AppProviders.jsx

import { useMemo } from 'react';

import { UserContext } from './contexts/UserContext';
import { ThemeContext } from './contexts/ThemeContext';
import { LanguageContext } from './contexts/LanguageContext';

export default function AppProviders({ children }) {
  const user = useMemo(
    () => ({
      id: 1,
      name: '홍길동',
    }),
    []
  );

  return (
    <UserContext value={user}>
      <ThemeContext value="dark">
        <LanguageContext value="ko">
          {children}
        </LanguageContext>
      </ThemeContext>
    </UserContext>
  );
}
```

`main.jsx`에서는:

```jsx
<AppProviders>
  <App />
</AppProviders>
```

만 작성합니다.

구조는:

```text
main.jsx
   │
   ▼
AppProviders
   │
   ├── UserContext
   ├── ThemeContext
   └── LanguageContext
   │
   ▼
  App
```

입니다.

---

# 19. 객체형 Context value는 참조에 주의

다음 코드를 보겠습니다.

```jsx
<UserContext value={{ name: '홍길동' }}>
  {children}
</UserContext>
```

객체 리터럴은 컴포넌트가 실행될 때마다 새 객체가 만들어집니다.

```text
Render #1

value ─────▶ Object A


Render #2

value ─────▶ Object B
```

JavaScript에서는:

```js
ObjectA !== ObjectB
```

입니다.

따라서 Provider가 다른 이유로 다시 렌더링되어도 Context의 `value` 참조가 달라질 수 있습니다.

필요하다면:

```jsx
const user = useMemo(
  () => ({
    name: '홍길동',
  }),
  []
);
```

로 참조를 안정화할 수 있습니다.

```jsx
<UserContext value={user}>
  {children}
</UserContext>
```

---

# 20. 문자열 같은 원시값은 이런 객체 참조 문제가 없다

다음 코드는:

```jsx
<ThemeContext value="dark">
  {children}
</ThemeContext>
```

문제가 없습니다.

문자열 `"dark"`는 객체 리터럴처럼 매번 새로운 객체 참조가 만들어지는 형태가 아니기 때문입니다.

따라서:

```jsx
const theme = useMemo(
  () => 'dark',
  []
);
```

처럼 할 필요는 없습니다.

---

# 21. 실전에서는 `null + Custom Hook` 패턴이 유용하다

다시 `createContext()`의 기본값 이야기로 돌아가 보겠습니다.

다음처럼:

```jsx
const ThemeContext = createContext('light');
```

만들면 Provider를 실수로 빼먹더라도 `"light"`가 반환되기 때문에 실수를 발견하기 어려울 수 있습니다.

그래서 실무에서는:

```jsx
const ThemeContext = createContext(null);
```

처럼 만드는 패턴도 자주 사용합니다.

전체 예제를 보겠습니다.

```jsx
// contexts/ThemeContext.jsx

import {
  createContext,
  useContext,
  useMemo,
  useState,
} from 'react';

const ThemeContext = createContext(null);

export function ThemeProvider({ children }) {
  const [theme, setTheme] = useState('light');

  const value = useMemo(
    () => ({
      theme,

      toggleTheme() {
        setTheme((prev) =>
          prev === 'light'
            ? 'dark'
            : 'light'
        );
      },
    }),
    [theme]
  );

  return (
    <ThemeContext value={value}>
      {children}
    </ThemeContext>
  );
}

export function useTheme() {
  const context = useContext(ThemeContext);

  if (context === null) {
    throw new Error(
      'useTheme must be used within <ThemeProvider>'
    );
  }

  return context;
}
```

컴포넌트에서는:

```jsx
import { useTheme } from '../contexts/ThemeContext';

export default function ThemeButton() {
  const {
    theme,
    toggleTheme,
  } = useTheme();

  return (
    <button onClick={toggleTheme}>
      현재 테마: {theme}
    </button>
  );
}
```

사용합니다.

---

# 22. 왜 Custom Hook으로 감싸는가?

직접:

```jsx
useContext(ThemeContext);
```

를 모든 컴포넌트에서 호출하는 대신:

```jsx
useTheme();
```

를 제공합니다.

구조는:

```text
Component
    │
    ▼
 useTheme()
    │
    ▼
useContext(ThemeContext)
    │
    ▼
ThemeContext
```

입니다.

장점은 다음과 같습니다.

### Provider 누락 감지

```jsx
if (context === null) {
  throw new Error(
    'useTheme must be used within <ThemeProvider>'
  );
}
```

Provider가 빠지면 즉시 명확한 에러가 발생합니다.

### 컴포넌트 코드 단순화

```jsx
const {
  theme,
  toggleTheme,
} = useTheme();
```

만 알면 됩니다.

### Context 객체 캡슐화

`ThemeContext` 자체를 export하지 않고:

```text
ThemeProvider
useTheme
```

만 외부에 공개할 수도 있습니다.

---

# 23. 다중 Context 실전 구조

프로젝트가 커진다면 다음처럼 구성할 수 있습니다.

```text
src/
│
├── contexts/
│   │
│   ├── AuthContext.jsx
│   ├── ThemeContext.jsx
│   └── LocaleContext.jsx
│
├── providers/
│   └── AppProviders.jsx
│
├── components/
│   ├── Header.jsx
│   ├── ThemeButton.jsx
│   └── LanguageMenu.jsx
│
├── App.jsx
│
└── main.jsx
```

그리고:

```jsx
// AppProviders.jsx

import { AuthProvider } from '../contexts/AuthContext';
import { ThemeProvider } from '../contexts/ThemeContext';
import { LocaleProvider } from '../contexts/LocaleContext';

export default function AppProviders({ children }) {
  return (
    <AuthProvider>
      <ThemeProvider>
        <LocaleProvider>
          {children}
        </LocaleProvider>
      </ThemeProvider>
    </AuthProvider>
  );
}
```

처럼 관리할 수 있습니다.

---

# 24. Context는 얼마나 나눠야 할까?

Context를 무조건 많이 만든다고 좋은 것은 아닙니다.

가장 중요한 판단 기준은 두 가지입니다.

```text
        Context 분리 기준

      함께 사용되는가?
             +
      함께 변경되는가?
             │
             ▼
       Context 경계 결정
```

예를 들어:

| Context             | 값                         |
| ------------------- | ------------------------- |
| `AuthContext`       | `user`, `login`, `logout` |
| `ThemeContext`      | `theme`, `toggleTheme`    |
| `LocaleContext`     | `locale`, `setLocale`     |
| `PermissionContext` | 권한 정보                     |

서로 **변경 이유가 다르고 사용하는 컴포넌트도 다르면** Context를 나누는 것이 자연스럽습니다.

---

# 25. 전체 흐름 정리

`createContext()`부터 다중 Context까지 전체 구조를 연결해보면:

```text
             createContext()
                   │
                   ▼
             Context 객체
                   │
                   ▼
          역할별 Context 생성
                   │
       ┌───────────┼───────────┐
       ▼           ▼           ▼
 UserContext  ThemeContext  LocaleContext
       │           │           │
       ▼           ▼           ▼
   Provider     Provider     Provider
       │           │           │
       └───────────┼───────────┘
                   ▼
             App Subtree
                   │
       ┌───────────┼───────────┐
       ▼           ▼           ▼
    useAuth     useTheme    useLocale
       │           │           │
       ▼           ▼           ▼
      user        theme       locale
```

특정 Context 값이 변경되면:

```text
ThemeContext value 변경
          │
          ▼
ThemeContext를 구독한
    Consumer Fiber
          │
          ▼
    업데이트 스케줄링
          │
          ▼
       re-render
```

됩니다.

---

# 26. 최종 핵심 정리

먼저 `createContext()`를 정확히 정의하면:

> **`createContext(defaultValue)`는 특정 종류의 값을 컴포넌트 트리 안에서 공급하고 구독할 수 있도록 식별하는 React Context 객체를 생성하는 함수입니다.**

그리고 역할은:

```text
createContext()
      │
      ▼
Context 객체 생성


Provider
      │
      ▼
Context value 공급


useContext(Context)
      │
      ▼
Context value 읽기 + 구독
```

입니다.

중요한 것은:

```text
createContext
      ≠
state 생성

createContext
      ≠
전역 저장소 생성
```

이라는 점입니다.

`state`는:

```text
useState
useReducer
외부 Store
```

등이 관리하고,

Context는:

```text
이미 존재하는 값
      │
      ▼
컴포넌트 트리에 공급
      │
      ▼
필요한 컴포넌트가 구독
```

하는 역할을 담당합니다.

그리고 **다중 Context**란:

> **`createContext()`를 역할별로 호출하여 서로 독립적인 Context 객체를 만들고, 컴포넌트가 자신에게 필요한 Context만 구독하도록 설계하는 방식입니다.**

핵심 흐름은:

```text
하나의 큰 Context
       │
       ▼
여러 관심사가 하나의
Context dependency로 묶임
       │
       ▼
역할별 createContext()
       │
       ▼
Context dependency 분리
       │
       ▼
필요한 Context만 구독
```

입니다.

따라서 다중 Context의 목적은 **Context 개수를 늘리는 것 자체가 아니라, 서로 다른 관심사와 업데이트 의존성을 분리하는 것**이라고 이해하면 가장 정확합니다.
