# JSX의 `ref` — DOM 접근부터 React 내부 흐름까지

React에서 `ref`는 **DOM 요소나 React 컴포넌트 인스턴스를 직접 참조하기 위한 공식적인 방식**입니다.
하지만 JSX에서 ref를 사용하는 문법, 호출 시점, 내부 동작 방식은 충분히 깊게 이해하지 않으면 헷갈리기 쉽습니다.

아래에서 **JSX의 ref 문법 → React Fiber 트리를 따라 ref가 연결되는 과정 → useRef와의 관계 → 함수형 컴포넌트의 ref 제한사항 → forwardRef까지** 모두 설명해드리겠습니다.

> ## 먼저 읽어주세요 — React 19에서 바뀐 것
>
> 이 문서의 상당 부분은 **React 18 이하 기준**의 설명입니다. **React 19에서 다음이 바뀌었습니다.**
>
> | 항목                    | React 18 이하                       | React 19                                    |
> | --------------------- | --------------------------------- | ------------------------------------------- |
> | 함수 컴포넌트가 `ref`를 받는 법 | X 못 받음 → `forwardRef` 필수 | O **일반 prop으로 직접 받음** (`function C({ ref })`) |
> | `forwardRef`          | 필수                                | 불필요 (동작은 하지만 **향후 deprecated 예정**)          |
> | 콜백 ref의 정리(cleanup) | `ref(null)` 호출로 해제 감지 | O **cleanup 함수 반환 가능** |
> | 콜백 ref가 값을 반환하면 | 무시됨 | X **에러** (cleanup으로 오해) |
>
> 아래 3⃣ ~ 4⃣, 9⃣, 1⃣1⃣ 항목에는 각각 정정 박스를 달아두었습니다.

---

# 1⃣ JSX에서 ref를 사용하는 기본 문법

## O DOM 요소에 ref

```jsx
function MyComponent() {
  const inputRef = useRef(null);

  return (
    <input ref={inputRef} />
  );
}
```

JSX에서 `ref={someRef}` 형태로 전달하면 React는 **커밋 이후에 해당 DOM 노드를 `someRef.current`에 넣어줍니다.**
(렌더 함수가 실행되는 시점에는 아직 `null`입니다.)

즉 **JSX의 `ref` 속성은 예약된 이름**이며, DOM 엘리먼트에서는 일반 props처럼 DOM 속성으로 전달되지 않습니다.

---

# 2⃣ JSX에서 ref는 왜 특별한가?

 **중요 포인트 (React 18 이하 기준):**
`ref`는 **props로 전달되지 않습니다.**

예:

```jsx
<MyInput ref={inputRef} />
```

→ React 18 이하에서는 **MyInput(props)에서 props.ref로 받을 수 없습니다.**

왜냐하면:

* React는 ref 속성을 잡아서 내부에서 직접 처리합니다.
* 일반 props로 내려가지 않습니다.

> ### O React 19 정정
>
> **React 19부터 `ref`는 함수 컴포넌트에게 “평범한 prop”으로 전달됩니다.**
>
> ```jsx
> function MyInput({ ref, placeholder }) {
>   return <input ref={ref} placeholder={placeholder} />; // 그냥 받아서 쓰면 끝
> }
> ```
>
> * `<div>`, `<input>` 같은 **호스트(DOM) 엘리먼트**와 **클래스 컴포넌트**에서는 여전히 React가 `ref`를 가로채 직접 처리합니다. (DOM 속성으로 내려가지 않음)
> * 반면 **함수 컴포넌트**에게는 `props.ref`로 그대로 도착합니다.
> * 참고로 `element.ref`(엘리먼트 객체에서 직접 꺼내 읽기)는 **React 19에서 deprecated** 되었고, `element.props.ref`를 사용합니다.

---

# 3⃣ 함수형 컴포넌트와 ref: 버전별로 완전히 다릅니다

## React 18 이하 — 받을 수 없었습니다

```jsx
function MyButton() {
  return <button>Click</button>;
}

<MyButton ref={btnRef} />
```

X **ref가 MyButton으로 전달되지 않습니다.**
React는 **함수형 컴포넌트에 인스턴스가 없기 때문**에 ref를 연결하지 않았습니다.

 그래서 경고 발생:

> Function components cannot be given refs. Attempts to access this ref will fail.
> Did you mean to use React.forwardRef()?

 이 문제를 해결하는 공식 API가 바로 **`forwardRef`** 였습니다.

## O React 19 — 그냥 받습니다

```jsx
function MyButton({ ref }) {
  return <button ref={ref}>Click</button>;
}

<MyButton ref={btnRef} />  // ✅ 정상 동작, forwardRef 불필요
```

* “함수 컴포넌트는 인스턴스가 없다”는 사실 자체는 **여전히 그대로**입니다.
* 달라진 건 **React가 ref를 props에 실어 그냥 내려주기로 했다**는 점입니다.
  → 그래서 “ref를 어디에 붙일지”는 **컴포넌트 작성자가 직접 결정**합니다. (DOM에 붙이든, `useImperativeHandle`로 가공하든)
* 즉 React 19에서 `ref`는 **“이름만 예약된, 조금 특별한 prop”** 에 가까워졌습니다.

---

# 4⃣ ref를 자식에게 전달하기

## O React 19 (권장)

```jsx
function MyInput({ ref, ...rest }) {
  return <input ref={ref} {...rest} />;
}
```

## React 18 이하 — `forwardRef` 사용 (레거시 코드 이해용)

```jsx
const MyInput = React.forwardRef((props, ref) => {
  return <input ref={ref} />;
});
```

두 경우 모두 사용하는 쪽은 동일합니다:

```jsx
const inputRef = useRef(null);

<MyInput ref={inputRef} />;
```

> `forwardRef`는 React 19에서도 **아직 동작**하므로 기존 코드가 깨지지는 않습니다.
> 다만 **새 코드에서는 쓰지 않는 것**이 좋습니다. 기존 코드는 React 19 마이그레이션 codemod 모음(`npx codemod@latest react/19/migration-recipe`)으로 일괄 정리할 수 있습니다.

---

# 5⃣ JSX에서 ref의 두 가지 형태

JSX는 ref를 두 가지 형태로 허용합니다.

## ① 객체 ref (`useRef`, `createRef`)

```jsx
const myRef = useRef(null);
<div ref={myRef} />;
```

## ② 콜백 ref

```jsx
<div ref={el => console.log('DOM:', el)} />
```

콜백 ref는 다음 상황에 유용합니다:

* DOM 노드 변화 감지
* unmount 시점 감지
* 외부 라이브러리 연동
* 리스트에서 **여러 DOM 노드를 Map에 모아두기**

### React 19: 콜백 ref가 cleanup 함수를 반환할 수 있습니다

```jsx
<div
  ref={(node) => {
    const ro = new ResizeObserver(() => { /* ... */ });
    ro.observe(node);

    return () => ro.disconnect(); // 🆕 해제 시점에 호출
  }}
/>
```

* cleanup을 반환하면 React는 **`ref(null)`을 호출하지 않습니다.**
* React 18 이하에서는 반환값이 무시되므로 `if (node) { ... } else { ... }` 분기를 써야 했습니다.

### 매우 흔한 실수 — 화살표 함수 축약형의 “암묵적 반환”

```jsx
// ❌ React 19에서 에러
<div ref={el => (myRef.current = el)} />
```

* 축약형(concise body) 화살표 함수는 **대입식의 결과값(DOM 노드)을 그대로 반환**합니다.
* React 19는 이 반환값을 **cleanup 함수로 간주**하는데 함수가 아니므로 런타임 에러가 납니다.
* O **반드시 블록 본문 `{ }`** 을 사용하세요.

```jsx
// ✅ 올바른 형태
<div ref={el => { myRef.current = el; }} />
```

* TypeScript에서도 `Ref<T>`의 반환 타입이 `void | (() => void)`로 좁혀져 **컴파일 에러**로 잡힙니다.

### 인라인 콜백 ref의 재생성 문제

인라인 화살표 함수는 **렌더마다 새 함수**이므로, React는 매 렌더마다
`이전 콜백 해제 → 새 콜백 연결`을 반복합니다.
연결/해제가 비싼 작업이라면 `useCallback`으로 안정화하세요.

```jsx
const setNode = useCallback((node) => {
  if (!node) return;
  // 무거운 초기화
}, []);

<div ref={setNode} />
```

---

# 6⃣ React 내부에서 ref가 연결되는 전체 과정 (Fiber 기반)

여기서는 “JSX → Fiber → commit → ref 연결”의 흐름을 완전하게 설명드립니다.

## 1단계: JSX 파싱

```jsx
<input ref={someRef} />
```

JSX는 다음으로 변환됩니다.

```js
React.createElement('input', { ref: someRef })
```

## 2단계: Fiber 생성 중 ref 정보 저장

React는 해당 element를 기반으로 **Fiber node**를 생성합니다.

Fiber에는 다음 정보가 저장됩니다:

* `pendingProps.ref`
* `ref` 필드

즉 **Fiber 구조 안에 ref가 보존됩니다.**

## 3단계: commit 단계에서 ref가 실제 DOM 유효화

commitPhase → layout effects 단계에서
DOM이 생성되면 React는 다음 작업을 수행합니다:

### 객체 ref일 경우:

```js
ref.current = domNode;
```

### 콜백 ref일 경우:

```js
ref(domNode);
```

 **unmount 시점**

* 객체 ref → `ref.current = null`
* 콜백 ref → `ref(null)` 호출
  * **React 19**: 해당 콜백이 **cleanup 함수를 반환했다면 `ref(null)` 대신 그 cleanup을 호출**합니다.

 **업데이트로 ref가 바뀔 때(예: 다른 콜백 함수로 교체될 때)**

1. 이전 ref 해제 (`ref(null)` 또는 cleanup 실행)
2. 새 ref 연결 (`newRef(domNode)`)

즉 React는 LifeCycle에 맞춰 ref를 설정/해제합니다.

---

# 7⃣ useRef와 JSX ref는 어떤 관계인가?

`useRef`는 ref 객체를 생성하는 훅일 뿐입니다:

```js
const myRef = { current: null };
```

JSX에 넘기면 React가 DOM node를 할당해주는 것입니다.

---

# 8⃣ ref가 업데이트되는 시점

ref는 **렌더 → commit → layoutEffect 전**에 DOM이 주입됩니다.

⏱ ref 업데이트 순서:

1. DOM 생성됨
2. ref 연결됨 (`ref.current = DOM`)
3. layoutEffect 실행 (`useLayoutEffect`)
4. 그 후 useEffect 실행

즉 ref는 **useLayoutEffect 내부에서 항상 최신값 보장**됩니다.

---

# 9⃣ ref 사용 시 주의해야 할 점

### 1. ref 값 변경은 리렌더를 일으키지 않음

```js
ref.current = 123;
```

→ 상태 업데이트가 아니므로 컴포넌트가 다시 렌더되지 않습니다.

### 2. 함수형 컴포넌트에 ref 붙이기 — 버전 확인 필수

* **React 18 이하**: 그냥 붙이면 안 됨 → 반드시 `forwardRef` 사용
* **React 19**: `function C({ ref })`로 **그대로 받으면 됨** → `forwardRef` 불필요

### 3. ref를 조건문으로 배치하면 DOM 연결이 깨질 수 있음

조건부 렌더링으로 노드가 사라지면 `ref.current`는 `null`이 됩니다.
따라서 **`ref.current`를 사용하기 전에는 항상 null 체크**(`ref.current?.focus()`)를 하세요.

### 4. 렌더링 중에 `ref.current`를 읽거나 쓰지 말 것

* 렌더 함수는 **순수**해야 합니다. `ref.current`는 렌더 중에 아직 채워지지 않았거나, 화면과 어긋난 값일 수 있습니다.
* 반드시 **이벤트 핸들러 / `useEffect` / `useLayoutEffect`** 안에서 접근하세요.

### 5. 콜백 ref에서 값을 반환하지 말 것 (React 19)

* `ref={el => (myRef.current = el)}` X → `ref={el => { myRef.current = el; }}` O

---

# 10⃣ 고급: class 컴포넌트에서의 ref 동작

class 컴포넌트는 인스턴스를 갖기 때문에 ref가 곧 인스턴스를 가리킵니다.

```jsx
class Modal extends React.Component {}
const ref = React.createRef();

<Modal ref={ref} />
```

`ref.current` → Modal 인스턴스
O setState
O 메서드 호출 가능

함수형 컴포넌트에는 인스턴스가 없습니다.

* **React 18 이하**: 그래서 ref를 붙일 대상 자체가 없어 **불가** (→ `forwardRef` 필요)
* **React 19**: 인스턴스가 없는 건 동일하지만, React가 `ref`를 **props로 넘겨주므로** 컴포넌트가 직접 “무엇을 ref에 담을지” 정할 수 있습니다.
  (클래스 컴포넌트의 ref 동작은 React 19에서도 **변경 없음**)

---

# 11⃣ 고급: `key`와 `ref`의 특별 취급 (React 19에서 달라진 부분)

JSX의 `key`는 **어느 버전에서도** props로 전달되지 않습니다.
반면 `ref`는 React 19에서 위치가 바뀌었습니다.

**React 18 이하**

| 속성      | props로 전달? | React 내부에서 특별 처리? |
| ------- | ---------- | ----------------- |
| 일반 props | O | X |
| `key` | X | O |
| `ref` | X | O |

**React 19**

| 속성      | props로 전달?                    | React 내부에서 특별 처리?                      |
| ------- | ----------------------------- | ----------------------------------- |
| 일반 props | O | X |
| `key` | X (여전히 전달 안 됨) | O |
| `ref` | O **함수 컴포넌트에는 prop으로 전달됨** | O (DOM 엘리먼트·클래스 컴포넌트에는 React가 직접 처리) |

* 즉 React 19에서 “진짜 특수 어트리뷰트”로 남은 것은 **`key`** 이고,
  `ref`는 **“이름이 예약된 일반 prop”** 에 가까워졌습니다.
* 단, `<div ref={...}>`처럼 **DOM 엘리먼트에 준 ref는 여전히 DOM 속성으로 내려가지 않고** React가 가로채 처리합니다.

---

# 12⃣ 실제 useRef와 ref 연결 완전 예제

```jsx
import { useRef, useEffect } from "react";

export default function App() {
  const boxRef = useRef(null);

  useEffect(() => {
    console.log("mounted:", boxRef.current); // DOM node
    boxRef.current.style.backgroundColor = 'yellow';
  }, []);

  return (
    <div>
      <h1>Ref Test</h1>
      <div
        ref={boxRef}
        style={{ width: 200, height: 100, background: '#eee' }}
      />
    </div>
  );
}
```

---

# 결론

### O Fiber commit 단계에서 DOM 생성 후 ref 연결 (→ useLayoutEffect → paint → useEffect)

### O 객체 ref / 콜백 ref 두 종류

### O ref는 렌더링 트리거가 되지 않는다 (렌더 중 읽기/쓰기 금지)

### O `key`는 어느 버전에서도 props로 전달되지 않는다

### O **React 18 이하**: 함수형 컴포넌트는 ref를 받을 수 없다 → `forwardRef` 필요

### O **React 19**: `ref`는 함수 컴포넌트에 **일반 prop으로 전달된다** → `forwardRef` 불필요 (향후 deprecated 예정)

### O **React 19**: 콜백 ref는 **cleanup 함수를 반환할 수 있고**, 그 외의 값을 반환하면 **에러**다
