리액트의 `useCallback`은 **“함수 버전의 useMemo”** 라고 생각하시면 이해가 훨씬 쉽습니다.
“함수도 값이다”라는 자바스크립트 특성 때문에 등장한 훅이에요.

아래에서 하나씩 아주 깊게 파보겠습니다.

---

## 1. useCallback이 왜 존재하나요?

먼저 문제 상황부터 보겠습니다.

```jsx
function Parent() {
  const [count, setCount] = useState(0);

  const handleClick = () => {
    console.log('clicked!');
  };

  return (
    <div>
      <button onClick={() => setCount(c => c + 1)}>+</button>
      <Child onClick={handleClick} />
    </div>
  );
}

const Child = React.memo(function Child({ onClick }) {
  console.log('Child render');
  return <button onClick={onClick}>Child Button</button>;
});
```

여기서 `Parent`가 렌더링될 때마다:

* `handleClick` **함수가 새로 만들어집니다**. (`() => { ... }`는 함수 리터럴)
* 자바스크립트 입장에서 **이전 렌더에서 만든 함수와 “참조(주소)”가 다릅니다.**
* 그래서 `Child` 컴포넌트 입장에서는
  `props.onClick`이 **매번 새 값처럼 보이고**,
  `React.memo`를 써도 “props가 변한 것으로 판단” → 매번 리렌더링

> 즉, “함수는 매 렌더마다 새로 만들어지는 값”이기 때문에
> **React.memo + 콜백 props 조합**에서 문제가 발생합니다.

이 문제를 해결하기 위해 등장한 것이 **`useCallback`** 입니다.

---

## 2. useCallback 한 줄 정의

> **`useCallback`은 “함수”를 기억(memoize)해서,**
> **의존성 배열(deps)이 변하지 않는 동안 같은 함수 객체를 재사용하게 해주는 훅**입니다.

시그니처는 다음과 같습니다:

```ts
const memoizedCallback = useCallback(
  (/* ...args */) => {
    // ...do something...
  },
  [dep1, dep2, ...]
);
```

* 첫 번째 인자: **저장할 콜백 함수**
* 두 번째 인자: **의존성 배열(deps)**
  → 여기에 있는 값이 바뀔 때에만 **새 콜백이 만들어지고 저장**됩니다.

기본 개념은 `useMemo`와 똑같습니다.

```jsx
// 값(value)을 기억하고 싶으면 useMemo
const memoizedValue = useMemo(() => expensiveComputation(a, b), [a, b]);

// 함수(function)를 기억하고 싶으면 useCallback
const memoizedCallback = useCallback(() => doSomething(a, b), [a, b]);
```

---

## 3. 가장 대표적인 사용처 3가지

### (1) React.memo + 콜백 props 최적화

아까 그 예제를 `useCallback`으로 수정해 봅시다.

```jsx
import React, { useState, useCallback } from 'react';

const Child = React.memo(function Child({ onClick }) {
  console.log('Child render');
  return <button onClick={onClick}>Child Button</button>;
});

function Parent() {
  const [count, setCount] = useState(0);

  const handleClick = useCallback(() => {
    console.log('clicked!');
  }, []); // ✅ deps가 빈 배열 → 항상 같은 함수 객체

  console.log('Parent render');

  return (
    <div>
      <p>count: {count}</p>
      <button onClick={() => setCount(c => c + 1)}>+</button>
      <Child onClick={handleClick} />
    </div>
  );
}

export default Parent;
```

이제:

* `Parent`가 `count` 때문에 계속 렌더링되어도
* `handleClick`의 **참조(주소)는 그대로 유지**됩니다.
* `Child` 입장에서는 `props.onClick`이 **안 변한 것처럼 보임**
* `React.memo`에 의해 `Child`는 **불필요한 리렌더링을 피함** O

---

### (2) 의존성 배열에 함수 넣어야 할 때 (useEffect 등)

`useEffect`나 `useMemo`에서 **함수를 의존성으로 넣어야 하는** 상황이 있습니다.

```jsx
function SearchBox({ query }) {
  const [result, setResult] = useState(null);

  const fetchData = () => {
    // query를 사용해서 API 호출
  };

  useEffect(() => {
    fetchData();
  }, [fetchData]); // ❌ fetchData는 매 렌더마다 새로 만들어짐
}
```

위 코드는:

* `fetchData`가 매 렌더마다 새로 만들어지므로,
* `useEffect`의 deps에 `fetchData`가 들어있으면
* **매 렌더마다 effect가 다시 실행** → 무한 API 호출까지 번질 수 있음

`useCallback`을 사용하면:

```jsx
function SearchBox({ query }) {
  const [result, setResult] = useState(null);

  const fetchData = useCallback(() => {
    // query를 사용해서 API 호출
    console.log('fetch with', query);
  }, [query]); // ✅ query가 변할 때만 새 함수

  useEffect(() => {
    fetchData();
  }, [fetchData]); // ✅ fetchData가 안정적으로 유지됨

  return <div>검색 결과: {JSON.stringify(result)}</div>;
}
```

* `query`가 바뀔 때에만 `fetchData`가 새로 생성됩니다.
* 그 때만 effect가 다시 실행됩니다.
* 의존성 배열에 함수를 넣어야 할 때, `useCallback`으로 **함수의 안정성을 보장**하는 패턴입니다.

> **다만 이 경우 1순위 해법은 `useCallback`이 아닙니다.**
> 그 함수를 **effect 안에서만 쓴다면, 함수를 effect 안으로 옮기는 것**이 공식 문서가 권장하는 정석입니다.
>
> ```jsx
> function SearchBox({ query }) {
>   const [result, setResult] = useState(null);
>
>   useEffect(() => {
> // O effect 안에 두면 deps에서 함수가 사라짐 → useCallback 자체가 불필요
>     const fetchData = () => {
>       console.log('fetch with', query);
>     };
>     fetchData();
>   }, [query]); // 의존성이 "진짜 값"인 query만 남는다
>
>   return <div>검색 결과: {JSON.stringify(result)}</div>;
> }
> ```
>
> `useCallback`은 **그 함수를 effect 밖(다른 핸들러, 자식 props)에서도 함께 써야 할 때**의 해법입니다.
> “deps 경고를 지우려고” 습관적으로 `useCallback`을 다는 순간,
> 그 함수가 참조하는 값이 늘어날수록 **deps 사슬(useCallback → useEffect → 또 다른 useCallback)** 이 번져 나갑니다.

---

### (3) stable reference가 필요한 곳들 (커스텀 훅, 외부 라이브러리 등)

일부 라이브러리나 커스텀 훅은 **콜백 참조가 바뀌지 않기를 요구**합니다.

예: 이벤트 리스너 등록용 커스텀 훅

```jsx
function useWindowEvent(eventName, handler) {
  useEffect(() => {
    window.addEventListener(eventName, handler);
    return () => window.removeEventListener(eventName, handler);
  }, [eventName, handler]);
}
```

이제 이 훅을 사용할 때:

```jsx
function Component() {
  const [count, setCount] = useState(0);

  const handleResize = () => {
    console.log('resize', count);
  };

  useWindowEvent('resize', handleResize); // ❌ handleResize 매 렌더마다 새로 생성

  return <button onClick={() => setCount(c => c + 1)}>+</button>;
}
```

이 경우:

* 렌더링마다 `handleResize`가 새로 생성 → effect 다시 실행 →
  **이벤트 핸들러 계속 등록/해제**를 반복할 수 있음.

`useCallback`으로 “일단” 고정:

```jsx
function Component() {
  const [count, setCount] = useState(0);

  const handleResize = useCallback(() => {
    console.log('resize', count);
  }, [count]); // ⚠️ count가 변할 때마다 새 함수 → 리스너도 다시 등록됨

  useWindowEvent('resize', handleResize);

  return <button onClick={() => setCount(c => c + 1)}>+</button>;
}
```

여기서 가장 많이 놓치는 부분을 짚고 가겠습니다.

* 이 코드는 **문제를 절반만 해결합니다.**
  `count`가 바뀔 때마다 `handleResize`가 새로 만들어지고,
  그 결과 `removeEventListener` → `addEventListener`가 **다시 실행**됩니다.
  “매 렌더마다 재등록”이 “`count`가 바뀔 때마다 재등록”으로 줄었을 뿐입니다.
* 그렇다고 deps를 `[]`로 두면 이번엔 **stale closure**가 되어 `count`가 영원히 `0`으로 찍힙니다.
* 즉 **“참조 고정”과 “최신 값”은 `useCallback`만으로는 동시에 얻을 수 없습니다.**

 두 가지를 모두 원한다면 최신 값을 `useRef`에 담아 두는 **latest-ref 패턴**이 필요합니다.
이 패턴은 다음 장 **「14.1 useCallback과 useRef 조합」**에서 자세히 다룹니다.

---

## 4. useCallback 내부 동작 (정신 나간 디테일 버전)

이 부분은 선생님이 좋아하시는 **Hook 메모리 슬롯 + Fiber 관점**으로 설명드릴게요.

아주 단순화해서 보면, React는 각 함수형 컴포넌트마다 다음과 같은 구조를 갖습니다:

```ts
type Hook = {
  memoizedState: any;  // useState면 state, useMemo/useCallback이면 { value, deps } 등
  next: Hook | null;
};

type FiberNode = {
  memoizedState: Hook | null; // 이 컴포넌트의 첫 번째 Hook
  // ...
};
```

`useCallback(fn, deps)`를 호출했을 때의 흐름(극단적 단순화):

1. **현재 렌더의 “hook cursor” 위치에 해당하는 Hook을 꺼냅니다.**
2. 첫 렌더라면:

   * 새로운 Hook을 만들고
   * `hook.memoizedState = { callback: fn, deps }` 형태로 저장합니다.
   * 이 때 **`fn`이 “원본 함수”**입니다.
3. 이후 렌더부터는:

   * 이전 렌더의 `hook.memoizedState.deps`와 이번 렌더의 `deps`를 **shallow equal 비교**합니다.
   * 같으면:

     * `hook.memoizedState.callback`을 그대로 반환 (이전 함수 객체 재사용)
   * 다르면:

     * `hook.memoizedState.callback`을 새로운 `fn`으로 교체
     * `hook.memoizedState.deps`도 새로운 deps로 교체
     * 새 함수 객체를 반환

개념적으로는 이렇게 의사 코드로 표현할 수 있습니다:

```js
function useCallback(fn, deps) {
  const hook = getCurrentHook();

  if (!hook) {
    // mount
    const state = { callback: fn, deps };
    mountWorkInProgressHook(state);
    return fn;
  } else {
    // update
    const prevState = hook.memoizedState;
    if (areHookInputsEqual(prevState.deps, deps)) {
      // deps가 그대로면 이전 callback 재사용
      return prevState.callback;
    } else {
      // deps가 바뀌면 새로운 callback 저장
      const state = { callback: fn, deps };
      hook.memoizedState = state;
      return fn;
    }
  }
}
```

핵심 포인트는:

> “**deps가 변하지 않으면, 이전 렌더에서 저장해 둔 함수 객체를 그대로 돌려준다**”
> → 그래서 **참조(주소)가 안정적으로 유지**됩니다.

---

## 5. useCallback vs useMemo 비교

자주 헷갈리는 부분이라, 명확하게 잘라서 정리해 보겠습니다.

### 공통점

* 둘 다 **memoization 훅**입니다.
* 둘 다 **deps 배열**을 기반으로 이전 값과 비교합니다.
* deps가 같으면 이전에 저장해둔 것을 재사용합니다.

### 차이점

```jsx
// useMemo: "값"을 메모이즈
const value = useMemo(() => {
  return someExpensiveCalculation(a, b);
}, [a, b]);

// useCallback: "함수"를 메모이즈
const callback = useCallback(() => {
  doSomething(a, b);
}, [a, b]);
```

* `useMemo`는 콜백을 **바로 실행해서 나온 결과(값)** 를 저장
* `useCallback`은 콜백 **자체(함수 객체)** 를 저장

엄밀하게 말하면:

```jsx
useCallback(fn, deps) === useMemo(() => fn, deps)
```

이 두 줄은 **개념적으로 동일**합니다.
(`useMemo`는 콜백을 실행한 결과를 저장하는데, `() => fn`을 실행하면 결과가 `fn` 자신이기 때문입니다.
React 공식 문서도 “`useCallback`은 `useMemo`로 이렇게 표현할 수 있다”고 설명합니다.)

**여기서 반드시 정정해야 할 오해**

> “`useCallback`을 쓰면 함수가 매 렌더 새로 만들어지지 않는다” → **틀렸습니다.**

```jsx
const handleClick = useCallback(() => {
  doSomething(a);
}, [a]);
```

* `() => { doSomething(a) }` 라는 함수 리터럴은 **렌더할 때마다 새로 생성되어 `useCallback`의 인자로 전달**됩니다.
* `useCallback`은 그중 하나를 골라 **“반환되는 참조”만 고정**할 뿐이고, 새로 만든 함수는 그냥 버려집니다.
* 즉 **함수 생성 비용은 그대로 발생**하며, 거기에 **deps 비교 비용과 메모리 비용이 추가**됩니다.

 그러므로 `useCallback`이 절약해 주는 것은 **함수 생성 비용(사실상 0)** 이 아니라
**참조가 바뀌었을 때 뒤따라오는 작업**(memo 자식의 리렌더, effect 재실행, 리스너 재등록)입니다.
그 “뒤따라오는 작업”이 없다면 **`useCallback`의 이득은 정확히 0**입니다.

**그리고 이 캐시는 “보장”이 아닙니다**

리액트 공식 문서는 `useCallback`/`useMemo`의 캐시를 **버릴 수 있다**고 명시합니다.
따라서 “`useCallback`을 썼으니 이 함수 참조는 절대 안 바뀐다”에 **로직이 의존하면 안 됩니다.**
`useCallback`을 전부 지웠을 때 **느려지기만 하면 정상, 동작이 달라지면 버그**입니다.

---

## 6. 의존성 배열(deps) 설계 & stale closure 문제

### (1) deps를 잘못 적으면 생기는 문제

```jsx
function Counter() {
  const [count, setCount] = useState(0);

  const log = useCallback(() => {
    console.log('count:', count);
  }, []); // ❌ deps를 []로 지정

  useEffect(() => {
    const id = setInterval(log, 1000);
    return () => clearInterval(id);
  }, [log]);

  return <button onClick={() => setCount(c => c + 1)}>+</button>;
}
```

여기서 `log`는:

* 마운트 시점의 `count` 값을 캡처한 **클로저**입니다.
* deps가 `[]`라서 이후 렌더에서 새로 만들어지지 않음.
* 따라서 **계속 `0`만 출력**합니다. (stale closure)

 해결하려면:

1. 진짜로 **최신 count를 쓰고 싶다**면, deps에 `count`를 넣어야 합니다.

   ```jsx
   const log = useCallback(() => {
     console.log('count:', count);
   }, [count]); // ✅ count 변경 시 새 함수
   ```

2. 또는 `setState`의 functional update를 사용하거나, `useRef`로 상태를 따로 추적하는 패턴도 있습니다. (좀 더 고급 패턴)

### (2) “eslint-plugin-react-hooks”를 적극적으로 믿기

* `useCallback` / `useMemo` / `useEffect`의 deps를 자동으로 분석해서 알려주는 플러그인입니다.
* `deps`에서 빠진 변수들을 경고해 줍니다.
* 대부분의 경우 **경고가 맞습니다.** (경고를 억지로 무시하면 stale closure 지옥으로...)

실전 팁:

> 1. 일단 개발할 때는 **경고를 모두 만족하는 방향으로 deps를 채운다.**
> 2. 정말로 “의도적으로” 빼야 할 때만 `// eslint-disable-next-line`을 붙인다.

---

## 7. useCallback을 “남용”하면 안 되는 이유

많은 초보자들이 “성능을 위해 모든 함수에 `useCallback`을 붙이면 좋겠지?”라고 생각합니다.
→ **절대 그렇지 않습니다.**

이유:

1. `useCallback` 자체도 **비용**이 있습니다. (deps 비교, Hook 관리 등)
2. 함수 하나 만드는 비용보다, `useCallback`이 돌아가는 비용이 **더 클 때가 많습니다.**
3. memoization은 항상 **“trade-off”**입니다.

   * 메모리 + 비교 연산 ↔ 불필요한 렌더링 감소

그래서 React 팀이 공식 문서에서 말하는 패턴:

> * `React.memo`와 함께 쓰는 콜백 props
> * deps에 넣어야 하는 함수
> * 외부 라이브러리가 **stable callback**을 요구하는 경우
>   이 정도가 “useCallback 쓸 만한 지점”입니다.

실전 가이드라인:

* 부모-자식 구조에서

  * 자식에 `React.memo`를 씌웠고
  * 부모에서 자식에게 넘기는 props 중 **함수**가 있다면
    → 그 함수에 `useCallback`을 고려
* 그 외의 그냥 내부에서 쓰는 이벤트 핸들러는
  그냥 매 렌더마다 새로 만들어도 대부분 상관 없음.

### 7.1 `useCallback`이 “아무 일도 안 하는” 대표 상황

**(1) 자식이 `React.memo`가 아니다**

```jsx
function Child({ onClick }) { /* memo 아님 */ }

function Parent() {
  const onClick = useCallback(() => {}, []); // 😴 완전히 무의미
  return <Child onClick={onClick} />;        // 부모가 렌더되면 Child는 무조건 렌더
}
```

부모가 리렌더되면 자식도 리렌더되는 것이 리액트의 **기본 동작**이고,
그 사슬을 끊는 것은 `useCallback`이 아니라 **`React.memo`(또는 `children`으로 주입하기)** 입니다.
즉 `useCallback`은 **혼자서는 어떤 렌더도 막지 못합니다.**

**(2) 자식이 `React.memo`지만, 다른 props가 리터럴이다**

```jsx
<MemoChild
  onClick={handleClick}      // ✅ useCallback으로 고정
  style={{ padding: 8 }}     // ❌ 렌더마다 새 객체 → 여기서 이미 비교 실패
  data={rows.map(toView)}    // ❌ 렌더마다 새 배열
/>
```

`React.memo`의 props 비교는 **얕은 비교**라 **하나만 어긋나도 전체가 무력화**됩니다.
이 상태에서는 `useCallback` + `React.memo`의 **비교 비용만 추가로 지불**하는 셈이 됩니다.

### 7.2 그래서 순서는 이렇습니다

1. **측정** – React DevTools Profiler (“Record why each component rendered” 옵션 켜기)
2. **렌더 원인 파악** – 렌더가 잦은가, 렌더 한 번이 무거운가
3. **구조 개선** – state를 실제 쓰는 곳으로 내리기, `children`으로 넘겨 리렌더 사슬 끊기, 컨텍스트 분리
4. **그래도 남으면 메모이제이션** – 핫스팟에만, 그리고 **적용 후 재측정**

`useCallback`은 1~3번을 건너뛰고 뿌리는 도구가 아니라, **4번에서 꺼내는 도구**입니다.

---

## 8. 간단한 실전 예제 정리

### 예제 1: React.memo + useCallback 기본 패턴

```jsx
import React, { useState, useCallback } from 'react';

const TodoItem = React.memo(function TodoItem({ todo, onToggle }) {
  console.log('TodoItem render:', todo.id);
  return (
    <li>
      <label>
        <input
          type="checkbox"
          checked={todo.done}
          onChange={() => onToggle(todo.id)}
        />
        {todo.text}
      </label>
    </li>
  );
});

function TodoList() {
  const [todos, setTodos] = useState([
    { id: 1, text: 'React 공부', done: false },
    { id: 2, text: 'useCallback 이해하기', done: false },
  ]);

  const handleToggle = useCallback((id) => {
    setTodos((prev) =>
      prev.map((todo) =>
        todo.id === id ? { ...todo, done: !todo.done } : todo
      )
    );
  }, []); // ✅ setState 함수는 stable이므로 deps 필요 없음

  return (
    <ul>
      {todos.map((todo) => (
        <TodoItem key={todo.id} todo={todo} onToggle={handleToggle} />
      ))}
    </ul>
  );
}
```

포인트:

* `TodoItem`은 `React.memo`로 감싸져 있음
* `onToggle`은 `useCallback`으로 안정화
* todos가 바뀌어 해당 항목만 렌더링되며, 나머지는 스킵

---

### 예제 2: useCallback + useEffect 조합

```jsx
function AutoSaver({ content }) {
  const save = useCallback(() => {
    console.log('자동 저장:', content);
    // 실제 API 호출 로직
  }, [content]); // ✅ content가 바뀌면 새 함수

  useEffect(() => {
    const id = setInterval(save, 5000);
    return () => clearInterval(id);
  }, [save]); // ✅ save가 바뀔 때만 타이머 재설정

  return <div>작성 중: {content}</div>;
}
```

---

## 9. 한 번에 정리하는 useCallback 체크리스트 O

마무리로, 강의 슬라이드에 바로 넣으셔도 될만한 체크리스트를 드리겠습니다.

**언제 `useCallback`을 쓰나?**

* [ ] 자식 컴포넌트가 `React.memo`로 감싸져 있다.
* [ ] 자식에게 **함수형 props**를 넘긴다.
* [ ] 그 함수 때문에 자식이 불필요하게 자주 리렌더링된다.
* [ ] `useEffect`/`useMemo`의 deps에 **함수**를 넣어야 한다.
* [ ] 외부 라이브러리/커스텀 훅이 **stable callback**을 요구한다.

**deps는 어떻게 작성하나?**

* [ ] 콜백 안에서 사용하는 모든 외부 변수(state, props, 함수)를 deps에 넣는다.
* [ ] `eslint-plugin-react-hooks`의 경고를 기본적으로 신뢰한다.
* [ ] 정말 의도적으로 고정하고 싶을 때만 deps를 `[]`로 둔다.

**사용하지 말아야 할 때**

* [ ] 컴포넌트 내부에서만 쓰는 간단한 onClick, onChange 핸들러들
* [ ] 성능 문제를 측정하지 않았는데, “그냥 좋은 것 같아서” 전부 useCallback으로 감쌀 때
* [ ] deps 관리가 너무 복잡해져 오히려 코드를 읽기 어렵게 만들 때

---

## 10. 마무리

정리하면, `useCallback`은:

* **“함수의 참조를 안정적으로 유지하는 훅”**이고
* **React.memo / deps 배열 / 외부 라이브러리**와의 상호작용 속에서 진가를 발휘합니다.
* 무조건 많이 쓰는 것이 아니라, **측정 가능한 성능 문제**가 있을 때 “정밀하게” 사용하는 도구입니다.

---

## 11. 보너스 – React 19와 React Compiler

“React 19부터는 `useCallback`을 안 써도 된다”는 말을 자주 듣게 되실 텐데,
사실관계를 정확히 정리하고 넘어가겠습니다.

* 자동 메모이제이션을 해 주는 주체는 **React 19 자체가 아니라 `React Compiler`(구 React Forget)** 입니다.
* React Compiler는 **React 19에 자동으로 내장된 기능이 아닙니다.**
  **빌드 도구에 직접 추가하는 별도의 opt-in 도구**(Babel 플러그인 / Vite·Next.js 설정)이며,
  설치하지 않으면 **아무 일도 일어나지 않습니다.** 이 장의 수동 `useCallback`이 그대로 필요합니다.
* 컴파일러를 켜면, 지금까지 손으로 붙이던 `useCallback`/`useMemo`/`React.memo`의 상당 부분을
  컴파일러가 **자동으로 생성**해 줍니다. 다만 전제 조건이 있습니다.

  * **“Rules of React”를 지키는 코드**여야 합니다(렌더 중 부수 효과·mutation 금지, 훅 규칙 준수).
  * 규칙 위반이 의심되면 컴파일러는 **그 컴포넌트의 최적화를 조용히 건너뜁니다.**
  * 기존 `useCallback` 코드는 그대로 두어도 정상 동작하므로, 급히 지울 필요는 없습니다.
* 컴파일러가 있어도 **구조 문제(잘못된 state 위치, 컨텍스트 과다 갱신, 가상화 부재)는 고쳐 주지 않습니다.**

 그러므로 “참조 동일성 → 얕은 비교 → 렌더 사슬”이라는 이 장의 원리는
컴파일러 시대에도 그대로 필요합니다. **컴파일러가 왜 최적화를 못 했는지 읽어내는 힘**이 거기서 나옵니다.
