

## 📌 캡처링(Capturing)이란?

> **이벤트 전파(Event Propagation)의 첫 번째 단계로**,
> **부모 요소에서 자식 요소로** 이벤트가 내려가는 과정입니다.

즉,
DOM 트리 상에서 `window → document → html → body → … → target`
**순서대로 내려가면서** 이벤트 핸들러가 실행되는 단계입니다.

---

## ✅ 전체 전파 과정 요약

이벤트가 발생하면 다음 **3단계로 전파**됩니다:

| 단계         | 방향               | 설명              |
| ---------- | ---------------- | --------------- |
| **1. 캡처링** | 부모 → 자식 (위 → 아래) | 선택적으로 처리 가능     |
| **2. 타깃**  | 해당 요소            | 이벤트가 실제로 발생한 지점 |
| **3. 버블링** | 자식 → 부모 (아래 → 위) | React 기본 동작     |

---

## 🧪 HTML 예시 코드

```html
<div id="parent">
  <button id="child">클릭</button>
</div>

<script>
  // 캡처링 단계
  document.getElementById('parent').addEventListener(
    'click',
    () => {
      console.log('📥 캡처링: 부모 div')
    },
    true // 캡처링 단계에서 실행되도록
  )

  // 버블링 단계 (기본)
  document.getElementById('child').addEventListener('click', () => {
    console.log('🫧 버블링: 버튼')
  })
</script>
```

### 🔍 결과 (버튼 클릭 시):

```
📥 캡처링: 부모 div
🫧 버블링: 버튼
```

➡️ `true`로 지정한 이벤트 핸들러는 **캡처링 단계에서 실행**됩니다.

---

## 🧠 React에서 캡처링은 어떻게 할까?

React도 기본적으로 **버블링**만 처리하지만, **캡처링도 가능**합니다.

### ✅ `onClickCapture` 사용

```jsx
function App() {
  return (
    <div
      onClickCapture={() => console.log('📥 캡처링: div')}
      onClick={() => console.log('🫧 버블링: div')}
    >
      <button
        onClickCapture={() => console.log('📥 캡처링: button')}
        onClick={() => console.log('🫧 버블링: button')}
      >
        Click Me
      </button>
    </div>
  )
}
```

### 📌 실행 순서 (버튼 클릭 시):

```
📥 캡처링: div
📥 캡처링: button
🫧 버블링: button
🫧 버블링: div
```

---

## ✅ 캡처링 vs 버블링 요약 비교

| 항목        | 캡처링 (Capturing)               | 버블링 (Bubbling)                 |
| --------- | ----------------------------- | ------------------------------ |
| 전파 방향     | 부모 → 자식                       | 자식 → 부모                        |
| 기본 설정     | `false` (비활성화)                | 기본 동작                          |
| 이벤트 등록 방식 | `addEventListener(..., true)` | `addEventListener(..., false)` |
| React 용법  | `onClickCapture`              | `onClick`                      |

---

## 🔒 캡처링 중단

캡처링 도중 이벤트 전파를 중단하고 싶다면:

```js
e.stopPropagation()
```

버블링과 동일하게 사용합니다.

---

## 🧠 부가 팁: 언제 캡처링을 쓸까?

| 상황      | 캡처링 활용 예                   |
| ------- | -------------------------- |
| 보안 / 제한 | 내부 요소 클릭을 막기 위해 부모에서 먼저 감지 |
| 이벤트 제어  | 이벤트가 타깃에 도달하기 전에 막고 싶을 때   |
| 드래그/드롭  | 외부 감지용 이벤트                 |

---


## 📌 버블링(Bubbling)이란?

### ✅ DOM 이벤트 전파는 3단계로 이루어집니다:

1. **캡처링(Capturing) 단계**

   * `window` → `document` → `html` → `body` → ... → 실제 이벤트 발생 요소까지
   * 이벤트가 **부모에서 자식 방향으로 내려갑니다**

2. **타깃(Target) 단계**

   * 이벤트가 **실제 발생한 요소**에 도달합니다

3. **버블링(Bubbling) 단계** 🫧

   * 이벤트가 **자식에서 부모 방향으로 다시 올라갑니다**
   * 우리가 가장 흔히 사용하는 이벤트 리스너는 이 단계에서 작동합니다

---

## 🧪 예시 코드 (HTML)

```html
<div id="parent">
  <button id="child">클릭</button>
</div>

<script>
  document.getElementById('parent').addEventListener('click', () => {
    console.log('부모 div 클릭됨')
  })

  document.getElementById('child').addEventListener('click', () => {
    console.log('버튼 클릭됨')
  })
</script>
```

### 결과 (버튼 클릭 시 콘솔):

```
버튼 클릭됨
부모 div 클릭됨
```

👉 버튼 클릭이 버튼에서 먼저 발생하고, 이후 **이벤트가 부모로 "버블링"되어 올라감**.

---

## 🧠 React에서도 버블링!

React의 대부분의 이벤트(`onClick`, `onChange`, `onMouseOver` 등)는 **버블링 단계에서 동작**합니다.

```jsx
function App() {
  const handleParentClick = () => {
    console.log('🧍‍♂️ 부모 div 클릭')
  }

  const handleButtonClick = () => {
    console.log('👆 버튼 클릭')
  }

  return (
    <div onClick={handleParentClick}>
      <button onClick={handleButtonClick}>Click me</button>
    </div>
  )
}
```

버튼을 클릭하면:

```
👆 버튼 클릭
🧍‍♂️ 부모 div 클릭
```

---

## ✋ 버블링 막기

```js
event.stopPropagation()
```

버블링을 멈추고 싶을 때 사용합니다.

```jsx
<button onClick={(e) => {
  e.stopPropagation()
  console.log('버튼만 클릭됨')
}}>
  Stop Bubbling
</button>
```

---

## ✅ 요약 정리

| 구분        | 설명                           |
| --------- | ---------------------------- |
| **버블링**   | 이벤트가 **자식 → 부모 방향으로 전파됨**    |
| **기본 동작** | 브라우저는 기본적으로 버블링을 지원          |
| **React** | 대부분의 이벤트 핸들러는 버블링 단계에서 동작    |
| **중단 방법** | `event.stopPropagation()` 사용 |



